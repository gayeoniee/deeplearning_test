# STEP 18-B — 전체 val 로 A4 를 다시 쟀습니다 (2026-09-05)

캐글에서 STEP 16 가중치로 추론만 (`notebooks/08`, `tools/local_logits.py --chunk auto`).
학습 없음. 이후 분석은 전부 로컬 CPU.

> ⚠️ **VL01 이 빠진 89%** 입니다. 캐글 `m2.5` 데이터셋에 VL01 크롭 39,508장이
> 통째로 없어서 `--chunk auto` 가 TL01·TL02 만 골랐습니다.
> 병변 **27,331장** (STEP 16 전체 val 2단계는 30,457장).
> macro-F1 **0.5959** 로 STEP 16 의 0.5999 와 거의 같습니다 — 재현 확인.

## 클래스별 (전체 val 89%)

| | n | recall | precision | F1 | CI 반폭 |
|---|---:|---:|---:|---:|---:|
| **A4** | 1,730 | **0.228** | 0.341 | 0.273 | ±0.018 |
| A5 | 1,468 | 0.500 | 0.581 | 0.537 | ±0.025 |
| A1 | 4,244 | 0.619 | 0.531 | 0.571 | ±0.014 |
| A6 | 1,540 | 0.628 | 0.768 | 0.691 | ±0.026 |
| A2 | 9,644 | 0.756 | 0.758 | 0.757 | ±0.009 |
| A3 | 8,705 | 0.761 | 0.729 | 0.745 | ±0.010 |

**A4 recall 0.228** — holdout 0.264 보다 낮고, VL01 에서 잰 0.356 보다 훨씬 낮습니다.
**VL01 의 A4 는 쉬운 쪽 소수였다**는 게 확인됐습니다.

## ★ A4 는 흩어지는 게 아니라 **A1 하나로** 갑니다

| | H̃ | 최다 행선지 | 그 비중 | 모양 |
|---|---:|---|---:|---|
| A6 | 0.965 | A3 | 0.29 | 흩어짐 |
| A5 | 0.897 | A1 | 0.42 | 흩어짐 |
| A1 | 0.846 | A2 | 0.40 | 그 사이 |
| **A4** | **0.824** | **A1** | **0.51** | 그 사이 |
| A3 | 0.687 | A2 | 0.56 | 그 사이 |
| A2 | 0.638 | A3 | 0.62 | 그 사이 |

A4 오답의 **51%** 가 A1 으로 갑니다 → **A4 사진의 39.4% 가 A1 이라고 불리고,
A4 라고 불리는 건 22.8%** 입니다. **A1 이라고 더 많이 부릅니다.**

⚠️ **멘토가 말한 "여러 클래스로 흩어짐" 패턴은 A4 가 아니라 A5·A6 입니다.**
A4 는 문턱(0.85) 아래이고 A1 쪽으로 쏠립니다. STEP 16 holdout 행렬(H̃ 0.890,
A1 0.33)보다 **더 짝 혼동에 가깝게** 나왔습니다.

## ★ A4 안에서 — **작은 것이 A1 에 먹힙니다**

| A4 사분위 | n | bbox 중앙 | recall | **A1 이라고 부름** |
|---|---:|---:|---:|---:|
| Q1 (작음) | 439 | 44px | **0.139** | **54.0%** |
| Q2 | 432 | 80px | 0.262 | 54.9% |
| Q3 | 432 | 134px | 0.331 | 32.6% |
| Q4 (큼) | 427 | 259px | 0.183 | **15.9%** |

`rho(크기, A1 이라고 부름) = −0.314` (p=6.8e-41)

**44px 짜리 A4 는 절반 이상이 A1 으로 갑니다.** 크기가 커지면 A1 오분류가
3배 넘게 줍니다 (54.0% → 15.9%). recall 은 Q3 에서 최고이고 Q4 에서 도로
떨어지는데(0.331 → 0.183), 큰 A4 는 큰 클래스(A2·A3·A6)와 헷갈리는 것으로 보입니다.

참고로 A1 안에서는 방향이 반대입니다 — 작은 A1 recall 0.790, 큰 A1 0.364.

## ⚠️ STEP 21 결론을 좁힙니다

STEP 21 에서 "A4 가 A1 보다 크다(VL01 1.68배, 같은 개 1.88배)" 를 근거로
"`m2.5` 가 그 크기 신호를 지운다" 고 했습니다. **전체 데이터에서는 반대입니다:**

| | A4 중앙 | A1 중앙 | AUROC |
|---|---:|---:|---:|
| VL01 (교란 없음) | 196px | 117px | 0.697 |
| **전체 val (VL01 제외)** | **102px** | **134px** | **0.411** |

⚠️ **다만 아래쪽은 교란된 비교입니다** — TL02 엔 A1 이 0장이라 이 비교는
**TL02 의 A4 vs TL01 의 A1** 입니다. 교란 없는 비교는 여전히 VL01 하나뿐입니다.
**심슨의 역설이 여기서도 반복됩니다.**

→ 그래도 **"A4 가 A1 보다 크다" 를 전역 사실로 쓰면 안 됩니다.** VL01 특유일 수
있고, 그렇다면 크기 차이는 임상이 아니라 **묶음별 라벨링 관행**일 가능성이 커집니다.

## prior 기각 — 전체 데이터에서 재확인

| τ | macro-F1 | A1 | A2 | A3 | **A4** | A5 | A6 |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 0.0 | 0.5959 | 0.619 | 0.756 | 0.761 | **0.228** | 0.500 | 0.628 |
| 0.4 | 0.5960 | 0.614 | 0.731 | 0.741 | 0.283 | 0.555 | 0.656 |
| 1.0 | 0.5612 | 0.560 | 0.660 | 0.668 | **0.383** | 0.634 | 0.696 |

A4 recall 이 0.228 → 0.383 까지 오르는데 macro-F1 은 **+0.0001** 이고 그 뒤
0.5612 로 떨어집니다. **VL01 에서 낸 기각이 전체에서도 그대로**이고 표본이
16배 커졌습니다 (1,730 vs 326).

## 다음

1. **작은 A4** 가 표적입니다 — "A4 전체" 가 아니라. Q1·Q2(<80px)가 절반 이상
   A1 으로 갑니다.
2. ⚠️ 그래서 **고정 창(STEP 22)이 A4 를 더 나쁘게 만들 수 있습니다.** `m2.5` 는
   작은 병변을 **확대**하지만(44px → 창 110px → 384 에서 154px), 고정 창은
   **축소**합니다(44px 병변이 320px 창에서 14%, 384 에서 53px).
   **결과를 보기 전에 적어둡니다.**
3. A1↔A4 가 임상적으로 갈리는지는 결국 사진을 봐야 합니다 (작업 규칙 6).

## 산출물

```
data/work/reports/step18_full/stage2_logits.npz · stage2_rows.parquet
data/work/reports/step18_full/logit_adjust.json
```
