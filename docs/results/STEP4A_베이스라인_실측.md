# STEP 4A 베이스라인 실측 — 2026-08 VL01 / ResNet50

> 이 문서는 **추정이 아니라 실행 결과**입니다. 이후 모든 실험은 여기와 비교합니다.
> 설정: VL01 45,885장 / 개체 단위 분할 fold 0 / ResNet50 / 224px / Colab T4

---

## 요약

| 항목 | 값 | 판정 |
|---|---|---|
| 1단계 AUROC (`full` 크롭) | **0.8031** | ✅ 게이트 0.80 통과 |
| 1단계 임계값 (recall 0.95) | 0.3057, precision 0.640 | 헛알림 36% |
| 2단계 macro-F1 (`m1.5` 크롭) | **0.4865** (CI 0.467–0.505) | ✅ 게이트 0.25 통과 |
| 2단계 macro AUROC | 0.8155 | 순위는 좋음 |
| 최악 클래스 | **A6 결절·종괴 recall 0.328** | 🚨 개선 필요 |

---

## 지름길은 없었습니다 — 예상과 반대 방향으로 확인됨

감사에서 크롭 배율이 정답을 흘릴 위험을 찾았고(A1 0.47% ~ A6 3.08%, 6.6배),
"CNN 이 피부 대신 확대 배율을 셀 수 있다"고 우려했습니다. 실측 결과는 반대입니다.

| 클래스 | 하한선 (기하만) | CNN | 차이 |
|---|---:|---:|---:|
| A1 구진·플라크 | 0.529 | 0.505 | **−0.024** |
| A2 비듬·각질 | 0.166 | 0.610 | +0.444 |
| A3 태선화·과다색소침착 | 0.110 | 0.555 | +0.445 |
| A4 농포·여드름 | 0.224 | 0.372 | +0.148 |
| A5 미란·궤양 | 0.146 | 0.517 | +0.371 |
| A6 결절·종괴 | 0.453 | 0.328 | **−0.125** |
| **macro-F1** | **0.2099** | **0.4865** | **+0.2766** |

**크기로 맞힐 수 있던 두 클래스에서 CNN 이 오히려 하한선보다 낮습니다.**
크기를 단서로 썼다면 최소한 하한선만큼은 나왔어야 합니다. 즉 CNN 은 질감·색을
보고 있고, 그게 A2·A3·A5 에서는 크게 유리하지만(+0.44) A1·A6 에서는 크기보다 약합니다.

→ **`f320` 재크롭은 필요 없습니다.** 배율 교란 검사(`robust.scale_stress`)로
   최종 확인하되, 이 표만으로도 지름길 의존은 낮다고 볼 수 있습니다.

1단계도 같습니다: 하한선 AUROC 0.6054 → CNN 0.8031 (+0.20).
게다가 `full` 크롭이라 배율 정보 자체가 없습니다.

---

## 아직 수렴하지 않았습니다

2단계 학습 곡선 — **12 에폭 전부가 best 갱신**입니다:

```
[ 9] train 1.3555 | val 1.4768 | macroF1 0.4797
[10] train 1.3510 | val 1.4749 | macroF1 0.4807
[11] train 1.3518 | val 1.4737 | macroF1 0.4823  ★ 마지막이 최고
```

- val loss 가 한 번도 안 올랐습니다 → **과적합 아님**
- train(1.352) 과 val(1.474) 격차가 작습니다 → **덜 학습됨**
- macro AUROC 0.8155 인데 macro-F1 0.4865 → 순위는 좋은데 argmax 결정이 나쁨

1단계는 반대로 5~6 에폭에서 평평해졌습니다 (수렴 완료).

---

## 우리는 이미 파인튜닝 중이었습니다 — 다만 백본이 거의 안 움직였습니다

용어 정리:

```
전이학습 (transfer learning)   ← 우산 개념
  ├─ linear probe   백본 얼리고 헤드만
  └─ fine-tuning    백본까지 같이       ← 처음부터 이쪽
```

`freeze_backbone()` 은 정의만 되어 있고 **어디서도 호출하지 않습니다.**
백본은 계속 학습됐습니다. 문제는 **속도**입니다:

```
헤드 lr = 3e-4
백본 lr = 3e-4 × 0.1 = 3e-5     ← 12 에폭 ≈ 11,800 스텝 동안 거의 제자리
```

`backbone_lr_mult=0.1` 은 "ImageNet 특징이 이미 쓸 만하다" 는 전제인데,
ImageNet 은 **물체**를 구분하도록 배웠고 우리 과제는 **피부 질감·색의 미세한 차이**
입니다. 도메인 격차가 커서 백본이 적응할 시간이 필요합니다.

→ `config.FT_PRESETS` 로 강도를 바꿔 실험합니다 (노트북 03 의 7번).
   `conservative`(0.1, 현재) / **`moderate`(0.3, 1순위)** / `aggressive`(1.0) /
   `linear_probe`(0, 대조군).

---

## 개선 지렛대 (효과 큰 순서)

| | 방법 | 근거 | 비용 |
|---|---|---|---|
| 1 | **에폭 25 + `moderate` 파인튜닝** | 12에폭 전부 best 갱신 + 백본 lr 3e-5 | GPU ~40분 |
| 2 | **클래스별 임계값 / 보정** | AUROC 0.8155 vs F1 0.4865 격차 | 학습 없음 (노트북 05) |
| 3 | **TL01 추가** | A6 학습 데이터 991장뿐 | 다운로드 몇 시간 |
| 4 | 백본 교체 (노트북 04) | ResNet50 은 기준선 | GPU 시간 |
| 5 | 1단계 입력 해상도 ↑ | `full` 에서 병변이 22×22px | GPU 시간 |

1·2 는 데이터를 더 안 받고도 됩니다. **먼저 하세요.**

⚠️ 배율 증강(`scale_robust`)은 **우선순위에서 내렸습니다.** 클래스별 하한선 비교에서
CNN 이 크기를 안 쓰는 게 확인됐으므로(A1 −0.024, A6 −0.126), 막을 지름길이 없습니다.
과소적합 상태에서 증강을 강화하면 오히려 반대 방향입니다.
6번 배율 교란 하락폭이 30% 를 넘을 때만 다시 보세요.

---

## 목표치 수정

로드맵의 "6종 macro-F1 0.70~0.80" 은 **데이터를 보기 전에 적은 추정치**였고
근거가 없었습니다. 실측 기준선을 반영해 다시 잡습니다.

| 지표 | 이전(추정) | 수정(실측 기준) |
|---|---|---|
| 2단계 macro-F1 | 0.70~0.80 | **0.55~0.65** (VL01 단독 기준) |
| A6 recall | — | **≥ 0.50** ← 종양 감별, 최우선 |
| 모든 병변 recall | ≥ 0.5 | ≥ 0.40 (VL01), 0.5 는 TL01 추가 후 |
| 스크리닝 recall | ≥ 0.95 | 유지 (임계값으로 달성 가능) |

> ⚠️ 목표를 낮추는 게 아니라 **측정 가능한 값으로 바꾸는** 것입니다.
> 못 넘을 숫자를 걸어두면 "실패"로 읽히고, 그러면 판단이 흐려집니다.
> 0.70+ 는 TL01 을 붙이고 백본을 바꾼 뒤 다시 볼 숫자입니다.

---

## 실행 환경 메모

- 1단계 8 에폭 **57.3분**, 2단계 12 에폭 **37.5분** (배치 16, 워커 2)
- 74 img/s — ResNet50@224 AMP 의 계산 능력(150~200 img/s)의 절반 이하
- 원인 추정: 배치 크기 2의 거듭제곱 내림(29.4 → 16) + 워커 부족
- → `e7dab5b` 에서 배치 16 → 32, 워커 자동으로 수정

### ⚠️ 그 수정은 효과가 없었습니다 (2차 실행에서 확인)

| | 1차 (배치 16) | 2차 (배치 32) |
|---|---|---|
| 1단계 에폭당 | 57.3분 / 8 = **7.16분** | 87.4분 / 12 = **7.28분** |

**배치를 2배로 올렸는데 에폭 시간이 그대로입니다.** 즉 GPU 는 병목이 아니었고,
"계산 능력의 절반을 흘리고 있다" 는 1차 진단이 틀렸습니다. `persistent_workers`
는 이미 켜져 있었고, 워커 수는 `max(2, min(cpu_count-1, 8))` 인데 Colab 무료
T4 의 vCPU 가 2개라 **그대로 2** 였습니다 — 바뀐 게 없었던 것입니다.

추측을 멈추고 `src/bench.py` 를 만들었습니다. 셋을 따로 잽니다:
① 한 장 디코딩·변환 시간 ② DataLoader 실측 img/s ③ GPU 만(합성 텐서) img/s.
②와 ③ 중 작은 쪽이 실제 처리량이고, 어느 쪽인지에 따라 처방이 정반대입니다.
노트북 03 의 2번 끝에서 1분이면 나옵니다.

---

## 2차 실행 — 1단계만 (2026-08, `moderate` 파인튜닝 / 12 에폭)

| 항목 | 1차 (bb×0.1, 8ep) | 2차 (bb×0.3, 12ep) | 차이 |
|---|---:|---:|---:|
| AUROC | 0.8031 | **0.8100** | +0.0069 |
| recall 0.95 지점 precision | 0.640 | 0.649 | +0.009 |
| 임계값 | 0.3057 | 0.2822 | — |
| best 에폭 | 5~6 | **6** | — |
| 소요 | 57.3분 | 87.4분 | +30분 |

### 읽는 법: 파인튜닝 강도는 1단계의 병목이 아니었습니다

+0.0069 는 검증셋 7,751장에서 **부트스트랩 오차 범위(±0.01 수준) 안**입니다.
"백본을 3배 세게 움직였더니 좋아졌다"고 말할 수 없습니다.

학습 곡선이 그 이유를 보여줍니다:

```
[ 5] train 0.5505 | val 0.5628 | macroF1 0.7286  ★ best
[ 6] train 0.5422 | val 0.5645 | macroF1 0.7286  ★ best   ← 여기가 최고
[ 7] train 0.5312 | val 0.5666 | macroF1 0.7238           ← val loss 상승 시작
[11] train 0.5133 | val 0.5723 | macroF1 0.7246
조기 종료 — 5 에폭 동안 개선 없음
```

- val loss 가 epoch 5 에서 바닥, 이후 계속 상승 → **수렴 후 과적합 진입**
- 2단계와 정반대입니다 (2단계는 12에폭 전부 best 갱신 = 과소적합)
- 즉 **1단계는 이 설정에서 이미 한계**입니다. 에폭을 늘려도 안 됩니다.

### 1단계의 실제 병목은 해상도로 보입니다

`full` 크롭은 1920×1080 의 중앙 정사각(1080×1080)을 잘라 224px 로 줄입니다.
병변 면적은 원본의 0.47%(A1) ~ 3.08%(A6) 이므로, 224px 입력에서 병변은
**대략 20~40 픽셀**입니다. ResNet50 의 마지막 특징맵은 224 입력에서 7×7
(격자 한 칸이 32px) 이라, 작은 병변은 **격자 한 칸 안에 갇힙니다.**

→ 다음에 시도할 것: **1단계 입력 384px**. 병변이 ~35~70px 이 되고 특징맵은 12×12.
   크롭은 디스크에 512px 로 저장돼 있으니 업스케일 없이 384 까지 갑니다.
   비용은 (384/224)² = 2.9배 계산이지만, **GPU 가 병목이 아니라면 벽시계
   시간은 그만큼 안 늘어납니다.** 그래서 `bench.report` 를 먼저 돌립니다.

### 바로 적용할 것: patience

best 가 epoch 6 인데 patience=5 라 11까지 돌아 **43분을 더 썼습니다.**
1단계는 5~6 에폭에서 수렴하는 게 두 번 확인됐으므로 `early_stop_patience=3`
으로 낮추면 다음 실행에서 ~30분을 아낍니다. (2단계는 아직 수렴 전이라 그대로)

---

- 노트북 04(모델 6종)를 돌리기 전에 반드시 `git pull` 하세요
