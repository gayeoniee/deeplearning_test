# Dog skin lesion screening - dependencies
#
# NOTE: This file is intentionally ASCII-only.
# Windows pip reads requirements.txt with the system locale (cp949 on Korean
# Windows) and crashes with UnicodeDecodeError on any non-ASCII byte.
#
# Colab / Kaggle already ship torch, numpy, pandas and sklearn.
# In a notebook you only need:
#     !pip install -q timm imagehash pyarrow grad-cam
#
# Local preprocessing does NOT need torch. Minimal install:
#     pip install numpy pandas pillow scikit-learn imagehash pyarrow tqdm matplotlib
#
# Full reproduction:
#     pip install -r requirements.txt

# --- deep learning ---
torch>=2.2
torchvision>=0.17
timm>=1.0.9              # backbone factory (ConvNeXt-V2 / EVA-02 / SigLIP / DINOv3)

# --- data ---
numpy>=1.26
pandas>=2.1
pyarrow>=15.0            # parquet manifests
Pillow>=10.2
opencv-python-headless>=4.9
imagehash>=4.3           # perceptual hash for duplicate detection

# --- metrics ---
scikit-learn>=1.4        # StratifiedGroupKFold, metrics
scipy>=1.11

# --- plotting ---
matplotlib>=3.8
seaborn>=0.13

# --- explainability ---
grad-cam>=1.5

# --- utils ---
tqdm>=4.66
PyYAML>=6.0

# --- optional: experiment tracking (CSV logging works without it) ---
# wandb>=0.16
