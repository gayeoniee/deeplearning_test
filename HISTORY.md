# HISTORY — 지나온 길

> 시간순 기록입니다. **지금 상태**는 [`STATUS.md`](STATUS.md),
> 작업 규칙과 결론표는 [`CLAUDE.md`](CLAUDE.md) 에 있습니다.
> 각 STEP 의 전체 실측은 [`docs/results/`](docs/results/) 에 있고,
> 여기 있는 건 **왜 그 결정을 했는지** 입니다.

## 한눈에

| STEP | 날짜 | 무엇을 | 결과 |
|---|---|---|---|
| 4A | 08-21 | 첫 실측 기준선 · 해상도 224→384 | 배율 하락 8~9%p 감소 ✅ |
| 4B | 08-21 | 증강 7종 스윕 | 전부 효과 없음. `zoom_both` 는 악화 ❌ |
| 4C | 08-22 | 크롭 m1.5 vs m2.5 | **m2.5 채택** ✅ |
| 4D | 08-22 | m2.5 재기준선 | 배율 −9.9%p / 위치 −4.4%p ✅ |
| 5 | 08-22 | holdout 첫 개봉 · 보정 · CAM · 촬영 가이드 | **1단계가 holdout 에서 안 됨** ❌ |
| 6 | 08-23 | 1단계 백본×증강 2×2 | 화질 지름길 발견 → effnetv2_s + photometric ✅ |
| 7 | 08-23 | 1단계 교체 후 재기준선 (val) | 같은 recall 에서 헛알림 −1.9%p ✅ |
| 8 | 08-23 | holdout 재개봉 | 좋아졌지만 목표 미달 ◐ |
| 9 | 08-23 | 2단계 백본 비교 | **기준선 미수렴 — 판정 무효** ❌ |
| 9-A | 08-23 | 1단계 입력 full vs f320 | **f320 압승** ✅ 이 프로젝트 최대 개선 |
| 10 | 08-24 | f320 으로 holdout 재개봉 | **사전 기준 3개 전부 통과** ✅ |
| 12 | 08-25 | 2단계 백본 판 A (STEP 9 재실행) | **convnextv2_base** +0.088 ✅ |
| 13 | 08-25 | 2단계 백본 판 B (어텐션 계열) | swinv2_base +0.069 ◐ / siglip2 미수렴 ❌ |
| 11 | 08-25 | 오답이 어디로 가는지 (학습 없음) | **A2 가 오류의 중심** — 헛알림의 55.8% ★ |
| — | 08-26 | 멘토링 — 출력에서 병변 이름 제거 | **이름 대신 확률 분포만** ★ 설계 확정 |
| — | 08-26 | 앱 연동 + 가이드 프레임을 bbox 로 | 서빙 크롭이 학습과 **0px 차이** ✅ |
| — | 08-26 | 릴리스 그대로 서빙 · 안구는 범위 밖 | 다음 주 백본만 교체 ◐ |

---

## STEP 4A — 첫 기준선과 해상도 (2026-08-21)

첫 실측: 1단계 AUROC 0.8031 / 2단계 macro-F1 0.4865
(conservative bb×0.1 / 1단계 8ep / 2단계 12ep / 배치 16).

파인튜닝 강도를 백본 lr ×0.1 → **×0.3(`moderate`)** 으로 올려 macro-F1 +0.065.

멀리서 찍은 사진(축소)에서 무너지는 문제입니다. 증강으로 두 번 실패한 뒤
**입력 해상도 224 → 384** 로 크게 줄였습니다.

| | 224px | 384px | |
|---|---:|---:|---|
| 1단계 하락 | 17.2% | **9.1%** | ✅ 목표 달성 |
| 2단계 하락 | 29.5% | **25.9%** | ❌ 남음 |

점수도 같이 올랐습니다 (AUROC 0.8103→0.8192, macro-F1 0.5466→0.5697).
견고성을 얻으려고 정확도를 판 게 아닙니다.

**★ 최악 조건은 단계마다 다릅니다.** (한 번 잘못 적었다가 고친 부분입니다)

| 조건 | 1단계 | 2단계 |
|---|---:|---:|
| 0.5x 축소 | 0.6952 | **0.4066** ← 2단계 최악 |
| 1x 원본 | 0.7474 | 0.5489 |
| 2x 확대 | **0.6790** ← 1단계 최악 | 0.4632 |

1단계는 확대에서, **2단계는 여전히 축소에서** 무너집니다.
1단계 결과를 2단계에 옮겨 적으면 안 됩니다.

**해상도 카드는 거의 소진됐습니다** — 크롭 짧은 변 중앙값이 242px 이고
**384 미만을 74.6% 확대해서 쓰는 중**입니다. 512 로 올려도 없는 디테일은 안 생깁니다.

**증강 축은 닫혔습니다** (2026-08-21, 7종 한 실행 비교). 6종이 default 와 구분되지
않았고, 유일하게 잡음을 넘은 신호는 `zoom_both` 가 **+4.0%p 악화**시킨 것이었습니다.
`photometric` 은 macro-F1 을 0.03 깎았습니다 — 흐림이 지운 건 화질 지름길이 아니고
병변 질감이었습니다. 전체 기록:
[`docs/results/STEP4B_증강스윕_실측.md`](docs/results/STEP4B_증강스윕_실측.md)

→ **남은 배율 문제는 배포 설계로 넘깁니다.** 배율별 점수표에서 1x 가 최고이므로,
그 구도로 찍도록 유도하면 최악 조건 자체가 안 생깁니다.
`robust.usable_range()` 가 그 구간을 학습 없이 뽑습니다 (노트북 05 의 5-b).


전체 기록: [`docs/results/STEP4A_베이스라인_실측.md`](docs/results/STEP4A_베이스라인_실측.md)

---

## STEP 4B — 증강 축을 닫았습니다 (2026-08-21)

7종을 한 실행에서 비교했습니다. 6종이 default 와 구분되지 않았고, 유일하게
잡음을 넘은 신호는 `zoom_both` 가 **+4.0%p 악화**시킨 것이었습니다.

`photometric` 은 2단계 macro-F1 을 0.03 깎았습니다 — 흐림이 지운 건 화질
지름길이 아니고 **병변 질감**이었습니다. (⚠️ 1단계에서는 정반대입니다 — STEP 6)

전체 기록: [`docs/results/STEP4B_증강스윕_실측.md`](docs/results/STEP4B_증강스윕_실측.md)

---

### 크롭 창 크기 — ✅ m2.5 채택 (마지막 전처리 축)

크롭 짧은 변 중앙값이 242px 라 **74.6% 를 확대해서 쓰고 있었고**, A1 이 가장 심했습니다
(원본 박스 99px → m1.5 에서 **2.6배** 확대 / m2.5 에서 1.6배).

풀 데이터·같은 실행에서 비교한 결과:

| 크롭 | macro-F1 | 배율 하락 | A1 recall |
|---|---:|---:|---:|
| m1.5 | 0.5536 | 23.2% | 0.560 |
| **m2.5** | 0.5395 | **18.8%** | **0.622** |
| | −0.014 (잡음) | **−4.4%p** | **+0.063** |

**★ 축소는 증강 말고 크롭으로 잡혔습니다.** 같은 목표를 두 방법으로 시도했는데
방향이 정반대였습니다:

| 방법 | 하는 일 | 결과 |
|---|---|---:|
| `zoom_both` 증강 | 자른 이미지를 **줄이고 여백으로 채움** (정보를 버림) | +4.0%p 악화 |
| **m2.5 크롭** | **더 넓은 실제 영역**을 자름 (정보를 더함) | **−4.4%p 개선** |

→ "작게 보이는 사진에 강해지려면, **작게 만들지 말고 넓게 찍은 걸 보여줘라.**"

macro-F1 −0.014 는 **잡음입니다** — 같은 두 모델을 교란검사 1x 로 다시 재면 부호가
뒤집힙니다 (m1.5 0.5230 vs m2.5 0.5257). 잡음 밖의 이득 하나, 잡음 안의 손해 하나.

전체 기록: [`docs/results/STEP4C_크롭비교_실측.md`](docs/results/STEP4C_크롭비교_실측.md)


---

## STEP 4D — m2.5 재기준선 (2026-08-22, 2시간 18분)

크롭을 바꾼 뒤 전부 한 실행에서 다시 쟀습니다.

| | 지표 | m2.5 | 이전(m1.5) | 목표 | |
|---|---|---:|---:|---:|---|
| 1단계 | AUROC | 0.8155 | 0.8192 | 0.80 | ✅ |
| 2단계 | macro-F1 | 0.5313 | 0.5697 | 0.55~0.65 | ⚠️ 하단 미달 |
| 파이프라인 | 스크리닝 recall | 0.9501 | 0.9501 | ≥0.95 | ✅ |
| 파이프라인 | 헛알림 | 51.4% | 52.9% | — | 감수 |
| 2단계 | 배율 교란 하락 | **16.0%** | 25.9% | <15% | ◐ 구분 불가 |
| 2단계 | 위치 교란 하락 | **16.2%** | 20.6% | <15% | ◐ 구분 불가 |

★ **크롭 하나로 두 방향이 같이 잡혔습니다** — 배율 −9.9%p, 위치 −4.4%p.
위치까지 좋아진 건 예상 밖이었습니다. 넓게 자르면 병변이 정중앙에만 있지
않게 되어서입니다.

⚠️ macro-F1 은 조금 내려갔습니다. m1.5 두 번(0.5697/0.5536) vs m2.5 두 번
(0.5395/0.5313) — 네 번 다 m1.5 가 위라, "잡음" 으로 읽으면 안 되고
**작지만 실재하는 대가**(−0.027 쯤)로 읽는 게 맞습니다. 견고성 이득이
잡음의 3배라 m2.5 유지입니다.

전체 기록: [`docs/results/STEP4D_m2.5_재기준선_실측.md`](docs/results/STEP4D_m2.5_재기준선_실측.md)

---

## STEP 5 — holdout 첫 개봉 (2026-08-22, 24.6분)

보정·거절·Grad-CAM·촬영 가이드를 한 번에 확인하고 holdout 을 처음 열었습니다.

| | val | holdout | |
|---|---:|---:|---|
| 1단계 AUROC | 0.8143 | **0.7412** | ❌ 목표 0.80 미달 |
| 2단계 macro-F1 | 0.5313 | 0.5582 | ✅ 오히려 상승 |
| 스크리닝 recall | 0.950 | 0.9303 | |
| 최종 7종 macro-F1 | — | **0.4432** | |

**2단계는 개체가 바뀌어도 좋아지는데 1단계만 안 됐습니다.** 여기서부터
STEP 10 까지가 그 하나를 고치는 이야기입니다.

같이 나온 것: 거절(abstention)로는 정확도를 크게 못 올림 (절반 거절해도
오답 40.9% → 29.4%). Grad-CAM lift 1.82, 78% 가 우연 이상 — 배경을 보고
있지는 않았습니다.

전체 기록: [`docs/results/STEP5_평가_보정_촬영가이드_실측.md`](docs/results/STEP5_평가_보정_촬영가이드_실측.md)

---

### ✅ STEP 6 — 1단계 원인 찾음 (2026-08-23)

**화질 지름길이 실재했습니다.** 사진을 흐리게 하면 기존 1단계가 반토막 났습니다
(macro-F1 0.7430 → 0.3316, balanced-acc 0.5000 = 동전 던지기).
정상 사진 선명도 중앙값이 50, 병변이 274 라 **"흐리면 정상"** 을 배운 것입니다.

2×2 실측 (서브셋 55% · 12에폭):

| 백본 | 증강 | val AUROC | 흐림 하락 |
|---|---|---:|---:|
| effnetv2_s | default | **0.8359** | 47.6% |
| **effnetv2_s** | **photometric** | 0.8284 | **14.0%** ← 채택 |
| resnet50 | default | 0.8116 | 55.4% |
| resnet50 | photometric | 0.8145 | 16.9% |

**두 축 다 사전 등록 기준을 넘었습니다** — 증강 −38.5%p, 백본 +0.0244.

⚠️ **val AUROC 최고(effnetv2_s/default 0.8359)를 안 골랐습니다.** 흐림 하락 47.6%
로 지름길에 기대고 있고, AUROC 차이 0.0075 는 잡음 안입니다. **val 로 고르다
holdout 에서 무너진 게 STEP 5 의 실패**라 같은 실수를 반복하지 않습니다.

전체 기록: [`docs/results/STEP6_1단계_2x2_실측.md`](docs/results/STEP6_1단계_2x2_실측.md)


---

### ✅ STEP 7 — 1단계 교체 후 재기준선 (2026-08-23)

풀 데이터로 학습 (1단계 18에폭 / 2단계 25에폭 / 2시간 40분). **val 기준**:

| | STEP 4D | **STEP 7** | |
|---|---:|---:|---|
| 1단계 AUROC | 0.8155 | **0.8334** | +0.018 (잡음 밖) ✅ |
| 1단계 precision | 0.650 | **0.664** | |
| 2단계 macro-F1 | 0.5313 | 0.5457 | 잡음 (2단계는 안 바꿈) |
| 헛알림 | 51.4% | **49.5%** | 같은 recall 에서 −1.9%p ✅ |
| 스크리닝 recall | 0.9501 | 0.9501 | 설계값 |

**같은 recall 을 유지하면서 헛알림만 줄었습니다** — 1단계가 실제로 더 잘 가릅니다.
전체 기록: [`docs/results/STEP7_1단계교체_재기준선_실측.md`](docs/results/STEP7_1단계교체_재기준선_실측.md)


---

### ◐ STEP 8 — holdout 재개봉: 좋아졌지만 목표 미달 (2026-08-23, 24.5분)

STEP 7 모델로 holdout 을 다시 열었습니다. **사전에 정한 세 기준 중 하나만 통과.**

| 확인할 것 | STEP 5 | **STEP 8** | 목표 | |
|---|---:|---:|---:|---|
| 1단계 holdout AUROC | 0.7412 | **0.7666** | ≥0.80 | ❌ |
| 스크리닝 recall | 0.9303 | **0.9158** | ≥0.95 | ❌ **더 나빠짐** |
| 최종 7종 macro-F1 | 0.4432 | **0.4594** | 상승 | ✅ |
| 헛알림 | 67.6% | **59.2%** | — | −8.4%p |

**★ AUROC 는 올랐는데 recall 은 내렸습니다.** 두 숫자가 다른 걸 재기 때문입니다:

| 문제 | 정체 | 고치는 값 |
|---|---|---|
| ① 변별력 부족 | holdout AUROC 0.7666 < 0.80 | 모델·입력을 바꿔야 함 (비쌈) |
| ② **임계값이 안 옮겨감** | val 0.9501 → holdout 0.9158 | 임계값 **선정 절차** (재학습 불필요) |

임계값 0.2706 은 val 에서 recall 0.9501 이 되도록 고른 값인데, holdout 에서는 0.9158 입니다.
holdout 에서 0.95 를 맞추는 값은 0.2167 (차이 0.0539) — ⚠️ **이 값을 쓰면 holdout 이 오염됩니다.**

**A7(정상) recall 0.408** 이 헛알림의 정체입니다. 정상 10장 중 6장을 이상으로 보냅니다.

2단계는 안 바꿨고 holdout 0.5582 → 0.5545 로 잡음 안. **바뀐 건 1단계뿐**임이 확인됩니다.

전체 기록: [`docs/results/STEP8_1단계교체_holdout_실측.md`](docs/results/STEP8_1단계교체_holdout_실측.md)

→ 카드 2장을 뽑았고, **2번을 돌려서 답이 나왔습니다**:
1. ~~임계값에 여유 두기~~ — **접었습니다.** recall 0.95 는 손잡이지 성적이 아닙니다.
   임계값을 낮춰 맞춰봐야 헛알림만 늘어납니다 (holdout 기준 틀린 것 2,482 → 2,582장).
   진짜 성적은 **"recall 0.95 를 사는 데 헛알림을 얼마 내나"** 입니다
2. ✅ **f320 재크롭** → STEP 9-A 에서 **큰 폭으로 이겼습니다** (아래)


---

## STEP 9 — 2단계 백본 비교, 판정 무효 (2026-08-23, 3시간 22분)

2단계를 처음으로 비교했습니다 (resnet50 / effnetv2_s / convnextv2_base ·
서브셋 55% · 12에폭). **결론이 안 났습니다.**

| 백본 | macro-F1 | 배율하락 | 수렴 |
|---|---:|---:|---|
| convnextv2_base | 0.5736 | 20.3% | ✅ (epoch 6) |
| effnetv2_s | 0.5445 | 26.7% | ❌ 마지막 에폭이 최고 |
| **resnet50** (기준선) | 0.4692 | 12.2% | ❌ **마지막 에폭이 최고** |

**기준선이 수렴하지 않았습니다.** 덜 학습된 모델은 배운 게 적어 교란 검사에서
잃을 것도 적습니다 — resnet50 배율 하락 12.2% 는 이 프로젝트의 다른 풀 학습
실행(23.8~25.0%)과 전혀 안 맞습니다. 그 낮은 값이 기준선이 되니 다른 백본이
부당하게 나빠 보였습니다.

고친 것 두 가지 (04 재실행 전에 못 박음):
* 04 의 `EPOCHS` 12 → **25** (조기 종료가 끊게)
* `backbone_report` 의 배율 하락 탈락 문턱 8%p → **12%p**
  (`SCALE_DROP_REJECT_PP`) — 우리가 잰 잡음이 9.6%p 라 8%p 는 잡음만으로
  걸립니다. 5~12%p 는 **◐ 구분 불가**로 찍습니다
* `backbone_report` 가 기준선 미수렴 시 경고를 먼저 찍도록

전체 기록: [`docs/results/STEP9_2단계_백본비교_실측.md`](docs/results/STEP9_2단계_백본비교_실측.md)

---

### ★ STEP 9-A — 1단계 입력을 f320 으로 (2026-08-23, 1시간 53분)

1단계 모델·증강은 STEP 6·7 채택값으로 고정하고 **입력만** 바꿔 비교했습니다.

| 크롭 | val AUROC | 흐림 하락 | precision@recall.95 | 수렴 |
|---|---:|---:|---:|---|
| full | 0.8272 | 11.1% | 0.661 | ✅ |
| **f320** | **0.9477** | **3.1%** | **0.793** | ✅ |
| 차이 | **+0.1205** | **−8.0%p** | **+0.132** | |

**세 지표가 전부 잡음을 훨씬 넘어 같은 방향으로 좋아졌습니다.**

믿을 근거: `full` 쪽이 STEP 7(0.8334)·STEP 6(흐림 14.0%)을 잡음 안에서 재현했습니다.
비교 세팅이 멀쩡하다는 뜻이고, 두 조건 다 조기 종료로 수렴까지 확인했습니다
(STEP 9 의 기준선 미수렴 문제가 여기서는 없습니다).

`full` 은 1920×1080 중앙 정사각이라 병변이 22×22px 로 들어갑니다. 384px 로 줄이면
사실상 몇 픽셀입니다. **못 보던 걸 보게 된 것**이지 새 지름길이 아닙니다 —
지름길이었다면 흐림 하락이 늘었어야 하는데 오히려 11.1% → 3.1% 로 줄었습니다.

전체 기록: [`docs/results/STEP9A_1단계_f320_실측.md`](docs/results/STEP9A_1단계_f320_실측.md)


---

### ✅ STEP 10 — f320 으로 holdout 재개봉, 사전 기준 3개 전부 통과 (2026-08-24)

노트북 `06` 으로 두 단계를 다시 학습하고 그대로 holdout 까지 갔습니다
(한 세션 3시간 34분, 인계 없음).

| 확인할 것 | STEP 8 | **STEP 10** | 목표 | |
|---|---:|---:|---:|---|
| 1단계 holdout AUROC | 0.7666 | **0.9304** | ≥0.80 | ✅ |
| 헛알림 | 59.2% | **33.3%** | 줄어야 함 | ✅ −25.9%p |
| 최종 7종 macro-F1 | 0.4594 | **0.5325** | 올라야 함 | ✅ +0.073 |
| 스크리닝 recall | 0.9158 | **0.9458** | | |
| A7(정상) recall | 0.408 | **0.667** | | |

**★ 임계값 전이 문제가 같이 풀렸습니다.** STEP 8 에서 문제를 ① 변별력 부족
② 임계값이 안 옮겨감 으로 나눠 적었는데, ①만 고쳤더니 ②도 따라왔습니다:

| | STEP 8 | **STEP 10** |
|---|---:|---:|
| val 임계값을 holdout 에 적용한 recall | 0.9158 | **0.9458** |
| holdout 최적 임계값과의 차이 | 0.0539 | **0.0126** |

임계값에 여유를 두는 카드를 접은 판단이 맞았습니다. **잘 가르는 모델은 임계값이
따라옵니다.** 못 가르는 모델의 임계값을 억지로 낮추면 헛알림만 늘어납니다.

1단계가 깎아먹는 양도 줄었습니다 — 2단계만 0.5645 vs 파이프라인 0.5325 로
차이 **0.032** (STEP 8 은 0.095).

남은 것: A4 recall 0.391 / A1 precision 0.311 / A7 recall 0.667
(정상 사진 3장 중 1장이 아직 병원행).

전체 기록: [`docs/results/STEP10_확정재학습_holdout_실측.md`](docs/results/STEP10_확정재학습_holdout_실측.md)


---

## ✅ STEP 12 — 2단계 백본 판 A, STEP 9 재실행 (2026-08-25, 3시간 21분)

에폭을 12 → 25 로 올려 셋 다 수렴시켰습니다.

| 백본 | macro-F1 | 기준선대비 | 배율하락 | 분 | 수렴 |
|---|---:|---:|---:|---:|---|
| **convnextv2_base** | **0.5744** | **+0.0882** | 18.4% | 117 | ✅ epoch 5 |
| effnetv2_s | 0.5608 | +0.0746 | 24.4% | 34 | ✅ epoch 9 |
| resnet50 (기준선) | 0.4862 | — | 15.1% | 30 | ✅ epoch 10 |

**STEP 9 를 무효로 본 판단이 실측으로 확인됐습니다.** 에폭만 바꿨을 때:

| 백본 | macro-F1 | 배율 하락 | STEP 9 의 수렴 |
|---|---:|---:|---|
| convnextv2_base | 0.5736 → 0.5744 (+0.0008) | 20.3% → 18.4% | ✅ 이미 수렴 |
| effnetv2_s | 0.5445 → 0.5608 (+0.0163) | 26.7% → 24.4% | ❌ |
| **resnet50** | 0.4692 → 0.4862 (+0.0170) | **12.2% → 15.1%** | ❌ |

이미 수렴했던 convnextv2_base 만 안 움직였고(+0.0008), 기준선의 배율 하락이
**12.2% → 15.1%** 로 올라갔습니다 — "덜 배운 기준선은 교란 검사에서 잃을 게
적어 하락이 낮게 나온다" 던 예측 그대로입니다. 그 낮은 12.2% 때문에 STEP 9
에서 convnextv2_base 가 +8.1%p 로 탈락했었는데, 이번엔 **+3.3%p** 입니다.

문턱을 8%p → 12%p 로 올린 것(04 를 돌리기 **전에** 결정)이 실제로 갈랐던 건
effnetv2_s(+9.3%p) 하나입니다. 우리가 잰 잡음이 9.6%p 인 이상 9.3%p 를
"탈락" 이라고 부를 근거가 없어서 **◐ 구분 불가**로 적었습니다.

⚠️ convnextv2_base 는 resnet50 의 **5.6배 느립니다** (에폭당 10.6분 vs 1.9분,
16 img/s vs 113 img/s, VRAM 8.9GB). 풀 데이터면 에폭당 ~19분입니다.

전체 기록: [`docs/results/STEP12_2단계_백본_판A_실측.md`](docs/results/STEP12_2단계_백본_판A_실측.md)

---

## ◐ STEP 13 — 2단계 백본 판 B, 어텐션 계열 (2026-08-25, 2시간 34분)

| 백본 | 해상도 | macro-F1 | 기준선대비 | 배율하락 | 분 | 수렴 |
|---|---:|---:|---:|---:|---:|---|
| **swinv2_base** | 256 | **0.5718** | **+0.0694** | 21.3% | 71 | ✅ |
| resnet50 (기준선) | 384 | 0.5024 | — | 12.6% | 48 | ✅ |
| siglip2_base | 256 | 0.3889 | −0.1134 | 31.4% | 17 | ❌ 미수렴 |

**siglip2 는 진 게 **아닙니다** — "못 재봤다" 가 맞습니다.** train loss·val loss 가 매 에폭
내려가는 중인데, macro-F1 이 epoch 1 에 튀어 그게 best 로 잡히고 patience 5 가
6에폭에 끊었습니다. 7에폭 train loss 감소폭이 resnet50 −0.48 / swinv2 −0.44 /
siglip2 **−0.16** 으로 3배 느립니다 — 학습률이 안 맞는다는 신호입니다.
STEP 9 와 같은 종류의 무효로 처리합니다.

**★ 같은 설정 resnet50 이 두 판에서 0.4862 / 0.5024 로 나왔습니다** (배율 하락
15.1% / 12.6%). 설정도 시드도 같은데 GPU 연산이 결정론적이지 않아 생기는 차이라,
이게 실행 간 잡음의 **직접 측정치**입니다. 우리 밴드(±0.02 / ±5%p) 안입니다.

판 A 후보와의 맞대결은 절대값 차이 0.0026, 기준선 대비 차이 0.0188 로 **둘 다 잡음
안**입니다. 점수로는 구분이 안 되고, swinv2 가 1.6배 싸고 convnextv2 가 배율
견고성에서 낫습니다. 확정하려면 한 판에서 나란히 돌리거나 풀 학습 holdout 이
필요합니다.

전체 기록: [`docs/results/STEP13_2단계_백본_판B_실측.md`](docs/results/STEP13_2단계_백본_판B_실측.md)

---

## ★ STEP 11 — 오답이 어디로 가는지 (2026-08-25, 10분)

번호는 07 노트북이 붙인 것이라 STEP 12·13 보다 뒤에 돌렸습니다. 학습 없이
STEP 10 가중치로 추론만 다시 돌려 혼동행렬의 **실제 칸**을 뽑았습니다.

**주변합 역산이 A1 을 과대평가했습니다.** STEP 10 직후 "헛알림의 89% 가 A1·A2 로
간다" 고 적었는데, 실제 칸은 다릅니다:

| 클래스 | 역산 (순 흐름) | 실제 칸 | 비중 |
|---|---:|---:|---:|
| **A2 비듬·각질** | +558 | **512** | **55.8%** |
| **A3 태선화·색소침착** | +168 | **184** | 20.0% |
| A1 구진·플라크 | **+372** | 148 | 16.1% |

A1 이 과예측되는 건 맞았지만 원인이 정상 사진은 **아니었습니다** — 병변 간 혼동이었습니다
(A3→A1 176장, A2→A1 116장). 순 흐름은 그 둘을 못 가릅니다.
→ **주변합 역산으로 "어느 칸" 을 말하면 안 됩니다.**

**A2 가 오탐(FP)도 미탐(FN)도 1위입니다.** 헛알림 918장 중 512장(55.8%)이
A2 로 가고, 놓친 병변 196장 중 109장(55.6%)이 A2 였습니다. 병변끼리 1위 혼동도
A3→A2(216장)입니다. A2 는 병변 학습 표본의 33.3% 로 최다 클래스이기도 합니다.

⚠️ **왜 A2 에 몰리는지는 아직 측정 안 했습니다.** 라벨 정의 / 표본 수(A2 가 병변의
33.3% 로 최다) / 시각적 유사성 중 무엇인지는 사진 판독과 임상 확인이 있어야
갈립니다. A5(6장)·A6(12장)은 정상 사진이 넘어온 게 거의 없습니다.

holdout 도 순서가 같습니다 (A2 749 / A3 204 / A1 172).

전체 기록: [`docs/results/STEP11_오답분석_실측.md`](docs/results/STEP11_오답분석_실측.md)

---

## ★ 멘토링 — 출력에서 병변 이름을 뺐습니다 (2026-08-26)

학습이 아니라 **설계** 결정입니다. STEP 11~13 의 숫자가 모여 결론이 났습니다.

### 멘토 의견

> 2단계에서 병변 이름을 단정하지 말고, 1단계로 정상/이상만 가른 뒤
> 2단계 **퍼센테이지만** 넘겨라. 자세한 건 판단 불가니 진료를 권하는 걸로.

### 받아들인 이유 — 세 숫자

**1. 이름이 절반 넘게 틀립니다.** holdout 에서 "병원 가보세요" 로 보낸 4,478건을
뜯어보면:

| | 건수 | 비중 |
|---|---:|---:|
| 병변이고 이름도 맞음 | 1,944 | 43.4% |
| 병변인데 **이름이 틀림** | 1,301 | 29.1% |
| **애초에 병변이 아님** (헛알림) | 1,233 | 27.5% |
| **화면의 이름이 틀린 합계** | **2,534** | **56.6%** |

**2. 틀리는 방향이 나쁩니다.** val 에서 A6(결절·종괴)를 A2(비듬)로 부른 게
56장, A3(태선화)로 부른 게 47장입니다. 종괴를 비듬이라고 하면 보호자는
**병원을 미룹니다** — 우리가 가장 피하려던 미탐 비용이 그대로 발생합니다.

**3. 백본으로는 못 넘습니다.** STEP 12·13 에서 CNN(convnextv2_base 0.5744)과
어텐션(swinv2_base 0.5718)이 **0.0026** 차이로 겹쳤습니다. 잡음(±0.02) 안입니다.
구조가 완전히 다른 두 모델이 같은 벽에 부딪혔다는 건 **모델이 아니라 데이터가
한계**라는 뜻입니다. 풀 학습으로 올려도 ~0.61 정도고, 그때도 화면의 이름
정확도는 54.3% 수준입니다.

### 거절로 덮으려 해봤지만 안 됐습니다

holdout 병변만 놓고 거절 곡선을 그려보면:

| 커버리지 | 오답률 |
|---:|---:|
| 100% | 40.4% |
| **18.2%** | **20%** ← 오답률 20% 를 사려면 여기까지 |

다섯 장 중 네 장을 "판단 못 하겠다" 로 물러서야 20% 가 됩니다. 게다가 이 곡선은
헛알림 1,233건을 **빼고** 그린 것이라 실제는 더 나쁩니다.

### 그럼 2단계를 버리나 — 아닙니다

**모델은 그대로 씁니다. 표현만 바꿉니다.** 분포 자체는 참고 정보로서 값이 있고,
여섯 개를 **다 보여주면** "확실하지 않다" 가 눈에 보입니다. 1등만 뽑아 굵게 쓰면
사람은 그걸 답으로 읽습니다 — 그게 문제였지 확률이 문제가 아니었습니다.

`src/infer.compose_screening_message()` 가 새 형식이고,
`tests/test_screening_message.py` 30개가 규칙을 못 박습니다:
1등 굵게 쓰기 금지 · 여섯 개 전부 노출 · `URGENCY_HINT` 금지 ·
"판단할 수 없습니다" 를 숫자 **위**에 · 깎기 전 원본 확률 사용.

**이름을 되돌리는 조건**도 같이 적어뒀습니다 — macro-F1 이 잡음 밖에서 오르고,
**커버리지 50% 이상에서 오답률 ≤ 20%** 가 되면 재검토합니다.

### 부수적으로 열린 문제

새 문구가 "이상 가능성 62%" 를 보호자에게 **직접 보여줍니다.** 그런데
온도 보정은 **2단계에만** 걸려 있습니다 (T=1.1063). 1단계는 한 번도 안 했습니다.
→ 06 재실행 때 1단계 보정 + ECE 를 같이 잽니다.

---

## ✅ 서빙 크롭 어긋남 — 가이드 프레임이 답이었습니다 (2026-08-26)

앱 연동을 만들다 제일 큰 위험으로 적어둔 것이 이거였습니다:

> 학습은 라벨 bbox 를 중심으로 잘랐는데 **보호자 사진에는 bbox 가 없습니다.**
> 그래서 서빙은 화면 중앙을 씁니다. 이 대응은 실측된 적이 없습니다.

**답은 이미 우리 계획 안에 있었습니다.** 촬영 가이드 프레임을 카메라에 띄우기로
한 게 STEP 10 부터였는데(밴드도 실측해 뒀고), 그 네모를 **보여주기만 하고 버리고**
있었습니다. 네모를 bbox 로 받으면 됩니다.

### 무엇을 했나

`agent.screen(image, box=...)` 로 정규화 `[x, y, w, h]` 를 받고,
넘어온 사진의 짧은 변을 1080 으로 맞춘 뒤(`to_train_space` — 원본이 전부
1920×1080 이었으므로 그 뒤로는 학습과 같은 픽셀 공간),
**학습이 쓰는 그 함수**(`crop.crop_window()`)를 부릅니다. 재구현하지 않습니다.

### 실측

같은 구도를 1920×1080(학습)과 4032×2268(휴대폰)로 찍었다고 놓고 비교:

| | 학습 창 | 서빙 창 | 차이 |
|---|---|---|---|
| 1단계 `f320` | (800, 380, 1120, 700) | (800, 380, 1120, 700) | **0px** |
| 2단계 `m2.5` | (785, 365, 1135, 715) | (785, 365, 1135, 715) | **0px** |

### 두 단계의 사정이 다릅니다 (이게 핵심)

| | bbox 를 어떻게 쓰나 | 네모가 부정확하면 |
|---|---|---|
| 1단계 `f320` | **중심만.** 창 320px 고정 | 거의 무관 |
| 2단계 `m2.5` | **크기.** 긴 변 × 2.5 | 배율이 통째로 달라짐 |

네모를 가로 +6%p, 세로 +10%p 헐렁하게 잡아 보니:

```
1단계 창  320px → 320px   (안 변함)
2단계 창  350px → 620px   (1.8배)
```

2단계는 이게 그대로 배율 교란입니다. convnextv2_base 의 배율 하락 18.4% 를
이미 재 뒀으니, 네모가 헐렁할수록 그쪽으로 갑니다.

→ **가이드 프레임은 고정 크기면 안 됩니다.** 끌고 늘릴 수 있어야 하고, 늘릴 때
**중심이 고정**돼야 합니다 (모서리 기준으로 늘렸더니 크기를 맞추는 동안 가운데
정렬이 풀려서 게이트에 걸렸습니다 — 데모에서 실제로 당했습니다).

### 덤 — 촬영 가이드가 드디어 쓰입니다

네모가 있으면 밴드(가로 28~68%, 중앙 10% 이내)를 **추론 전에** 검사하고,
밖이면 모델에 넣지 않고 재촬영으로 돌려보냅니다. STEP 10 에서 `usable_range()`
로 재 놓고 문서에만 있던 값이 처음으로 코드에서 쓰입니다.

### 남은 것 — 그리고 여기서 한 번 잘못 적었습니다

크롭 **구성**은 같아졌지만 네모의 **분포**는 다릅니다 — 라벨러는 답을 알고
병변에 딱 맞춰 그렸고, 사용자는 모르는 채 대충 맞춥니다.

처음에 이걸 "**앱으로 찍은 사진 100장에 수의사 라벨**" 로 적었는데,
지적을 받고 보니 **실행할 수 없는 계획**이었습니다. 수의사도 앱 사용자도
지금 없고 이 프로젝트 안에서는 앞으로도 안 생깁니다. 못 하는 항목을 점검표에
두면 위험이 관리되는 것처럼 **보이기만** 합니다.

**다시 보니 수의사가 필요 없는 문제였습니다.** 수의사는 *병명*을 붙이는
사람인데, 재야 할 건 *네모를 얼마나 잘 그리는가* 입니다. 그 정답(bbox)은
이미 매니페스트 안에 있고 병명도 이미 압니다.

→ `tools/box_error.py`. AI Hub 사진을 띄우고 사람이 네모를 맞추면 정답과
비교합니다(정답은 넘긴 **뒤에** 보여줍니다). 한국 PC 에서 `uv sync` 만으로
돌고 30분이면 끝납니다.

### 덤 — 밴드를 뒤집으니 앱 사양이 나왔습니다

`ZoomView` 는 원래 "멀리서/가까이서 찍은 것처럼" 만드는 교란인데, **네모를
크게/작게 그린 것과 같은 것**입니다. 그러면 STEP 10 의 배율 밴드를 뒤집기만
해도 "네모를 얼마나 정확히 그려야 하는가" 가 나옵니다:

| | 배율 밴드 | ⇒ 네모 크기 오차 |
|---|---|---|
| 권장 (하락 5% 이내) | 0.85 ~ 1.4× | **0.71 ~ 1.18배** |
| 허용 (하락 10% 이내) | 0.7 ~ 1.7× | **0.59 ~ 1.43배** |

1.5배로 그리면 이미 허용 밖입니다. 새로 잰 게 아니라 **이미 있던 곡선을
다른 방향으로 읽은 것**뿐입니다.

---

## 2026-08-26 (오후) — 디스크 정리 · 데이터 확장 판정 · 헛알림 원인 추적

GPU 할당량이 4시간 26분밖에 안 남아서 06 재실행(5.1h)을 못 돌리는 날이었습니다.
그 시간에 **GPU 없이 할 수 있는 것**을 골라 했고, 결과적으로 안 될 길 세 개를
지웠습니다.

### 1. 크롭·원본 정리 — 5.3GB 확보 (66.5 → 72.2GB)

`tools/crops.py --prune` / `--prune-raw`. 원본은 zip 과 `.downloaded_keys` 만
남기고 압축 해제본을 지웁니다. 크롭은 zip 안에서 직접 읽으므로(`--mode zip`)
해제본은 아무도 안 읽습니다.

**한 번 틀렸습니다** — 처음엔 `data/raw` 의 **최상위만** 보고 "폴더 안에 zip 이
있으면 통째로 지킴" 으로 짰습니다. 그런데 실제 배치는 zip 이 청크 폴더 **안**에
있습니다 (`prepare_local.py:115` — `raw = env.data_root() / name`):

```
data/raw/VL01/VL01.zip                          ← 지킴
data/raw/VL01/152.반려동물_피부질환_데이터/…      ← 지울 것
```

그 배치에선 **아무것도 못 지웁니다.** 가연님이 "여기에 반려견이라는 압축 해제된
폴더가 있는데?" 하고 짚어서 알았습니다. 끝까지 걸어서 파일 단위로 가르고,
지킬 게 하나도 없는 **가장 얕은 폴더**를 삭제 단위로 잡게 고쳤습니다.

### 2. TL02 는 **물리적으로** 못 받습니다 — 소스를 읽어서 확정

`--plan TL02` 가 "필요 — 보수 170.2GB" 를 내걸고 있었는데, **그 근거가
틀렸습니다.** `src/aihub.py:498` 의 경고를 옮긴 것이고 그 경고는 *"zip 을 받은 뒤
**압축을 풀므로**"* 라는 이유로 붙은 값인데, 우리는 `--mode zip` 으로 압축을
**안 풉니다.**

가연님이 "보수값 쌉에바" 라고 했고 맞았습니다. 그런데 제대로 다시 재니
**숫자는 더 커졌습니다.** 추정하지 않고 `aihubshell` 스크립트를 직접 읽었습니다
(`--shell-peek`):

| 줄 | 하는 일 | 그 순간 사는 것 |
|---|---|---|
| 111 | `curl -o download.tar` | 1배 |
| 124 | `tar -xvf download.tar` ← 뒤에 `rm` 없음 | 2배 |
| 139 | `find\|sort\|xargs cat > "${prefix}"` | 3배 |
| 142 | `rm "${prefix}".part*` | 합친 **다음**이라 소용없음 |

⇒ **받는 도중 최대치 = 청크의 2~3배.** TL02(80GB) 는 **170~250GB** 이고,
이 PC 디스크 **전체가 237.8GB** 입니다. 여유의 문제가 아닙니다.

**여기서도 두 번 틀렸습니다.**
① 정규식으로 6줄만 뽑게 짜서 정작 중요한 줄이 잘렸습니다 — 138줄의
`#cat part* > file` 이 **주석**이었고 진짜 병합은 가려진 줄에 있었습니다.
셸 스크립트는 덩어리째 봐야 합니다.
② `rm *.tar` 이 **어디에** 있는지를 안 봤습니다. 줄 100 은 다운로드(111)보다
**앞**이라 이전 찌꺼기 청소인데 "푼 뒤 지운다" 로 읽었습니다.

→ 외장 드라이브(2TB, exFAT/NTFS) 로 가기로 했습니다. `docs/OTHER_PC.md` 의
"최소 90GB" 도 **250GB** 로 정정했습니다.

### 3. 헛알림의 원인 — 눈이 아니라 자로

`false_alarms.png` 30장을 눈으로 보는 건 접었습니다 (수의사가 아니면 못 하는
일). 접은 건 맞지만 질문은 남아서, **병변 지식이 필요 없는 값**으로 대신
쟀습니다: 흐림·밝기·대비·채도·질감·색온도 (`tools/false_alarm_stats.py`).

먼저 이 도구가 STEP 10 을 **재현했습니다** — 헛알림 1,234(vs 1,233) ·
놓침 186(vs 186) · 정상/병변 정확히 일치. 로컬 CPU 가 캐글 학습과 같은 답을
냅니다.

| | AUROC | |
|---|---:|---|
| **`hair`** (털처럼 가는 선) | **0.749** | d 0.80 — **차이 있음** |
| `blur` (선명도) | 0.731 | d 0.47 — 조금 |
| 나머지 4개 | ≤0.58 | 차이 없음 |

⚠️ **방향**: `blur` 는 클수록 **선명**합니다. **헛알림 사진이 더 선명하고 결이
많습니다.** 흐려서 틀리는 게 아니라 반대입니다.

**놓친 병변 186장은 전부 문턱 아래** — 사진 조건의 공통점이 없습니다.

세 실측이 같은 곳을 가리킵니다: STEP 6("정상 사진이 계통적으로 흐림"),
STEP 11("헛알림의 55.8% 가 A2 비듬·각질로 감"), 그리고 이번 `hair`.
**털의 잔선을 각질로 읽는** 모양입니다.

### 4. 그럼 촬영 가이드로 잡을 수 있나 — **아니오** (축 세 개를 다 재봄)

| 축 | 헛알림과 | |
|---|---|---|
| 조명 (`sat`–`warm` 0.792) | AUROC 0.53/0.54 | 무관 |
| **촬영 거리** (`area_ratio`) | AUROC **0.462** | **기각** |
| 털·선명도 (`blur`–`hair` **0.896**) | 0.75/0.73 | ← 여기 |

촬영 거리 가설은 제가 세웠고 **틀렸습니다.** `area` 는 `blur`/`hair` 와 상관이
−0.043/−0.067 이고, 빼도 `hair` 가 0.749 → 0.747 로 그대로입니다.
STEP 6 의 "정상 사진이 흐리다" 는 거리 때문이 **아닙니다** (무엇 때문인지는
안 쟀습니다).

남은 축이 **"더 선명하고 털처럼 가는 선이 많으면 헛알림"** 이라 보호자에게 시킬 행동이
없습니다. 거리도 조명도 무관하니 가이드로 손댈 게 없습니다.
→ **모델·데이터 쪽 문제**로 확정.

### 5. 오늘 제가 틀린 것 네 개 (전부 실측이 잡아냄)

| 무엇 | 어떻게 틀렸나 | 어떻게 알았나 |
|---|---|---|
| 사전 등록한 **처방** | "차이 있음 → 촬영 가이드로 잡을 수 있다" 고 썼는데 차이가 **반대 방향**으로 났습니다. "덜 선명하게 찍으세요" 는 말이 안 됩니다. 기준에 **방향**을 안 적은 게 실수 | 결과가 나오고 나서 |
| 교란 검사 (1차) | `blur` 를 **사분위로 묶어** 재려 했는데, 독립 성분이 **0인 합성 데이터에서도** "독립"(0.657) 이 나왔습니다. 칸 안에서 control 이 계속 변하고 target 이 그걸 주워담습니다 | 답을 아는 합성 데이터 |
| 매니페스트 컬럼 이름 | 계획 문서에서 베껴 왔습니다 (`lesion_area_ratio`/`width`/`height`). 실물은 `area_ratio`/`img_w`/`img_h`. **못 찾았는데 조용히 NaN 으로 넘어가** 분석이 끝까지 돌았습니다 — 없는 걸 재고 있었습니다 | `area 0/7,134장` |
| 요약 판정 | (값, 뺀 것) 짝 중 **가장 센 것**으로 판정 → `hair` vs `area`(상관 −0.067) 라는 만만한 상대로 이겨 **"hair 가 원인 ✅"**. 진짜 경쟁자 `blur`(0.896)에겐 문턱을 못 넘는데도요 | 표를 눈으로 |

앞의 둘은 **순위 잔차**로, 뒤의 둘은 **멈추게 + 최악 기준**으로 고쳤습니다.
`tests/test_false_alarm_stats.py` 35개가 실측값 그대로 못 박습니다.

**최종 판정** — 값마다 제일 센 상대를 뺐을 때:

| 값 | 상대 | 남은 세기 | |
|---|---|---:|---|
| `hair` | `blur` | 0.123 | 문턱 0.15 **미달** |
| `blur` | `hair` | 0.067 | 방향 **뒤집힘** |
| `area` | `hair` | 0.000 | 기각 |

`hair` 가 가장 가깝지만 **못 박지는 못합니다.** 그게 정직한 결론입니다.

### 6. 다음 — 03f 판 A 돌리는 중

06(5.1h)이 4시간 26분에 안 들어가서, **다음 주 06 을 더 좋게 만드는** 실험으로
돌렸습니다. 순위도 바꿨습니다 — 2단계 백본을 아무리 갈아도 **화면은 안 바뀝니다**
(병변 이름을 안 띄우기로 확정). 1단계 AUROC 는 헛알림에 직결됩니다.

`03f` — 1단계 백본을 `f320` 으로 비교. ⚠️ 03d 를 그냥 다시 돌리면 안 됩니다
(그건 `full` 시절이라 지금 안 쓰는 입력으로 백본을 고르게 됩니다).

할당량이 세 종을 못 담아서 **판으로 나눴습니다** (04 판 A/B 와 같은 방식):

| 판 | 백본 | |
|---|---|---|
| **A** | `effnetv2_s`(기준선) + `swinv2_base` | ← 돌리는 중 |
| B | `effnetv2_s`(기준선) + `convnextv2_base` | 할당량 생기면 |

**모든 판에 기준선이 들어갑니다.** 04 에서 같은 설정 `resnet50` 이 판 A 0.4862 /
판 B 0.5024 (+0.0162) 로 달랐기 때문입니다 — 시드까지 같은데도요.

**이번 실측이 03f 에 예측을 겁니다**: 가는 선의 양은 **국소 질감 통계**라, 전역 문맥을
보는 `swinv2` 가 덜 기대면 헛알림이 줄어야 합니다. 안 줄면 백본이 아니라
데이터 문제입니다.

그리고 **새 후보 하나**가 생겼습니다 — `hair` 가 AUROC 0.749 로 헛알림을
예측한다는 건 **그걸 그대로 "어려운 표본" 점수로 쓸 수 있다**는 뜻입니다.
털 많은 정상 사진을 학습 샘플러에서 더 자주 뽑으면 "털 ≠ 병변" 을 배울 수
있습니다. 새 데이터도 새 라벨도 필요 없습니다. 이번 실측이 없었으면 못 세웠을
실험입니다.

---

## 2026-08-26 (밤) — 임대 GPU 첫 시도 · 축 두 개를 더 닫음 · 06 의 큰 버그

캐글 주간 할당량이 떨어져서 **런팟(RTX 4090 $0.74/hr)** 을 처음 썼습니다.
결과는 실험 둘과 버그 다섯입니다.

### 1. 실험 — 헛알림을 줄이는 길 두 개를 더 지웠습니다

**STEP 15 — 털 가중 샘플러** ❌

| alpha | AUROC | 헛알림 | 흐림 하락 | 분 |
|---:|---:|---:|---:|---:|
| 0.0 (기준선) | 0.9454 | **20.2%** | 3.3% | 11 |
| 1.0 | 0.9437 | 22.0% (**+1.8%p**) | 2.8% | 10 |
| 3.0 | 0.9423 | 21.7% (+1.5%p) | 0.2% | 10 |

헛알림이 **늘었고** 단조도 아닙니다. 흐림 하락 3.3%→0.2% 는 커 보이지만
잡음 폭(±5%p) 안이라 못 박습니다.

> `hair` 는 헛알림을 **예측**합니다 (AUROC 0.749). 그런데 그걸 **학습 신호로
> 바꿔도 안 고쳐집니다.** 예측과 개선은 다릅니다 — 이걸 실측으로 확인했습니다.

**STEP 14 판 B — `convnextv2_base` 1단계** ❌ 그리고 판 A 결론이 정확해졌습니다

| 백본 | 종류 | 파라미터 | AUROC | 흐림 하락 |
|---|---|---:|---:|---:|
| **`effnetv2_s`** | CNN | 20M | **0.9454** | **3.3%** |
| `convnextv2_base` | **CNN** | 89M | 0.9439 | **3.5%** |
| `swinv2_base` | **어텐션** | 87M | 0.9447 | **10.3%** |

**4.4배 큰 모델도 못 이깁니다** — 1단계는 백본 표현력이 병목이 아닙니다.

판 A 만 봤을 때 "swinv2 가 화질에 더 기댄다" 로 끝냈는데, **원인을 잘못 짚을
뻔했습니다.** 같은 크기 CNN 의 흐림 하락이 3.5% 입니다. 즉 화질 의존이 커진 건
**모델이 커서가 아니라 어텐션이라서**입니다.
⚠️ 어텐션 표본이 하나라 계열 전체로 일반화는 못 합니다.

**이걸로 오늘 지운 길이 넷입니다**: 촬영 거리 · 조명 · 1단계 백본 · 털 가중치.
남은 건 **앱 촬영 문구**와 **데이터(TL02)** 입니다.

### 2. ★ 06 이 2단계를 `resnet50` 로 하드코딩하고 있었습니다

**서빙 릴리스를 만드는 노트북**인데 2단계 학습 셀이 STEP 10 시절 그대로였습니다.
STEP 12 에서 `convnextv2_base` 가 **macro-F1 +0.0882** 로 이겼는데, 06 을 다시
돌려도 그게 안 들어갑니다. 그대로 돌렸으면 **개선이 하나도 안 실린 릴리스**가
나왔을 겁니다.

네 군데였습니다:

| 셀 | 무엇 |
|---|---|
| 30 | `model_name` / `models.build` / `exp_name` 전부 `"resnet50"` |
| 20 | 시간 추정도 resnet50 기준 → **5배** 빗나감 (에폭당 1.9분 vs 10.6분) |
| 47 | 백본 되찾기 fallback 이 `"resnet50" if thr else ...` — 바로 위에서 `thr` 을 쓰고 오므로 **항상 참**이라 백본을 바꿔도 되돌아감 |
| 48 | 체크포인트 후보 이름에 resnet50 만 |

`STAGE2_MODEL = "convnextv2_base"` 상수를 세우고 전부 거기서 읽게 했습니다.
**이름 생성 → `model_key_from_exp` 되읽기 왕복**까지 실제로 돌려서 확인했습니다.

⚠️ 왜 `swinv2` 가 아니라 `convnextv2` 냐 — 둘이 0.0026 차이로 구분 불가고
swinv2 가 1.6배 싼데, **오늘 판 B 에서 어텐션이 화질에 더 기댄다**는 걸 봤습니다.
2단계는 병변 **질감**을 보는 과제라 CNN 쪽을 골랐습니다. 배율 하락도 낫습니다.

### 3. 나머지 버그 넷 — 전부 "Colab/Kaggle 이면 알아서 되던 것"

| 무엇 | 왜 |
|---|---|
| 첫 셀이 `FileNotFoundError` | `env._search_roots()` 에 `/workspace` 가 없었음 |
| `externally managed` 설치 거부 | PEP 668 — 임대 이미지의 파이썬. `--break-system-packages` |
| `No module named 'pandas'` | 임대 이미지엔 **torch 만** 있음. 첫 셀이 numpy·pandas 가 있다고 전제 |
| `notebooks/deeplearning_test` **중첩 clone** | "리포 안인가" 를 **폴더 이름으로만** 판정. 주피터에서 열면 cwd 가 `notebooks/` |
| 보정 셀 `NameError: calibrate` | 40분 학습이 다 끝나고 터짐 |

마지막 것 때문에 **검사를 하나 더 만들었습니다** — 셀을 순서대로 AST 로 훑으며
"이 셀 전에 import/대입된 적 없는 `src` 모듈을 쓰는가". 처음엔 정규식으로 짰다가
**주석 속 글자**를 코드로 착각했고(`infer.Engine` 을 설명하는 주석), 여러 줄
import 도 앞줄만 읽혀서 놓쳤습니다.

중첩 clone 은 이제 **위로 거슬러 올라가며 진짜 리포 루트**를 찾습니다
(`.git` 과 `src/env.py` 가 같이 있는 폴더). 네 경우로 확인했습니다.

### 4. 런팟에서 배운 것

| | |
|---|---|
| **4090 이 T4 보다 5.4배** | 같은 설정 59분 → 11분 |
| 판이 달라도 기준선이 겹침 | AUROC 0.9465 → 0.9454, 헛알림 20.3% → 20.2% |
| 캐글 CLI 인증 실패 | 토큰 형식이 바뀜 (37자, `kaggle.json` 안 줌). **30분 태우고 포기** |
| 주피터 업로드가 느림 | 런팟 HTTP 프록시 경유. `runpodctl` 이 훨씬 빠름 |
| Stop 하면 볼륨 밖은 다 사라짐 | 제가 "패키지도 남는다" 고 했는데 **틀렸습니다** |
| Stop 뒤 못 켤 수 있음 | GPU 재고. 실제로 겪었고 다른 GPU 로 옮겨서 해결 |

⚠️ **캐글 새 토큰 형식은 확인 못 했습니다.** 안 되는 걸 된다고 적어두면 다음에
또 태우니, 큰 파일은 `runpodctl` 로 옮기는 걸 기본으로 삼습니다.

### 5. 다음 — TL02 를 붙여 한 번에

06 은 중간에 멈췄습니다 (위 `calibrate` 버그). 다시 돌리지 않고 **TL02 를 넣은
뒤 한 번에** 가기로 했습니다 — 어차피 `--finalize` 가 전체를 다시 분할해서
**holdout 이 일부 바뀌기** 때문입니다. 지금 돌려도 금요일에 또 돌려야 합니다.

절차는 `STATUS.md` 맨 위에 정리해뒀습니다. ⚠️ 06 전에
`checkpoints/stage1_*` 를 치워야 합니다 — 오늘 만든 서브셋 실험과 **이름이
글자 하나까지 같아서** 그걸 "이미 끝난 학습" 으로 이어받습니다.

---

## ✅ (해소) 열어뒀던 위험: 1단계가 **먼 사진**으로 배웠던 문제

**STEP 9-A·10 에서 f320 으로 바꿔 해결했습니다.** 아래는 그 위험을 적어둔
원래 기록입니다 — 왜 f320 으로 갔는지의 근거라 남겨둡니다.

학습했습니다 (병변은 22×22px 수준). 그런데 배포에서 보호자는 **촬영 가이드대로
병변에 다가가서** 찍습니다 — 1단계 입장에서는 한 번도 못 본 구도입니다.
`infer.Pipeline` 은 같은 사진을 두 엔진에 그대로 넣으므로, 2단계는 맞고 1단계만
분포가 어긋납니다.

지금 숫자(AUROC 0.8192)는 이 어긋남이 **없는** 검증셋에서 나온 것이라 낙관적일 수
있습니다. 05 의 holdout·촬영 가이드에서 확인하고, 필요하면 f320 재크롭으로 갑니다
(고정 픽셀 창은 배율도 일정하고 구도도 사용자 사진에 가깝습니다).

## STEP 16 — 전체 데이터로 재학습, 새 기준선 (2026-08-28)

**왜 했나.** 데이터가 VL01 45,885행뿐이었습니다. 결론이 여러 개 "잡음 안이라
구분 불가" 로 끝났고(A6 개선, 백본 비교), 그건 모델이 아니라 표본이 작아서일
수 있었습니다. AI Hub 561 의 나머지 청크를 다 가져오면 그 벽이 어디까지
물러나는지 볼 수 있습니다.

**어떻게 가져왔나.** 본 PC 는 디스크가 모자라 TL02(최대 250GB)를 물리적으로
못 받습니다. 맥북에서 받아 크롭만 옮겨 오는 길(`tools/merge_incoming.py`)로
풀었습니다. 그 과정에서 **에러 없이 조용히 틀리는** 것 세 개를 밟았습니다:

1. **zip 조각 순서** — aihubshell 이 `sort -V` 로 붙이는데 그게 안 먹으면
   문자열 순서로 붙습니다. TL02 는 `BadZipFile` 로 죽었고(안 열림), TL01 은
   **열리는데 97.6% 를 못 읽었습니다.** 증상이 정반대라 "깨짐" 을 목차 위치로
   판정하면 TL01 을 놓칩니다 → 항목이 **실제로 읽히는지**로 판정.
   `tools/fix_split_zip.py`
2. **JSON 조용한 손실** — TL01 263,340개 중 6,280개만 남았는데 "✅ 완료" 가
   찍혔습니다. `except Exception: continue` 한 줄. 옆의 "매칭 실패 0건" 이
   사람을 안심시켰습니다(읽기에 성공한 것만 세니까) → `labels.JsonSkips`,
   2% 넘게 못 읽으면 멈춤
3. **`unzip` 잘림** — RunPod 볼륨이 차서 16개가 전부 `Exit 50` 로 죽었는데
   `pgrep -c unzip` 이 0 이라 "전부 끝남" 으로 보였습니다. 파일 개수만 세면
   331,630 이라는 그럴듯한 숫자가 나옵니다(있어야 할 건 365,428) →
   `tools/fetch_crops.py` 가 **zip 이 적어둔 크기와 대조**

**데이터.** 45,885 → **365,428행 / 5,333마리**. 세 청크가 서로 다른 클래스를
담고 있어 합쳐야 A1~A7 이 다 모입니다. A7 이 크게 들어와 1단계가 거의
균형(49.4%)이 됐습니다. 누수 점검 전부 0건.

**06 을 돌리다 잡은 것 — 1단계가 `m2.5` 로 학습되고 있었습니다.**
06 헤더에는 "1단계 확정: f320" 이라고 써놓고 코드는 `choose_stage1_tag()` 로
매 실행 다시 골랐습니다. 그 판정은 `배율 격차` 하나에 달려 있는데 데이터가
바뀌자 문턱 아래로 내려가 ROI 크롭을 골랐습니다. **실험 이름에 `m2.5` 가
찍혀서야 드러났습니다.** 실측으로 닫은 결론을 휴리스틱이 매번 다시 열게 둔
구조가 잘못이었습니다 → `STAGE1_CROP = "f320"` 고정.

**결과 (holdout 73,853장).**

| | val | holdout |
|---|---:|---:|
| 1단계 AUROC | 0.9504 | *미측정* |
| 2단계 macro-F1 | 0.5999 | **0.5990** |
| 스크리닝 recall | 0.9560 | **0.9526** |
| 헛알림 | 27.3% | **21.2%** |
| 최종 7종 macro-F1 | 0.5814 | **0.5799** |

**A6 이 풀렸습니다** — recall 0.328 → **0.619**. 잡음(±0.085)의 3배가 넘습니다.
"지금은 못 고친다, 측정부터" 라고 접어뒀던 클래스인데, 답은 데이터였습니다.

**A4(농포)가 새로운 최악입니다** — 0.264. 학습 8,225장으로 A5(6,150)·A6(6,664)
보다 많은데 제일 못 맞힙니다. 장수만의 문제가 아닙니다.

**2단계 best 가 0에폭입니다.** 6에폭에서 조기 종료된 걸 14에폭으로 연장해
5에폭을 더 돌렸는데(2.4시간) 못 넘었습니다. 8에폭부터 train loss ↓ / val loss ↑
로 외우기 시작했고 macro-F1 도 꺾였습니다. warmup 이 2에폭이라 0에폭은 학습률이
낮은 구간에서 돈 것이고, 한 에폭이 7,569 스텝이라 "1에폭밖에 안 했다" 가
아닙니다. **같은 설정으로 에폭만 늘리는 데 시간 쓰지 말 것.**

**부위별 헛알림 순서가 바뀌었습니다.** STEP 11 은 다리 41.0% > 몸통 37.4% >
머리 18.9% > 연접부 16.6% (2.5배)였는데, 지금은 몸통 27.6% > 다리 23.5% >
머리 14.8% > 연접부 13.6% (2.0배)입니다. 앞 두 개가 뒤집혔습니다.
그리고 **놓침률은 순서가 정반대**입니다(머리 7.6% > 연접부 7.0% > 몸통 5.0% >
다리 2.8%) — 털이 많이 찍히는 부위일수록 헛알림은 늘고 놓침은 줍니다.
털 가설과 방향이 맞지만 관찰이지 개입 실험이 아닙니다.

**안 좋은 것.** 배율 하락 **26.8%** / 위치 하락 29.2% — 문턱(12%p) 밖입니다.
촬영 가이드 밴드도 바뀌어서(권장 0.7~1.2x) **크게 잡는 쪽이 빡빡해졌습니다.**
2.0x 에서 macro-F1 이 0.449 까지 떨어집니다. 0에폭 모델이라 덜 배운 탓인지는
아직 안 봤습니다.

원본: [`docs/results/STEP16_전체데이터_재학습_실측.md`](docs/results/STEP16_전체데이터_재학습_실측.md)

## STEP 17~20 — A4 의 원인을 좁혔습니다 (2026-09-05)

**왜 했나.** 멘토 지적: *"짝 혼동이 아니라 한 클래스가 여러 클래스로 동시에
흩어진다 — 그 클래스의 핵심 표현을 못 잡은 것"*. STEP 16 에서 A4 recall 0.264 로
최악인데 **원인이 미측정**이었습니다. GPU 할당량이 병목이라 **최대한 GPU 없이**
갈라내는 걸 목표로 잡았습니다.

**먼저 판을 다시 깔았습니다.** `dogskin_06.tgz`(778MB)가 이미 로컬에 있었고 거기에
STEP 16 릴리스 전체(`best.pt` 둘 포함)가 들어 있었습니다. 그리고 로컬 torch 가
**CPU 전용**(`2.13.0+cpu`)이라 RTX 3050 이 놀고 있었습니다 — `pyproject.toml` 에
`torch>=2.2` 만 있어서 uv 가 윈도우 기본인 CPU 휠을 고른 것입니다.
`cuda.is_available()` 이 **조용히 False** 였고, 추론이 몇 시간 걸려서야 알았습니다.
cu126 인덱스를 **윈도우 마커로만** 묶어 고쳤습니다(리눅스 해석 불변).
→ 이후 모든 분석이 로컬에서 돌았고 **캐글 할당량을 한 시간도 안 썼습니다.**

**멘토의 지적을 수치로 만들었습니다** (`errors.class_dispersion`). 행에서 대각선을
뺀 분포의 정규화 엔트로피 H̃. A4 0.890 · A5 0.933 · A6 0.909 · A1 0.903 이 "흩어짐",
A2 0.758 · A3 0.695 는 "그 사이". **지적은 맞았고, 다만 A4 만이 아니었습니다.**

⚠️ 여기서 **제가 한 번 틀렸습니다.** 처음엔 "소수→다수로 17배 한 방향 = prior 의
지문" 이라고 밀었는데, 그 비의 대부분이 **클래스 빈도에서 자동으로 생기는 것**
이었습니다. 제일 드문 A6 는 다른 전부가 "더 흔한 쪽" 이라 **모델과 무관하게
무한대**가 나옵니다. 우연 기대값으로 나누는 `lift` 로 바꾸니 A4 1.36 · A6 는
**잴 수 없음**. 검사 40개로 못 박았습니다.

**그래서 서술 말고 개입으로 갈랐습니다** (`tools/logit_adjust.py`). 저장된 로짓에서
학습 prior 를 빼봤습니다(`logit ← logit − τ·log π`, 재학습 없이 몇 초):

* prior 는 **실재하고 걷어집니다** — A4 쏠림 lift 1.20 → 0.63
* A4 recall 도 **단조로 오릅니다** — 0.356 → 0.515
* 그런데 **macro-F1 은 +0.0021** (문턱 +0.02). A2·A3 가 정확히 그만큼 내려갑니다

**걷어낼 수는 있는데 걷어내도 얻는 게 없습니다.** prior 는 맞바꿈 곡선 **위의
자리**만 정했지 곡선을 만들지 않았습니다 → **A4 는 결정 규칙이 아니라 표현의
문제**이고, 멘토의 지적이 개입 실험으로 확인됐습니다. **로짓 보정 축을 닫습니다.**

**가설 하나가 정반대로 틀렸습니다.** "A4 는 병변이 제일 작아서(창 295px, 66.7%
확대) 1단계가 못 본다" 고 예측했는데, 실측은 **모든 클래스에서 큰 병변일수록
1단계 점수가 낮았습니다**(rho −0.201, 선명도 통제 후에도 −0.230). 놓친 병변이
잡은 것보다 컸고(A1 은 261px vs 95px), 창 채움별로 단조였습니다
(occ<0.25 에서 0.7% → occ>2.0 에서 8.8%).

**원인을 자연 대조군으로 갈랐습니다.** 2단계는 `m2.5`, 즉 병변에 **비례**하는
창이라 채움 비율이 늘 같습니다. 거기선 클래스 안 rho 가 전부 0 근처였습니다.
→ "큰 병변이 어렵다" 가 아니라 **고정 창이 병변으로 꽉 차서 주변 정상 피부가
안 보이는 것**. 전체 병변의 **23.2%** 가 320px 창을 넘칩니다.

**청크 지름길은 경보를 올렸다가 실측으로 내렸습니다.** 클래스와 다운로드 묶음이
극단적으로 얽혀 있습니다(A1·A7 은 TL02 에 0장, A3~A6 은 TL01 에 0장). 그런데
청크만으로 낸 macro-F1 은 0.2197(모델의 36.6%)뿐이고 — **VL01 에 7개 클래스가
다 있어서** 청크가 라벨을 결정 못 합니다 — VL01 **안에서만** 평가해도 성능이
그대로였습니다(+0.0194, 잡음 안). 게다가 주된 청크가 **다른** 쌍(A4↔A1)이 같은
쌍보다 **더** 헷갈립니다(비 1.04). 청크를 쓰고 있다면 반대여야 합니다.
→ **축을 닫되 기록해 둡니다.**

⚠️ **두 번째로 틀린 것**: "문서와 릴리스의 배율 하락이 어긋난다"고 경보를 올렸는데
**문서가 맞았습니다.** STEP 16 문서의 배율 표에 **최악점(2.0x)이 빠져 있어**
검산이 안 됐던 것입니다. 전체 표로는 26.1%. → **표를 옮길 땐 최악점을 꼭 포함.**

**덤으로 알게 된 것.** `save_crop_size=512` 는 목표가 아니라 **상한**입니다.
`m2.5` 크롭에는 **64px 짜리도 있고** 384 입력에 넣으면 6배 확대입니다.

**남은 것.** 1단계 창 크기 `f320` vs `f448` 2판(VL01, 로컬)이 진행 중입니다.
A4 를 제대로 재려면 캐글에서 전체 val 로짓이 필요한데, **크롭은 이미 캐글에
있으므로 원본 전송은 필요 없습니다** — 지금 A4 에 대해 아는 건 전부 VL01
(전체의 11.8%, 게다가 1.7배 큰 병변)로 잰 것입니다.

원본: [`STEP17`](docs/results/STEP17_1단계_병변크기_실측.md) ·
[`STEP18`](docs/results/STEP18_로컬로짓_prior기각_실측.md) ·
[`STEP19`](docs/results/STEP19_청크지름길_실측.md)

---

## STEP 24 — 1단계 놓침: 병변인가 창 배치인가 (2026-09-06, GPU 0)

STEP 23(2단계 고정 창 전체 확인)이 캐글에서 도는 동안 GPU 없이 할 수 있는
것을 팠습니다. 열려 있던 질문 하나가 정확히 그런 종류였습니다 — STEP 20 이
"창이 작아서" 를 기각한 뒤 **'큰 병변이 원래 어렵다' 와 '큰 네모 = 대충 그린
라벨' 이 안 갈린 채** 남아 있었습니다.

**뜻밖의 발견부터.** 매니페스트 **365,428행 100%에 `polygon`(병변 외곽선)이
있습니다.** 그리고 `bbox` 는 그 폴리곤의 외접사각형입니다 — 오차 **0px**,
표준편차 0. 즉 라벨러가 그린 원본은 폴리곤이고 네모는 **우리가 뽑아 쓴 값**
입니다. STEP 4A 이후 "병변 크기" 라고 불러 온 게 전부 그 네모였습니다.

**그래서 가설이 다시 섰습니다.** "라벨러가 네모를 대충 그렸나" 가 아니라
"**폴리곤을 네모로 줄이면서 창이 병변을 벗어났나**" — 우리 파이프라인 탓이고,
맞다면 창을 폴리곤 무게중심에 놓으면 됩니다.

### ⚠️ 제 사전등록 판정이 **설계부터 틀렸습니다**

`occ`(창 안 병변 점유율)에 두 가설이 정반대를 예측하니 그걸로 가르자고
기준을 `src/experiments.py` 에 박고(작업 규칙 2) 돌렸습니다. "H-A(병변 자체)"
가 깔끔하게 나왔습니다. **그런데 무효입니다** —

    rho(occ, 폴리곤 √넓이) = +0.995

`occ ≈ 폴리곤넓이/320²` 이라 **occ 는 병변 크기의 다른 이름**입니다. occ 로
나눈 층은 크기로 나눈 층과 같고, 무슨 값이 나와도 *"크면 놓친다"* 의 재진술
입니다. 코드는 아무 말 없이 판정을 찍었습니다.

**사전등록을 했다는 사실이 설계 결함을 못 막습니다** — 오히려 "미리 정했으니
괜찮다" 는 안심을 줍니다. → `PLACEMENT_MAX_COLLINEARITY = 0.80` 을 걸고,
**표본 수 검사보다 먼저** 보게 했습니다 (못 가르는 검사는 표본이 많아도 못
가릅니다). `tests/test_polygon_geometry.py` 가 그 함정을 못 박습니다.

### 옳은 검사 — 크기를 고정하고 모양을 봅니다

모양 변수(`slack` 네모 안의 빈 곳 · `center_off` 중심 이탈 · `ecc` 길쭉함)는
크기와 rho 0.10~0.26 으로 **분리됩니다.** 크기 4분위 안에서 재니 층 안 rho 가
전부 |0.074| 이하이고 **방향이 뒤집힙니다** (`slack` 은 S2 에서 나쁜 쪽,
S4 에선 좋은 쪽). 반대로 모양을 고정하면 크기는 남습니다 (0.009 → 0.042).

→ **창 배치 가설 기각.** 네모 중심이 병변 밖인 행은 2.3%뿐이고, 놓침 77건 중
6건이라 고쳐도 제품이 안 바뀝니다.

### 덤 — 다시 못 잴 한계 하나

**365,428장이 사실상 전부 1920×1080** 입니다. 그래서 `rho(절대 픽셀, 화면
점유율) = +1.000` — 두 값이 **같은 변수**이고, "커서 놓치나 확대해 찍어서
놓치나" 는 이 데이터로 영원히 못 가릅니다. 더 중요한 건 **앱 사진은 1920×1080
이 아니라는 것**입니다. 1단계 `f320` 은 절대 픽셀 창이라 그대로 노출됩니다.

**남은 것.** 소거 끝에 '큰 병변이 원래 어렵다' 가 남았지만 **증명된 게 아니라
남은 것**입니다. 그리고 VL01 val 은 놓침이 77건뿐이라 층마다 15건꼴 —
이 축을 진짜로 열려면 전체 val 로짓(놓침 ~700건)이 필요합니다. 다만
**같은 GPU 를 2단계 고정 창(STEP 23)에 쓰는 쪽이 지금은 더 큽니다.**

원본: [`STEP24`](docs/results/STEP24_1단계_놓침_폴리곤_실측.md)

---

## STEP 23 — 2단계 고정 창을 전체 데이터로: **기각** (2026-09-06, 캐글 9.1시간)

STEP 22 에서 사전등록 4기준을 전부 통과했던 **유일한 살아남은 후보**를
전체 데이터(TL01+TL02, 학습 109,327장, A4 val **1,730장**)로 확인했습니다.

    macro-F1    0.5894 → 0.5795   (−0.0099, 잡음 안)
    A4 recall   0.2815 → 0.2561   (−0.0254)
    A4→A1       35.1%  → 32.3%
    배율 하락    26.0%  → 35.1%    (+9.1%p)

→ **채택 없음.** 2단계 크롭은 `m2.5` 유지.

⚠️ **"나빠졌다" 고 쓰면 과합니다.** A4 차이 −0.0254 는 **차이의** CI 반폭
±0.0297 안입니다 (사전등록 규칙은 한 팔의 ±0.0212 만 봐서 아슬아슬하게
문턱을 넘겼습니다). 정확한 표현은 **"이득이 없다"**. 명확히 나쁜 것은
배율 하락 +9.1%p 하나입니다.

### 왜 STEP 22 는 정반대였나

VL01 의 A4 는 **196px**, TL02 는 **113px** 입니다. 기준선 A4 recall 이
0.393(VL01) vs 0.282(전체)로 다릅니다. 그리고 STEP 23 의 0.2815 는 STEP 16
**holdout 0.264** 와 거의 같습니다 — **VL01 쪽이 낙관적 이상치**였습니다.

VL01 단독으로 헛짚은 게 이번이 **두 번째**입니다 (첫 번째는 STEP 22 의
사전등록 예측을 전체 val 의 A4 크기로 세워 놓고 실험은 VL01 에서 돌린 것).

### ★ 제 판정 기준에 구멍이 있었습니다

기준 하나가 "A4→A1 혼동이 줄 것" 이었고, `f320` 은 그걸 **통과**합니다
(35.1% → 32.3%). 그런데 줄어든 만큼 정답이 아니라 **나머지 4종으로
흩어졌습니다** — 그 밖 36.8% → **42.1%**. 멘토가 말한 *"한 클래스가 여러
클래스로 흩어진다"* 가 **더 심해진** 것을 개선으로 읽을 뻔했습니다.
→ 클래스별 혼동을 기준으로 쓸 땐 **행선지 하나가 아니라 분포 전체**
(`errors.class_dispersion()` 의 H̃)를 같이 봅니다.

### 곁다리

* `effnetv2_s` best_epoch **4 / 7**, 둘 다 수렴 → STEP 16 의 "0에폭 best" 는
  **convnextv2_base 특유**였고 **03h 재개 조건은 발동하지 않습니다.**
* 캐글 `f320` 데이터셋에 VL01 크롭이 아직 없어 이번에도 제외됐습니다
  (두 팔이 같은 데이터를 보므로 비교는 유효).
* **1단계 보정은 이미 돼 있었습니다** — T=0.9225, ECE 0.0282 → 0.0305
  (2단계와 같이 고칠 게 없었음). 릴리스가 `threshold_calibrated` /
  `threshold_raw` 를 둘 다 저장하고 배선도 맞습니다. STATUS 의 "한 번도 안
  했음" 이 낡은 기록이었습니다.

### 48시간을 정리하면

열었던 축이 전부 닫혔습니다 — prior(18) · 청크(19) · 1단계 창 크기(20) ·
2단계 고정 창(23) · 1단계 창 배치(24). **성능은 안 올랐고 기각 다섯 개를
얻었습니다.** STEP 22 만 보고 갈아탔다면 배율 하락이 9%p 나빠진 채로
배포했을 것입니다.

원본: [`STEP23`](docs/results/STEP23_2단계_고정창_전체_실측.md)

---

## STEP 25 — 2단계 크롭 뷰 앙상블: 채택 후보 (2026-09-06, 로컬, 재학습 0)

STEP 23 으로 축이 전부 닫힌 뒤, 사용자가 *"가능하면 2단계를 살리고 싶다"* 고
해서 로컬에서 할 수 있는 것을 전부 돌렸습니다.

**착상은 STEP 22·23 을 다시 읽다 나왔습니다.** 두 STEP 은 *"m2.5(비례 창)와
f320(고정 창) 중 뭐가 나은가"* 를 물었고 답이 갈렸습니다 (VL01 은 f320,
전체는 m2.5). **갈렸다는 건 둘이 다른 실수를 한다는 뜻입니다** — 그러면
고를 게 아니라 더하면 됩니다. 실측하니 한쪽만 맞히는 몫이 **27.0%** 였습니다.

    릴리스 m2.5(cnx)   macro-F1 0.5805  acc 0.6689  A4 0.356  배율하락 24.6%
    f320(eff)          macro-F1 0.5873  acc 0.6449  A4 0.525  배율하락 35.3%
    앙상블(확률 평균)     macro-F1 0.6384  acc 0.7047  A4 0.540  배율하락 27.3%

짝지은 부트스트랩 CI 가 셋 다 0 을 안 넘습니다 (macro-F1 +0.0579
[+0.0397, +0.0751] · accuracy +0.0358 · A4 recall +0.1840). **여섯 클래스가
전부 오르고** 잃는 클래스가 없습니다. 사전등록 관문 4개 전부 통과.

### ★ 이득의 출처가 '모델 두 개' 가 아니라 '창 두 개' 입니다

가장 중요한 대조군입니다:

    같은 크롭, 다른 백본 (cnx + eff m2.5)   +0.019   ← 잡음권
    다른 크롭         (cnx + eff f320)      +0.058
    다른 크롭         (cnx + eff f448)      +0.045

백본 다양성은 값이 거의 없고 **크롭 다양성만 값이 있습니다.** 기전과 맞습니다 —
`m2.5` 는 사진마다 확대율이 다르고 `f320` 은 확대율이 일정한 대신 큰 병변이
잘립니다. 사각지대가 서로 다릅니다.

그리고 앙상블은 `f320` 의 배율 약점(35.3%)을 **물려받지 않습니다** — 27.3% 로
`m2.5`(24.6%) 쪽에 붙고, 7개 교란 조건 **전부**에서 두 단독보다 높습니다.

### 관문 4가 실제로 일했습니다

STEP 23 에서 "짝 혼동이 줄 것" 만 보다 **흩어짐을 개선으로 오독할 뻔했던**
구멍을 `ENS_NO_SCATTER` 로 막았는데, 이번엔 A4→A1(20.9→13.8%)과
A4→그밖(43.6→32.2%)이 **같이** 줄어 진짜였습니다.
`tests/test_ensemble_gates.py` 가 "macro-F1 은 올랐는데 흩어진" 가짜 후보를
코드가 실제로 기각하는지 검사합니다 (15/15).

### 부수적으로 닫고 고친 것

* **TTA(좌우반전) 기각** — +0.0027 / −0.0032 / +0.0005. 전부 잡음 안이고
  부호도 갈립니다. 이틀 동안 유일하게 안 건드린 모델 축이었고, 이제 닫혔습니다.
* **"거절로 이름을 살릴 수 없다" 의 숫자가 낡았습니다.** STEP 11 의
  "오답률 20% → 커버리지 18.2%" 는 resnet50·45,885행 값입니다. 릴리스로
  **전체 val 27,331행**에서 재면 **62.0%** 입니다 (3.4배). 결정을 뒤집자는 게
  아니라 근거를 다시 놓고 판단해야 한다는 뜻입니다.
* **VL01 이 늘 낙관적인 게 아닙니다** — macro-F1·A4 에선 후하고(STEP 23),
  커버리지-위험에선 오히려 **박합니다** (10% 목표에서 0.0% vs 전체 21.5%).
  "VL01 은 후하다" 로 뭉뚱그리면 또 틀립니다.
* **미측정을 '통과' 로 찍던 것을 고쳤습니다.** `--scale-drop` 을 안 주면
  관문 2가 조용히 통과로 나왔습니다 — 이 리포가 반복해 당한 모양이라
  (`temperature.json` 누락, `unzip -n`) 판정을 **'미완'** 으로 바꿨습니다.

### 남은 것 — 전체 val 확인은 **GPU 0으로** 됩니다

VL01 결과이고, STEP 22 → 23 이 정확히 이걸로 뒤집혔습니다. 그런데 STEP 23 이
이미 두 팔을 같은 전체 데이터·같은 val 로 학습해 뒀으므로(A4 val 1,730장),
그 `logits_val.npz` 두 개만 받으면 재학습 없이 확인됩니다.

원본: [`STEP25`](docs/results/STEP25_2단계_크롭뷰_앙상블_실측.md)

### STEP 25 이어서 — 전체 데이터 확인 (같은 날, GPU 0): **절반만 재현**

사용자가 STEP 23 의 두 `logits_val.npz` 를 꺼내 왔습니다. 릴리스 로짓과 `y`
배열이 **완전히 동일**해 행 정렬이 보장됐고, 재학습 없이 바로 확인했습니다
(val **27,331행**, A4 **1,730장**).

⚠️ **정정**: 이 27,331행은 **TL01+TL02** 이고 VL01 이 빠져 있습니다 (캐글
`m2.5` 데이터셋에 VL01 크롭이 없어 `--chunk auto` 가 제외). 제가 앞서
"전체 val, 청크 3개" 라고 적은 것은 틀렸습니다 — 전체의 89% 입니다.

    릴리스 단독      macro-F1 0.5959  acc 0.6677  A4 0.228  커버리지(20%) 62.0%
    3팔 앙상블       macro-F1 0.6254  acc 0.7037  A4 0.262  커버리지(20%) 71.5%

**재현된 것** — macro-F1 +0.0295 [+0.024, +0.035], **여섯 클래스 전부 상승**
(A6 +0.047 · A5 +0.041 · A4 +0.034), 이름 오답률 33.2% → 29.6%,
커버리지 +9.5%p. 세 조합 모두 CI 가 0 을 안 넘습니다.

**재현되지 않은 것** — **A4 구조 개선.** VL01 에선 A4 recall +0.184
[+0.128, +0.237] 였는데 전체에선 **+0.034** 이고, A4→A1 은 오히려
35.1% → 36.8% 로 **늘었습니다.** 2팔 조합 둘 다 흩어짐 관문(관문 4)에서
**기각**되고 3팔만 −0.7%p 로 겨우 통과합니다.

이유는 STEP 23 과 똑같습니다 — VL01 의 A4 는 196px, A4 의 88%가 사는 TL02 는
113px. `f320` 고정 창은 **큰 A4** 에서만 값이 있습니다.

→ **"크롭 다양성이 A4 를 살린다" 는 죽었습니다.** 남는 주장은 **"크롭 다양성이
전반적 변별력을 올린다"** 이고, 그건 전체 데이터에서 확인됐습니다.

**판정은 '미완'** 입니다 — 배율 강건성(관문 2)을 전체 데이터에서 못 쟀습니다
(TL01·TL02 크롭이 로컬에 없음). `f320` 단독이 35.1% 였으므로 필수입니다.
그리고 3팔은 추론이 3배인데 **2단계는 보호자 화면을 안 바꿉니다** — 값을
치를지는 *"확신 있는 것만 이름을 말할 것인가"* 판단이 먼저입니다.

---

## STEP 26 — 앙상블의 배율 강건성: **마지막 관문 통과** (2026-09-06, 캐글 추론 25분)

STEP 25 를 '미완' 으로 묶어둔 관문 하나를 풀러 갔습니다. 물은 것은 하나입니다 —
**앙상블이 `f320` 의 촬영 거리 의존을 물려받는가?** `f320` 단독은 배율 하락이
36.9% 라, 물려받으면 macro-F1 이 아무리 올라도 못 씁니다.

전체 val 27,331행 중 **모든 팔이 공유하는 2,000장** × 배율 5종 + 위치 2종 ×
3모델 = 42,000회 추론. 학습 없음.

    배율 하락    릴리스 m2.5 23.5%  ·  m2.5(eff) 25.9%  ·  f320(eff) 36.9%
                 3팔 앙상블  24.4%     ← 기준선 대비 +0.9%p
    위치 하락    릴리스 17.9%  ·  앙상블 15.2%          ← 오히려 좋아짐

**안 물려받습니다.** +0.9%p 는 허용치(+12%p)는 물론 우리가 잰 교란 잡음
±5%p 안이라 **"나빠졌다고 잴 수 없다"** 가 정확한 표현입니다.

세 가지가 더 나왔습니다:

1. **부품의 평균보다 낫습니다.** 세 팔 하락률의 산술평균은 28.8% 인데 앙상블은
   24.4% 입니다. 섞으면 중간이 되는 게 아니라 **좋은 쪽에 붙습니다** — 배율
   2.0x 에서 `f320` 이 0.3499 로 무너질 때 두 `m2.5` 팔이 0.405·0.414 로
   버티고 확률 평균이 그쪽을 따라갑니다.
2. **7조건 전부에서 앙상블이 모든 단일 팔을 이깁니다** (+0.012 ~ +0.051).
   "기준 조건에서만 좋고 교란에서 무너지는" 종류가 아닙니다.
3. 관문 4개 **전부 통과** → STEP 25 의 '미완' 이 풀립니다.

### 2팔로 줄이려다 관문 4에 걸렸습니다

추론 3배가 비싸서 2팔을 다시 봤습니다 (로컬, 저장 로짓, 몇 초).
릴리스+`f320` 은 macro-F1 +0.0245 로 3팔(+0.0295)의 **83%** 를 냅니다.
그런데 **A4→그밖이 37.7% → 38.2% 로 늘어납니다** (3팔은 −0.7%p) — 흩어짐
관문 실패. macro-F1 과 accuracy 만 봤으면 **33% 싸게 골랐을 겁니다.**

★ `ENS_NO_SCATTER` 는 STEP 23 에서 제 기준에 뚫려 있던 구멍을 막은 관문입니다.
그게 실제로 후보 하나를 걸러낸 첫 장면입니다. **사전등록이 값을 한 순간.**

### 순위가 뒤집히는 걸 봤습니다

이번 표의 macro-F1 은 `robust.ZoomView` 값이라 배율 1.0x 조차 437px→384
중앙크롭(약 1.14배)이 걸려 있습니다. 같은 모델·같은 val 인데:

    릴리스 cnx    표준 평가 0.5959  →  ZoomView 1.0x 0.5291   (−0.067)
    m2.5(eff)     표준 평가 0.5894  →  ZoomView 1.0x 0.5593   (−0.030)

**ZoomView 표만 보면 릴리스가 꼴찌**로 보이지만 표준 평가에서는 **1등**입니다
(짝지은 부트스트랩 +0.0065 [+0.0003, +0.0126]). 파이프라인이 다른 절대값을
나란히 놓으면 이렇게 됩니다 → 비교 가능한 것은 **같은 파이프라인 안의
하락률뿐**입니다.

### 그리고 24분을 태웠습니다

첫 판이 `[X] 체크포인트가 없습니다` 로 죽는 데 24분이 걸렸습니다. 검사 자체는
1초짜리인데 그 앞에 `env.load_prepared()`(크롭 36만 장 존재 확인)가 있었습니다.
원인은 `train.import_previous_run()` 누락 — 붙인 데이터셋은 `/kaggle/input` 에
있는데 셀은 `/kaggle/working/...` 만 봤습니다. **붙여놨는데 못 찾은 게 아니라
볼 곳을 안 봤습니다.** 05·09 엔 그 줄이 있고 10 에만 없었습니다.

→ 확인을 크롭 연결 **앞**으로 옮기고, 실패하면 `/kaggle/input` 트리를 통째로
찍습니다. **비싼 단계 앞에 싼 검사를 두세요 — 순서만 바꾸면 됩니다.**

### 남은 것은 제품 판정

기술 판정은 끝났습니다. 그런데 추론이 3배인데 **2단계는 화면에 병변 이름을
안 띄웁니다.** 이 이득이 보호자에게 닿는 경로는 하나뿐입니다 — *"확신 있을
때만 이름을 말한다"* 로 출력 규격을 바꾸는 것. 커버리지(오답 20% 목표)
**71.5%** 는 그 판단을 다시 놓고 볼 만한 숫자입니다 (STEP 11 의 18.2% 는
resnet50·45,885행 시절 값이라 낡았습니다). 멘토·사용자와 같이 정합니다.

원본: [`docs/results/STEP26_2단계_앙상블_배율강건성_실측.md`](docs/results/STEP26_2단계_앙상블_배율강건성_실측.md)

---

## STEP 27 — 이름을 켤 수 있나: 분모를 고치니 71.5% 가 33.7% 로 (2026-09-06, 로컬)

STEP 26 끝에 제가 이렇게 말했습니다: *"커버리지가 18.2% 에서 71.5% 로 3.9배
나아졌으니 '이름을 말할 것인가' 를 다시 놓고 보자."* 사용자가 *"그럼 그렇게 하는
게 좋은 거 아니야?"* 라고 물었고, 재보자고 답한 게 이 STEP 입니다.

**제 숫자가 틀렸습니다.** 71.5% 는 **진짜 병변인 사진만** 놓고 잰 값입니다.
화면에 뜨는 건 **1단계가 넘긴 사진**이고 거기엔 헛알림이 섞여 있습니다.

더 나쁜 건, STEP 11 이 **이 한계를 정확히 적어놨다**는 것입니다 —
*"그 곡선은 헛알림 1,233건을 뺀 것이라 실제는 더 나쁩니다."* 두 달 동안
아무도 안 쟀고, 저는 같은 분모로 새 숫자를 계산하면서 **각주를 안 들고
왔습니다.** 한계를 적어두는 것은 재는 것의 대체가 아니었습니다.

    1단계가 넘긴 사진 3,830장 (VL01 val 6,332장 중)
      진짜 병변 3,033 (79.2%)  /  헛알림 797 (20.8%)

    오답률 20% 목표에서의 커버리지
      릴리스 m2.5 단독    0.7%    ← 축을 닫음
      3팔 앙상블         33.7%    ← 판단 보류 (논의 50% / 닫음 30% 사이)
      (옛 분모, 병변만)  36.1% / 59.1%

★ **헛알림 20.8% 만으로 목표(20%)를 넘습니다.** 2단계가 완벽해도 전부에게
이름을 말하면 20.8% 틀립니다. 거절이 헛알림을 골라 버려줘야만 내려갑니다.

### 릴리스가 0.7% 인 이유가 예상과 달랐습니다

정확도가 낮아서가 아니라 **자기가 언제 맞는지 몰라서**였습니다. 확신도
10분위표에서 **최상위 분위가 이미 25.1% 오답**입니다 — 목표를 지키는 구간이
처음부터 없습니다. 앙상블은 최상위 12.5%, 누적으로 30% 근처까지 20% 아래를
버팁니다. **릴리스의 1분위(25.1%)가 앙상블의 3분위(18.6%)보다 나쁩니다.**

→ 그래서 앙상블의 값어치를 다시 씁니다. macro-F1 +0.030 은 부수적이고,
**쓸 수 있는 커버리지 0.7% → 33.7% (48배)** 가 본체입니다. 확률 평균은
정확도만 올리는 게 아니라 **과신을 걷어냅니다.**

### 표를 만들다 제 라벨에 걸렸습니다

10분위표 첫 판이 "상위 10% / 상위 20% …" 라고 찍었는데 실제로는 **그 칸만의**
값이었습니다. 그대로 뒀으면 *"상위 20% 오답률 18.0%"* 라는 문장이 나갔을
텐데, 누적으로는 21.5% 라 **정반대 결론**입니다. 이제 두 열을 나란히 찍습니다.

### 덤 둘

* 확신도는 `p1 × p2max` 가 맞습니다 (33.7% vs p2max 만 3.2%, p1 만 0.5%).
  서빙이 이미 그렇게 하고 있었고 **근거는 이제 생겼습니다.** 오답이 두
  종류라 한쪽만 보면 다른 쪽이 통과합니다.
* 1단계 임계값 지렛대는 실재합니다 (0.70 에서 커버리지 44.5%). 그런데
  recall 이 0.970 → 0.865 — 놓침이 3마리에서 13마리로. **안 당깁니다.**

### 결정은 사람 몫으로 넘깁니다

33.7% 는 미리 박아둔 논의 문턱(50%)과 닫음 문턱(30%) **사이**입니다. 기준을
결과 보기 전에 박아둔 덕에 *"제가 정할 수 없는 구간"* 이라고 말할 수 있습니다.
남은 확인: 전체 데이터(캐글 1회) · holdout(06) · A6 안전 관문 표본 53장.

원본: [`docs/results/STEP27_이름을_말할_것인가_정직한_커버리지.md`](docs/results/STEP27_이름을_말할_것인가_정직한_커버리지.md)

---

## STEP 28 — 알갱이를 바꾸니 축이 살아났습니다 (2026-09-06, 추론 0회)

STEP 27 뒤 사용자가 물었습니다: *"아예 방법이 없어? 더 뭔가 해볼 거라도..."*
있었습니다. 그리고 그건 **새 모델이 아니라 새 질문**이었습니다.

두 달 동안 우리는 *"6종 이름을 말할 수 있나"* 만 물었습니다. 답은 계속
"아니오" 였고(46.3% 오답 → 커버리지 33.7%), 그래서 화면에서 이름을 뺐습니다.
**물어야 했던 건 "무엇을 말할 것인가" 였습니다.**

    같은 모델 · 같은 확률 벡터 · 추론 0회 · 알갱이만 바꿈

    ① 6종 이름         커버리지 33.7%   긴급도 하향  4.7%   기각
    ② 형태 계열 3군     커버리지 62.1%   긴급도 하향  1.7%   통과
    ③ 긴급도 3등급      커버리지 42.7%   긴급도 하향  5.6%   기각
    ④ 두 이름(top-2)   커버리지 80.4%   긴급도 하향  2.1%   통과
    ⑤ 긴급도 2등급      커버리지 92.2%   긴급도 하향 46.0%   기각 ← 관문이 일함

전체 데이터(27,331행)에서도 형태 계열 정확도 **0.8365**, 두 이름 적중률
**87.4%** 로 재현됩니다.

### 방법론이 결과보다 중요했습니다

묶음을 데이터로 만들면 **무슨 묶음이든 좋아 보입니다.** 그래서 두 곳에서만
가져왔고 둘 다 이 분석보다 먼저 있었습니다 — `config.URGENCY_HINT` 와
임상 해설의 "🟢 비교적 안전한 혼동"(A1↔A4 · A5↔A6). 형태 계열의 셋째
묶음(A2·A3)은 **고른 게 아니라 남은 것**입니다.

그리고 굵게 말할수록 커버리지는 **공짜로** 오릅니다. 극단적으로 "이상이
있습니다" 하나만 말하면 100% 입니다. 그래서 관문을 둘로 뒀습니다 —
커버리지와 **긴급도 하향**. ⑤ 가 정확히 그걸로 걸렸습니다: 커버리지만
봤으면 92.2% 짜리를 골랐을 텐데 실제로 급한 것의 **46%** 를 "안 급하다"
고 말합니다.

### 오답의 정의를 바꾼 것도 이 STEP 입니다

임상 해설이 꼽은 위험한 혼동 셋(A6→A2 · A5→A1 · A6→A1)의 공통점은 *"급한
걸 안 급하다고 말했다"* 입니다. 반대 방향은 병원에 가게 만들 뿐이라 안전하고,
**1단계 헛알림도 같은 종류**입니다. 그래서 6종 이름을 전부 말할 때

    이름 오답률 45.6%   vs   긴급도 하향 11.1%   (전체 데이터 13.5%)

*"두 번에 한 번 틀린다"* 와 *"여덟 번에 한 번 위험하게 틀린다"* 는 다른
문장입니다. 둘 다 사실인데 결정이 달라집니다.

### 값이 달라진 것

추론 3배가 **선택**이 됐습니다. 형태 계열이면 릴리스 단독도 52.5% 로 문턱을
넘습니다 (앙상블 +9.6%p). 6종 이름일 때만 앙상블이 필수였습니다 (0.7% → 33.7%).

### 결론이 뜻하는 것

모델이 좋아진 게 아닙니다. **모델이 실제로 아는 것의 크기를 알아낸 것**입니다.
A1(구진)과 A4(농포)를 가르는 건 *속의 고름*이고 사진 각도·조명에 따라 안
보입니다 — 임상 해설이 처음부터 그렇게 적어놨습니다. 모델은 사진에 없는 걸
못 봅니다. 그런데 "융기·발진인가, 표면 변화인가, 덩어리인가" 는 사진에 있고
그건 83.7% 로 맞힙니다.

화면 문구는 **아직 안 바꿨습니다** — 제품 결정입니다.

원본: [`docs/results/STEP28_무엇을_말할_것인가_알갱이.md`](docs/results/STEP28_무엇을_말할_것인가_알갱이.md)

---

## STEP 29 — 제 근거를 물렸습니다, 그리고 A6 경보가 살아났습니다 (2026-09-06, 추론 0회)

STEP 28 을 쓰고 나서 사용자가 물었습니다: *"그것도 임상적으로 비슷한 성질이라서?
맞아?"* 그 질문을 확인하려고 묶음을 갈라 재다가, **다른 것 두 개가 걸렸습니다.**

### 하나 — 제가 작업 규칙 1 을 어겼습니다

STEP 28 에서 "계열 정확도 0.8365, 6종은 0.7037" 을 개선 근거로 앞세웠습니다.
**하한선을 안 적었습니다.**

    묶음                  하한선(최빈)     모델      하한선 대비
    6종                     35.3%    0.7037      +35.1%p
    A6 만 따로 (4군)          67.1%    0.8315      +16.0%p
    형태 계열 3군              67.1%    0.8365      +16.5%p

'표면 변화'(A2+A3)가 18,349장으로 전체의 67% 입니다. 그 묶음을 만든 순간
하한선이 0.67 이 됩니다. **하한선 대비로는 방향이 뒤집힙니다** — 문제를 쉽게
만들면 모델의 몫은 오히려 줄어듭니다. 규칙 1 은 *"하한선과 비교하지 않은
정확도는 숫자가 아니다"* 인데, 그 규칙을 만들어 놓고 제가 어겼습니다.

**다만 커버리지는 자유롭습니다.** '늘 최빈 묶음만 말하기' 는 오답률이 어디서나
32.9% 라 20% 목표를 한 번도 못 지킵니다 — 하한선의 커버리지는 **0%** 입니다.
61.4% 는 전부 모델의 확신도 순서가 벌어온 것입니다. 그래서 이 프로젝트에서는
**커버리지가 정확도보다 정직한 지표**입니다.

### 둘 — A6 경보를 버릴 뻔했습니다

STEP 28 에 "표본 124장이라 얇다" 고 적어놨습니다. 전체 val 로 재니 1,540장이고,
숫자가 완전히 달랐습니다:

    정밀도 90%   재현율  전체 50.4%   vs   VL01 12.1%
    정밀도 80%   재현율  전체 68.1%   vs   VL01 39.5%

**VL01 이 4배 넘게 깎아 보고 있었습니다.** "덩어리가 의심됩니다" 는 10에 9가
진짜이면서 실제 덩어리의 절반을 잡습니다. 릴리스 단독으로도 90%/42.8% 라
추론 3배 없이 켤 수 있습니다.

그리고 이번엔 **경고를 달고 끝내지 않았습니다.** 전체 val 에는 1단계 헛알림이
없어서 정밀도가 낙관적일 수 있는데, VL01 에서 두 조건을 다 재보니 정밀도 80%
에서 39.5% / 39.5% 로 **같았습니다.** 멀쩡한 피부를 덩어리로 보지는 않습니다.
STEP 27 에서 각주만 달고 두 달을 버린 직후라, 같은 실수를 안 하려고 쟀습니다.

### 셋 — 4군 안에서 결절·종괴의 실제 상태

거절 없이 전부 말하면 recall **0.645**, 긴급도 하향 **31.0%** 입니다.
허용치 5% 를 한참 넘습니다. **다만 '관찰' 로 부른 건 0.0%** — A2·A3 묶음의
긴급도를 보수적으로 높은 쪽으로 잡아둔 덕에 어떤 경로로 틀려도 병원에 가라는
말은 나갑니다.

그리고 A6 을 따로 빼니 **미란·궤양(A5) recall 0.510** 이 드러났습니다.
표면 변화가 0.917 로 평균을 떠받치고 있어 가려져 있던 약점입니다.

원본: [`docs/results/STEP29_하한선과_A6경보_전체데이터.md`](docs/results/STEP29_하한선과_A6경보_전체데이터.md)

---

## STEP 30 — A5 는 데이터로 안 풀립니다, 그리고 관문을 한쪽만 세고 있었습니다 (2026-09-06)

STEP 29 끝에 "다음 약점은 미란·궤양(A5) 0.510" 이라고 적고 팠습니다.

### 장수 문제가 아니라는 걸 **대조군 하나로** 끝냈습니다

    A6   val 1,540장 · 개체 102 · recall 0.675
    A5   val 1,468장 · 개체 113 · recall 0.541

A5 가 개체가 **더 많은데** 0.134 낮습니다. A6 은 STEP 16 에서 데이터를 8배로
늘려 0.328 → 0.619 로 풀린 클래스입니다 — A5 는 같은 크기를 받고도 안 풀렸으니
**더 준다고 풀릴 자리가 아닙니다.** A4 때는 이 논법을 "장수는 많은데 못 맞힌다"
로 폈는데, 이번엔 **거의 동일한 조건의 대조군**이 옆에 있어서 훨씬 셉니다.

### 그럼 무엇과 안 갈리나 — 짝별 AUROC 가 답했습니다

    A1↔A4  0.768     A4↔A5  0.861     A1↔A5  0.877     …     A2↔A6  0.978

**최악의 짝 셋 중 둘이 A5 이고 상대는 전부 A1·A4** 입니다.
★ 그리고 이게 **계열 묶음이 A5 를 못 살린 이유**입니다 — 4군은 A1↔A4 와
A2↔A3 를 *묶음 안*으로 넣어 없앴는데, A5↔A1·A4 는 *묶음 사이*라 남습니다.
4군에서 A5 가 제 묶음으로 가는 비율이 0.510 인 게 그 뜻입니다.

크기가 매개합니다: **A5 는 작을수록**(Q1 0.449 → Q4 0.604), **A1 은 클수록**
(Q1 0.777 → Q4 0.424) 틀리고, 중앙값이 겹칩니다(A1 134px / A5 167px).
작은 미란과 큰 플라크가 같은 크기대에서 만나 서로가 됩니다.

### 남는 설명은 **입체감**입니다 (소거법)

최악의 짝 셋이 요구하는 게 전부 같은 종류입니다 — *융기 안에 고름이 있나 /
솟았나 파였나 / 부풀었나 터졌나.* 전부 **표면이 솟았는지 파였는지**입니다.
사진 한 장은 비스듬한 조명 없이는 그걸 잘 안 담습니다.

**대조군이 지지합니다**: A3(태선화·과다색소침착)의 단서는 **색**인데
A1↔A3 는 **0.934** 로 잘 갈립니다. 색은 사진에 있고 입체감은 없습니다.

→ 고치려면 **모델이 아니라 촬영**입니다. 촬영 가이드 축이 지금까지
"털을 헤쳐라" 하나뿐이었는데 두 번째 후보가 생겼습니다.
⚠️ 소거법이고 관찰입니다 — 직접 시험하려면 같은 병변을 조명 각도만 바꿔 찍은
쌍이 필요한데 이 데이터엔 없습니다.

### ★ 재다가 제 관문의 구멍을 찾았습니다

`granularity_report` 는 **긴급도 하향(위험)만** 셌습니다. 과잉(안 급한 걸
급하다고)은 병원에 가게 만들 뿐이라 관문에서 뺀 건 맞는데, **찍지도 않은 게**
문제였습니다:

    6종            하향 4.7%   과잉  9.1%
    권고안 4군       하향 1.7%   과잉 49.3%

**권고안의 하향 1.7% 는 과잉 49.3% 로 산 것이었습니다.** A2(관찰)를
A3(진료 권장)와 묶고 묶음의 긴급도를 높은 쪽으로 잡았으니 당연한 결과인데,
저는 "하향 1.7%" 만 보고 안심하고 있었습니다. 극단적으로 전부 "조기 진료"
라고 말하면 하향은 0% 입니다 — **한쪽만 재는 관문은 반대쪽으로 도망칩니다.**

지금은 화면에 긴급도를 안 띄워서 안 보입니다. 문제는 그다음입니다 —
**계열 이름과 긴급도를 같이 띄우면 절반이 한 단계 부풀려집니다.**

### 탐색하고 닫은 것

실측이 "A5 의 상대는 A1·A4" 라고 하니 셋을 합쳐 봤습니다. 커버리지 67.4% ·
A5 포착 0.510 → **0.804** · 정확도 0.8490 로 전부 낫습니다.
**그래도 안 합니다.** ① 혼동행렬을 보고 만든 묶음이고 임상 해설은 A1↔A5 를
🔴 위험한 혼동(*"궤양을 구진으로 → 지켜보다 악화"*)으로 꼽았습니다.
② 말한 것의 **29.1%** 가 "조기 진료" 로 나갑니다(권고안 5.0%) — 구진에도
조기 진료라고 하면 그 말의 값이 없어집니다.
**데이터가 좋아 보인다고 문서가 위험하다고 적은 구별을 없애지 않습니다.**

원본: [`docs/results/STEP30_A5_미란궤양과_과잉분류.md`](docs/results/STEP30_A5_미란궤양과_과잉분류.md)

---

## STEP 31 — 전체 데이터 확인: VL01 이 비관적이었습니다 (2026-09-06, 캐글 추론 1회)

STEP 27~30 의 커버리지는 전부 VL01(전체의 10.8%) 값이었습니다. 사용자가
*"다른 컴퓨터에 있는 데이터를 어떻게 주지? USB 밖에 없나?"* 라고 물었을 때
확인해 보니 **옮길 필요가 없었습니다** — TL01·TL02 크롭은 이미 캐글에 있고
(노트북 09·10 이 그걸로 돌았습니다), 제가 VL01 로만 잰 이유는 데이터가
없어서가 아니라 **로컬에 GPU 가 있어서**였습니다.

노트북 11 을 만들어 배열 하나(`p1` · `truth` · `p2 × 3팔`)만 뽑아 오게 하고,
나머지 분석은 로컬에서 공짜로 돌렸습니다.

    알갱이            VL01     전체      차이     하향     과잉
    6종 이름          33.7%   41.4%   +7.7%p      —       —
    계열 4군          61.4%   69.8%   +8.4%p    3.1%   48.1%
    계열 3군          62.1%   70.8%   +8.7%p    3.1%   48.1%
    긴급도 3등급       42.7%   49.2%   +6.5%p    8.8%    6.8%

**VL01 이 넷 다 비관적이었습니다.** 계열 4군이 논의 문턱(50%)을 넉넉히 넘고,
A6 안전 관문은 표본 **53 → 859장**에서 **10.1%** 로 통과했습니다.
STEP 28 에 적어둔 "표본 53장이라 통과를 안전으로 읽으면 안 된다" 는 걱정이
실제로 해소된 드문 경우입니다.

### ★ 인용해온 1단계 recall 도 틀렸습니다

`recall 0.970` 은 **VL01 값**이었습니다. 전체 val 51,983장에서는 **0.9434**
입니다. 병변 100마리 중 놓치는 게 3마리가 아니라 **5.7마리**입니다.
`target_recall: 0.95` 로 뽑은 임계값인데 목표보다 0.7%p 낮게 나옵니다.

이 프로젝트에서 **"VL01 로 잰 값을 전체 값처럼 인용한" 세 번째 사례**입니다
(A4 recall · 커버리지 · 이제 1단계 recall). 작은 청크로 재는 것 자체가
문제가 아니라, **재고 나서 어느 청크였는지를 숫자 옆에 안 적은 것**이 문제였습니다.

### VL01 은 후한가 짠가 — 물음이 틀렸습니다

    macro-F1 · A4 recall   → VL01 이 낙관적   (STEP 23·25)
    커버리지 · A6 경보      → VL01 이 비관적   (STEP 29·31)

**같은 청크가 지표에 따라 반대로 틀립니다.** "VL01 은 후하다" 로 뭉뚱그리면
계속 틀립니다. 방향을 짐작하지 말고 재는 수밖에 없습니다.

### 그래서 모델 쪽 일이 끝났습니다

결정에 필요한 숫자가 다 모였습니다 — 무엇을 말할지(계열 4군), 얼마나
말할 수 있는지(69.8%), 틀리면 얼마나 위험한지(하향 3.1% · A6 오명명 10.1%),
대가가 무엇인지(과잉 48.1%). **남은 건 사람의 판단입니다.**

원본: [`docs/results/STEP31_이름_커버리지_전체데이터.md`](docs/results/STEP31_이름_커버리지_전체데이터.md)

---

## STEP 32 — 수의학 축으로 검증했고, 앙상블은 필요 없었습니다 (2026-09-06, 추론 0회)

사용자가 세 가지를 물었습니다: holdout 은 언제 필요한가 · **4군으로 나눠도
수의학적으로 되는지 먼저 리서치하자** · 앙상블은 쓰는 건가.

### 리서치 — 표준이 우리 묶음을 지지했습니다

수의피부과 표준 축은 **primary / secondary** 입니다. 대입해 보니:

    A1 구진·플라크 → primary      A4 농포      → primary
    A6 결절·종괴   → primary(>1cm) A5 미란·궤양 → secondary
    A2 · A3        → **둘 다 mixed** (secondary 이거나 '둘 다 됨')

STEP 28 에서 저는 A2+A3 를 *"문서가 인정한 게 아니라 남은 것"* 이라고 적었는데,
표준 문헌에 **더 나은 근거**가 있었습니다 — 둘이 **똑같이 애매한 자리**에 있고,
출처가 *"secondary 는 여러 primary 에서 나올 수 있어 진단 가치가 낮다"* 고
적습니다. **덜 진단적인 둘을 묶는 것**이라 사후 정당화가 아닙니다.
그리고 A1·A4(≤1cm)와 A6(>1cm)의 **1cm 는 교과서 경계**였습니다.

### 🚫 그런데 그 축으로 묶으면 기각이었습니다

    묶음                          커버리지    하향     과잉
    ★ 계열 4군 (권고)              69.8%    3.1%   48.1%
    교과서 primary/secondary(2군)  74.6%    0.0%   88.4%  ← 기각
    교과서 + 1cm 경계 (3군)        72.4%    1.3%   83.4%  ← 기각

**커버리지 1등에 하향 0.0%** 인데 과잉이 88.4% 입니다. `primary` 에 A6(조기
진료)이 들어 있어서 "primary" 라고 말하는 순간 **구진 하나에도 조기 진료**가
붙습니다. ★ STEP 30 에서 만든 과잉 관문이 **두 번째로** 후보를 걸렀습니다
(첫 번째는 '긴급도 2등급').

교훈: **교과서는 진단 순서의 축이지 보호자에게 뭐라고 부를지의 축이 아닙니다.**
표준이라고 그대로 쓰면 안 됩니다.

### 앙상블 — 안 써도 됐습니다

STEP 31 의 69.8% 가 **3팔 앙상블 값**이라는 걸 사용자 질문 덕에 확인했습니다.
노트북 11 이 릴리스 단독을 안 쟀더군요. 배열을 받아 다시 재니:

    묶음               릴리스 단독   앙상블    값어치
    6종 이름             26.9%    41.4%   +14.6%p
    ★ 계열 4군           59.4%    69.8%   +10.4%p

**계열 4군은 릴리스 단독으로 문턱(50%)을 넘습니다** (하향 3.3%).
추론 3배를 치를 이유가 없습니다.

반대로 **6종 이름을 켜려면 앙상블이 필수**입니다 — 릴리스 단독 26.9% 는 닫음
문턱(30%) **아래**이고 앙상블이 41.4% 로 올립니다.

규칙성 하나: **굵게 말할수록 앙상블의 몫이 줄어듭니다** (+16.6 → +10.4 → +7.5%p).
앙상블이 메우는 건 세밀한 구분이고, 굵게 말하면 그 구분이 애초에 필요 없습니다.

### 또 같은 실수를 할 뻔했습니다

69.8% 를 "우리 성능" 으로 적어뒀는데 그건 **앙상블 값**이었습니다. 어제
"VL01 로 잰 값을 전체 값처럼 인용" 한 걸 세 번 찾아놓고, 오늘은 **"무엇으로
쟀나" 를 안 적어서** 같은 종류를 반복했습니다. 청크에 이어 두 번째 축입니다.

원본: [`docs/results/STEP32_수의학축_검증과_앙상블_값어치.md`](docs/results/STEP32_수의학축_검증과_앙상블_값어치.md)

---

## STEP 33 — holdout 을 열었습니다. 버팁니다 (2026-09-07, 추론만)

결정이 굳었으니 마지막 시험지를 열었습니다. **한 번 보고 그 값을 적습니다** —
나빠서 설정을 바꿔 다시 돌린 게 아닙니다.

    알갱이                 val    holdout     차이
    6종 이름              41.4%    41.1%   −0.3%p
    ★ 계열 4군 (권고)      69.8%    67.9%   −1.9%p
    계열 4군 · 릴리스 단독   59.4%    58.4%   −1.0%p

**관문 셋 다 통과** — 커버리지 67.9%(≥50%) · 하향 3.7%(≤5%) · A6 오명명
12.5%(≤30%). STEP 27~32 가 알갱이·문턱·묶음을 전부 val 에서 골랐는데도
**낙관 편향이 1~2%p 로 작았습니다.** 결정을 바꿀 이유가 없습니다.

### ⚠️ 딱 하나 크게 떨어졌습니다 — A6 경보

    정밀도 90%   재현율  val 50.4%  →  holdout 25.1%   (−25.3%p)
    정밀도 80%   재현율  val 68.1%  →  holdout 48.8%   (−19.3%p)

**반토막**입니다. 다른 지표가 1~2%p 인데 여기만 25%p 이고, holdout 의 A6 이
1,003장이라 표본 탓도 아닙니다. **왜인지는 못 잽니다** — A6 이 holdout 에서
더 어렵거나, val 에서 A6 쪽으로 과적합했거나 둘 다일 수 있습니다.

결론은 분명합니다: **"덩어리가 의심됩니다" 를 90% 정밀도로 켜면 실제 덩어리의
4분의 1만 잡습니다.** STEP 29 에서 "VL01 이 4배 깎아 봤다" 며 살려낸 기능인데,
**전체 val 은 반대로 2배 부풀려 보고 있었습니다.** 같은 지표가 청크에 따라
양쪽으로 틀립니다.

### 관문이 holdout 에서도 일했습니다

    교과서 primary/secondary (2군)   커버리지 72.9%  하향 0.0%   과잉 89.0%  🚫
    긴급도 2등급                     커버리지 89.4%  하향 40.7%  과잉 1.7%   🚫

STEP 28(하향)과 STEP 30(과잉)에서 만든 두 관문이 **holdout 에서도 같은 둘을**
걸렀습니다. 커버리지만 봤으면 둘 중 하나를 골랐을 겁니다.

### 여는 김에 규칙이 거짓이었던 것도 찾았습니다

"holdout 은 06 만 엽니다" 가 문서에만 있고 검사가 없었습니다. 검사를 넣자마자
**05 와 07 도 열고 있었다**는 게 드러났습니다 — 규칙이 05 → 06 으로 바뀔 때
05 를 안 고쳤고 07 이 따라 열었습니다. 에러는 한 줄도 안 났습니다.

이미 본 것을 안 본 것으로 못 만듭니다. **있는 그대로 적고**
(`tests/test_holdout_discipline.py` 의 ALLOWED), 앞으로 새 노트북이 여는 것을
막습니다. ⚠️ 그 대가로 위 −1.9%p 도 **낙관 쪽으로 조금 치우쳐 있을 수** 있습니다.

원본: [`docs/results/STEP33_홀드아웃_확인.md`](docs/results/STEP33_홀드아웃_확인.md)

