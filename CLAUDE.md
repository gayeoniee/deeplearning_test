# CLAUDE.md — 작업 규칙과 결론 (사람/AI 공용 온보딩)

반려견 피부 병변 **스크리닝 보조** 모델. 진단은 하지 않습니다.
"이건 좀 의심되니 병원 가보세요" 까지가 목적입니다.

## 세 파일을 나눠 씁니다

| 파일 | 무엇이 들어가나 | 언제 바뀌나 |
|---|---|---|
| **CLAUDE.md** (이 파일) | 작업 규칙 · 용어 · 폴더 구조 · **결론 난 것** · 함정 | 결론이 새로 날 때 |
| [**STATUS.md**](STATUS.md) | 지금 숫자 · 확정 설정 · 열려 있는 문제 · 다음 할 일 | 실험할 때마다 |
| [**HISTORY.md**](HISTORY.md) | STEP 별 시간순 기록 — **왜 그 결정을 했는지** | 추가만 (안 지움) |

숫자의 원본은 전부 [`docs/results/`](docs/results/) 에 있습니다.

> **처음 오셨다면**: 이 파일의 작업 규칙 → `STATUS.md` → 필요하면 `HISTORY.md`.


## 스택

* **학습**: PyTorch 2.x + timm (백본), AMP + EMA + cosine warmup
* **패키지**: uv (Python ≥3.10) — 프로젝트 1개 / lock 1개. **pip 안 씁니다**
* **데이터**: AI Hub 561 「반려동물 피부 질환」 — 반려견 + 일반카메라만
* **매니페스트**: parquet (pandas + pyarrow) — DB 없음, 파일 하나가 진실
* **실행 분리**: 전처리는 **한국 PC**(AI Hub 가 해외 IP 차단), 학습은 **Kaggle/Colab**
* **서빙**: FastAPI 데모 서버(`serve.py`) + 단일 HTML 데모(`demo/`).
  안드로이드 앱(**DAENGS_APP**)이 붙을 자리이고 **아직 합치지 않았습니다** —
  계약과 남은 위험은 [`docs/SERVING.md`](docs/SERVING.md)

```bash
uv sync                 # 전처리용 (torch 없이, ~30초)
uv sync --extra train   # 학습까지 (torch/torchvision/timm/grad-cam)
uv sync --extra serve   # 데모 API 서버 (torch 없이도 --mock 으로 화면 확인)
uv run python prepare_local.py --chunk VL01
uv run --extra serve python serve.py --mock          # → http://127.0.0.1:8000/
```

---

## 문제 정의 — 2단계

① **정상/이상** → ② 이상이면 **병변 6종**

| 코드 | 병변 | 영어 |
|---|---|---|
| A1 | 구진·플라크 | papule / plaque |
| A2 | 비듬·각질·상피성잔고리 | scale / crust / epidermal collarette |
| A3 | 태선화·과다색소침착 | lichenification / hyperpigmentation |
| A4 | 농포·여드름 | pustule / acne |
| A5 | 미란·궤양 | erosion / ulcer |
| A6 | 결절·종괴 | nodule / mass |
| A7 | 무증상(정상) | — (파이프라인 최종 7종에만 등장) |

병명이 **아닙니다** — **병변 형태**입니다. 두 단계는 **학습 때 서로 안 봅니다** —
`stages.to_stage2()` 가 정답 라벨로 병변만 걸러 학습하고, 두 모델은 **추론에서만**
이어집니다. 그래서 한쪽만 따로 실험할 수 있습니다.

---

## 작업 규칙

1. **숫자는 실측만.** 추정치를 문서에 쓰지 않습니다. 돌려서 나온 값만
   `docs/results/` 에 남기고, 이후 모든 실험은 그 값과 비교합니다.
   (로드맵의 "macro-F1 0.70~0.80" 은 데이터를 보기 전 추정이었고 **틀렸습니다**.
   실측 기준으로 0.55~0.65 로 내려 잡았습니다.)
2. **판정 기준은 실험 전에 못 박습니다.** 결과를 보고 기준을 고르면
   무슨 숫자가 나와도 성공담이 됩니다.
3. **바뀔 수 있는 로직은 `src/` 에.** 노트북 셀은 `git pull` 로 갱신되지
   않습니다 — 한 번 Colab/Kaggle 에 올린 `.ipynb` 는 다시 Import 하기 전까지
   그대로입니다. 그래서 게이트(`gates.py`)와 비교 실험(`experiments.py`)을
   노트북 말고 `src/` 에 둡니다.
4. **막힌 원인을 고치기 전에 다음 단계로 안 갑니다.**
   1단계가 holdout 에서 안 되던 동안 04(2단계 백본 비교)를 미뤄뒀습니다.
   STEP 10 에서 1단계가 풀렸으니 이제 04 차례입니다.
5. **커밋/푸시는 지시가 있을 때.** 데이터·가중치·키는 절대 커밋하지 않습니다.
6. **수작업 데이터 큐레이션은 후반에 합니다** (멘토 피드백 5·6번).
   클래스 병합·데이터 삭제·라벨 교정은 자동 실험을 다 짜낸 뒤에. 조건이 바뀌면
   어떤 데이터가 문제인지도 바뀌는데, 되돌리기는 어렵습니다.
7. ★ **판정은 보호자 조건에서 합니다 (2026-09-12).** 라벨러가 병변에 딱 맞춰 그린 네모로 잰 값
   (1단계 0.93 · 2단계 0.60 · 커버리지 66.5%)은 **상한**이지 보호자가 보는 성능이 아닙니다 —
   보호자 네모는 2.8배 크고 중심이 어긋나 1단계 recall 이 87 → 73% 로 떨어집니다(프록시).
   새 후보의 채택 관문은 **앱처럼 찍은 사진 + 사용자 네모(또는 네모 없음) + 배포 팔 구성**
   (`tools/detect_coverage.py`)에서 정하고, 라벨 네모 값은 옆에 참고로만 적습니다.
   STEP 36 에서 이 차이를 알고도 각주로만 두고 기준을 안 바꿨습니다 — 두 달 뒤 STEP 50·51 이
   1팔·라벨 조건에서 "통과" 로 보였다가 3팔·보호자 조건에서 뒤집힌 것이 그 대가입니다.

---

## 용어 표기 규칙

한국어로 쓰되 **학술 용어는 영어를 병기**합니다. 논문·문서·회의에서 통하는 건
영어 쪽이라, 한국어만 쓰면 검색도 안 되고 팀 밖에서 말이 안 통합니다.

| 우리 표기 | 영어 | 주의 |
|---|---|---|
| 데이터 누수 | **data leakage** | |
| 배율 교란 / 배율 강건성 | **scale perturbation / scale robustness** | ⚠️ multi-scale 아님 |
| 위치 교란 | **shift perturbation** | |
| 하한선 | **shortcut baseline** | 사진 없이 메타데이터만으로 맞히기 |
| 파인튜닝 | **fine-tuning** | 우리는 처음부터 이것 (전이학습은 우산 개념) |
| 백본 / 헤드 | **backbone / head** | |
| 정성평가 | **qualitative evaluation** | |
| 확률 보정 | **calibration** | |
| 거절 | **abstention** | "판단 어려움" 으로 물러서기 |
| 미탐 / 오탐 | **false negative / false positive** | |
| 촬영 가이드 | **capture guideline** | |
| 개체 단위 분할 | **group-aware split** | groups = animal_id |
| 털처럼 가는 선 | **fine texture / high-frequency energy** | ⚠️ "결" 이라고 쓰지 마세요 — 제가 만든 말이라 아무도 못 알아봅니다. 그리고 이 값은 털·각질·주름을 **구분 못 합니다** — 털인지는 견종·부위로 따로 잽니다 |

⚠️ **`multi-scale` 과 `scale perturbation` 은 반대 방향입니다.**
전자는 여러 배율로 **학습**시키는 기법(= `zoom_both`, 우리는 실패),
후자는 학습을 마친 모델을 배율 바꿔가며 **시험**하는 검사입니다.


---

## 폴더 구조

```
deeplearning_test/
├── pyproject.toml / uv.lock   의존성. 기본=전처리용(torch 없음) / --extra train=학습
├── CLAUDE.md                  이 파일 — 작업 규칙 · 용어 · 결론 · 함정
├── STATUS.md                  ★ 지금 숫자 · 확정 설정 · 열려 있는 문제 · 다음 할 일
├── HISTORY.md                 ★ STEP 별 시간순 기록 (왜 그 결정을 했는지)
├── README.md                  프로젝트 소개 + 빠른 시작 + 설계 근거 8가지
│
├── prepare_local.py           ★ 한국 PC 전용 CLI: 다운로드 → 정제 → 크롭 → zip
│                                --chunk VL01 / --finalize / --package
├── diagnose.py                원본 JSON 스키마 확인용 일회성 도구 (스키마 3곳이 틀렸었음)
├── serve.py                   ★ 데모 API 서버 (FastAPI). 앱이 붙을 자리
│                                --mock 이면 torch 없이 화면만
├── demo/index.html            데모 UI — DAENGS 챗봇 화면 재현 (앱 색 그대로)
│
├── notebooks/                 ★ 클라우드(Colab/Kaggle) 전용. 첫 셀이 git clone 까지 합니다
│   ├── 03_학습_베이스라인       1·2단계 학습 + 견고성 검사 (384px) — val 까지
│   ├── 03b_증강_빠른스윕         증강 7종 ✅ 끝 (효과 없음)
│   ├── 03c_크롭비교              m1.5 vs m2.5 ✅ 끝 (m2.5 채택)
│   ├── 03d_1단계_고치기          1단계 백본×증강 2×2 ✅ 끝 (effnetv2_s+photometric)
│   ├── 03e_1단계_f320비교        1단계 입력 full vs f320 ✅ 끝 (f320 채택)
│   ├── 03f_1단계_백본확장       ★ **1단계** 백본 3종 (f320) — 헛알림 줄이기. 대기
│   ├── 04_학습_최신모델_비교     ★ **2단계** 백본 비교 — 25에폭으로 재실행 대기
│   ├── 05_평가_보정_GradCAM     보정·거절·CAM·holdout ✅ 끝
│   └── 06_확정재학습_홀드아웃    ★ 학습+holdout 한 세션 ✅ 끝 (STEP 10)
│                              ⚠️ **holdout 을 여는 노트북은 넷** — 05·06·07·12
│                                 (03 은 val 까지). `tests/test_holdout_discipline.py`
│   README.md                  ★ 어떤 노트북을 언제·무엇을 붙여서 돌리나
│                              ⚠️ 00~02 가 없는 이유: 데이터 확보/전처리는
│                                 노트북 말고 prepare_local.py 가 합니다
│
├── src/                       ★ 로컬과 클라우드가 **공유하는** 실제 로직 (8.5k줄)
│   ├── env.py                 Colab/Kaggle/로컬 자동 감지, 경로·시크릿·GPU 확인
│   ├── config.py              모든 하이퍼파라미터 + 증강/파인튜닝 프리셋 한 곳
│   │  ── 데이터 만들기 (로컬에서만 돌아감) ──
│   ├── aihub.py               aihubshell 래퍼 — 반려견+일반카메라 filekey 자동 선별
│   ├── scan.py                폴더/JSON 스키마 **추론** (가정하지 않음)
│   ├── labels.py              JSON → manifest.parquet
│   ├── dedup.py               phash 중복 제거. 라벨 충돌 그룹은 전부 버림
│   ├── split.py               ★ 개체 단위 그룹 분할. verify() 가 누수 시 에러
│   ├── crop.py                ROI 크롭 (m1.5 / m2.5 / full / f320) + 감사
│   │  ── 학습하기 ──
│   ├── stages.py              ★ 2단계 구조 — **하나의 분할을 공유하는** 두 뷰
│   ├── data.py                Dataset / 증강(albumentations) / DataLoader
│   ├── models.py              timm 백본 팩토리 (없는 모델명은 대체 + 경고)
│   ├── train.py               학습 루프 + **세션 끊김 이어받기** + 로짓 캐시
│   │  ── 검사하기 ──
│   ├── evaluate.py            지표·혼동행렬·부트스트랩 CI
│   ├── robust.py              배율/위치/**화질** 교란 + 촬영 가이드 도출
│   ├── gates.py               품질 정지선. STOP(깨짐)에서만 멈추고 WANT 미달은 경고
│   ├── experiments.py         비교 실험 판정 규칙 + 시작 전 시간 추정 (규칙 3)
│   ├── calibrate.py           온도 보정 · ECE · coverage-risk
│   ├── explain.py             Grad-CAM 정렬도 (배경 보는지 **수치로**)
│   ├── infer.py               단일 사진 추론 (문구는 message.py 에서 재수출)
│   ├── effectsize.py          ★ `d` · `AUROC` · 판정 문턱 — **정의는 여기 하나뿐**
│   ├── texture.py             `blur`/`hair` 등 사진 값 — **정의는 여기 하나뿐**
│   ├── message.py             ★ 보호자에게 보여줄 문구. **torch 안 씁니다**
│   │                          ⚠️ 문구 함수가 **둘**입니다 — `compose_message`
│   │                          (단일 모델, 이름을 말함) / `compose_screening_message`
│   │                          (★ 2단계 파이프라인, **이름을 말하지 않음**)
│   └── agent.py               ★ 앱이 받는 파이프라인 + JSON 계약
│                              가이드 프레임을 bbox 로 받아 crop.crop_window() 호출
│
├── tests/                     200+ 검사. `uv run python tests/<파일>`
│   ├── test_notebook_kaggle_e2e.py  ★ Kaggle 환경을 통째로 흉내 내 03 전 셀 실행
│   ├── test_kaggle_layout.py        데이터셋 경로·심볼릭링크·분할 업로드
│   ├── test_resume.py               세션 끊김 이어받기 + 로짓 캐시 지문
│   ├── test_crop_geometry.py        크롭 창 계산
│   ├── test_robust.py               교란 뷰
│   ├── test_stages.py               2단계 분할 공유
│   ├── test_windows_encoding.py     cp949 회귀 (같은 버그로 3번 막혔음)
│   ├── test_screening_message.py    ★ 최종 출력이 병변 이름을 단정하지 않는가
│   ├── test_calibration_wiring.py   ★ 보정값이 서빙까지 도착하는가
│   ├── test_agent.py                ★ 앱 계약에 "1등 병변" 필드가 생기는지 감시
│   ├── test_crops_tool.py           쓰는 크롭을 지우지 않는가
│   ├── test_capture_guide.py        촬영 가이드 밴드 계산
│   └── test_notebook_names.py       노트북 셀의 이름·인자 오타 (실행 없이)
│
├── docs/
│   ├── 00_로드맵.md            전체 STEP (현재 위치는 STATUS.md)
│   │  ── tools/box_error.py — 사람이 그린 네모 vs 정답 bbox (수의사 불필요)
│   │  ── tools/class_overlap.py — ★ 두 병변이 **데이터에서** 겹치나 (모델·GPU 불필요)
│   │  ── tools/false_alarm_stats.py — 헛알림 난 사진이 무엇과 다른가
│   │  ── tools/crops.py    — 크롭 용량 보고 · 안 쓰는 태그 정리 · 새 청크 예상
│   │  ── tools/merge_incoming.py — 다른 PC 에서 온 청크 합치기 (미리보기가 기본)
│   │  ── tools/fix_split_zip.py — 조각 순서가 뒤바뀐 zip 고치기 (재다운로드 없이)
│   ├── SERVING.md             ★ 앱 연동 — API 계약 · 서빙 크롭 · 아직 안 된 것
│   ├── OTHER_PC.md            다른 PC 에서 청크 처리해서 크롭만 가져오기
│   ├── results/               ★ 실측 기록 — **숫자는 전부 여기**. 추정치 금지
│   ├── data/DATASET_CARD.md   실물 스키마 (추론이 틀렸던 3곳 포함)
│   ├── basics/                딥러닝 기초 10편 (ML 경험자 전제, DL 은 처음부터)
│   └── cautions/              주의사항 9편 (누수·라이선스·2단계 설계·세션 끊김 …)
│
└── data/                      ★ 전부 git 미추적
    ├── raw/                   AI Hub 원본 (크롭 후 삭제)
    └── work/{crops,manifests} 크롭 + manifest_final.parquet → zip 으로 클라우드에
```

### 커밋하면 안 되는 것

AI Hub 데이터는 **재배포 금지**입니다. `.gitignore` 가 막고 있습니다:
`/data/`, `*.jpg`, `*.parquet`, `*.pt`, `.env`, `*apikey*`

* 클라우드 업로드는 **반드시 Private** (Kaggle `datasets create` 기본값이 private —
  `--public` 을 붙이지 마세요)
* API 키는 코드·노트북 셀에 붙여넣지 말고 환경변수 `AIHUB_API_KEY` 또는
  Colab/Kaggle Secrets 로만. `env.secret()` 이 읽고 로그에서 마스킹합니다
* 해외 IP 차단을 VPN 으로 우회하지 않습니다

---

## 결론 난 것 (다시 하지 말 것)

| 질문 | 결론 | 근거 |
|---|---|---|
| 파인튜닝 강도 | 백본 lr ×0.1 → **×0.3**(`moderate`) | macro-F1 +0.065 |
| 크롭 배율 지름길이 있나 | **없음** — CNN 이 크기를 안 씀 (A1 −0.024, A6 −0.125) | 클래스별 하한선 비교 |
| `f320` 이 **2단계**에 필요한가 | **불필요** — 지름길도 없고 배율 견고성에도 도움 안 됨 | STEP 4A |
| `f320` 이 **1단계**에 필요한가 | **예 — 크게 필요합니다.** val AUROC +0.1205, 흐림 하락 −8.0%p. ⚠️ 위 줄과 질문이 다릅니다 | STEP 9-A |
| ★★ **병변이 안 잘리게 random crop 을 걸면 되나 (멘토 제안)** | **아니오 — 기각 (STEP 45).** 1차(STEP 44)에서는 *"이득 없음, 미결"* 이었습니다: 문턱 0.5 에서 macro-F1 0.3457 vs 일반 crop 0.6488 로 재앙처럼 보이는데 **AUROC 는 오히려 위**입니다 (0.7203 vs 0.7114) — 실력이 아니라 **문턱이 0.96 근처로 밀린 것**입니다. fold 밖에서 문턱을 고르면 **0.6550 vs 0.6498**, 차이 0.0052 로 재현 폭(±0.016) 안. 게다가 그 문턱은 **같은 검증셋을 뒤져** 고른 낙관적인 값입니다. ⚠️ **하한선 미측정** — STEP 42 에서 데이고도 또 안 적었습니다. ★ **2차(STEP 45)에서 닫혔습니다** — EfficientNetV2-S·384px 로 올리고 대조군을 **고정 ROI**(둘 다 병변 100% 보존)로 바꾸니 **AUROC 까지 졌습니다** (고정 0.8552/0.9342 vs 보존 random 0.8157/0.8980, 모든 동일 epoch). 순위 지표가 지면 문턱으로 못 따라잡습니다. ⚠️ **단 두 파일럿 다 채택 학습법이 아닙니다** — `photometric`·backbone LR 배율·warmup·EMA 가 빠졌고 백본 LR 이 3e-4(채택 9e-5)입니다. *"이 설정에서 졌다"* 이지 *"채택 레시피에서도 진다"* 가 아닙니다 | STEP 44 → 45 |
| ★★★ **진짜 object detection(D-FINE)이면 위치가 풀리나** | **아니오 — 기각 (STEP 51, 2026-09-12). 그리고 이걸로 위치 축을 닫습니다.** D-FINE-L(Apache, 위치 정밀도 특화) 을 15만 장으로 파인튜닝: best epoch 2 중심 오차 **0.118**, 이후 과적합. 배포 3팔에서 2단계만 배선 **26.3% vs 사용자 31.6% (−5.4%p)**, 중심 오차 0.124 — STEP 50 회귀 헤드(0.103)보다 나쁨. ★ **회귀 헤드(VL01) 0.132 → 회귀 헤드(7배) 0.107 → D-FINE 0.12: 모델·데이터를 바꿔도 같은 자리** = 벽은 **라벨**. 번진 병변(비듬·각질·태선화)엔 중심이 정의되지 않고(사람도 47.5%, STEP 37) bbox 는 외곽선의 외접사각형이라 그 정확한 중심을 사진만으로 다시 맞힐 정답이 없습니다. → *"위치를 더 정확히 찾아 꽉 맞는 창"* 은 STEP 42·50·51 로 닫음. 남는 길: 창이 어긋나도 견디는 2단계(STEP 49 `safe`) · 검출기를 **"병변이 있나"** 에 쓰기(STEP 52). ⚠️ EMA 는 **워밍업** 없이 쓰면 첫 epoch 평가가 엉망입니다(실측 크기비 3.7배) | STEP 51 |
| ★★★ **검출기를 데이터 7배로 다시 배우면 되나** | **아니오 — 기각 (STEP 50, 2026-09-11). 그런데 두 번 뒤집혔습니다.** 원본 191GB 로 149,800장(6.8배)·fold 분할·`Resize` 만(STEP 42 는 CenterCrop 이 가장자리 12% 를 잘라 정답과 어긋남). 검출기 자체는 좋아졌습니다 — 밴드 둘 다 29.3 → **39.2%**, 중심 오차 0.132 → **0.107**. ★ 판정은 밴드가 아니라 **커버리지**(배포 지표)로: 릴리스 **1팔**로 재니 2단계에만 쓰는 배선 **+8.3%p** 로 관문(+5) 통과처럼 보였는데, **배포 3팔**로 재니 **−0.9%p** — 1팔의 이득은 `convnextv2 m2.5` 한 팔이 창 포화에 약한 성질이었고 3팔의 `f320` 팔이 이미 메우고 있었습니다(STEP 39). 검출기 크롭(중심 오차 111px)은 `m2.5` 창 여유(~147px)에 자주 걸려 **사진 전체보다 못합니다** — STEP 41 의 *"포화는 우연한 안전장치"* 그대로. ⚠️ **1단계 recall 은 72.9 → 84.8% 로 회복되지만 헛알림 34 → 52%** (별개 결정) — 원인은 *정상에 네모가 없어서* 가 **아니라**(A7 도 라벨 네모가 100% 있음, 면적 중앙값이 병변보다 작을 뿐) **검출기를 병변 사진으로만 가르쳐** 정상 피부에서도 병변 같은 곳을 찾게 된 것. ⚠️ 2026-09-12 정정: 제가 검출 데이터셋에서 A7 을 **뺀 선택**을 데이터 사실처럼 말했었음. ⚠️ 교훈: **판정은 반드시 배포 구성으로** — 1팔은 상대 차이의 부호까지 틀렸습니다. 남는 질문(새 사전등록): 히트맵/폴리곤 검출기 · **STEP 49 `safe` 2단계 + 검출기 크롭** | STEP 50 |
| ★★ **같은 기법이 2단계에서는 되나 — "정상을 빼면"** | **예 — 후보 (STEP 49 파일럿, 2026-09-11).** 사용자 관찰: 1단계에서 진 이유가 *"정상 사진엔 보존할 병변이 없다"* 면 **2단계엔 그 비대칭이 없다.** A7 을 빼고 병변 6종에서 `fixed` vs `safe`: epoch 5 **shift 하락률 31.1% → 5.7%** (−25.5%p, 잡음 ±5%p 의 5배, 5 epoch 모두 같은 방향). clean macro-F1 −0.019(관문 −0.02 안) · 계열 4군 −0.004 · A6 recall −0.040 (문턱 −0.03 에 **1장** 미달, 사전등록이 미리 잡음(±0.05)으로 못 박음). ★ **`fixed` 는 배울수록 위치에 더 기댑니다** (9 → 31%) — shift F1 이 5 epoch 동안 한 번도 안 올랐습니다. STEP 16 위치 교란 32.2% 의 기전 후보. ⚠️ **정확한 문장: 위치 강건성 +25%p 를 clean −0.02 로 산다.** EfficientNetV2-S · 10K · val · 단일 seed — **배포 모델이 좋아진다는 뜻이 아니고 1단계 결론도 그대로.** 다음은 확대 실험(전체 데이터 · convnextv2_base · seed ≥2 · 커버리지 66.5% 관문)의 **새 사전등록** | STEP 49 |
| ★ 분류 지표가 무너지면 기각해도 되나 | **아니오 — 순위 지표를 같이 보세요.** STEP 44 에서 macro-F1 이 반토막(0.6488→0.3457)인데 AUROC 는 올랐습니다. **실력이 사라진 것**과 **문턱이 밀린 것**은 macro-F1 하나로 안 갈립니다. 문턱을 다시 고르니 붙었습니다 | STEP 44 |
| ★★ **`photometric` 이 새 분할·고정 ROI 에서도 사나** | **예 — 복원 확인 통과 (STEP 46).** 두 팔 다 고정 ROI 로 두고 **증강만** 바꿨습니다. clean macro-F1 .851→.857(잡음권)인데 **흐림 sigma2 F1 .746→.849(+10.40%p)**, **clean→sigma2 하락이 10.57%p → 0.73%p** 로 거의 사라집니다. STEP 6 과 같은 방향입니다. ★ **clean 만 봤으면 "잡음권" 으로 버렸을 것** — 판정 지표를 돌리기 전에 정한 게 일했습니다. ⚠️ **clean 이상 recall 은 −0.88%p** 로 조금 낮아졌습니다(1단계는 놓치지 않는 게 우선이라 다음 실험에서 같이 볼 것). ⚠️ photo 의 clean F1 은 **epoch 1~3 에서 오히려 낮습니다** — *"모든 epoch 우세"* 는 틀린 말 | STEP 46 |
| `photometric` 이 위치 교란도 잡나 | **아니오 — 화질만입니다 (STEP 47).** VL01 700장에서 **흐림만** 구간이 0 을 안 품습니다(+4.60~+11.29%p). 일반 입력(−2.08~+2.00)과 위치 이동(−1.84~+3.13)은 **구분 불가**. ⚠️ 이 비교엔 safe crop 모델이 없어 **멘토 제안의 위치 강건성을 잰 것이 아닙니다**. ⚠️ 6,183장 중 6,026장이 파일럿 검증과 **같은 그룹**이라 독립 외부 시험이 아닙니다 | STEP 47 |
| ⚠️ **이력 감사가 준 목록을 순서대로 하면 되나** | **아니오 — 각 항목의 근거를 다시 여세요.** 감사가 *"LR 배율도 빠졌다"* 를 `photometric` 과 나란히 적었는데, 원본을 열어 보니 **1단계에선 뚜렷한 개선이 없었습니다** (AUROC .8031→.8100 오차 범위 · 3차 .8020). `+0.065` 는 **2단계에서 기간 연장과 함께** 얻은 값이라 학습률 단독 효과로 분리가 안 됩니다. **목록에 나란히 있다고 근거의 세기가 같지 않습니다** | STEP 46 |
| 🚫 **photo+safe 후보를 배포에 연결해도 되나** | **아니오 — 제외 (STEP 48).** 앱 입력 형태(축소·JPEG·40% 네모)로 VL01 reserved 5,727장: macro-F1 0.7076 → **0.5973**, **AUROC 0.8745 → 0.7915**, 오탐 +484. ★ **STEP 44 와 반대 모양** — 그땐 AUROC 가 살아 문턱 문제였는데 이번엔 변별력 자체가 내려갔습니다. 같은 recall 에서 오탐 +617 이라 문턱을 맞춰도 안 됩니다. **피해가 정상 사진에 몰림** (후보만 틀린 751장 중 정상 669장, 정상 사진 이상확률 중앙값 0.157 → 0.683). **A6 놓침 0 → 6.** 284개체 중 96 나빠짐 · 23 좋아짐 — 흔들림이 아니라 일관된 하락. ⚠️ 연결 검사이지 실사용 평가가 아니라 좋았어도 채택 근거는 못 됐습니다 — **비대칭 기준** | STEP 48 |
| ★ **멘토 제안(병변 보존 random crop)은 "판단 불가" 인가** | **아니오 — 판단했고 두 번 졌습니다 (STEP 45 · 48).** *"검증할 독립 데이터가 없다"* 와 *"검증했는데 졌다"* 는 다른 문장입니다. 이번(STEP 48)은 `photometric` 까지 얹은 가장 유리한 판이었는데 더 크게 졌습니다(F1 −0.110 vs STEP 45 의 −0.040). 실사용 사진에서는 못 재지만 그건 배포 모델도 같습니다. 가설: `safe` crop 은 병변을 보존하는데 **정상 사진엔 보존할 병변이 없어** 크롭 분포가 갈림 → 정상 인식을 잃음. 증명 아님 | STEP 48 |
| ⚠️ 프록시 검사의 절대값을 인용해도 되나 | **아니오.** STEP 48 에서 **배포 모델도 정상의 51%** 를 병원에 보냈는데, 그건 고정 40% 네모 + JPEG 재압축이라는 일부러 가혹한 프록시 탓이고 holdout 18.7% 와 다른 숫자입니다. 프록시의 결론은 **두 모델의 차이**뿐입니다 | STEP 48 |
| ★★★ **"축이 닫혔다" 가 "다 해봤다" 인가** | **아니오 — 각 실험 조건에서의 결론입니다.** 이력을 대조해 보니 **새 전체 데이터(370,671장) 재학습 · 근접 중복(pHash) 검사 · 라벨/ROI 검수 · 실사용 구도 검증**이 전부 미실시였고, 최근 파일럿은 채택 학습법(`photometric`·LR 배율·warmup·EMA)이 빠진 별도 레시피였습니다. → **다음 우선순위는 새 기법이 아니라 "이미 효과가 확인된 설정이 새 데이터에서도 사는가" 입니다.** 새 백본·털 가중 샘플러·증강 스윕을 **새 아이디어처럼 반복하지 마세요** — 전부 이미 닫힌 축입니다. → 첫 복원 확인은 `notebooks/16_photometric_복원_비교.ipynb` (대기) | 이력 감사 |
| ★★ **두 팔이 두 가지로 다르면 무엇을 잰 건가** | **아무것도 못 잽니다 — STEP 44 가 그랬습니다.** 대조군이 *"병변을 자를 수도 있는 일반 random crop"* 이라 `병변을 자르나` × `창이 흔들리나` 두 축이 섞였습니다. STEP 45 에서 대조군을 **고정 ROI**(병변 100% 보존)로 바꾸니 남는 차이가 **창을 흔드는 것** 하나가 되고 그제야 답이 나왔습니다 — **해롭습니다**. ⚠️ STEP 38 이 *"대조군이 결정적이었다"* 로 배운 것의 다른 얼굴입니다 | STEP 44 → 45 |
| 증강으로 배율을 잡을 수 있나 | **아니오** — 3회 실패. 384 의 `zoom_both` 는 오히려 악화 | 31.6% / 27.7% / +4.0%p |
| 흐림·노이즈 증강이 도움되나 | **아니오** — macro-F1 −0.03. 질감이 곧 라벨입니다 | `photometric` |
| 증강 폭을 좁히면 나은가 | 0.85 에서 꺾이는 모양은 보이나 **잡음 안** | 17.6→15.9→16.5% |
| 서브셋 하락률을 풀에 대입해도 되나 | **아니오** — 약한 모델은 잃을 것도 적어 덜 떨어집니다 | 서브셋 17.6% vs 풀 25.9% |
| 교란 검사 잡음 폭은 얼마인가 | **±5%p** (±3%p 는 낙관적이었음) | 같은 모델 n=2000 21.6% / n=3000 16.8% |
| 1단계는 일반화되나 | **예 (해결됨)** — f320 으로 holdout AUROC 0.9304, val 과 격차 0.023. ⚠️ `full` 이던 시절엔 아니었습니다 (0.8143 → 0.7412) | STEP 10 |
| 2단계는 일반화되나 | **예** — holdout 에서 오히려 +0.027 | STEP 5 |
| 거절로 정확도를 올릴 수 있나 | **아니오** — 절반 거절해도 오답 40.9%→29.4% | STEP 5 |
| Grad-CAM 이 배경을 보나 | **아니오** — STEP 16 holdout 에서 mean lift **4.55**, **96%** 가 우연 이상 (n=150). STEP 5 는 1.82 / 78% 였습니다 | STEP 16 |
| 1단계에 화질 지름길이 있나 | **예** — 흐리게 하면 0.7430 → 0.3316 (동전 던지기) | STEP 6 |
| photometric 이 1단계에도 나쁜가 | **아니오** — 2단계와 반대. 흐림 하락 −38.5%p, AUROC 는 그대로 | STEP 6 |
| 1단계 백본을 바꾸면 되나 | **예** — effnetv2_s 로 AUROC +0.0244 (서브셋) / +0.018 (풀) | STEP 6·7 |
| 1단계 백본을 **더** 바꾸면 되나 | **아니오 — 축을 닫았습니다.** swinv2_base −0.0018 / convnextv2_base −0.0014 (둘 다 잡음 ±0.01 안). **4.4배 큰 모델도 못 이깁니다** — 1단계는 백본 표현력이 병목이 아닙니다 | STEP 14 판 A·B |
| 흐림 하락이 큰 건 모델이 커서인가 | **아니오 — 어텐션이라서.** CNN 은 크기와 무관하게 3%대 (effnetv2_s 20M 3.3% / convnextv2_base 89M 3.5%), swinv2_base 87M 만 **10.3%**. ⚠️ 어텐션 표본이 하나라 계열 전체로 일반화 금지 | STEP 14 판 B |
| 털 많은 정상 사진을 더 자주 보여주면 헛알림이 주나 | **아니오.** alpha 1/3 에서 헛알림 20.2% → 22.0%/21.7% (**늘었고** 단조 아님). 흐림 하락은 3.3%→0.2% 로 줄어 보이나 잡음(±5%p) 안. ⚠️ `hair` 가 헛알림을 **예측**한다(AUROC 0.749)는 것과 그걸로 **고칠 수 있다**는 건 다릅니다 | STEP 15 |
| f320 과 photometric 이 겹쳐서 듣나 | **예** — 흐림 하락 `full` 14.0% → `f320` **3.0%** (같은 백본·증강) | STEP 6 vs STEP 14 |
| 2단계 백본을 바꾸면 되나 | **예 — convnextv2_base.** 기준선 대비 macro-F1 +0.088 (잡음 ±0.02의 4배), 배율 하락 +3.3%p (잡음 안). ⚠️ 서브셋 결과라 풀 학습 확인 필요 | STEP 12 |
| 2단계도 effnetv2_s 로 바꿔야 하나 | **아니오** — macro-F1 은 이겼지만(+0.075) 배율 하락 +9.3%p 로 구분 불가. convnextv2_base 가 두 지표 다 낫습니다 | STEP 12 |
| 미수렴 기준선이 교란 검사를 왜곡하나 | **예 — 실측으로 확인.** resnet50 을 수렴시키니 배율 하락 12.2% → 15.1%. 덜 배운 모델은 잃을 것도 적습니다 | STEP 9→12 |
| 어텐션 계열이 CNN 을 이기나 | **구분 불가.** swinv2_base 0.5718 vs convnextv2_base 0.5744 — 차이 0.0026 으로 잡음 안. swinv2 가 1.6배 쌈 | STEP 12·13 |
| siglip2_base 는 왜 졌나 | **못 잰 것입니다** — train/val loss 가 내려가는 중에 조기 종료가 끊었습니다 (7에폭 train loss −0.16, 남들은 −0.45). 학습률 문제 | STEP 13 |
| 헛알림이 어디로 가나 | **A2(비듬·각질)로 55.8%**, A3 20.0%, A1 16.1%. A5·A6 은 거의 안 감 | STEP 11 |
| 헛알림 난 사진에 공통점이 있나 | **예 — 털처럼 가는 선이 많고 선명합니다.** `hair` d=0.80/AUROC 0.749, `blur` d=0.47/0.731. 밝기·채도·색온도는 무관. ⚠️ 흐려서 틀리는 게 **아닙니다** — 반대입니다 | 헛알림 사진통계 |
| 놓친 병변에 공통점이 있나 | **아니오** — 6개 값 전부 문턱 아래 (n=186) | 위와 같음 |
| 촬영 거리(가까이 찍음)가 헛알림 원인인가 | **아니오 — 기각.** `area` AUROC 0.462, `blur`/`hair` 와의 상관 −0.043/−0.067. 빼도 `hair` 가 0.749→0.747 로 그대로 | 헛알림 사진통계 |
| 그럼 촬영 가이드로 헛알림을 줄일 수 있나 | **예 — 하나 있습니다: "털을 헤쳐서 피부가 보이게".** 부위별 헛알림률이 털 덮인 순서 그대로입니다 (연접부 16.6% → 다리 41.0%, 2.5배) 그리고 **같은 부위 안에서도** 털이 덜 찍힌 사진이 낫습니다 (0.727). ⚠️ 거리·조명은 무관 — 바꿀 건 털뿐입니다. ⚠️ 관찰이지 개입 실험이 아닙니다 | 헛알림 사진통계 |
| 그게 진짜 **털** 때문인가 | **예 — 다만 품종이 아니라 부위로 확인됩니다.** 견종으로는 안 보입니다(비숑 8.2% 최저) — 견종은 "그 개의 털 양" 이고 문제는 "그 사진에 찍힌 털 양" 이라서요. 부위가 훨씬 나은 대용입니다. 장모치와와 28.7% vs 단모 치와와 26.5% (2.2%p), 비숑프리제(곱슬 장모)가 **8.2%** 로 최저. 견종을 고정해도 `hair` 가 0.749→0.739 로 그대로 | 헛알림 사진통계 |
| 그럼 뭐가 헛알림을 만드나 | **그 사진 한 장에 잔선이 얼마나 찍혔나.** 견종(0.739)·부위(0.727)·거리(0.747) 어느 것으로도 환원 안 되고, 얽힌 건 `blur` 하나(0.623)인데 그쪽은 방향이 뒤집힙니다 | 위와 같음 |
| 부위(`region`)가 헛알림에 영향 있나 | **예 — 다만 순서가 바뀌었습니다.** STEP 16 holdout: 몸통(B) 27.6% / 다리(L) 23.5% / 머리(H) 14.8% / **연접부(A) 13.6%** (2.0배). 예전(STEP 11)엔 L 41.0% > B 37.4% > H 18.9% > A 16.6% (2.5배)로 **앞 두 개가 반대**였습니다. 코드 뜻은 `docs/data/DATASET_CARD.md` | STEP 11 → STEP 16 |
| 놓침도 같은 부위 순서인가 | **아니오 — 정반대입니다.** 머리 7.6% / 연접부 7.0% > 몸통 5.0% > **다리 2.8%**. 털이 많이 찍히는 부위일수록 **헛알림은 늘고 놓침은 줍니다.** 털 가설과 방향이 맞지만 관찰이지 개입 실험이 아닙니다 | STEP 16 |
| 헛알림이 A2 로 가는 비율이 모델 탓인가 | **아니오 — 안 변합니다.** STEP 11(45,885행·resnet50) 55.8% → STEP 16(365,428행·convnextv2_base) **57.1%**. 데이터 8배·백본 교체에도 그대로라 **A2 라벨 자체의 성질**일 가능성이 큽니다. ⚠️ 원인은 여전히 미측정 | STEP 11 → 16 |
| A4 recall 0.264 는 장수 부족인가 | **아니오 — A1 에 흡수됩니다.** holdout 혼동행렬에서 A4→A1 이 **0.25 로 대각선(0.25)과 동률**입니다. 2,606장 중 652장을 A4 로, 똑같이 652장을 A1 으로 부릅니다. A5 도 같은 방향(A5→A1 0.17) | STEP 16 |
| 어느 병변을 가장 많이 놓치나 | **A4 12%** > A6 8% > A5 6% > A1·A2 5% > A3 2%. A4 는 **못 맞히기도 하고 놓치기도** 합니다. A6 8% 는 종양 감별이 필요한 병변이라 위험도로 따로 봐야 합니다 | STEP 16 |
| 주변합 역산으로 칸을 말해도 되나 | **아니오** — A1 을 2위(+372)로 봤는데 실제 칸은 3위(148). 순 흐름에 병변 간 혼동이 섞입니다 | STEP 11 |
| 오류가 한 클래스에 몰리나 | **예 — A2.** 오탐(FP) 966 + 미탐(FN) 452 = 1,418 (2위 A7 1,113). 놓친 병변의 55.6% 도 A2. ⚠️ 원인은 아직 미측정 | STEP 11 |
| 같은 설정을 두 번 돌리면 얼마나 다른가 | **macro-F1 0.4862 vs 0.5024 (±0.016), 배율 하락 15.1% vs 12.6%.** 시드까지 같아도 GPU 가 결정론적이지 않습니다 | STEP 12·13 |
| 배율 하락 8%p 로 후보를 떨어뜨려도 되나 | **아니오** — 우리가 잰 잡음이 9.6%p 라 잡음만으로 걸립니다. 문턱을 **12%p** 로 올리고 5~12%p 는 구분 불가로 적습니다 | STEP 10 잡음 측정 |
| 03 이 holdout 을 여나 | **아니오** — 03 은 val 까지 | STEP 7 |
| ★ **holdout 을 여는 노트북이 몇 개인가** | **넷입니다 — 05·06·07·12.** 문서에는 *"06 만 엽니다"* 라고 적혀 있었는데 **사실이 아니었습니다.** 규칙이 05 → 06 으로 바뀔 때 05 를 안 고쳤고, 07 은 그 뒤에 생기면서 따라 열었습니다. **검사가 없으니 아무 일도 안 일어났습니다.** ⚠️ 그 대가로 우리 holdout 숫자는 *"한 번만 봤다"* 보다 **노출이 큽니다.** 이미 본 것을 안 본 것으로 못 만드니 **있는 그대로 적고** 앞으로 새 노트북이 여는 것을 `tests/test_holdout_discipline.py` 가 막습니다 | STEP 33 |
| 화질 지름길을 막으니 1단계가 일반화되나 | **부분적으로** — holdout AUROC 0.7412 → 0.7666. 나머지는 입력 크롭(f320)이 해결했습니다 | STEP 8·10 |
| AUROC 가 오르면 recall 도 오르나 | **조금 오르면 아니오, 크게 오르면 예.** +0.025 일 땐 recall −0.015 (STEP 8), +0.164 일 땐 +0.030 (STEP 10) | STEP 8·10 |
| val 에서 정한 임계값을 holdout 에 그대로 써도 되나 | **예 — 모델이 잘 가르면.** 0.9501 → 0.9458 (STEP 8 은 0.9158 이었음). 고칠 곳은 임계값이 아닌 변별력이었습니다 | STEP 10 |
| val AUROC 최고를 고르면 되나 | **아니오** — 지름길 모델이 val 에서 높다. 견고성도 같이 볼 것 | STEP 5·6 |
| 해상도를 512 로 더 올릴까 | **아니오** — 크롭이 이미 중앙값 242px | 384 미만 74.6% 확대 중 |
| 해상도를 올리면 되나 | **예** — 224 → 384 로 8~9%p 감소 | 위 표 |
| 크롭 창을 넓히면 되나 | **예** — m1.5 → **m2.5** 로 배율 −9.9%p, 위치 −4.4%p | STEP 4C·4D |
| 위치 교란도 크롭으로 잡히나 | **예** (예상 밖) — 20.6% → 16.2%. 넓게 자르면 병변이 정중앙에만 있지 않음 | STEP 4D |
| m2.5 가 macro-F1 을 깎나 | **조금 깎습니다** — 네 번 재서 m1.5 가 네 번 다 위 (−0.027 쯤) | STEP 4D |
| A6 을 지금 개선할 수 있나 | **예 — 풀렸습니다 (STEP 16).** recall 0.328 → **0.619**. 답은 백본이 아니라 **데이터**였습니다 (45,885 → 365,428행). ⚠️ 예전엔 잡음 ±0.085 에 묻혀 "측정부터" 였습니다 | STEP 16 |
| 에폭 12 로 충분한가 | 아니오 — 12에폭 전부 best 갱신. **25**로 | 학습 곡선 |
| 전체 데이터에서도 에폭을 더 주면 나은가 | **아니오 — 반대입니다.** 2단계 best 가 **0에폭**이고 14에폭까지 돌려도 못 넘었습니다. 8에폭부터 train loss ↓ / val loss ↑. ⚠️ 한 에폭이 7,569 스텝이라 "1에폭밖에 안 했다" 가 아닙니다 | STEP 16 |
| 데이터를 8배로 늘리면 뭐가 좋아지나 | **A6 recall 0.328→0.619 · 헛알림 33.3%→21.2% · 놓침 7.0%→4.5% · 이름 오답률 56.6%→46.3% · Grad-CAM lift 1.82→4.55.** 반면 **A4(농포)는 0.264 로 새 최악**이 됐고 **위치 교란 하락 32.2%** 로 나빠졌습니다 | STEP 16 |
| 2단계 백본을 전체 데이터로 확인했나 | **예 — convnextv2_base 확정.** holdout macro-F1 **0.5990** [0.5914, 0.6058]. ⚠️ 서브셋 결과였던 STEP 12 의 조건부가 풀렸습니다 | STEP 16 |
| 임계값에 여유를 둬야 하나 | **아니오** — 변별력을 고치면 임계값이 따라옵니다. holdout 최적값과의 차이 0.0539 → 0.0126 | STEP 10 |
| 헛알림을 줄일 수 있나 | **예** — 59.2% → 33.3%. 같은 recall 에서 | STEP 10 |
| 2단계는 holdout 에서 val 보다 잘 나오나 | **계속 그렇습니다** — 0.5263 → 0.5645. 네 번 다 같은 방향 | STEP 5·8·10 |
| ★★★ **그래서 화면에 뭘 말하나 — 정해졌습니다** | **계열 4군을 말합니다 (2026-09-08 사용자 결정).** `message.SHOW_GROUP` 기본값이 **켜짐**이고 되돌리려면 `DOG_SKIN_SHOW_GROUP=0`. holdout 커버리지 **66.5%**(하향 방지 규칙 적용값) · 긴급도 하향 2.9% · A6 오명명 12.5%. **6종 이름·top1·긴급도는 여전히 안 나갑니다.** ⚠️ 배포 서버는 `DAENGS_dev` 사본을 쓰므로 `tools/sync_screening.py` 로 가져가야 실제로 뜹니다 | 결정 |
| ★★ **4묶음에 문헌 근거가 있나** | **축은 셋 다 있고 조합은 우리 것입니다 (2026-09-08 확인).** ① primary/secondary (MSD·Clinicians Brief) ② **1cm 경계** — 구진 ≤1cm / 결절 >1cm, 결절은 진피·피하로 더 깊이 (Cornell·Veterian Key) → **A6 를 따로 둔 근거**. 🔴 **단 "A1·A4 = ≤1cm" 로 말하면 틀립니다 (2026-09-10)** — A1 의 **플라크는 보통 >1cm** 입니다. 1cm 는 *구진 vs 결절* 의 경계이지 묶음 전체 크기가 아닙니다 ③ **표피 소실 여부** — *"Unlike erosions and ulcers which involve loss of epidermis, collarettes…"* → **A5 를 가르는 선.** 그리고 A2+A3 는 **Hensel et al. 2015, BMC Vet Res**(ICADA 가이드라인) 한 문장에 같이 있습니다 — *"secondary skin lesions are excoriations, alopecia, **lichenification, hyperpigmentation, crusting, and seborrhea**"*. ⚠️ 단 그 목록엔 `excoriations` 도 있는데 우리는 A5 쪽으로 가릅니다 — **조합의 선례는 못 찾았습니다.** ⚠️ 그리고 이건 *"묶어도 되나"* 에 답하지 *"말해도 되나"* 에는 답 안 합니다 (교과서 2군은 과잉 88.4%로 기각됐습니다) | STEP 32 · 결정 |
| ★★ **계열 이름을 보호자가 알아듣나** | **아니었습니다 — 2026-09-10 에 바꿨습니다.** `융기·발진 → 솟아오른 변화` · `표면 변화 → 피부 표면·색·두께 변화` · `미란·궤양 → 벗겨지거나 패인 상처` · `결절·종괴 → 깊거나 단단한 혹`. ★ **이름보다 "특징 한 줄"이 본체입니다** — `표면 변화` 만으로는 보호자가 자기 개 사진과 대조할 수 없지만 `딱지, 둥근 비늘, 검어진 피부` 는 바로 확인됩니다 (`GROUP_FEATURE`). primary/secondary 는 `GROUP_DETAIL`(자세히 보기)로 내렸습니다 — 그 축으로 앞에 세우면 STEP 32 의 과잉 88.4% 자리입니다. ⚠️ 조건부 긴급도(*"피가 나면 신속 진료"*)는 **안 넣습니다** — STEP 30 자리. ⚠️ 길이 문제는 **막대 라벨을 위로 올려서** 풀었습니다 (50칸 → 28칸, 이름은 그대로) | 2026-09-10 |
| 🔴 **묶음 이름을 바꿀 때 조용히 죽는 곳이 있나** | **`URGENT_GROUPS` 였습니다.** 이름 문자열로 매칭해서, 이름만 바꾸고 여기를 안 고치면 **하향 방지 규칙이 에러 없이 안 걸립니다** (A5 하향 36.8% → 43.8% 로 되돌아감). → `URGENT_CODES = ("A5","A6")` 에서 **파생**하게 바꾸고, 이름이 하드코딩된 **9개 파일**을 전부 파생으로 고쳤습니다. 검사가 *"코드에서 파생되는가"* 와 *"네 묶음 전부에 특징·자세히보기가 있는가"* 를 봅니다 | 2026-09-10 |
| 🔴 **우리가 적어둔 수의학 근거가 정확한가** | **셋이 틀렸었습니다 — 2026-09-10 정정.** 외부 수의학 근거와 대조해 고쳤습니다: ① *"융기·발진 = primary, ≤1cm"* → **플라크는 보통 >1cm** 라 묶음 전체를 그렇게 부르면 틀립니다 ② *"표면 변화 = 장벽 온전"* → **표피잔고리는 농포가 터진 뒤** 남고 가피도 표면 손상 뒤라 온전하지 않습니다 ③ *"미란·궤양 = 2차 감염 위험"* → 그건 **뒤따르는 위험**이고 정의는 **표피(미란)·더 깊은 조직(궤양)의 소실**입니다. ★ **판정(4군 채택)은 안 바뀝니다** — 틀린 건 축의 *이름*이고, A6 를 따로 둔 1cm 근거는 그대로 섭니다. ⚠️ **화면 문구에는 이 정의가 원래 안 나갑니다** (나가는 건 *"모양만 보면 OO 계열에 가깝습니다"* 한 줄) — 고친 것은 **근거 서술**입니다. ⚠️ ②③ 은 `config.py` 가 이미 고쳐놨는데 **`STEP32` 문서에 안 따라왔습니다** — *정정이 한 곳에만 들어가는* 그 함정 | 외부 근거 대조 |
| ⚠️ **LLM 이 준 근거를 그대로 쓰면 되나** | **아니오 — 결론이 맞아도 논증이 빗나갈 수 있습니다.** 제미나이가 A2+A3 병합 근거로 *"태선화와 과색소침착이 만성 아토피에서 함께 나타난다"* 를 댔는데, **그 둘은 **둘 다 A3 안에** 있습니다** — A3 의 내부 구성을 정당화할 뿐 A2 와 묶는 근거가 아닙니다. 게다가 **출처를 하나도 안 댔습니다.** 실제 근거(Hensel 2015)는 따로 있었고 찾아보고서야 나왔습니다. → **주장을 우리 라벨 정의에 대입해 보고, 출처를 직접 여세요** | 2026-09-08 |
| 화면에 병변 이름을 띄워도 되나 | **아니오 (2026-08-26 확정, STEP 16 재확인)** — holdout 에서 그 이름이 **46.3%** 틀립니다 (STEP 10 은 56.6%. 10.3%p 나아졌지만 여전히 두 번에 한 번). 2단계는 계속 돌리되 **확률 분포만** 보여주고 "판단 불가 → 진료 권함" 으로 끝냅니다 | 멘토링 · STEP 11 |
| **1단계도 보정했나** | **예 — 이미 돼 있습니다.** T=0.9225, ECE 0.0282 → **0.0305**(2단계와 똑같이 조금 나빠짐). 릴리스가 `threshold_calibrated`(0.1466)와 `threshold_raw`(0.1645)를 **둘 다** 저장하고 `stage1_threshold.json` 은 보정된 쪽을 씁니다 — `infer.Engine` 이 `softmax(logit/T)` 를 돌려주므로 배선이 맞습니다. ⚠️ STATUS 에 "한 번도 안 했음" 으로 남아 있던 건 **낡은 기록**이었습니다 | STEP 16 릴리스 |
| 온도 보정(temperature scaling)이 늘 도움이 되나 | **아니오 — STEP 16 에선 효과 없었습니다.** ECE 0.0287 → **0.0299** (오히려 조금 나빠짐), accuracy 변화 없음, T=0.889. 원래 ECE 가 이미 낮아 고칠 게 없었던 것으로 보입니다. **"보정했다" 를 성과로 말하면 안 됩니다** | STEP 16 |
| 교란 검사 값을 어느 것으로 보고하나 | **holdout 쪽.** 06 은 교란 검사를 두 번 합니다 — val(셀 23)은 `step4a_summary.json`, holdout(셀 37)은 `final_*.json` 에 들어갑니다. STEP 16 은 배율 val 26.8% / **holdout 20.5%**, 위치 val 29.2% / **holdout 32.2%** 로 값이 다릅니다 | STEP 16 |
| A4 의 실패 방향이 그대로인가 | **아니오 — 뒤집혔습니다.** STEP 11 에선 1.43배로 **남발**했는데(precision 0.296 / recall 0.424), STEP 16 에선 0.78배로 **말을 안 하면서** precision 0.321 입니다. 남발이 아니라 그냥 못 맞히는 상태 | STEP 11 → 16 |
| 거절로 이름의 정확도를 살릴 수 있나 | **아니오** — 오답률 20% 를 맞추려면 커버리지가 **18.2%** 로 떨어집니다. 그 곡선은 헛알림 1,233건을 뺀 것이라 실제는 더 나쁩니다 | STEP 11 |
| 서빙에 bbox 가 없는 문제를 풀 수 있나 | **예 — 촬영 가이드 프레임을 bbox 로 받습니다.** 학습이 쓰는 `crop.crop_window()` 를 그대로 불러 창이 **0px 차이**로 같아집니다 | 2026-08-26 |
| 가이드 프레임을 고정 크기로 둬도 되나 | **아니오** — 1단계 f320 은 중심만 쓰지만 2단계 m2.5 는 **크기**를 씁니다. 네모 +6%p 면 창이 350→620px (1.8배) = 배율 교란 | 실측 |
| 그럼 크롭 문제가 다 끝났나 | **아니오** — 크롭 **구성**은 같아졌지만 네모의 **분포**가 다릅니다 (라벨러는 답을 알고 그림) | 남은 일 |
| 그 분포를 재려면 수의사가 필요한가 | **아니오 — 제가 틀리게 적었던 것입니다.** 수의사는 *병명*을 붙이는 사람이고, 재야 할 건 *네모 그리기*입니다. 정답 bbox 는 이미 매니페스트에 있습니다 → `tools/box_error.py` (30분) | 정정 |
| ★ **네모 잡음 증강으로 모델을 튼튼하게 할 수 있나** | **아니오 — 기각 (STEP 38).** 학습 때부터 사용자처럼 엉성한 네모로 잘라 보여줬습니다. 사용자 네모 **+0.072~0.077** (문턱 +0.03 통과) 인데 **라벨 네모 −0.071~0.084** 로 허용치(−0.03)를 두 배 넘게 초과 → 기각. `p=0.5` 로 섞어도 `p=1.0` 과 거의 같습니다 — **모델이 둘 다는 못 합니다.** ⚠️ **대조군이 결정적이었습니다** — 기준 모델은 원본 1920px 크롭으로 배웠는데 평가가 1080 근접 크롭이라 **어떤** 파인튜닝이든 오릅니다. 분포 적응만으로 **+0.109**. 대조군이 없었으면 그걸 "잡음 덕분" 으로 보고할 뻔했습니다 | STEP 38 |
| ⚠️ **그럼 관문(라벨 −0.03)이 맞는 관문인가** | **모르겠습니다 — 그래서 판정을 안 뒤집습니다.** 창 포화가 사실이면 실사용에 라벨 품질 네모는 안 오므로, 그 관문은 **일어나지 않는 상황을 지키는** 셈입니다. 그래도 결과를 보고 전제를 고쳐 통과시키면 **사후 합리화**입니다 (STEP 24 `occ` 사건과 같은 함정). 다시 정하려면 **새로 사전등록**해야 하고, 그때 물을 것은 *"배포에서 라벨 품질 네모가 정말 안 오나"* 입니다 — 그 가정이 깨지면 잡음 모델은 그 자리에서 **0.07 나쁜 모델**입니다 | STEP 38 |
| ★★ **그럼 `f320`(포화 면역)으로 바꾸면 되나 — 커버리지로 보면** | **아니오 — 마지막 갈래도 닫혔습니다 (STEP 39).** `f320` 은 네모 크기를 안 써서 포화에 면역인데, 커버리지로 재보니 **라벨 네모에서도 8.0%** 뿐입니다 (사용자 네모 0.7%). **포화 면역이어도 밑천이 없으면 소용없습니다** — 커버리지는 문턱 지표라 밑천이 낮으면 아예 못 넘습니다. ★ **그런데 배포 3팔(22.4%)이 `m2.5` 만(19.2%)보다 낫습니다** — `f320` 팔은 **앙상블 안에서는 값을 합니다** (STEP 25 의 '비례 창 vs 고정 창 다양성' 이 사용자 네모에서도 그대로). ⚠️ 팔 수가 다릅니다(3 vs 2 vs 1) — 앙상블 효과와 크롭 효과가 안 갈립니다 | STEP 39 |
| ★★★ **모델에게 네모 찾는 일을 직접 가르치면 되나** | **아니오 — 기각 (STEP 42). 다만 열 갈래 중 제일 멀리 갔고, 처음으로 벽이 어느 축인지 갈렸습니다.** 밴드 둘 다 드는 비율 **29.3%** < 사전등록 문턴 50%. 그래도 사람 5.0% · 창 탐지기 6.7% 의 **4.4~5.9배** 입니다. ★ **축을 쪼개면: 배율 75.0% · 위치 38.7%** — 둘은 거의 정확히 독립이라 곱으로 떨어집니다 (0.750×0.387=0.290, 관측 0.293). 둘 다 50% 를 넘으려면 **위치가 67%** 여야 합니다 | STEP 42 |
| ★★★ **그 검출기가 실제로 무엇을 배웠나** | **위치입니다 — 배율은 거의 전부 하한선이었습니다.** 같은 val 에서 *아무것도 안 배운 네모*(화면 한가운데 · 크기 0.215 고정)가 **배율 72.3% · 위치 10.1% · 둘 다 6.1%** 입니다. 순이득은 **배율 +2.7%p · 위치 +28.6%p**. 중심 오차를 0.248 → **0.132** 로 절반 줄였습니다. ⚠️ **하한선 6.1% 가 사람 5.0% · 창 탐지기 6.7% 와 같은 구간**입니다 — STEP 36·37 에서 잴 둘은 사실 **아무것도 안 한 것과 구분이 안 됐습니다** | STEP 42 |
| 검출기를 더 돌리면 되나 | **아니오** — train loss 는 0.3406 → 0.1112 (−67%) 로 계속 내려가는데 밴드는 26.6 → 29.3% 에서 평평합니다. **loss 와 판정 지표는 다른 것을 재니다** | STEP 42 |
| ★★ **모델 가중치를 어떻게 배포하나** | **허깅페이스 리포에서 받고 리비전을 고정합니다 (2026-09-09, `DAENGS_dev`).** 예전엔 서버 디스크의 릴리스 폴더를 `:ro` 마운트했는데, 그러면 **폴더에 넣은 것이 곧 배포**라 버전이란 게 없었고 실제로 **팔이 강등돼도 200 OK** 로 나갔습니다. 지금은 `backend/.env` 의 `SCREENING_RELEASE_REPO`/`_REVISION`/`HF_TOKEN` 으로 받고, **`SCREENING_EXPECT_STAGE2_ARMS`(REPO 있으면 기본 3)가 안 맞으면 모델을 안 올리고 503** 입니다. ⚠️ `REVISION` 을 비우면 `main` 을 따라가 **푸시하는 순간 운영 모델이 바뀝니다** — PR 도 리뷰도 없이. ⚠️ *"폴더가 비었으면 받는다"* 로 하면 **받다 끊긴 폴더가 통과**하므로 늘 부릅니다 (최신이면 HEAD 한 번). ⚠️ 최상단 `.env` 아니라 **`backend/.env`** — compose `environment:` 에 넣으면 **빈 값으로 덮입니다** | `DAENGS_dev` |
| ★★ **검출기에 데이터를 더 주면 되나 — 원본을 다시 받아야 하나** | **더 줄 것이 없습니다 — 이미 바닥입니다.** 검출기는 **VL01 하나로** 배우고 VL01 로 평가했습니다 — 로컬에 남은 원본이 `VL01.zip` 뿐이고 **다른 PC 도 크롭만 남았습니다.** 로컬 매니페스트 45,885행 → 검출 후보 풀(A7 제외) **23,070장** → 이미 **94.8%** 를 썼습니다. `--n 23000` 은 문턴이 아니라 **바닥**이었고 남은 여지는 **1.05배**뿐입니다. ⚠️ 매니페스트 365,428행은 **캐글에 올라간 것**이지 로컬에 있는 것이 아닙니다. ~~원본 재다운로드는 디스크가 막습니다~~ → **2026-09-09 정정: 맥에서 191GB 를 받았습니다** (STEP 44). 그 문장은 **윈도우 PC 기준**이었는데 기계를 안 적어 일반 사실처럼 읽혔습니다. ⚠️ **평가도 VL01** 이라 밖에서 어떤지 모릅니다 — VL01 은 병변이 더 큽니다 (A4 196px vs TL02 113px) | STEP 42 |
| ★★ **크기를 버리고 중심만 써서 크롭을 걸면 되나** | **아니오 — 기각 (STEP 41). 그리고 왜 안 되는지가 핵심입니다.** 네모 크기를 실측 병변 중앙값(21.5%)으로 대체해 `m2.5` 창이 포화 안 되게 했더니 커버리지 22.4% → **21.3% (−1.1%p)**. ★ **이유: 두 요구가 상충합니다.** 창을 581px 로 줄이면 창 반지름 290px 인데 **실측 중심 오차가 174px** 이라 여유가 117px 뿐 — 병변이 자주 **창 밖**으로 나갑니다. 포화된 1080px 창은 여유가 366px 라 중심이 틀려도 병변을 품습니다. **지금의 포화는 버그가 아니라 우연한 안전장치**였습니다 — 배율 정보를 잃은 대신 **중심 오차에 대한 관용**을 얻고 있었습니다. `크게 자르면 크롭이 안 걸리고 · 작게 자르면 병변을 놓칩니다` | STEP 41 |
| ★★★ **가이드 프레임이 실제로 결과를 바꾸나** | **아니오 — 네 번 확인했습니다 (STEP 40).** 배포 모델의 2단계 macro-F1 이 `사용자 네모` 와 `네모 없음`(사진 전체) 에서 **소수점까지 같습니다**: 0.3720=0.3720 · 0.3798=0.3798 · 0.3798=0.3798 · 0.3706=0.3706. 파인튜닝한 팔에서도 같습니다. **사용자가 네모를 그리는 것과 아예 안 주는 것이 모델에게 같은 입력**입니다. ⚠️ 그러니 네모 맞추는 시간 · *"너무 커요/작아요"* · 밴드 · 기본값 44% 가 **2단계 출력에 영향이 없습니다.** (1단계 `f320` 은 **중심**을 쓰므로 영향 있음 — 가이드를 통째로 빼려면 그쪽을 따로 봐야 합니다) | STEP 40 |
| ★★★ **그럼 틀(가이드 프레임)을 없애도 되나** | **아니오 — 기각 (STEP 43). 1단계가 무너집니다.** `f320` 은 **중심**을 쓰므로 틀을 없애면 중심이 화면 한가운데로 갑니다. recall **0.739 → 0.617**(씨앗 17) · **0.761 → 0.649**(씨앗 99) 로 −0.112~−0.122 — 사전등록 허용치(−0.01)의 **11~12배**. 닻은 `STAGE1_MISS_RATE`(0.057): 1%p 더 빠지면 놓침 6.7% 로 `UNDER_TRIAGE_MAX`(0.05)의 근거가 깨집니다. ⚠️ **"틀이 아무 일도 안 한다" 는 2단계 한정입니다** — 통째로 말하면 틀립니다 | STEP 43 |
| ★★ **중심이 틀리면 보수적이 되나 (recall↓ 헛알림↓)** | **아니오 — 둘 다 나빠집니다.** 라벨 0.881/0.272 → 사용자 0.739/0.324 → 틀없음 0.617/0.377. AUROC 도 0.90 → 0.67 로 **변별력 자체가 사라집니다.** 중심이 틀리면 모델이 **엉뚱한 자리를 보는 것**이지 판단을 아끼는 게 아닙니다 — 병변을 더 놓치면서 멀쩡한 피부를 더 병원으로 보냅니다 | STEP 43 |
| ★★★ **사용자 네모가 1단계도 깎나** | **예 — 그런데 두 달 동안 안 쟀습니다.** *"사람 네모의 대가"* 를 전부 **2단계 지표**로만 쟀습니다 (STEP 36 macro-F1 −22~24% · STEP 39 커버리지 −55.5%). 재보니 1단계 recall 이 라벨 중심 대비 **−0.14** 입니다 — 크기가 아니라 **중심**만으로요(`f320` 은 크기를 안 씁니다). **지금 배포는 1단계에서 상한의 84% 만 씁니다.** ⚠️ 이건 *"틀을 없애자"* 의 **반대 방향** 숙제인데, 중심을 더 잘 받는 축은 STEP 37 에서 이미 닫혔습니다(탭 0.111 > 0.10) | STEP 43 |
| ★ **그럼 아예 네모 없이 2단계를 배우면 나은가** | **아니오 — 기각 (STEP 40). 재현에서 뒤집혔습니다.** 대조군 대비 **사용자 +0.0945**(문턱 +0.03) · 라벨 −0.0296(허용 −0.03) 로 통과. ⚠️ **라벨 손실이 허용치와 0.0004 차이**로 잡음 폭(±0.016)의 40분의 1 — 사실상 문턱에 걸쳐 있습니다. **다른 씨앗(99)에서 라벨 −0.0578 로 허용치를 두 배 가까이 초과 → 기각.** ★ **사용자 이득은 일관되게 큽니다**(+0.0945 / +0.1018) — **라벨 손실이 흔들려서** 1차만 간신히 통과한 것입니다. 0.0004 차이로 걸친 통과는 통과가 아닙니다 | STEP 40 |
| ★★★ **지금 앱은 네모를 실제로 쓰고 있나** | **거의 안 씁니다 (STEP 37).** `m2.5` 창 = 네모 긴 변 × 2.5 인데, 네모가 화면의 **47% 만 넘으면 창이 사진을 통째로 덮습니다**(1080px 기준 47%→1269px→잘려서 1080). 실측 사용자 네모는 **47.2~70.6%** 로 **40장 전부(100%)가 창을 포화**시킵니다. 즉 **2단계는 이미 사진 전체를 보고 있습니다** — 사용자가 네모를 어디에 어떻게 그리든 결과가 거의 같습니다. ⚠️ 이건 *"네모를 잘 받아야 한다"* 는 전제 자체를 흔듭니다. 우리가 잰 "사용자 네모에서 −22~24%" 도 사실상 **"사진 전체를 봤을 때의 하락"** 입니다 | STEP 37 |
| ★★ **점 하나만 받으면(탭) 되나** | **아니오 — 기각 (STEP 37).** `f320` 은 중심만 쓰므로 크기를 안 물어보면 *"뭘 기준으로 44%인지 모르겠다"* 가 사라집니다. 그래서 실측했더니 **탭 오차 중앙값 0.111** > 사전등록 문턱 **0.10**. ★ 그런데 분포가 **갈라집니다** — 25분위 0.028(아주 정확) vs 75분위 0.328, **탭이 병변 안에 든 것 47.5%**. 경계가 뚜렷하면 콕 찍히고 **번진 것(비듬·각질·태선화)은 못 찍습니다** — 그런 병변엔 사람이 답할 **'중심' 이 애초에 없습니다** | STEP 37 |
| ★★ **그럼 모델이 네모를 제안하면 되나** | **아니오 — 기각 (STEP 37).** 1단계를 320px 창 탐지기로 써서 제안을 뽑으니 **둘 다 밴드 안 6.7%** (문턱 50%). ★ 모델이 사람보다 **두 축 다 낫습니다** (배율 39.0% vs 10.0% · 위치 19.0% vs 12.5%)인데 **같이 맞는 일이 드뭅니다.** ⚠️ Grad-CAM lift 4.55 는 *"배경을 안 본다"* 는 뜻이지 **네모를 뽑는 능력이 아닙니다** — 그걸로 기대치를 잡으면 안 됩니다 | STEP 37 |
| ★★★ **그럼 무엇이 남았나** | **입력 쪽은 다 막혔습니다.** 네모 크기 ❌ · 점 위치 ❌ · 모델 제안 ❌ · 기본값 조정 ❌ · `f320` 전환 ❌. → 남은 것은 **모델을 잡음에 튼튼하게** 만드는 쪽입니다 (학습 때부터 사용자처럼 엉성한 네모를 보여주기, STEP 38) | STEP 37 |
| ★ **사람이 실제로 그 밴드 안에 그리나** | **아직 모릅니다 — 못 쟀습니다 (2026-09-07).** `tools/box_error.py` 를 40장 돌렸는데 **원본 사진이 없어** 도구가 `m2.5` 크롭으로 조용히 물러섰고, 그건 **정답을 가운데 놓고 자른 것**이라 측정이 성립하지 않습니다. 나온 숫자(크기 1.00배 · 위치 46.4%)는 **버렸습니다.** 재려면 **원본 전체 사진**이 필요합니다 — 한국 PC 에서 청크 하나를 `--finalize` 전에 도세요 | 무효 |
| 네모를 얼마나 정확히 그려야 하나 | **크기 0.71~1.67배 (허용) / 0.83~1.43배 (권장)** — STEP 16 기준. `usable_range()` 배율 밴드를 뒤집은 값입니다. ⚠️ STEP 10 은 0.59~1.43 / 0.71~1.18 이었습니다 — **작게 잡는 쪽은 너그러워지고 크게 잡는 쪽이 빡빡해졌습니다** (2.0x 에서 macro-F1 0.449) | STEP 16 역산 |
| AI Hub 에서 조각내서 받을 수 있나 | **아니오 — 실측(2026-08-26).** `-mode l` 이 zip **6개**만 줍니다 (TS01/TS02/TL01/TL02/VS01/VL01). 안쪽 filekey 가 없어 통째로 받는 수밖에 없습니다 | `--list` |
| 받는 도중 디스크가 얼마나 드나 | **실측 2.93배 — 그런데 2.03배로 내릴 수 있습니다 (2026-09-09).** aihubshell 이 `download.tar`(1) → `tar -xvf` 로 푼 내용(2) → `cat >` 로 합친 파일(3) 을 겹쳐 놓고 tar 를 지우는 줄이 없습니다. **해제 직후 `download.tar` 만 치우면 2.03~2.04배** (VL01 2.93 / TL01 2.03 / TL02 2.04). ⚠️ 전에 적었던 "압축 해제 때문에 2배" 는 **틀렸습니다** — `--mode zip` 이라 압축을 안 풉니다 | STEP 44 실측 |
| TL02 를 받을 수 있나 | **기계마다 다릅니다 — 뭉뚱그리면 틀립니다.** 윈도우 PC(디스크 전체 237.8GB)에서는 **물리적으로 불가**였습니다. 맥(460GB)에서는 **받았습니다** — TL01·TL02·VL01 **191GB** 가 `data/raw/` 에 있습니다 (2026-09-09). ⚠️ 옛 문장은 *"이 PC"* 가 어느 PC 인지 안 적어서 **다른 기계에서도 못 하는 것처럼** 읽혔고, STEP 42 가 실제로 그렇게 읽고 원본 확보를 포기했습니다 | STEP 44 |
| 다른 PC 에서 청크를 처리해 크롭만 가져와도 되나 | **예 — 단 `phash` 를 같이 가져와야 합니다.** 원본이 있을 때 재 두지 않으면 `--finalize` 가 그 청크의 중복을 **에러 없이** 건너뜁니다 | `tools/merge_incoming.py` |
| 데이터를 늘리면 holdout 이 그대로인가 | **대체로 그렇지만 일부 바뀝니다.** holdout 은 그룹 해시라 결정론적이고, `animal_id` 는 (견종·나이·성별·날짜) 대용이라 안정적입니다. 다만 그룹은 `dup_cluster`(phash)와 합집합이라, 새 사진이 두 클러스터를 이으면 그 그룹만 배정이 바뀝니다 | `split.build_groups` |
| 안구 질환도 같이 할까 | **아니오 — 지금은 배제 (2026-08-26).** 막힌 것(2단계 백본·1단계 보정·네모 오차)이 남았고 GPU 할당량이 병목입니다. 버리는 게 아니라 미루는 것 — 틀은 거의 그대로 재사용됩니다. 조건과 근거는 STATUS "범위 밖" | 검토 |
| 2단계 백본을 더 갈면 이름이 맞나 | **아니오 — 데이터 한계.** CNN(convnextv2 0.5744)과 어텐션(swinv2 0.5718)이 **0.0026** 차이로 겹칩니다 | STEP 12·13 |
| **로짓 보정(prior)으로 A4 를 살릴 수 있나** | **아니오 — 개입 실험으로 기각.** A4 recall 은 0.356→0.515 로 오르고 쏠림 lift 도 1.20→0.63 으로 걷힙니다. 그런데 **macro-F1 이 +0.0021** (문턱 +0.02) — A2·A3 가 정확히 그만큼 내려가는 **순수 맞바꿈**입니다. prior 는 곡선 **위의 자리**만 정할 뿐 곡선을 만들지 않습니다. ⚠️ A4 recall 이 급하면 τ 로 **살 수는** 있습니다 (macro-F1 을 내주고) | STEP 18 |
| 그럼 A4 는 무엇의 문제인가 | **표현입니다.** 위 기각이 그 뜻입니다 — 멘토의 *"핵심 표현을 못 잡았다"* 가 서술이 아니라 개입 실험으로 확인됐습니다 | STEP 18 |
| 청크(다운로드 묶음)가 지름길인가 | **아니오.** 설계는 극단적으로 얽혀 있는데(A1·A7 은 TL02 에 0장, A3~A6 은 TL01 에 0장) **모델은 안 씁니다** — 청크만으로 macro-F1 0.2197(모델의 36.6%) · VL01 안에서만 평가해도 +0.0194(잡음 안) · 주된 청크가 **다른** 쌍이 오히려 더 헷갈림(비 1.04). 상한선이 낮은 이유는 **VL01 에 7개 클래스가 다 있어서** 청크가 라벨을 결정 못 하기 때문 | STEP 19 |
| 1단계가 **큰** 병변을 놓치나 | **예.** rho(크기, 점수) −0.201, 6개 클래스 전부 음수. 창 안 놓침 1.8% vs 창 밖 4.6%(2.5배), 창의 2배를 넘으면 8.8%. 전체 병변의 **23.2%** 가 320px 창을 넘칩니다 ⚠️ 제 첫 가설("작아서 못 본다")과 **정반대**였습니다 | STEP 17·18 |
| 그게 병변 탓인가 창 탓인가 | **창입니다.** 2단계(`m2.5`, 비례 창)가 자연 대조군인데 거기선 클래스 안 rho 가 전부 0 근처입니다. 고정 창일 때만 나타납니다 | STEP 18 |
| 그럼 창을 키우면 낫나 | **아니오 — 기각 (STEP 20).** `f320`→`f448` 로 넘친 병변 놓침이 8.0% → **9.1%** 로 **오히려 늘었습니다** (CI 반폭 ±2.0%). AUROC·헛알림은 그대로. **창 채움 설명이 틀렸습니다** — 크기·놓침 상관은 사실이나 원인은 창이 아닙니다. 남은 설명은 '큰 병변이 원래 어렵다' 와 '큰 bbox = 대충 그린 라벨' 이고 STEP 24 에서 갈렸습니다 (아래) | STEP 20 |
| **`bbox` 는 어디서 왔나** | **폴리곤(병변 외곽선)의 외접사각형입니다 — 오차 0px, 표준편차 0.** 그리고 폴리곤은 **365,428행 100%에 들어 있습니다** (점 중앙값 8개). STEP 4A 이후 우리가 "병변 크기" 라고 부른 건 전부 이 네모였고, 실제 병변은 따로 있었습니다 → `crop.polygon_in_window()` | STEP 24 |
| 그럼 '대충 그린 네모' 가 놓침의 원인인가 | **아니오 — 기각.** 크기를 4분위로 **고정하고** 모양(`slack`·중심이탈·길쭉함)을 보면 층 안 rho 가 전부 \|0.074\| 이하이고 **방향이 뒤집힙니다** (`slack` 은 S2 에서 0.013→0.028, S4 에선 0.054→0.031). 반대로 모양을 고정하면 크기는 남습니다 (0.009→0.042). 네모 중심이 병변 **밖**인 행은 2.3%뿐이고 놓침 77건 중 6건입니다 | STEP 24 |
| 그럼 남은 건 | **'큰 병변이 원래 어렵다'** — 다만 **소거법으로 남은 것**이지 증명된 게 아닙니다. 기전 후보(창이 병변 내부에만 놓여 경계가 안 보인다)는 STEP 20 이 반증에 가깝습니다. A2 를 빼도 크기 효과는 그대로라(rho −0.189) 특정 병변 현상도 아닙니다 | STEP 24 |
| ★ 병변 크기를 픽셀로 재도 되나 | **⚠️ 이 데이터에선 어쩔 수 없지만 배포에선 위험합니다.** 365,428장이 사실상 전부 **1920×1080** 이라 `rho(절대 픽셀, 화면 점유율) = +1.000` — 두 값이 **같은 변수**입니다. "커서 놓치나 확대해 찍어서 놓치나" 는 이 데이터로 **영원히 못 가릅니다.** 앱 사진은 1920×1080 이 아니지만 `agent.to_train_space()`(짧은 변 1080)가 **이미 막고 있습니다** — 남는 건 가로세로비와 광학 줌 | STEP 24 |
| 창을 넓히면 부작용이 있나 | **예 — 화질에 더 기댑니다.** 흐림 하락 4.6% → **9.0%** (거의 두 배). `f320`+`photometric` 이 눌러둔 화질 지름길(STEP 6) 쪽으로 되돌아갑니다 | STEP 20 |
| **2단계에 고정 창을 쓰면 되나** | **★ 예 — 후보 (STEP 22).** `m2.5`(비례) → `f320`(고정)에서 **A4 recall 0.393 → 0.525**, A4→A1 혼동 23.6% → 14.7%, macro-F1 +0.035. 사전등록 4기준 전부 통과. `f448` 도 같이 통과. ⚠️ **VL01·effnetv2_s 결과**이고 A4 의 88%는 TL02 에 있습니다 | STEP 22 |
| **그래서 2단계 고정 창을 쓰나** | **아니오 — 기각 (STEP 23).** 전체 데이터(TL01+TL02, A4 val 1,730장)에서 A4 recall 0.2815 → **0.2561**, macro-F1 −0.0099, 배율 하락 **+9.1%p**. STEP 22 의 +0.132 는 **VL01 특유**였습니다 — 거기 A4 는 196px 인데 TL02 는 113px 입니다. 기준선도 0.393 vs 0.282 로 다릅니다. ⚠️ "나빠졌다" 는 과합니다 — A4 차이 −0.025 는 **차이의** CI(±0.0297) 안이라 정확히는 **"이득 없음"** | STEP 23 |
| VL01 단독 결과를 믿어도 되나 | **아니오 — 두 번 데었습니다.** VL01 은 전체의 10.8%이고 A4 가 1.7배 큽니다. STEP 23 의 `m2.5` A4 recall 0.2815 는 STEP 16 **holdout 0.264** 와 거의 같은데 VL01 은 0.393 이었습니다 — **VL01 이 낙관적 이상치**입니다 | STEP 22 → 23 |
| "짝 혼동이 줄었다" 를 개선으로 봐도 되나 | **아니오 — 흩어짐일 수 있습니다.** `f320` 은 A4→A1 을 35.1%→32.3% 로 줄였지만 정답도 28.2%→25.6% 로 줄고 **그 밖 4종이 36.8%→42.1% 로 늘었습니다.** 행선지 하나만 보면 멘토가 말한 '흩어짐' 이 심해진 걸 개선으로 오독합니다 → 기준에 `errors.class_dispersion()`(H̃)을 같이 넣으세요 | STEP 23 |
| 2단계 best 가 0에폭인 게 레시피 탓인가 | **아니오 — convnextv2_base 특유입니다.** 같은 전체 데이터에서 `effnetv2_s` 는 best_epoch **4 / 7** 이고 둘 다 수렴했습니다. → 03h(학습률) 재개 조건(`best_epoch ≤ 2`)은 **발동 안 함** | STEP 23 |
| 고정 창이 좋은 이유가 '절대 크기' 인가 | **아니오 — 확대율로 보입니다.** 크기 구간별 이득이 작은 쪽에 안 몰리고(<64px 는 −0.023) 큰 A4 에서 +0.239 가 납니다. `m2.5` 는 60px 병변을 2.6배 확대, 400px 를 0.4배 축소해 **질감 통계가 사진마다 달라집니다.** 고정 창은 확대율이 일정합니다 | STEP 22 |
| 고정 창의 대가는 | **촬영 거리 의존.** 배율 하락 27.9% → 33.4%. `m2.5` 는 가이드 프레임에 비례해 거리에 불변인데 고정 창은 아닙니다 — 채택하려면 **촬영 가이드 밴드를 다시 뽑아야** 합니다 | STEP 22 |
| **★ 2단계를 살릴 길이 있나 — 크롭 뷰 앙상블** | **예 — 채택 후보 (STEP 25).** `m2.5`(비례 창)와 `f320`(고정 창) 모델의 **확률을 평균**하면 macro-F1 0.5805 → **0.6384** (+0.0579, 짝지은 CI [+0.040, +0.075]), accuracy 0.669 → **0.705**, A4 recall 0.356 → **0.540**. **여섯 클래스가 전부 오릅니다.** 배율 하락은 24.6% → 27.3% 로 `f320` 의 약점(35.3%)을 안 물려받습니다. 재학습 0회. **⚠️ 전체 데이터(27,331행)에서 절반만 재현됐습니다 — 아래 두 줄** | STEP 25 |
| 그 이득이 전체 데이터에서도 나오나 | **예, 다만 절반입니다.** TL01+TL02 val 27,331행(A4 1,730장): 3팔 macro-F1 **+0.0295** [+0.024, +0.035] · accuracy 0.6677 → **0.7037** · **여섯 클래스 전부 상승** · 커버리지(오답 20% 목표) 62.0% → **71.5%**. VL01 의 +0.058 이 +0.030 으로 반토막 | STEP 25 |
| **앙상블이 A4 를 살리나** | **아니오 — 기각.** VL01 에선 A4 recall +0.184 였는데 전체에선 **+0.034**, A4→A1 은 오히려 35.1% → 36.8% 로 **늘었습니다.** 2팔 조합은 흩어짐 관문에서 기각되고 3팔만 겨우 통과. 이유는 STEP 23 과 같습니다 — VL01 의 A4 는 196px, A4 의 88%가 사는 TL02 는 113px. **"크롭 다양성이 A4 를 살린다" 는 죽고, "전반적 변별력을 올린다" 만 남습니다** | STEP 25 |
| **1단계에도 앙상블이 먹히나** | **아니오 — 기각.** `f320` 릴리스 + `f448` 로 AUROC **+0.0019** [−0.0002, +0.0040] (잡음 ±0.01 안), 헛알림@rec.95 는 18.3% → 18.8% 로 **오히려 늘었습니다.** 서로 보완하는 몫이 **12.4%** 뿐입니다 (2단계 27.0%). ⚠️ 정확히는 **"창 크기만 바꾼 앙상블이 안 먹힌다"** 입니다 — 1단계의 두 뷰는 **둘 다 고정 창**이라 2단계에서 먹힌 '비례 vs 고정' 축을 아예 못 시험했습니다 | STEP 25 |
| 앙상블 이득의 정체가 뭔가 | **'비례 창 vs 고정 창' 이라는 종류의 다양성**입니다. 창 크기(f320 vs f448)나 백본(cnx vs eff)을 바꾼 건 잡음권이고, `m2.5`(비례) ↔ `f320`(고정)만 값이 있습니다. 두 창은 확대율 처리가 정반대라 **사각지대가 겹치지 않습니다** | STEP 25 |
| **그럼 앙상블을 채택하나** | **★ 예 — 관문 4개 전부 통과 (STEP 26).** 마지막 관문이던 배율 하락이 **23.5% → 24.4% (+0.9%p)** 로, 허용치 +12%p 는 물론 **교란 잡음 ±5%p 안**입니다. 기술 판정은 끝났습니다. 남은 건 **추론 3배 값을 치를 것인가** — 2단계는 화면을 안 바꾸므로 '확신 있을 때만 이름을 말한다' 로 출력 규격을 바꿀 때만 보호자에게 닿습니다 (제품 판정) | STEP 26 |
| **앙상블이 `f320` 의 배율 약점을 물려받나** | **아니오 — 좋은 쪽에 붙습니다.** `f320` 단독 **36.9%** / 두 `m2.5` 팔 23.5%·25.9% 인데 앙상블은 **24.4%** 입니다. 세 팔 하락률의 산술평균(28.8%)보다도 낫습니다 — 섞으면 중간이 되는 게 아닙니다. 배율 2.0x 에서 `f320` 이 0.3499 로 무너질 때 `m2.5` 두 팔이 0.405·0.414 로 버티고 확률 평균이 그쪽을 따라갑니다. **위치 교란도 좋아집니다** (17.9% → 15.2%) | STEP 26 |
| 앙상블이 교란에서도 단일 팔을 이기나 | **예 — 7조건 전부.** 배율 5종 + 위치 2종 어디서도 앙상블이 모든 단일 팔보다 위입니다 (이긴 폭 +0.012 ~ +0.051). '기준 조건에서만 좋은' 종류가 아닙니다 | STEP 26 |
| 추론을 아끼려고 **2팔**로 줄여도 되나 | **아니오 — 흩어짐 관문에서 기각.** 릴리스+`f320` 2팔은 macro-F1 +0.0245 로 3팔(+0.0295)의 83%를 내지만, A4→그밖이 37.7% → **38.2%** 로 **늘어납니다** (3팔은 −0.7%p). macro-F1·accuracy 만 봤으면 33% 싸게 골랐을 겁니다 — `ENS_NO_SCATTER` 관문이 실제로 일한 장면 | STEP 26 |
| ★ 교란 검사의 macro-F1 **절대값**을 다른 STEP 과 비교해도 되나 | **아니오 — 순위까지 뒤집힙니다.** `robust.ZoomView` 는 배율 1.0x 조차 437px→384 중앙크롭(약 1.14배 확대)이라 표준 평가 변환이 아닙니다. 같은 모델·같은 val 인데 릴리스 cnx 는 0.5959 → 0.5291(−0.067), eff `m2.5` 는 0.5894 → 0.5593(−0.030) 이라 **ZoomView 표에서는 릴리스가 꼴찌로 보이고 표준 평가에서는 1등**입니다. **비교 가능한 것은 같은 파이프라인 안의 하락률뿐** | STEP 26 |
| 그 이득이 '모델을 두 개 써서' 인가 | **아니오 — 창을 두 개 써서입니다.** 백본만 바꿔 더하면 +0.019(잡음권), 크롭을 바꿔 더하면 **+0.058**. STEP 22·23 이 *"둘 중 뭐가 나은가"* 를 물어 답이 갈렸는데, 갈렸다는 건 **다른 실수를 한다**는 뜻이었습니다 — 한쪽만 맞히는 몫이 **27.0%** | STEP 25 |
| TTA(좌우반전 평균)가 도움되나 | **아니오 — 기각.** m2.5 +0.0027 / f320 **−0.0032** / 앙상블 +0.0005. 전부 잡음(±0.02) 한참 안이고 부호도 갈립니다. 추론 2배 값이 없습니다 | STEP 25 |
| 거절로 이름의 정확도를 살릴 수 있나 **(갱신 2)** | **62.0%·71.5% 도 틀린 분모였습니다.** 그 값들은 전부 **진짜 병변인 사진만** 놓고 잰 것인데, 화면에는 **1단계가 넘긴 사진**이 뜨고 거기엔 헛알림이 **20.8%** 섞여 있습니다. 정상 사진에 이름이 붙으면 무조건 오답입니다. 정직하게 다시 재면 **33.7%** (3팔 앙상블) / **0.7%** (릴리스 단독) — VL01 val | STEP 27 |
| **그래서 이름을 켤 수 있나** | **릴리스 단독은 축을 닫습니다(0.7%). 3팔 앙상블은 33.7% — 미리 박아둔 논의 문턱(50%)과 닫음 문턱(30%) 사이라 제가 정할 수 없는 구간**입니다. 멘토·사용자 결정. ⚠️ VL01 은 커버리지에서 **비관적**인 청크라 전체에서는 더 높을 수 있습니다 | STEP 27 |
| ★ 헛알림만으로 목표 오답률을 넘나 | **예 — 이게 문제의 절반입니다.** 1단계가 넘긴 사진의 **20.8%** 가 멀쩡한 개인데 목표 오답률이 20% 입니다. 2단계가 완벽해도 전부에게 이름을 말하면 20.8% 틀립니다. **거절이 헛알림을 골라 버려줘야만** 20% 아래로 갑니다 | STEP 27 |
| **릴리스 단독이 0.7% 인 게 정확도 탓인가** | **아니오 — 자기가 언제 맞는지 모릅니다.** 확신도 **최상위 10분위에서 이미 25.1% 틀립니다** — 목표(20%)를 지키는 구간이 처음부터 없습니다. 앙상블은 최상위 12.5% · 누적으로 30% 근처까지 20% 아래를 버팁니다. **릴리스의 1분위(25.1%)가 앙상블의 3분위(18.6%)보다 나쁩니다** | STEP 27 |
| ★ **그럼 앙상블의 값어치가 뭔가** | **macro-F1 이 아니라 '언제 맞는지 아는 것' 입니다.** +0.030 은 부수적이고, 쓸 수 있는 커버리지가 **0.7% → 33.7% (48배)** 인 게 본체입니다. 확률을 평균하면 정확해질 뿐 아니라 **과신이 걷힙니다** | STEP 26 → 27 |
| 확신도를 무엇으로 재야 하나 | **`p(이상) × p(그 병변)` — 서빙이 이미 맞게 하고 있었습니다** (근거는 이제야 생겼습니다). 커버리지 **33.7%** vs p2max 만 3.2% · p1 만 0.5% · 1등−2등 마진 3.3%. **곱이 10배** 나은 이유는 오답이 두 종류(헛알림/이름 틀림)라 한쪽만 보면 다른 쪽이 통과하기 때문입니다 | STEP 27 |
| 1단계 임계값을 올려 헛알림을 줄이면 되나 | **지렛대는 있지만 당기지 않습니다.** 0.147→0.70 이면 헛알림 몫 20.8%→7.0%, 이름 커버리지 33.7%→44.5%. 그런데 recall 이 **0.970 → 0.865** — 병변 100마리 중 놓치는 게 3마리에서 **13마리**가 됩니다. 놓친 건 *"괜찮아 보여요"* 로 나가므로 이름이 틀리는 것보다 **훨씬 나쁩니다.** 수의사·멘토 결정 | STEP 27 |
| ★ **6종 이름이 안 되면 아무것도 못 말하나** | **아니오 — 알갱이의 문제였습니다 (STEP 28).** 같은 모델·같은 확률로 **형태 계열 3군** (A1·A4 / A2·A3 / A5·A6)을 말하면 커버리지 **62.1%** (긴급도 하향 1.7%), **두 이름**이면 **80.4%** (2.1%). 둘 다 논의 문턱(50%)을 넘습니다. 6종 이름은 33.7% 였습니다. 전체 데이터에서도 형태 계열 정확도 **0.8365** · 두 이름 적중률 **87.4%** 로 재현 | STEP 28 |
| 그 묶음을 혼동행렬 보고 만든 건 아닌가 | **출처는 이 분석보다 먼저 있던 것입니다** — `config.URGENCY_HINT` 와 임상 해설의 **'🟢 비교적 안전한 혼동'**(A1↔A4 · A5↔A6). `tests/test_granularity_gates.py` 가 대응을 고정합니다. **⚠️ 그런데 그것만으로는 부족합니다 — 아랫줄을 꼭 같이 읽으세요** | STEP 28 |
| ★ **"임상적으로 비슷해서 묶었다" 가 설명이 되나** | **절반만 맞습니다 — 갈라서 쟀습니다.** 문서가 명시한 병합(A1·A4, A5·A6)만 하면 커버리지 **42.5%** 로 문턱을 못 넘습니다. 33.7% → 42.5% 가 임상적으로 옹호되는 몫(+8.8%p)이고, 42.5% → 62.1% 가 **A2+A3 을 덮어서 얻은 몫(+19.6%p)** 입니다 — **이득의 3분의 2 이상이 문서가 인정하지 않은 병합**에서 옵니다. A2+A3 이 근거 없진 않지만(둘 다 표피 표면 변화) 그건 **제 판단**이고, 긴급도도 다릅니다(A2 관찰 / A3 진료 권장) | STEP 28 |
| ★ **어느 묶음을 권하나** | **A6 만 따로 둔 4군** (A1·A4 / A2·A3 / A5 / **A6**). 3군 대비 커버리지 62.1% → **61.4%** (−0.7%p), 전체 정확도 0.8365 → **0.8315** 로 거의 안 잃으면서 **종양 감별이 필요한 유일한 클래스의 이름을 지킵니다.** A5 와 묶어 "손상·덩어리" 로 부르면 그 뜻이 사라집니다. ⚠️ 4군 macro-F1 은 0.7247 로 3군(0.7674)보다 낮습니다 — A6 이 작아서인데, **여기선 macro-F1 이 제품 지표가 아닙니다** | STEP 28 |
| ★★★ **커버리지 67.9% 를 실사용 값으로 인용해도 되나** | **아니오 — 라벨 네모로 잰 값입니다 (STEP 36).** 우리 평가는 전부 **라벨러가 병변에 딱 맞춰 그린 bbox** 로 잘랐습니다. 실사용 네모로 바꾸면 2단계 macro-F1 이 **상대 −22~24%** 떨어집니다 (VL01, n=2,836). ★ **커버리지는 더 크게 무너집니다 — 상대 −55.5%** (VL01, n=3,064: 라벨 네모 50.3% → 사용자 네모 **22.4%**, STEP 39). macro-F1 하락(−23%)의 **두 배 넘습니다** — 커버리지가 **문턱 지표**라 작은 하락을 증폭하기 때문입니다 — 정확도 67.6 → 61.7%(−8.6%) · 확신도 AUROC 0.7192 → 0.6484(−9.8%) 인데 커버리지만 −55.5% 입니다 (**6배 증폭**). ⚠️ 그 비율을 67.9% 에 그대로 대입하지 마세요 (VL01 은 커버리지에서 **비관적**인 청크, STEP 29·31). 확실한 것은 **67.9% 가 상한**이고 **하락이 macro-F1 보다 크다**는 것입니다. ⚠️ **발표·문서에서 인용할 때 이 단서를 같이 다세요.** STEP 11 에서 *"실제는 더 나쁩니다"* 를 각주로만 적고 안 재서 두 달을 버린 자리입니다 | STEP 36 |
| ★★★ **holdout 에서도 버티나** | **예 — 계열 4군 커버리지 69.8% → **67.9%**, 단 −1.9%p (STEP 33).** 긴급도 하향 3.1 → **3.7%**(허용 5%), A6 오명명 10.1 → **12.5%**(허용 30%) 로 **관문 셋 다 통과**합니다. 릴리스 단독도 59.4 → **58.4%** 로 문턱 위. **결정을 바꿀 이유가 없습니다.** ⚠️ 단 이 holdout 은 05·07 이 이미 열어본 적이 있어 *"한 번만 봤다"* 보다 노출이 큽니다 | STEP 33 |
| ★ **1단계는 holdout 에서 어떤가** | **recall 은 같고 헛알림률은 좋아졌습니다** — recall 0.9434 → **0.9430**, 헛알림률 24.5% → **18.7%** (같은 임계값 0.1466). ⚠️ **그런데 넘긴 것 중 헛알림 몫은 19.0 → 20.3% 로 늘었습니다.** 모순이 아니라 **holdout 에 정상 사진 비율이 더 높기 때문**입니다 (47.4 → 56.2%). 커버리지를 좌우하는 건 **몫** 쪽이라, 계열 4군이 −1.9%p 떨어진 이유의 일부가 이것입니다. ⚠️ **'1단계가 holdout 에서 더 좋다' 로 읽으면 안 됩니다** — 구성이 다를 뿐입니다 | STEP 33 |
| ⚠️ **holdout 에서 크게 떨어진 것이 있나** | **A6 경보 하나입니다 — 반토막.** 정밀도 90% 에서 재현율이 val **50.4% → holdout 25.1%** (80% 에서는 68.1 → 48.8%). 다른 지표는 1~2%p 인데 여기만 25%p 입니다. holdout 의 A6 이 1,003장이라 표본 탓도 아닙니다. **왜인지는 못 잽니다** — ★ **결론: '덩어리가 의심됩니다' 를 90% 정밀도로 켜면 실제 덩어리의 4분의 1만 잡습니다.** val 값으로 기대치를 잡으면 안 됩니다 | STEP 33 |
| ★★ **A6 경보가 왜 반토막인가** | **모델이 아니라 유병률입니다 (STEP 34).** **같은 문턱**에서 재보면 재현율은 거의 같고(0.30 문턱: val 68.6% / ho **68.2%**, 0.70: 38.4 / **36.9%**) **정밀도만 5~13%p 낮습니다.** 재현율은 유병률과 무관하고 정밀도는 직접 좌우되기 때문입니다. 원인: **A6 이 holdout 에 절반만큼 드뭅니다** — 전체 중 3.0% → **1.7%**, 넘긴 사진 중 4.61% → **2.91%**. 부위·청크 구성은 같으니 **빈도만 바뀐 것**입니다 (개체 단위 분할이라 A6 개체가 어디로 가느냐로 흔들립니다). 크기도 조금 작아졌습니다 (25분위 138 → 114px) | STEP 34 |
| ★ **그럼 A6 경보를 어떻게 정하나** | **정밀도가 아니라 재현율로** 정하세요. *"정밀도 90%" 는 모델의 성질이 아니라 **모델 × 유병률**의 성질*이라, 문턱을 고정해도 정밀도는 안 고정됩니다. 실제 앱의 A6 유병률은 val(4.61%)도 holdout(2.91%)도 아닙니다. **문턱 0.40 이면 재현율 약 60%** 가 양쪽에서 유지되고 정밀도는 71~81% 사이에서 움직입니다. 임상 해설이 *"A6 오탐은 상대적으로 안전"* 이라 했으니 정밀도를 양보하는 쪽이 맞습니다 | STEP 34 |
| ★★ **하향 문턱 5% 에 근거가 있나** | **생겼습니다 — 제품이 이미 받아들이는 하향이 닻입니다 (STEP 34).** 1단계가 병변의 **5.70%** 를 놓치고 그건 *"괜찮아 보여요"* 로 나가는 **완전한 하향**입니다(병원에 아예 안 감). 2단계의 3.7% 는 **부분 하향**(조기진료 → 진료권장, 병원에는 감)이라 **더 작고 더 약해야** 합니다 — `0.05 < 0.057`. 클래스별로도 같습니다: 1단계는 **A6 을 8%** 완전히 놓치는데 2단계 부분 하향은 3.7% 입니다. `experiments.STAGE1_MISS_RATE` 와 관계를 `tests/test_granularity_gates.py` 가 지킵니다. ⚠️ 그래도 *'나쁜 걸 받아들일까'* 는 사람 몫입니다 | STEP 34 |
| ★ **릴리스 단독 58.4% 가 "쓸만한" 수준인가** | **아니오 — 제 표현이 과했습니다 (STEP 35).** 정확히는 *"문턱은 넘지만 융기·발진을 셋에 하나밖에 못 잡는다"* 입니다. holdout 포착률: 융기·발진 **31.9%** / 표면 변화 71.9% / 미란·궤양 38.0% / 결절·종괴 46.7%. 말한 것의 **77%가 "표면 변화" 한 묶음**이고(실제로는 51%) 앙상블을 빼면 사실상 **표면 변화 감별기**입니다 | STEP 35 |
| ★★ **그럼 앙상블의 값어치가 커버리지인가** | **아니오 — 포착률입니다 (STEP 35).** 융기·발진 31.9 → **51.3% (1.6배)**, 결절·종괴 46.7 → 54.5%. 커버리지 +9.5%p 는 그 **결과**입니다. ⚠️ 단 A5·A6 은 +4%p 뿐 — 계열 묶음은 **묶은 자리(A1↔A4 · A2↔A3)에서만** 좋아지고 A5 는 묶음 **사이**라 그대로입니다 | STEP 35 |
| ★★★ **긴급도 하향 관문이 안전한가 — 전체 3.7%면 되나** | **아니오 — 한 클래스가 묻힙니다 (STEP 35).** 전체 하향 **3.7%** 로 관문(5%)을 통과하는 동안 **말한 A5 의 43.8%** 가 하향이었습니다. 표면 변화(전체의 51%)가 평균을 떠받쳐서요. ★ **STEP 30 의 재발**입니다 — 그땐 *"하향만 세면 과잉으로 도망친다"*, 이번엔 *"전체만 세면 한 클래스가 묻힌다"*. **축이 다를 뿐 같은 실수.** → `granularity_report` 가 `under_by_class`·`under_worst` 를 같이 찍습니다. ⚠️ **문턱은 안 겁니다** — 닻이 없습니다(1단계의 'A5 6% 놓침'은 **A5 전체** 분모, 43.8%는 **말한 A5** 분모라 비교 불가). `over_triage` 와 같은 처방: 관문으로 안 쓰되 **찍습니다** | STEP 35 |
| **앙상블이 A5 를 더 위험하게 만드나** | **아니오 — 선택 효과입니다 (STEP 35).** 릴리스와 **같은 장수(20,121)만** 말하게 자르면 A5 하향 **38.0%** vs 릴리스 37.4% (차이 +0.6%p, `ARM_SAME_COVERAGE_TOL` 안). 43.8% 는 **6,271장을 더 말하기로 한 대가**입니다. ⚠️ **STEP 34 가 A6 경보를 푼 방법과 똑같습니다** — 비율은 분모가 바뀌면 같이 바뀝니다. 팔별 폭도 6.9%p 로 '한 팔 탓' 기각 | STEP 35 |
| ★ **A5 하향을 줄일 수 있나** | **예 — 하향 방지 규칙 (STEP 35).** *"급한 쪽(미란·궤양+결절·종괴) 확률 합이 **0.25** 이상이면 덜 급한 묶음은 답 후보에서 뺀다."* holdout 커버리지 67.9 → **66.5%(−1.3%p)**, A5 하향 43.8 → **36.8%(−7.0%p)**, 전체 하향 3.7 → 2.9%. **추론 0회**이고 릴리스 단독보다 **두 축 다** 낫습니다. 문턱은 **val 에서 고르고 holdout 은 확인만** 했습니다. ✅ **서빙에 들어갔습니다** (`config.DOWNGRADE_BLOCK_MIN` · `agent.lesion_group`). ⚠️ `stage2.distribution`(6종 분포)은 **안 건드립니다** — 바뀌는 건 `stage2.group` 한 줄뿐이라 **앱은 고칠 게 없습니다**(앱은 `distribution` 만 읽습니다) | STEP 35 |
| ⚠️ **묶음을 고르는 코드가 몇 곳인가** | **하나여야 합니다 — `agent.lesion_group()`.** `message.lesion_group_line()` 이 묶음 합·문턱·argmax 를 **따로 계산**하고 있었습니다. 하향 방지 규칙을 넣을 때 한쪽만 고쳤으면 **두 화면이 다른 계열을 말합니다** — 갈라져도 아무도 모릅니다(촬영 가이드 밴드에서 이미 당한 모양). 이제 `message` 는 **문장만** 만들고 선택은 `agent` 가 합니다. `tests/test_granularity_gates.py::[8]` 이 *"message 가 `MORPH_GROUP_KEEP_A6` 를 직접 쓰지 않는다"* 로 감시합니다 | STEP 35 |
| 팔 가중치로 A5 를 살릴 수 있나 | **아니오 — 기각.** 릴리스 팔 비중을 0.33 → 1.00 까지 올려도 val A5 하향이 **35% 언저리에서 안 움직이고** 커버리지만 **−10.4%p**. 위 '선택 효과' 가 이유입니다 — 같은 장수로 맞추면 세 팔이 A5 에서 같으므로, 비중을 실으면 **말수가 줄어드는 효과**만 얻습니다 | STEP 35 |
| 비용 행렬(기대비용 최소)로 결정 규칙을 바꾸면 되나 | **아니오 — 기각.** 같은 커버리지 구간에서 문턱 규칙보다 **1~3%p 나쁩니다.** 비용 행렬은 하향뿐 아니라 **과잉·이름 오류도 같이** 벌해서 A5 와 무관한 사진의 답까지 흔듭니다. ⚠️ 설계 결함도 있었습니다 — '정상인데 이름 말함' 비용이 네 후보에 **똑같이** 붙어 `argmin` 에서 **상쇄**됩니다 (아무 일도 안 함) | STEP 35 |
| ★★ **그럼 문턱 0.25 는 무슨 뜻인가** | **"하향은 다른 오답의 약 2배로 나쁘다" 입니다 (STEP 35).** 비용 행렬이 규칙으로는 졌지만 **문턱을 판단의 언어로 번역**했습니다. 이제 사람이 정할 수 있습니다 — 3배면 커버리지 62.7%, 5배면 55.0%, 8배면 39.8%. *"0.25 로 하자"* 는 반박 불가지만 *"하향은 과잉보다 2배 나쁘다"* 는 **반박 가능**합니다 | STEP 35 |
| 계열 한 줄을 만들었나 | **예 — 기본은 꺼져 있습니다** (`message.SHOW_GROUP`, 환경변수 `DOG_SKIN_SHOW_GROUP=1` 로 켬). 확신도(`p1 × p(계열)`)가 **0.469** 미만이면 **아무 말도 안 합니다**. 검사 9개가 켠 상태에서도 **6종 이름이 안 새어 나가는지**와 **긴급도 문구가 안 붙는지**를 지킵니다 (붙이면 말한 것의 절반이 한 단계 부풀려집니다) | STEP 34 |
| 앙상블 값어치가 holdout 에서도 유지되나 | **예 — +10.4%p → +9.5%p.** 6종 이름에서는 +15.7%p 로 오히려 큽니다. *굵게 말할수록 앙상블의 몫이 줄어든다* 는 규칙성도 그대로입니다 | STEP 33 |
| 과잉·하향 관문이 holdout 에서도 일하나 | **예 — 같은 자리에서 걸립니다.** 교과서 2군은 커버리지 1등(72.9%)에 하향 0.0% 인데 **과잉 89.0%**, 긴급도 2등급은 커버리지 89.4% 에 **하향 40.7%**. STEP 28·30 에서 만든 두 관문이 **holdout 에서도** 같은 둘을 걸렀습니다 | STEP 33 |
| ★★ **전체 데이터에서도 계열을 말할 수 있나** | **예 — VL01 이 오히려 비관적이었습니다 (STEP 31).** 헛알림 포함 커버리지가 계열 4군 61.4% → **69.8%**, 6종 이름 33.7% → **41.4%**, 계열 3군 62.1% → 70.8%, 긴급도 42.7% → 49.2% — **넷 다 올랐습니다.** 계열 4군은 긴급도 하향 **3.1%** (허용 5%) 로 관문을 통과하고, A6 안전 관문도 표본 53 → **859장**에서 **10.1%** (허용 30%) 입니다. **결정에 필요한 숫자는 다 모였습니다** | STEP 31 |
| ★★ **수의학 표준 분류가 우리 묶음을 지지하나** | **예 — 다만 그 축으로 묶으면 기각입니다 (STEP 32).** 표준은 **primary**(병이 만든 것: papule · plaque · pustule · nodule) / **secondary**(그 뒤에 생긴 것: collarette · erosion · ulcer · lichenification) / **둘 다 됨**(scale · crust · 색소) 입니다. ⓐ **A2·A3 가 똑같이 애매한 자리**(둘 다 mixed)이고 출처가 *"secondary 는 진단 가치가 낮다"* 고 적으므로, A2+A3 병합은 **덜 진단적인 둘을 묶는 것** — 사후 정당화가 아닌 근거입니다. ⓑ A1·A4(≤1cm)와 A6(>1cm)의 **1cm 는 교과서 경계**라 A6 를 따로 둔 게 표준과 맞습니다. ★ **개(canine) 한정 자료가 더 직접적입니다**: *"Secondary lesions in canine dermatology include **erosions, ulcers, crusts, epidermal collarettes, hyperpigmentation and lichenification**"* — **A2·A3·A5 가 한 줄에** 있고, *"lichenified skin **frequently is also hyperpigmented**"* 라 A3 의 두 성분이 같이 갑니다. secondary 를 다시 가르는 선은 **'장벽이 뚫렸는가'** (A2·A3 온전 / A5 깨짐)이고 그건 임상 해설이 이미 A5 의 정의로 적어둔 것입니다. ⚠️ 저는 수의사가 아닙니다. **AI Hub 561 로 A1~A6 를 묶은 선행 연구는 못 찾았습니다** — 선례가 없습니다 | STEP 32 |
| 🚫 그럼 primary/secondary 로 2군을 만들면 | **기각 — 커버리지는 1등인데 과잉이 88.4%.** 74.6%(하향 **0.0%**)로 제일 좋아 보이지만 `primary` 에 A6(조기 진료)이 들어 있어 **구진 하나에도 '조기 진료'** 가 붙습니다. ★ **STEP 30 의 과잉 관문이 두 번째로 일한 장면**입니다 (첫 번째는 긴급도 2등급). ⚠️ 교과서는 *진단 순서*의 축이지 *보호자에게 뭐라고 부를지*의 축이 아닙니다 | STEP 32 |
| ★★ **앙상블을 써야 하나** | **예 — 그런데 제가 '추론 3배' 라고 한 게 안 재고 한 말이었습니다 (STEP 32).** 3팔 중 둘이 **4.4배 작은** `effnetv2_s`(20.2M) 라 실제 비용은 배포와 같은 조건(**CPU**, 스레드 4개, 1장)에서 **전체 파이프라인 774ms → 1052ms = 1.36배**(2단계만 보면 1.44배), 메모리 +0.16GB 입니다. **+278ms 로 커버리지 +10.4%p**(59.4 → 69.8%)를 삽니다. 게다가 **위치 교란은 오히려 좋아집니다**(17.9 → 15.2%, STEP 26). ⚠️ 남는 비용은 **유지보수** — 두 팔이 STEP 16 릴리스에 없어 `export_release` 에 더해야 합니다 | STEP 32 |
| 앙상블 없이도 계열을 켤 수 있나 | **예 — 릴리스 단독 59.4%** 로 문턱(50%)은 넘습니다(하향 3.3%). 다만 **10%p 를 버리는 것**입니다. ⚠️ **6종 이름은 반대로 앙상블이 필수**: 릴리스 단독 **26.9%** 로 닫음 문턱(30%) 아래인데 앙상블이 41.4% 로 올립니다 | STEP 32 |
| 앙상블의 몫에 규칙이 있나 | **굵게 말할수록 줄어듭니다.** 6종 +14.6%p → 문서 인정 병합 +16.5%p → **계열 4군 +10.4%p** → 교과서 2군 +7.5%p. 앙상블이 메우는 건 **세밀한 구분**이고, 굵게 말하면 그 구분이 애초에 필요 없습니다 | STEP 32 |
| ⚠️ STEP 31 의 69.8% 는 무엇으로 잰 값인가 | **3팔 앙상블입니다.** 노트북 11 이 앙상블만 쟀고 릴리스 단독은 안 쟀습니다 — 배열을 받아 다시 재니 **59.4%** 였습니다. **숫자 옆에 '무엇으로 쟀나' 를 안 적으면 또 이럽니다** (청크에 이어 두 번째 종류) | STEP 31 → 32 |
| ⚠️ **VL01 은 후한가 짠가** | **지표마다 반대입니다 — 뭉뚱그리면 계속 틀립니다.** macro-F1·A4 recall 에선 **낙관적**이었고(STEP 23·25), 커버리지·A6 경보에선 **비관적**입니다(STEP 29·31). 같은 청크가 지표에 따라 방향이 바뀝니다. **방향을 짐작하지 말고 재세요** | STEP 25 → 31 |
| ★ **1단계 recall 0.970 이 맞나** | **아니오 — 그것도 VL01 값이었습니다.** 전체 val 51,983장에서 **0.9434** 입니다 (헛알림률 24.5%, 임계값 0.1466). `target_recall: 0.95` 로 뽑은 임계값인데 목표보다 0.7%p 낮게 나옵니다. 우리가 인용해온 숫자가 **2.7%p 후했습니다** — 병변 100마리 중 놓치는 게 3마리가 아니라 **5.7마리**입니다. ⚠️ holdout 은 아직 (06 만 엽니다) | STEP 31 |
| 넘긴 사진 중 헛알림 몫이 얼마인가 | **19.0%** (31,827장 중 6,044장). VL01 은 20.8% 였습니다. 목표 오답률 20% 와 거의 같아서, **거절이 헛알림을 골라 버려줘야만** 20% 아래로 내려간다는 구조는 전체에서도 같습니다 | STEP 31 |
| ★★ **"계열 정확도 0.84" 를 근거로 써도 되나** | **아니오 — 제가 하한선을 안 적었습니다 (STEP 29).** 굵게 묶으면 *아무것도 안 하는 하한선*도 같이 오릅니다: 3군의 최빈 묶음('표면 변화')이 전체의 **67.1%** 입니다. **하한선 대비로 보면 6종이 +35.1%p, 3군은 +16.5%p** 로 오히려 반대입니다 — 0.7037 → 0.8365 는 모델이 좋아진 게 아니라 **문제가 쉬워진 것**입니다. 작업 규칙 1 을 제가 어겼습니다 | STEP 29 |
| 그럼 커버리지도 같은 문제인가 | **아니오 — 커버리지는 자유롭습니다.** *'늘 최빈 묶음만 말하기'* 는 오답률이 어디서나 32.9% 라 20% 목표를 한 번도 못 지킵니다 — **하한선의 커버리지는 0%** 입니다. 61.4% 는 전부 모델의 **확신도 순서**가 벌어온 것입니다. → **정확도 말고 커버리지를 쓰세요** | STEP 29 |
| ★ **A6 경보('덩어리가 의심됩니다')를 켤 수 있나** | **예 — VL01 이 4배 넘게 깎아 보고 있었습니다.** 표본 124 → **1,540장**(전체 val)으로 늘리니 **정밀도 90% 에서 재현율 50.4%** (VL01 은 12.1%), 80% 에서 68.1%. **릴리스 단독으로도 90%/42.8%** 라 추론 3배 없이 됩니다. VL01 숫자로 판단했으면 이 기능을 버렸을 겁니다 | STEP 29 |
| 전체 val 에 헛알림이 없는데 A6 정밀도를 믿어도 되나 | **예 — 경고를 달지 말고 쟀습니다.** VL01 에서 '헛알림 포함' 과 '병변만' 을 둘 다 재니 정밀도 80% 에서 재현율이 **39.5% / 39.5%** 로 같습니다. 멀쩡한 피부를 덩어리로 보지는 않습니다. ⚠️ STEP 27 에서 '실제는 더 나쁩니다' 를 각주로만 달았다가 두 달을 버린 뒤라, 이번엔 **차이를 실측**했습니다 | STEP 29 |
| 4군에서 결절·종괴가 얼마나 지켜지나 | **recall 0.645, 긴급도 하향 31.0%** (거절 없이 전부 말했을 때, 전체 val). 허용치 5% 를 한참 넘습니다. **다만 '관찰' 로 부른 건 0.0%** 입니다 — A2·A3 묶음의 긴급도를 보수적으로 높은 쪽(진료 권장)으로 잡아둔 덕에, 어떤 경로로 틀려도 **병원에 가라는 말은 나갑니다.** ⚠️ STEP 28 의 '하향 1.7%' 는 **거절 뒤·VL01** 값이라 다른 숫자입니다 | STEP 29 |
| 계열로 가면 다음 약점은 뭔가 | **미란·궤양(A5) recall 0.510** 입니다 (융기·발진으로 25.8%). A6 을 따로 빼니 A5 가 혼자 남아 드러났습니다. 표면 변화는 0.917 로 전체 정확도를 떠받치고 있어 평균에 가려져 있었습니다 | STEP 29 |
| **A5(미란·궤양)도 장수 문제인가** | **아니오 — 대조군이 바로 옆에 있습니다 (STEP 30).** A6 은 val 1,540장·개체 **102** 로 A5(1,468장·개체 **113**)보다 **적은데** recall 이 0.675 vs **0.541** 입니다. A6 은 STEP 16 에서 데이터 8배로 풀렸는데(0.328→0.619) A5 는 같은 크기를 받고도 안 풀렸습니다 — **더 준다고 풀릴 자리가 아닙니다** | STEP 30 |
| ★ **그럼 A5 는 무엇과 안 갈리나** | **융기·발진(A1·A4)입니다.** 짝별 AUROC 최악 셋 중 둘이 A5 입니다 — **A4↔A5 0.861 · A1↔A5 0.877** (최악은 A1↔A4 0.768). ★ **이게 계열 묶음이 A5 를 못 살린 이유**입니다: 4군은 A1↔A4·A2↔A3 를 **묶음 안**으로 넣어 없앴는데 A5↔A1·A4 는 **묶음 사이**라 그대로 남습니다 (4군에서 A5 포착 0.510) | STEP 30 |
| 크기가 A5 를 설명하나 | **매개합니다.** A5 는 **작을수록** 틀리고 (Q1 0.449 → Q4 0.604) A1 은 **클수록** 틀립니다 (Q1 0.777 → Q4 0.424) — **방향이 정반대**이고 중앙값이 겹칩니다 (A1 134px / A5 167px). 작은 미란과 큰 플라크가 같은 크기대에서 만납니다. 부위는 설명 안 됩니다 (폭 0.11) | STEP 30 |
| ★ 그럼 A1·A4·A5 혼동의 정체는 | **입체감이 사진에 안 찍히는 것** — **소거법으로 남은 설명**입니다. 세 짝이 요구하는 게 전부 같은 종류입니다: 융기 안에 고름이 있나(A1↔A4) · 솟았나 파였나(A1↔A5) · 부풀었나 터졌나(A4↔A5). 사진 한 장은 비스듬한 조명이 없으면 그걸 잘 안 담습니다. **대조군이 지지합니다** — A3 의 단서는 **색**인데 A1↔A3 는 **0.934** 로 잘 갈립니다. 색은 사진에 있고 입체감은 없습니다. ⚠️ 직접 시험하려면 같은 병변을 **조명 각도만 바꿔 찍은 쌍**이 필요한데 이 데이터엔 없습니다 | STEP 30 |
| 그럼 A5 를 어떻게 고치나 | **모델이 아니라 촬영입니다.** 촬영 가이드 축이 지금까지 *'털을 헤쳐서 피부가 보이게'* 하나뿐이었는데, **두 번째 후보(비스듬한 조명·두 각도)** 가 생겼습니다. ⚠️ 관찰이지 개입 실험이 아닙니다. 앙상블은 A5 에 **잘 듣습니다** — 세 팔이 전부 0.50 언저리인데 앙상블이 0.541 (+0.04) 이라 **서로 다른 A5 를 틀립니다** | STEP 30 |
| ★★ **긴급도 하향이 낮으면 안전한가** | **아니오 — 반대쪽을 안 세고 있었습니다.** 권고안(4군)의 하향 1.7% 는 **과잉 분류 49.3%** 로 산 것입니다 (6종은 9.1%). A2(관찰)를 A3(진료 권장)와 묶고 묶음의 긴급도를 높은 쪽으로 잡았으니 당연한데, **값을 안 찍는 동안 몰랐습니다.** 지금은 화면에 긴급도를 안 띄워서 안 보이지만, **계열 이름과 긴급도를 같이 띄우면 절반이 한 단계 부풀려집니다 — 두 기능을 같이 켜지 마세요** | STEP 30 |
| A5 를 융기·발진에 합치면 되나 | **숫자는 낫지만 권고하지 않습니다.** 커버리지 67.4% · A5 포착 0.510 → **0.804** · 전체 정확도 0.8490 로 전부 위입니다. 그런데 ① **혼동행렬을 보고 만든 묶음**이고 임상 해설은 A1↔A5 를 **🔴 위험한 혼동**(*"궤양을 구진으로 → 지켜보다 악화"*)으로 꼽았습니다. ② 말한 것의 **29.1%** 가 '조기 진료' 로 나갑니다(권고안 5.0%) — 구진에도 조기 진료라고 하면 그 말의 값이 없어집니다. **데이터가 좋아 보인다고 문서가 위험하다고 적은 구별을 없애지 않습니다** | STEP 30 |
| ★ **오답을 어떻게 세야 하나** | **행동을 바꾸는 것만.** 임상 해설이 꼽은 위험한 혼동(A6→A2 · A5→A1 · A6→A1)의 공통점은 *'급한 걸 안 급하다고 말했다'* 입니다 — 반대 방향은 병원에 가게 만들 뿐이라 안전하고, **1단계 헛알림도 같은 종류**입니다. 6종 이름을 전부 말할 때 **이름 오답률은 45.6% 인데 긴급도 하향은 11.1%** (전체 데이터 13.5%). *'두 번에 한 번 틀린다'* 와 *'여덟 번에 한 번 위험하게 틀린다'* 는 다른 문장입니다 | STEP 28 |
| 굵게 말하면 커버리지가 오르니 그걸로 되나 | **아니오 — 그래서 관문이 둘입니다.** '급한가/안 급한가' 둘로만 말하면 커버리지가 **92.2%** 로 제일 좋은데, 실제로 급한 것의 **46.0%** 를 '안 급하다' 고 말합니다. 커버리지만 봤으면 **이걸 골랐을 겁니다** → `UNDER_TRIAGE_MAX = 0.05` | STEP 28 |
| 굵게 말해도 앙상블이 필요한가 | **6종 이름일 때만.** 릴리스 단독 vs 3팔: 6종 이름 0.7% → 33.7%(+33.1%p) 인데 **형태 계열은 52.5% → 62.1% (+9.6%p)** 로, 릴리스 단독도 이미 문턱을 넘습니다. **추론 3배는 알갱이를 굵게 하면 '필수' 가 아니라 '선택'** 이 됩니다 | STEP 28 |
| A6 경보('덩어리가 의심됩니다')를 따로 낼 수 있나 | **커버리지로 재면 안 됩니다** — A6 은 넘긴 사진의 3.2% 라 최대치가 3.2% 입니다. 정밀도· 재현율로 보면 **정밀도 50% 에서 재현율 71.8%**, 80% 에서 39.5%. 임상 해설이 *'A6 오탐은 상대적으로 안전'* 이라 했으므로 쓸 만합니다. ⚠️ 표본 124장 — 켜려면 전체로 다시 | STEP 28 |
| VL01 은 늘 낙관적인가 | **아니오 — 지표마다 방향이 다릅니다.** macro-F1·A4 recall 에선 낙관적(STEP 23)인데, 커버리지-위험에선 **비관적**입니다 (오답률 10% 목표에서 VL01 0.0% vs 전체 21.5%). "VL01 은 후하다" 로 뭉뚱그리면 또 틀립니다 | STEP 25 |
| 저장된 크롭은 몇 px 인가 | **`save_crop_size=512` 는 목표가 아니라 상한**입니다. `f320` 은 항상 320이지만 `m2.5` 는 창 크기 그대로라 **64px 짜리도 있습니다** — 384 입력에 넣으면 6배 확대 | 실측 |
| 로컬 GPU 를 쓸 수 있나 | **예 — 고쳤습니다.** `torch>=2.2` 만 지정하면 윈도우는 **CPU 휠**(`2.13.0+cpu`)이 깔리고 `cuda.is_available()` 이 조용히 False 가 됩니다. cu126 인덱스를 **윈도우 마커로만** 묶었습니다 (리눅스 해석 불변) | 2026-09-05 |

각 결론이 **언제 어떻게** 나왔는지는 [`HISTORY.md`](HISTORY.md) 에,
원본 숫자는 [`docs/results/`](docs/results/) 에 있습니다.

---

## 알려진 함정 (전부 실제로 당한 것)

* **캐글은 `main` 을 받고 있었습니다 — 작업 브랜치가 아니라.** 첫 셀은
  리포가 이미 있으면 그 브랜치를 쓰고, 없으면 기본값을 씁니다. 그런데
  캐글/콜랩은 **리포가 없는 상태로 시작**하므로 항상 기본값(`"main"`)을
  받았습니다. main 이 뒤처져 있으면 **셀은 최신인데 `src/` 만 옛것**인 채로
  돕니다 (실제로 며칠: main 75445c0 / 작업 브랜치 3ea74ec).
  → 노트북마다 `NB_BRANCH` 로 못 박습니다 (`DOG_SKIN_BRANCH` 로 덮어쓰기 가능).
  ⚠️ 이걸 잡아준 건 `MY_NOTEBOOK_VERSION` vs `config.NOTEBOOK_VERSION` 경고
  **하나뿐**이었습니다 — 그게 없었으면 또 못 봤습니다
* **첫 셀이 "코드 버전" 을 찍는다고 그게 최신이란 뜻은 아니었습니다.**
  `fetch`/`reset` 을 둘 다 `check=False` 로 불러서, 캐글 클론이 **원격에 더
  이상 없는 커밋**(75445c0)에 붙박인 채 며칠을 돌았습니다. src/ 를 고쳐
  푸시해도 하나도 안 실렸고 **에러는 한 줄도 안 났습니다.**
  shallow clone 은 히스토리가 갈리면 `origin/<브랜치>` 가 옛 커밋을 가리킨
  채 남습니다 → 이제 `FETCH_HEAD` 로 맞추고, 실패하면 말하고 다시 clone 하고,
  `ls-remote` 로 한 번 더 대조합니다 (`tests/test_notebook_git_sync.py`)
  ⚠️ Kaggle Persistence 를 'Files' 로 켜두면 `/kaggle/working` 이 살아남아
  낡은 클론이 계속 재사용됩니다
* ★ **환경 셀을 통째로 베끼면 `NB_BRANCH` 까지 따라옵니다 — 설정을 로직으로
  착각했습니다.** 노트북 13 을 만들며 12번의 환경 셀을 **그대로 복사**했습니다.
  클론·검증 로직을 베껴 쓰는 건 맞는 판단이었는데(베껴 적으면 그쪽이 고쳐져도
  안 따라옵니다), 그 셀 안에는 `NB_BRANCH = "claude/dog-disease-…"` 도 같이
  있었습니다. 그 브랜치는 **main 보다 44 커밋 뒤**이고 `src/detect.py` 가
  없습니다. 캐글은 데이터셋을 붙이고 개체를 갈라 놓고 **56초 뒤에** 죽었습니다
  (`ModuleNotFoundError: No module named 'src.detect'`).
  ⚠️ **그 앞에서 첫 셀은 `[nb] 노트북 최신 (2026-09-04.5)` 을 태연히 찍었습니다.**
  버전 대조는 *셀 vs 리포* 를 볼 뿐 **브랜치가 맞는지는 안 봅니다** — 위의
  *"코드 버전을 찍는다고 최신이란 뜻은 아니다"* 와 같은 함정의 다른 얼굴입니다.
  → 생성기(`tools/build_detect_notebook.py`)가 베낀 셀의 `NB_BRANCH` **한 줄만**
  덮어쓰고, `tests/test_notebook_branch_has_src.py` 가 *"못 박은 브랜치의 git
  트리에 그 노트북이 import 하는 `src/<모듈>.py` 가 실제로 있나"* 를 봅니다
  (옛 브랜치로 되돌려 **FAIL 이 나는지 확인**했습니다 — 못 터지는 검사는 검사가
  아닙니다).
  ⚠️ 일반화: **베끼는 것이 로직인지 설정인지 가르세요.** 로직은 베끼는 게 맞고
  설정(브랜치·경로·이름)은 **덮어써야** 합니다
* **캐글 입력 경로는 `/kaggle/input/datasets/<계정>/<데이터셋>` 입니다.**
  크롭과 매니페스트를 각각 붙이면 둘이 `<계정>` 아래 **형제**가 되는데,
  탐색이 그 부모에서 멈춰 크롭을 아예 못 봤습니다 (`찾은 입력:
  [('…/gayoniee', 'dir')]`, `사용 가능한 태그: []`). 붙여놓고도 못 찾는
  종류라 원인이 안 보입니다 → 크롭이 **실제로 들어 있을 때만** 멈춥니다
* **캐글에 올리면 폴더가 한 겹 더 싸일 수 있습니다** (`m2.5/m2.5/<hh>/*.jpg`,
  `manifests/manifests/*.parquet`). 크롭 쪽은 링크가 걸리는데 경로가 한 칸씩
  어긋나 `0/365,428장 (0.0%)` 로 죽습니다 — "붙었다" 고 보이면서 한 장도
  안 읽히는, 제일 알아보기 어려운 실패입니다 → `_unwrap_tag_dir()`
* **노트북 셀은 `git pull` 로 안 바뀝니다.** 첫 셀의 `MY_NOTEBOOK_VERSION` 이
  `config.NOTEBOOK_VERSION` 과 다르면 경고가 뜹니다 → `.ipynb` 를 다시 Import
* **`Path.rglob` 는 심볼릭 링크 하위로 안 들어갑니다** (두 번 당함)
* ★ **캐글은 Dataset 을 만들 때 ZIP 을 자동으로 풉니다 — ZIP 을 찾는 재개는 조용히
  새 실행이 됩니다.** 노트북 18 이 `rglob('*_resume.zip')` 으로 재개 파일을 찾았는데
  캐글엔 풀린 폴더만 있어서 *재개 없음* 으로 넘어가 **처음부터 다시 돌았습니다**
  (2026-09-11, 할당량 20분 손실). 아침 실행과 확률이 **비트 단위로 같아서** 알았습니다.
  그 재개 경로는 로컬 스모크만 통과했지 **캐글에서 실제로 이어받은 적이 한 번도
  없었습니다** (14·15·16 은 전부 한 세션에 끝남). → ZIP 과 풀린 폴더를 둘 다 찾고,
  **"재개 없음 — 처음부터"** 를 크게 찍습니다.
  ⚠️ 일반화: **이어받기는 그 플랫폼에서 한 번 실제로 이어받아 봐야 검증된 것입니다**
* **부분 업로드**: 크롭이 30%만 올라간 채로 조용히 학습됐습니다 →
  `crop.MIN_CROP_COVERAGE = 0.95` 미만이면 에러
* **서빙 크롭을 직접 계산하지 마세요.** `crop.crop_window()` 가 "크롭 창을 정하는
  **단 하나의** 함수" 입니다. 서빙에서 "중앙 몇 %" 를 따로 계산했다가 학습과
  갈라졌었습니다 — 갈라져도 아무도 모릅니다. `agent.crop_for()` 처럼 그 함수를
  부르세요
* **`f320` 과 `m2.5` 는 bbox 를 다르게 씁니다.** f320 은 **중심만**(창 320px 고정),
  m2.5 는 **크기**(긴 변 × 2.5). 그래서 가이드 프레임이 부정확할 때 1단계는
  멀쩡하고 2단계만 흔들립니다. 하나로 뭉뚱그려 말하면 틀립니다
* **앱 계약에 "1등 병변" 필드를 추가하지 마세요.** 주는 순간 앱은 그걸 제일 크게
  띄웁니다 — holdout 에서 56.6% 틀린 이름을요. `tests/test_agent.py` 가
  `top1`/`predicted`/`diagnosis` 같은 키를 감시합니다
* **`serve.py` 에 `from __future__ import annotations` 를 넣지 마세요.**
  `UploadFile` 이 문자열 어노테이션이 되는데 라우트가 `build_app` 안에 있어
  pydantic 이 이름을 못 찾고 **500** 으로 죽습니다 (실제로 당했습니다)
* **`calibrate.apply()` 는 확률을 돌려줍니다 — logits 가 아닙니다.** 거기에
  `stages.stage1_scores()` 를 걸면 softmax 가 **두 번** 먹어 전부 0.5 로 뭉개집니다.
  ECE 가 0.03 → 0.20 으로 나빠져서 잡았습니다. 보정된 점수가 필요하면
  **`logits / T` 를 넘기세요**
* **aihubshell 이 붙인 zip 이 `BadZipFile` 로 죽을 수 있습니다 — 다시 받지 마세요.**
  조각을 `find | sort -zt'.' -k2V | xargs cat` 으로 붙이는데, 맥에서 `-V`
  (버전 정렬)가 안 먹으면 **문자열 순서**로 떨어집니다 — `part1, part10,
  part11 … part19, part2, …`. 크기는 정확하고 `file` 도 "Zip archive data"
  라고 합니다 (앞머리만 보니까요). 목차가 파일 끝이 아니라 중간에 있어서
  파이썬만 죽습니다. **바이트는 다 있습니다** → `tools/fix_split_zip.py`
  ⚠️ **밀린 양만 보고 고치면 안 됩니다.** "뒤 1조각만 밀렸다" 는 틀린 가설이
  그 계산을 **똑같이 통과했습니다** (TL02 에서 실제로). 목차를 읽어 항목
  40개의 로컬 헤더와 이름을 찍어보는 검사만이 1/24 로 걸렀습니다.
  ★ **2026-09-09 재확인 — 세 zip(VL01·TL01·TL02)이 100% 이렇게 왔습니다.**
  가끔이 아니라 **맥에서는 늘 그렇습니다.** 그리고 **TL01 이 위험한 쪽이었습니다**:
  목차 위치가 있어야 할 값과 **정확히 일치**(96,637,313,103 = 96,637,313,103)해서
  `zipfile` 로 **열리고 526,681개가 다 보이는데 읽으면 전부 깨집니다**
  (로컬 헤더 61/63 불일치 · 표본 CRC 0/12). `BadZipFile` 조차 안 납니다 —
  → **"열린다" 를 검증으로 쓰지 마세요.** 목차만 보는 검사(①)는 통과시킵니다.
  TL02 복구 때도 후보 3개가 ①을 통과하고 로컬 헤더 대조(②)에서 탈락했습니다
* **JSON 을 못 읽어도 조용히 넘어갔습니다 — 청크의 97% 가 사라졌습니다.**
  TL01(90GB, JSON 263,340개)에서 **6,280개만** 남았는데 "✅ 완료" 가 찍혔습니다.
  범인은 `labels.py` 의 카운터 없는 `except Exception: continue` 한 줄.
  ⚠️ 옆에 찍히는 **"이미지 매칭 실패 0건" 이 사람을 안심시킵니다** — 그 카운터는
  JSON 읽기에 성공한 것만 세서, 앞에서 다 죽으면 당연히 0 입니다.
  같은 코드로 TL02 는 177,605개 중 1개만 놓쳤습니다 (청크마다 다릅니다).
  → 이제 종류별로 세고, 2% 넘게 못 읽으면 **멈춥니다** (`labels.JsonSkips`).
  인코딩도 cp949 를 한 번 더 시도합니다
* **다른 PC 에서 처리한 청크는 `phash` 를 들고 와야 합니다.** `--finalize` 의
  중복 제거는 **원본 사진**을 다시 읽는데, 저쪽 PC 는 크롭 후 원본을 지웁니다.
  그런데 `dedup.compute_hashes` 는 **한 장도 못 읽었을 때만** 에러를 냅니다 —
  VL01 이 읽히니까 조용히 넘어가고 그 청크는 전부 "중복 없음" 이 됩니다.
  청크 경계 누수를 막으려고 만든 단계가 아무 일도 안 합니다.
  → `--chunk` 가 원본을 지우기 **전에** phash 를 재서 매니페스트에 넣고,
  `dedup.run` 이 그걸 재사용합니다. 못 읽은 게 2% 넘으면 **멈춥니다**
* **확정된 설정을 코드가 매 실행 다시 고르게 두지 마세요.** 06 헤더엔
  "1단계 확정: `f320`" 이라고 써두고 정작 코드는 `crop.choose_stage1_tag()` 로
  다시 골랐습니다. 그 판정은 `배율 격차` 하나에 달려 있는데, TL01·TL02 를
  합치자 문턱 아래로 내려가 **1단계를 `m2.5`(ROI 크롭)로 학습하기 시작했습니다.**
  실험 이름에 `m2.5` 가 찍혀서야 알았습니다 — 그대로 뒀으면 3시간을 버렸습니다.
  → 값은 고정하고, 그 함수는 **어긋날 때 알려주기만** 합니다.
  ⚠️ 실험 이름에 크롭·백본·데이터 크기를 다 넣어두는 이유가 이것입니다
* **커널을 끊으면 그 시점의 DataLoader 는 못 씁니다.** 워커 프로세스가 같이
  죽는데 로더 객체는 남아서, 다음에 쓰면 `Empty` / `_try_get_data` 로 죽습니다
  (에러가 `queue.Empty` 라 원인이 안 보입니다). → 그 셀에서 로더를 다시 만드세요.
  평가는 반드시 **`data.eval_loader`** 로 — 행 순서가 보존돼야 1·2단계 출력을
  같은 사진끼리 짝지을 수 있습니다
* **`IOPub message rate exceeded` 는 학습과 무관합니다.** 주피터 **서버가
  브라우저로 보내는 출력**을 잠깐 끊은 것뿐이라 커널은 정상 속도로 돕니다.
  대신 **그동안의 출력이 노트북에 안 남습니다** — 진행 상황은 화면 말고
  `checkpoints/<실험>/history.csv` 를 보세요
* **GPU 0% 라고 멈춘 게 아닙니다.** `crop.switch_tag()` 가 크롭 36만 장을
  **하나씩 존재 확인**합니다. 네트워크 볼륨(Kaggle 입력 · RunPod MooseFS)에서는
  왕복 비용 때문에 몇 분씩 걸리고, 그동안 GPU 는 0% 입니다
* **`unzip` 이 디스크가 차서 죽으면 `unzip -n` 으로 이어받을 수 없습니다.**
  쓰다 만 파일이 남는데 `-n` 은 그걸 "있으니까 건너뜀" 으로 처리합니다 — 학습
  중간에 깨진 JPEG 로 죽습니다. 그리고 `Exit 50`(디스크 가득) 로 16개가 전부
  죽어도 `pgrep -c unzip` 은 0 이라 **"전부 끝남" 처럼 보입니다.**
  ⚠️ 네트워크 볼륨 할당량은 `df` 에 **안 보입니다** (MooseFS 는 클러스터 전체인
  2.0P 를 찍습니다) — 콘솔에서 봐야 합니다.
  → `tools/fetch_crops.py` 가 **zip 이 적어둔 크기와 대조**해 잘린 걸 다시 꺼내고,
  매니페스트가 쓰는 것만 꺼내며(태그당 423,080 → 365,428), 끝나고 다시 세어
  100% 가 아니면 멈춥니다
* **매니페스트 컬럼 이름을 계획 문서에서 베끼지 마세요.** 실물은
  `area_ratio` / `img_w` / `img_h` 인데 계획서엔 `lesion_area_ratio` /
  `width` / `height` 로 적혀 있습니다. 이름이 틀리면 병합이 조용히 NaN 을
  만들고 **없는 걸 재면서 분석이 끝까지 돕니다.** 실물은
  `docs/data/DATASET_CARD.md` 에 있습니다 ("추론이 틀렸던 3곳" 포함).
  붙일 값이 0장이면 **멈추게** 하세요 — `tools/false_alarm_stats.py` 참고
* **`export_release` 에서 빠진 파일은 조용히 기본값이 됩니다.** `temperature.json`
  이 안 실려서 릴리스로 서빙하면 `Engine.load` 가 T=1.0 으로 물러섰습니다 —
  보정 안 된 확률이 보호자에게 갑니다. 에러도 안 납니다.
  `tests/test_calibration_wiring.py` 가 감시합니다
* **`Prediction.topk` 를 화면에 그대로 띄우면 안 됩니다.** 2단계 파이프라인에서
  이 값은 1단계 확률을 곱해 낮춰둔 **거절 판정용**입니다. 보호자에게 보여줄 분포는
  깎기 전 원본인 `Prediction.stage2_probs` (합 = 1) 입니다 — 깎은 값을 띄우면
  여섯 개가 다 작아져 "다 낮네, 별 거 아닌가" 로 읽힙니다
* **두 단계가 서로 다른 백본을 쓸 수 있습니다** — 1단계 effnetv2_s / 2단계 resnet50.
  05 가 "체크포인트는 항상 resnet50" 으로 하드코딩돼 있다가 shape mismatch 로
  죽었습니다 (bn1 24 vs 64). 백본은 **폴더 이름에서** 읽습니다
  (`train.model_key_from_exp`)
* **Kaggle 이미지에도 `/content` 와 `google.colab` 이 있습니다** →
  환경 감지는 Kaggle 신호를 **먼저** 봅니다
* **Kaggle 은 노트북마다 세션이 따로입니다** — 03 이 3시간 걸려 만든 체크포인트는
  05 를 열면 없습니다. 03 을 Commit 으로 돌리고 그 출력을 05 의 입력으로 붙이세요
  (`train.import_previous_run()` 가 가중치 + `stage1_threshold.json` 을 자동 탐색).
  안 붙이면 05 가 시작 직후 죽습니다
* ⚠️ **Kaggle 노트북 하나를 돌려쓰지 마세요.** 03 을 지우고 05 를 붙여넣으면
  05 에 붙일 03 이 **없어집니다** (자기 자신은 입력이 안 됩니다).
  실제로 이렇게 막혔습니다 → `03` / `03b` / `03c` / `05` 를 **각각 별도 노트북**으로.
  이미 덮어썼어도 **예전 버전의 출력은 남아 있습니다** — 그 버전의 Output 탭에서
  **New Dataset (Private)** 으로 만들어 붙이면 됩니다. 재학습 불필요
* **긴 학습은 반드시 [Save Version] → Save & Run All (Commit)** 으로 돌리세요.
  브라우저를 닫아도 끝까지 돌고 출력이 보존됩니다. 대화형 세션으로 돌리면
  탭을 닫는 순간 끝납니다
* ⚠️ **표본 수가 다르면 교란 하락폭을 비교하면 안 됩니다.** 같은 설정을
  3,000장/2,000장으로 재서 20.4% / 25.9% 가 나왔습니다. 프리셋 비교는 반드시
  **같은 실행·같은 표본 수**에서 합니다
* **Colab 무료 한도 초과 시 말없이 CPU 런타임을 줍니다** (20~30배 느림) →
  `env.require_gpu()` 가 막습니다
* ★ **"두 곳이 일치하는가" 검사는 둘 다 옛것이면 아무것도 못 잡습니다.**
  촬영 가이드 밴드가 STEP 10 → STEP 16 에서 바뀌었는데(`0.85~1.4` → `0.7~1.2`),
  `tools/box_error.py` 는 옛 값을 **베껴 둔 채** 몇 주를 돌았습니다. 그런데
  `tests/test_agent.py` 에 "네모 밴드가 줌 밴드의 역수인가" 검사가 **있었고
  통과했습니다** — 두 상수가 같은 파일 안에서 서로 일치했으니까요. 거기 더해
  "1.5배는 허용 밖" 이라는 **옛 밴드 기준 단언**까지 같이 통과했습니다.
  에러는 한 줄도 안 났고, 그 도구가 찍는 "허용 안" 판정만 조용히 틀렸습니다.
  → 밴드의 출처를 **`src/robust.py` 하나**로 모으고(`ZOOM_RECOMMEND`/`ZOOM_ALLOW`),
  쓰는 넷(`agent.py` · `demo/index.html` · `box_error.py` · `SERVING.md`)이
  거기서 끌어 쓰게 했습니다. `tests/test_capture_guide.py::test_band_has_one_source`
  가 넷을 한꺼번에 대조합니다.
  ⚠️ 일반화: **값을 복사한 곳끼리 비교하지 말고, 출처와 비교하세요.**
* ★ **가르는 변수가 가르려는 원인과 붙어 있으면 판정이 성립하지 않습니다 — 그런데
  코드는 조용히 판정을 찍었습니다.** "큰 병변을 놓치는 게 병변 탓인가 창 배치
  탓인가" 를 `occ`(창 안 병변 점유율)로 가르기로 하고 기준을 미리 박아 뒀는데,
  막상 재 보니 **`rho(occ, 병변 넓이) = +0.995`** 였습니다 — `occ ≈ 폴리곤넓이/320²`
  이라 **`occ` 는 병변 크기의 다른 이름**입니다. 그러면 어떤 결과가 나와도
  *"크면 놓친다"* 의 재진술이지 두 가설을 가른 게 아닙니다. 사전등록을 했다는
  사실이 **설계 결함을 못 막습니다** — 오히려 "미리 정했으니 괜찮다" 는 안심을 줍니다.
  → `PLACEMENT_MAX_COLLINEARITY = 0.80`. **표본 수 검사보다 먼저** 봅니다
  (못 가르는 검사는 표본이 아무리 많아도 못 가릅니다).
  ⚠️ 일반화: **층을 나누는 변수와 원인 변수의 상관을 판정 전에 찍으세요.**
* **갈래 축을 leave-one-out 으로 인코딩하면 AUROC 가 거꾸로 무너집니다.**
  LOO 점수는 `(합 − 자기 y) / (n − 1)` 이라, 신호가 하나도 없는 축에서도 양성이
  **항상** 음성보다 `1/(n−1)` 만큼 낮습니다. 순위만 보는 AUROC 는 그 미세한 차이를
  **완벽한 분리(0.000)** 로 읽습니다 — 갈래가 하나뿐인 `synthetic`(전부 False)이
  "차이 있음" 으로 찍혀서 잡았습니다. 그냥 인코딩하면 반대로 **부풀어** 오릅니다
  (견종 120종에서 0.754). → **fold 를 섞는 교차 인코딩**만 0.5 근처로 옵니다
  (`class_overlap.cat_auroc`, `tests/test_class_overlap.py`)
* ★ **안전 관문을 한쪽만 세면, 그 반대쪽으로 도망칩니다.**
  `granularity_report` 는 **긴급도 하향(위험)만** 셌습니다 — 과잉(안 급한 걸
  급하다고)은 병원에 가게 만들 뿐이라 관문에서 뺀 게 맞습니다. 그런데 **찍지도
  않은 게 문제**였습니다: 권고안(4군)의 하향 1.7% 는 **과잉 49.3%** 로 산
  것이었고(6종은 9.1%), 저는 "하향 1.7%" 만 보고 안심하고 있었습니다.
  극단적으로 **전부 "조기 진료" 라고 말하면 하향은 0%** 입니다.
  → **관문으로 안 쓰더라도 반대쪽을 같이 찍으세요.** 이제 `over_triage` 가
  같이 나오고 `tests/test_granularity_gates.py::[3]` 이 그 값이 사라지지
  않게 지킵니다.
  ⚠️ 실무 결론: **계열 이름과 긴급도를 같이 띄우지 마세요** — 절반이 한 단계
  부풀려집니다. 지금은 긴급도를 화면에 안 띄워서 안 보일 뿐입니다
* ★ **굵게 묶으면 하한선도 같이 오릅니다 — 정확도만 보면 늘 이깁니다.**
  STEP 28 에서 저는 "계열 정확도 **0.8365**, 6종은 0.7037" 을 개선 근거로
  앞세웠습니다. 그런데 3군의 최빈 묶음이 전체의 **67.1%** 라 하한선이 이미
  0.671 입니다. 하한선 대비로는 **6종 +35.1%p vs 3군 +16.5%p** 로 **방향이
  뒤집힙니다** — 문제를 쉽게 만들면 모델의 몫은 오히려 줄어듭니다.
  작업 규칙 1 이 "하한선과 비교하지 않은 정확도는 숫자가 아니다" 인데
  **제가 그 규칙을 어겼습니다.**
  → 묶음·병합·클래스 통합을 실험할 때는 **최빈 클래스 비율을 같은 표에**
  적으세요. 그리고 이 프로젝트에서는 **커버리지가 정확도보다 정직합니다** —
  하한선의 커버리지는 0% 라 순위가 안 부풀려집니다.
* ★★ **문턴은 사전등록했는데 하한선을 안 적었습니다 — 같은 실수를 한 단계 앞에서 다시 했습니다.**
  STEP 42 에서 `DETECT_MIN_USABLE = 0.50` 은 돌리기 전에 박았는데, **하한선은
  안 적었습니다.** 결과가 `배율 75.0% · 위치 38.7%` 로 나오자 *"배율은 풀렸다"*
  로 읽을 뻔했습니다. 나중에 재보니 같은 val 에서 **아무것도 안 배운 네모**(화면
  한가운데 · 크기는 실측 병변 중앙값 고정)가 **배율 72.3%** 입니다 — 순이득은
  **+2.7%p**. 검출기가 실제로 배운 것은 **위치**였습니다 (10.1% → 38.7%).
  ⚠️ 더 나쁜 것 — 하한선이 **6.1%** 라서, STEP 36·37 에서 잴 *사람 5.0%* 와
  *창 탐지기 6.7%* 는 **아무것도 안 한 것과 구분이 안 되는 값**이었습니다.
  두 STEP 동안 그걸 몰랐습니다.
  → 이제 `detect.band_report()` 가 하한선을 **같이 냅니다** (`print_report` 가 찍고,
  `tests/test_detect.py::[8]` 이 *하한선을 예측으로 넣으면 순이득 0* 까지 감시).
  ⚠️ 일반화: **사전등록에는 문턴과 하한선을 같이 적으세요.** 문턴만 있으면
  *"이 문턴이 무엇으로부터 얼마나 먼가"* 를 모릅니다. 그리고 **축이 둘 이상이면
  쪼개서 찍으세요** — `둘 다 29.3%` 하나만 보면 "미달" 로 끝나지만, 쪼개면
  다음 사람이 **위치 하나만** 겴냥하면 된다는 것을 압니다.
* ★ **질문의 알갱이를 안 의심하면, 모델이 아는 것을 통째로 놓칩니다.**
  STEP 27 까지 두 달 동안 우리는 *"6종 이름을 말할 수 있나"* 만 물었고 답은
  계속 "아니오" 였습니다 (46.3% 오답 → 커버리지 33.7%). 그런데 같은 모델·같은
  확률로 **형태 계열 3군**을 물으면 정확도 **0.8365** 입니다. 모델이 못 하는
  건 *A1(구진)과 A4(농포)를 가르는 것*이고, 그건 **사진에 안 찍힌 정보**
  (속의 고름)를 요구합니다 — 임상 해설이 처음부터 그렇게 적어놨습니다.
  → **"모델이 못 한다" 와 "우리가 너무 잘게 물었다" 를 먼저 갈라 보세요.**
  같은 확률 벡터로 알갱이만 바꿔 보는 데 **추론 0회**가 듭니다
  (`tools/naming_granularity.py`).
  ⚠️ 단 묶음은 **데이터를 보기 전에** 정해야 합니다 — 혼동행렬을 보고 묶으면
  무슨 묶음이든 좋아집니다. 그리고 굵게 말할수록 커버리지는 공짜로 오르므로
  **안전 관문(긴급도 하향)을 같이** 두세요
* ★ **"실제는 더 나쁩니다" 라고 적어두고 안 재면, 그 각주는 사라지고 숫자만
  남습니다.** STEP 11 은 커버리지 18.2% 옆에 *"이 곡선은 헛알림 1,233건을 뺀
  것이라 실제는 더 나쁩니다"* 라고 **정확히 적어놨습니다.** 그리고 아무도 안
  쟀습니다. 그 18.2% 로 화면 규격(이름을 안 띄운다)이 정해졌고, 두 달 뒤 제가
  같은 분모로 62.0%·71.5% 를 다시 계산해 **"3.9배 나아졌으니 재검토하자"** 고
  말했습니다 — 각주는 안 따라왔습니다. 정직하게 재니 **33.7%** 였습니다.
  → 한계를 각주로 적는 것은 **재는 것의 대체가 아닙니다.** 각주를 달 거면
  **재는 코드를 같이 만들어 두세요** (`tools/naming_coverage.py`,
  `tests/test_naming_gates.py::[4]` 가 이 구멍을 회귀로 잡습니다).
  ⚠️ 일반화: **분모를 바꾸면 결론이 뒤집힙니다.** 지표 이름이 같아도요
* ★ **비싼 단계 앞에 싼 검사를 두세요 — 순서만 바꾸면 됩니다.** 노트북 10 이
  `[X] 체크포인트가 없습니다` 로 죽는 데 **24분**이 걸렸습니다. 검사 자체는
  1초짜리인데 그 앞에 `env.load_prepared()`(크롭 36만 장 존재 확인)가 있었습니다.
  원인은 `train.import_previous_run()` 누락 — 붙인 데이터셋은 `/kaggle/input`
  에 있는데 셀은 `/kaggle/working/data/work/checkpoints` 만 봤습니다.
  **붙여놨는데 못 찾은 게 아니라 볼 곳을 안 봤습니다** (05·09 엔 그 줄이 있고
  10 에만 없었습니다). 그리고 실패 메시지가 "(없음)" 만 찍어서, 데이터셋을
  안 붙인 건지 붙였는데 못 읽은 건지 **구분할 수 없었습니다** →
  이제 `find_checkpoint_sources()` 결과와 `/kaggle/input` 트리를 같이 찍습니다
* ★ **배포에서 코드보다 산출물을 먼저 넣었습니다 — 조용히 더 나빠졌습니다.**
  콘솔에 앙상블을 켜면서 체크포인트 둘을 릴리스 폴더에 **머지 전에** 넣었습니다.
  그때 서버가 돌던 옛 `from_release` 는 앙상블을 모르고, 게다가
  `found[st] = d / "best.pt"` 로 **덮어씁니다**(`setdefault` 가 아니라). 이름 순으로
  훑으며 매번 덮어써서 **마지막 팔 하나만** 남았고, 릴리스의 `convnextv2_base` 가
  빠지고 더 약한 `effnetv2_s` 가 그 자리를 대신했습니다. 3팔이 되기는커녕
  **기준 팔이 강등된 것**입니다. `POST /screen/v1/screen` 은 **200** 이었고 에러도
  경고도 없었습니다 — 앱도 같은 백엔드라 그동안 보호자 쪽도 그 모델을 받았습니다.
  알아챈 유일한 단서는 로그에 `[agent] … 3팔:` 이 **안 찍힌 것**, 즉 **성공 로그의
  부재**였습니다.
  → **코드가 먼저, 가중치가 나중.** *"릴리스에 넣은 것이 곧 구성"* 이라는 설계는
  그것을 **읽는 코드가 새 것일 때만** 성립합니다.
  ⚠️ 일반화: **성공했을 때만 찍히는 줄을 하나 두세요.** 조용한 실패는 실패 로그로
  못 잡습니다 — 없는 줄을 찾아야 잡힙니다.
  → **처방이 생겼습니다 (2026-09-07 오후).** *"지금 몇 팔인가"* 를 알아내는 데
  찌르기 **6번 + 합성 병변 사진** 한 장이 들었습니다 (`meta.stage2_arms` 는
  **2단계가 실제로 도는 사진**이 있어야 나옵니다). 이제 `agent.describe()` 가
  그 요약을 내고 `/healthz` 가 실어 보냅니다 — 사진 없이 `curl` 한 번입니다.
  값을 **둘로** 나눈 게 핵심입니다: `stage2_arms_available`(릴리스 폴더에
  **준비된** 팔, 모델 안 올려도 나옴) vs `stage2_arms`(지금 **올라와 있는** 팔).
  **둘이 다르면 릴리스는 새것인데 프로세스가 옛것을 물고 있다**는 뜻입니다.
  → ★★ **2026-09-09 — 구조로 막았습니다.** 배포가 릴리스 폴더 마운트를 버리고
  **허깅페이스에서 `snapshot_download`** 로 받습니다 (`DAENGS_dev`). 핵심은
  **`SCREENING_EXPECT_STAGE2_ARMS`** — REPO 가 설정돼 있으면 3 이 기본이고,
  **팔 수가 안 맞으면 모델을 아예 안 올리고 `/screen/` 이 503** 입니다.
  *조용한 실패가 시끄러운 실패가 됐습니다.*
* ★ **export 가 import 를 안 따라가면, 릴리스를 다시 만드는 순간 팔이 사라집니다.**
  `agent.from_release()` 는 릴리스의 `stage2_*` 폴더를 **전부** 훑어 앙상블을
  구성하는데, `export_release` 는 노트북이 넘긴 **둘**(1단계·2단계)만 담았습니다.
  지금 콘솔이 3팔로 도는 건 사람이 `best.pt` 를 **손으로 복사**해서입니다 —
  누가 06 을 다시 돌리면 **1팔짜리가 조용히 나갑니다** (커버리지 67.9 → 58.4%).
  → `train.stage2_arms_on_disk()` 를 만들고 `export_release` 가 **자동으로**
  같이 담습니다. 노트북 셀은 `git pull` 로 안 바뀌므로(규칙 3) 호출부가 아니라
  **함수 안에서** 챙깁니다.
  ⚠️ 일반화: **내보내는 쪽과 읽는 쪽이 같은 규칙을 쓰는지 보세요.**
  한쪽이 "폴더를 전부 훑는다" 면 다른 쪽도 "전부 담는다" 여야 합니다
* ★★ **커버리지는 작은 하락을 크게 증폭합니다 — 6배까지 봤습니다.**
  사용자 네모로 바꿨을 때(STEP 39, VL01 n=3,064):
  **정확도 67.6 → 61.7%** (상대 −8.6%) · **확신도 변별력 AUROC 0.7192 → 0.6484**
  (상대 −9.8%) 인데 **커버리지는 50.3 → 22.4% (상대 −55.5%)** 입니다.
  어느 한쪽 탓이 아닙니다 — **둘이 각각 9%씩 빠지는데 커버리지는 55% 무너집니다.**
  이유: 커버리지는 *"누적 오답률 20% 를 안 넘는 지점까지"* 라는 **문턱 지표**라,
  기준 정확도(67.6% = 오답 32.4%)가 목표(20%)에 가까울수록 **가파른 구간**에
  있습니다. 정확도와 순위 품질이 조금만 나빠져도 잘리는 지점이 크게 밀립니다.
  ⚠️ 일반화: **커버리지는 정직하지만(하한선 0%) 취약합니다.** 좋아질 때도
  나빠질 때도 과장돼 보이므로, **정확도와 확신도 변별력을 같이 찍으세요** —
  그래야 '덜 맞히는 것' 과 '틀렸는데 확신하는 것' 과 '증폭' 을 가릅니다
* ★★ **이미지를 여는 코드에는 닫는 코드를 같이 쓰세요 — 학습을 세 번 죽였습니다.**
  `PIL` 로 1920×1080 원본을 장마다 열고 `full.close()` 를 안 했습니다. 1,800장 ×
  여러 패스가 쌓여 **시스템 메모리가 터졌고** 학습이 통째로 죽었습니다.
  고친 뒤에도 두 번 더 죽었습니다 — 15.9GB 시스템에서 **파인튜닝 여러 팔 ×
  평가 세 조건**은 무리였습니다.
  → **학습 전에 `팔 수 × 평가 조건 수` 로 메모리를 먼저 재세요.** GPU 메모리만
  보면 안 됩니다 (GPU 는 3GB 쓰는데 시스템 RAM 이 터졌습니다)
* ★★★ **대신 재는 값(proxy)이 이미 포화돼 있는지 계산해 보세요 — 몇 달 헛짚었습니다.**
  `m2.5` 창 = 네모 긴 변 × 2.5 인데, 사진이 1080px 이면 네모가 화면의 **47% 만
  넘어도 창이 사진을 통째로 덮습니다.** 실측 사용자 네모는 47.2~70.6% 라
  **40장 전부(100%)가 포화**입니다 — **2단계는 이미 사진 전체를 보고 있었습니다.**
  ★ **모델 출력으로도 확인했습니다 (STEP 40)**: 배포 모델의 macro-F1 이
  `사용자 네모` **0.3720** / `네모 없음` **0.3720** 으로 **소수점까지 같습니다.**
  계산이 맞았다는 걸 실측이 확인해 줬습니다.
  그러니 *"네모를 잘 받아야 한다"* 는 전제가 실사용에서는 **이미 무의미**했고,
  STEP 36 의 "사용자 네모에서 −22~24%" 도 사실은 **"크롭이 안 걸렸을 때의 하락"**
  이었습니다. 앱 밴드(`ALLOW 0.28~0.68`)도 포화점 0.47 을 한참 넘게 허용합니다.
  → 이 계산은 **모델도 GPU 도 필요 없었습니다.** `crop_window()` 를 몇 번 부르면
  나옵니다. 사람 40장을 두 번 그리게 하고 파인튜닝을 세 번 돌린 **뒤에야** 했습니다.
  ⚠️ 일반화: **비싼 측정 전에 '이 입력이 실제로 쓰이기는 하나' 를 산수로 확인하세요.**
  파이프라인에 클리핑·클램프가 있으면 상류 변화가 **하류에 아예 안 닿을 수 있습니다**
* ★★★ **앱이 검사하는 것과 모델이 필요한 것이 다릅니다 — 둘 다 옳은데 대상이 틀렸습니다.**
  앱 가이드 밴드는 `네모 ÷ 화면`(ALLOW 0.28~0.68)을 봅니다. 모델이 필요한 건
  `네모 ÷ 병변` 입니다. 실측(STEP 36): 사람이 그린 네모 47~70% 는 **거의 다 밴드
  안**인데 실제로는 **병변의 2.80배**입니다. 둘이 같아지려면 *"병변이 화면의
  45%쯤 되게 찍었다"* 가 필요한데 실측 병변은 화면의 **5.6~59.6%** 였습니다.
  **앱은 병변을 못 보니 이 오차를 검사할 수 없습니다.**
  ⚠️ **서버도 같은 것을 봅니다** (밴드가 사본) — 둘이 일치하니 아무도 못 봅니다.
  → 그래서 *"기본값 44% 를 줄이자"* 는 **취소했습니다.** 어떤 고정값도
  5.6~59.6% 를 못 덮습니다. 남은 길은 **자른 결과를 사람에게 보여주기** 이거나
  **1단계가 본 이상 부위로 서버가 되묻기** 입니다.
  ⚠️ 일반화: **대신 재는 값(proxy)이 진짜와 언제 갈라지는지 적어 두세요.**
  두 곳이 일치한다는 건 대상이 맞다는 뜻이 아닙니다
* ★ **사람은 병변 크기에 네모를 안 맞춥니다 — 고정된 습관입니다.**
  실제 병변 긴 변은 화면의 5.6~59.6%(중앙값 21.5%)인데 사람이 그린 건
  47.2~70.6%(중앙값 58.7%)이고 **상관이 −0.05** 입니다. 자극이 완전히 다른 두
  측정에서 **2.82배 / 2.80배** 로 같은 값이 나왔고, 건너뜀은 12 → 1 로 줄었는데
  크기 오차는 그대로였습니다 — **안 보여서가 아닙니다.**
  → `m2.5`(크기를 씀)와 `f320`(중심만) 둘 다 사용자 네모에서 **20% 넘게**
  무너집니다 (−23.7% / −22.4%, 차이 +0.021 로 문턱 0.03 미달 → **f320 전환 기각**).
  **크롭 선택이 지렛대가 아니라 네모 자체가 지렛대입니다**
* ★ **자극을 정답으로 만들어 놓고 그 정답을 재려 했습니다 — 30분을 버렸습니다.**
  `tools/box_error.py` 는 *"사람이 그린 네모가 정답 bbox 와 얼마나 다른가"* 를
  재는데, **원본 전체 사진**이 있어야 합니다. 그런데 원본은 크롭 후 지워지고
  (`data/raw` 0장) 도구는 **경고 없이 `m2.5` 크롭으로 물러섰습니다.**
  `m2.5` 는 **정답 bbox 를 가운데 놓고** 2.5배로 자른 것이라:
  ① 중심 오차 — 가운데 아무 네모나 그려도 "정확" 이 됩니다. **자극이 정답으로
  만들어졌으므로 재려는 것과 같은 변수**입니다 (STEP 24 `occ` 사건과 같은 모양).
  ② 화질 — 40장 중 **11장이 256px 미만**(최소 105px)이라 화면에 늘리면 뭉개집니다.
  실제로 *"뭐가뭔지 모르겠다"* 로 12장을 건너뛰었고, 나온 숫자(크기 1.00배 ·
  위치 46.4%)는 **전부 못 씁니다.**
  → 이제 `tag != "full"` 이면 **멈추고** 왜 안 되는지 적습니다.
  ⚠️ 일반화: **자극이 정답에서 파생됐는지 보세요.** 그리고 **비싼 사람 작업 앞에는
  더 강한 검사를 두세요** — 30분을 쓰고 나서야 알았습니다
* ★ **평균이 통과하는 관문은 한 클래스가 나빠도 안 보입니다 — 같은 실수를 축만
  바꿔 다시 했습니다.** STEP 30 에서 *"하향만 세면 과잉 쪽으로 도망친다"* 를
  배우고 `over_triage` 를 같이 찍게 했는데, STEP 35 에서 **전체 하향 3.7% 가
  관문(5%)을 통과하는 동안 말한 A5 의 43.8% 가 하향**인 걸 발견했습니다.
  표면 변화(전체의 51%)가 평균을 떠받쳐서 A5 가 묻힙니다.
  → `granularity_report` 가 `under_by_class` · `under_worst` 를 같이 찍습니다.
  ⚠️ **문턱은 일부러 안 겁니다** — 닻이 없습니다. 전체 관문의 5% 는
  `STAGE1_MISS_RATE` 에서 나왔는데 클래스별로는 **분모가 다릅니다** (1단계의
  'A5 를 6% 놓친다' 는 **A5 전체** 기준, 43.8% 는 **말한 A5** 기준).
  결과를 보고 문턱을 고르면 성공담이 되므로(규칙 2) `over_triage` 와 같은
  처방을 씁니다 — **관문으로 안 쓰되 찍습니다.**
  ⚠️ 일반화: **평균 하나로 관문을 세우면 분모가 큰 클래스가 평균을 삽니다.
  쪼개서 같이 찍으세요.**
* ★ **비율이 나빠졌다고 모델을 탓하기 전에 분모를 맞춰 보세요 — 두 번 당했습니다.**
  STEP 34 에서 A6 경보 정밀도가 반토막 나 보인 게 **유병률** 때문이었는데,
  STEP 35 에서 똑같이 *"앙상블이 A5 를 더 위험하게 만든다"* 로 읽을 뻔했습니다
  (릴리스 37.4% vs 앙상블 43.8%). **같은 장수(20,121)만 말하게 자르니 38.0%** —
  차이 +0.6%p 로 **선택 효과**였습니다. 43.8% 는 6,271장을 더 말하기로 한 대가고
  늘어난 몫에 오답이 몰린 것입니다.
  → `experiments.ARM_SAME_COVERAGE_TOL`, `tools/a5_downgrade.py` 의 판 A.
  ⚠️ 일반화: **커버리지(또는 유병률)가 다른 두 설정의 비율을 그대로 비교하지
  마세요.** 먼저 같은 장수로 맞추고, 그래도 남는 차이만 모델의 몫입니다
* ★★★ **요약이 조건을 잃어버립니다 — 그리고 그 요약으로 다음 결정을 합니다.**
  STEP 40 의 결론은 정확히는 *"가이드 프레임은 **2단계** macro-F1 을 안 바꾼다"*
  였고, 그 줄 옆에 *"1단계 `f320` 은 중심을 쓰므로 영향 있음 — 따로 봐야 합니다"*
  라고 **적어두기까지 했습니다.** 그런데 몇 STEP 지나 그것이
  *"가이드 프레임은 아무 일도 안 합니다"* 로 줄어들었고, 자연스럽게
  *"그럼 틀을 없애자"* 가 나왔습니다. 재보니 **1단계 recall −0.12** 로
  허용치의 12배였습니다 (STEP 43).
  ⚠️ *"실제는 더 나쁩니다"* 를 각주로 적고 안 재서 두 달을 버린 STEP 11·27 과
  **같은 모양**입니다 — 이번엔 각주가 *"조건"* 이었을 뿐입니다.
  → **결론 문장에 조건이 붙어 있으면, 그 조건을 문장에서 떼지 마세요.**
  제목이 `가이드 프레임은 아무 일도 안 합니다` 면 다음 사람은 조건을 못 봅니다.
  그리고 **조건이 붙은 결론에는 "그 조건 밖은 아직 안 쟀다" 를 같이 적으세요** —
  안 적으면 안 잰 것이 "재봤더니 괜찮더라" 로 읽힙니다.
* **증강 병목**: torchvision(PIL) 이 384px 한 장에 22.6ms 였습니다 →
  albumentations 로 **7.3ms** (3.1배). 단 GPU 가 112 img/s 상한이라
  실제 단축은 1.24배입니다

## 증강 규칙 (멘토 피드백)

* **학습 중 실시간 랜덤**만 씁니다. 미리 만들어 저장하지 않습니다
  (`SkinDataset.__getitem__` 에서 매 호출 적용 — 테스트로 못 박아 뒀습니다)
* **검증 변환은 결정론적**으로 유지합니다. 여기를 건드리면 기준선 비교와
  로짓 캐시가 무효가 됩니다 — 그래서 albumentations 는 **학습 경로에만** 넣었습니다
* 배율 범위는 **넓히는 쪽만 시도하지 않습니다.** 좁히는 쪽(`narrow`)도 후보입니다
