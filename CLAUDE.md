# CLAUDE.md — 작업 규칙 + 현재 상태 (사람/AI 공용 온보딩)

반려견 피부 병변 **스크리닝 보조** 모델. 진단이 아니라
"이건 좀 의심되니 병원 가보세요" 까지가 목적입니다.

---

## 스택

* **학습**: PyTorch 2.x + timm (백본), AMP + EMA + cosine warmup
* **패키지**: uv (Python ≥3.10) — 프로젝트 1개 / lock 1개. **pip 안 씁니다**
* **데이터**: AI Hub 561 「반려동물 피부 질환」 — 반려견 + 일반카메라만
* **매니페스트**: parquet (pandas + pyarrow) — DB 없음, 파일 하나가 진실
* **실행 분리**: 전처리는 **한국 PC**(AI Hub 가 해외 IP 차단), 학습은 **Kaggle/Colab**
* **프론트엔드 없음** — 아직 모델 단계입니다

```bash
uv sync                 # 전처리용 (torch 없이, ~30초)
uv sync --extra train   # 학습까지 (torch/torchvision/timm/grad-cam)
uv run python prepare_local.py --chunk VL01
```

---

## 작업 규칙

1. **숫자는 실측만.** 추정치를 문서에 쓰지 않습니다. 돌려서 나온 값만
   `docs/results/` 에 남기고, 이후 모든 실험은 그 값과 비교합니다.
   (로드맵의 "macro-F1 0.70~0.80" 은 데이터를 보기 전 추정이었고 **틀렸습니다**.
   실측 기준으로 0.55~0.65 로 내려 잡았습니다.)
2. **판정 기준은 실험 전에 못 박습니다.** 결과를 보고 기준을 고르면
   무슨 숫자가 나와도 성공담이 됩니다.
3. **바뀔 수 있는 로직은 `src/` 에.** 노트북 셀은 `git pull` 로 갱신되지
   않습니다 — 한 번 Colab/Kaggle 에 올린 `.ipynb` 는 다시 Import 하기 전까지
   그대로입니다. 그래서 게이트(`gates.py`)와 비교 실험(`experiments.py`)을
   노트북이 아니라 `src/` 에 둡니다.
4. **막힌 원인을 고치기 전에 다음 단계로 안 갑니다.**
   지금 04(백본 비교)가 보류 중인 이유입니다 — 1단계가 holdout 에서
   무너지는데 2단계 백본 6개를 비교하면 시간만 씁니다.
5. **커밋/푸시는 지시가 있을 때.** 데이터·가중치·키는 절대 커밋하지 않습니다.

---

## 용어 표기 규칙

한국어로 쓰되 **학술 용어는 영어를 병기**합니다. 논문·문서·회의에서 통하는 건
영어 쪽이라, 한국어만 쓰면 검색도 안 되고 팀 밖에서 말이 안 통합니다.

| 우리 표기 | 영어 | 주의 |
|---|---|---|
| 데이터 누수 | **data leakage** | |
| 배율 교란 / 배율 강건성 | **scale perturbation / scale robustness** | ⚠️ multi-scale 아님 |
| 위치 교란 | **shift perturbation** | |
| 하한선 | **shortcut baseline** | 사진 없이 메타데이터만으로 맞히기 |
| 파인튜닝 | **fine-tuning** | 우리는 처음부터 이것 (전이학습은 우산 개념) |
| 백본 / 헤드 | **backbone / head** | |
| 정성평가 | **qualitative evaluation** | |
| 확률 보정 | **calibration** | |
| 거절 | **abstention** | "판단 어려움" 으로 물러서기 |
| 미탐 / 오탐 | **false negative / false positive** | |
| 촬영 가이드 | **capture guideline** | |
| 개체 단위 분할 | **group-aware split** | groups = animal_id |

⚠️ **`multi-scale` 과 `scale perturbation` 은 반대 방향입니다.**
전자는 여러 배율로 **학습**시키는 기법(= `zoom_both`, 우리는 실패),
후자는 학습을 마친 모델을 배율 바꿔가며 **시험**하는 검사입니다.


---

## 폴더 구조

```
deeplearning_test/
├── pyproject.toml / uv.lock   의존성. 기본=전처리용(torch 없음) / --extra train=학습
├── CLAUDE.md                  이 파일 (작업 규칙 + 현재 상태)
├── README.md                  프로젝트 소개 + 빠른 시작 + 설계 근거 8가지
│
├── prepare_local.py           ★ 한국 PC 전용 CLI: 다운로드 → 정제 → 크롭 → zip
│                                --chunk VL01 / --finalize / --package
├── diagnose.py                원본 JSON 스키마 확인용 일회성 도구 (스키마 3곳이 틀렸었음)
│
├── notebooks/                 ★ 클라우드(Colab/Kaggle) 전용. 첫 셀이 git clone 까지 합니다
│   ├── 03_학습_베이스라인       1·2단계 학습 + 견고성 검사 (384px)
│   ├── 03b_증강_빠른스윕         증강 7종 스윕 ✅ 끝 (결론: 효과 없음)
│   ├── 03c_크롭비교              m1.5 vs m2.5 ✅ 끝 (결론: m2.5 채택)
│   ├── 05_평가_보정_GradCAM     보정·거절·CAM·holdout ✅ 끝 (촬영 가이드 확정)
│   ├── 03d_1단계_고치기          1단계 백본×증강 2×2 ✅ 끝 (effnetv2_s+photometric)
│   └── 04_학습_최신모델_비교     ★ **2단계** 백본 비교 — 아직 resnet50 (05 다음)
│   README.md                  ★ 어떤 노트북을 언제·무엇을 붙여서 돌리나
│                              ⚠️ 00~02 가 없는 이유: 데이터 확보/전처리는
│                                 노트북이 아니라 prepare_local.py 가 합니다
│
├── src/                       ★ 로컬과 클라우드가 **공유하는** 실제 로직 (8.5k줄)
│   ├── env.py                 Colab/Kaggle/로컬 자동 감지, 경로·시크릿·GPU 확인
│   ├── config.py              모든 하이퍼파라미터 + 증강/파인튜닝 프리셋 한 곳
│   │  ── 데이터 만들기 (로컬에서만 돌아감) ──
│   ├── aihub.py               aihubshell 래퍼 — 반려견+일반카메라 filekey 자동 선별
│   ├── scan.py                폴더/JSON 스키마 **추론** (가정하지 않음)
│   ├── labels.py              JSON → manifest.parquet
│   ├── dedup.py               phash 중복 제거. 라벨 충돌 그룹은 전부 버림
│   ├── split.py               ★ 개체 단위 그룹 분할. verify() 가 누수 시 에러
│   ├── crop.py                ROI 크롭 (m1.5 / m2.5 / full / f320) + 감사
│   │  ── 학습하기 ──
│   ├── stages.py              ★ 2단계 구조 — **하나의 분할을 공유하는** 두 뷰
│   ├── data.py                Dataset / 증강(albumentations) / DataLoader
│   ├── models.py              timm 백본 팩토리 (없는 모델명은 대체 + 경고)
│   ├── train.py               학습 루프 + **세션 끊김 이어받기** + 로짓 캐시
│   │  ── 검사하기 ──
│   ├── evaluate.py            지표·혼동행렬·부트스트랩 CI
│   ├── robust.py              배율/위치/**화질** 교란 + 촬영 가이드 도출
│   ├── gates.py               품질 정지선. STOP(깨짐)에서만 멈추고 WANT 미달은 경고
│   ├── experiments.py         비교 실험 판정 규칙 + 시작 전 시간 추정 (규칙 3)
│   ├── calibrate.py           온도 보정 · ECE · coverage-risk
│   ├── explain.py             Grad-CAM 정렬도 (배경 보는지 **수치로**)
│   └── infer.py               단일 사진 추론 + 안전 문구
│
├── tests/                     200+ 검사. `uv run python tests/<파일>`
│   ├── test_notebook_kaggle_e2e.py  ★ Kaggle 환경을 통째로 흉내 내 03 전 셀 실행
│   ├── test_kaggle_layout.py        데이터셋 경로·심볼릭링크·분할 업로드
│   ├── test_resume.py               세션 끊김 이어받기 + 로짓 캐시 지문
│   ├── test_crop_geometry.py        크롭 창 계산
│   ├── test_robust.py               교란 뷰
│   ├── test_stages.py               2단계 분할 공유
│   ├── test_windows_encoding.py     cp949 회귀 (같은 버그로 3번 막혔음)
│   ├── test_capture_guide.py        촬영 가이드 밴드 계산
│   └── test_notebook_names.py       노트북 셀의 이름·인자 오타 (실행 없이)
│
├── docs/
│   ├── 00_로드맵.md            전체 STEP 과 현재 위치
│   ├── results/               ★ 실측 기록 — **숫자는 전부 여기**. 추정치 금지
│   ├── daily/                 ★ 데일리 로그 — **과정의 기록** (results 는 숫자의 기록)
│   │                            날짜별 한 편 + 「오늘의 스킬」 표 + 누적 스킬 정리
│   ├── data/DATASET_CARD.md   실물 스키마 (추론이 틀렸던 3곳 포함)
│   ├── basics/                딥러닝 기초 10편 (ML 경험자 전제, DL 은 처음부터)
│   └── cautions/              주의사항 9편 (누수·라이선스·2단계 설계·세션 끊김 …)
│
└── data/                      ★ 전부 git 미추적
    ├── raw/                   AI Hub 원본 (크롭 후 삭제)
    └── work/{crops,manifests} 크롭 + manifest_final.parquet → zip 으로 클라우드에
```

### 커밋하면 안 되는 것

AI Hub 데이터는 **재배포 금지**입니다. `.gitignore` 가 막고 있습니다:
`/data/`, `*.jpg`, `*.parquet`, `*.pt`, `.env`, `*apikey*`

* 클라우드 업로드는 **반드시 Private** (Kaggle `datasets create` 기본값이 private —
  `--public` 을 붙이지 마세요)
* API 키는 코드·노트북 셀에 붙여넣지 말고 환경변수 `AIHUB_API_KEY` 또는
  Colab/Kaggle Secrets 로만. `env.secret()` 이 읽고 로그에서 마스킹합니다
* 해외 IP 차단을 VPN 으로 우회하지 않습니다

---

## 현재 상태

**2단계 구조**: ① 정상/이상 → ② 병변 6종 (A1 구진·플라크 / A2 비듬·각질 /
A3 태선화·과다색소침착 / A4 농포·여드름 / A5 미란·궤양 / A6 결절·종괴)

데이터: VL01 45,885행 / 개체 2,476마리 / ResNet50 / **384px** / fold 0
크롭: **m2.5 확정** (2026-08-22, STEP 4C)

**★ holdout 을 처음 열었습니다** (STEP 5, 2026-08-22). 아래 표는 val 기준이고,
**holdout 에서 1단계가 무너집니다** — 그게 지금 가장 큰 문제입니다:

| | val | **holdout** | |
|---|---:|---:|---|
| 1단계 AUROC | 0.8143 | **0.7412** | ❌ 목표 0.80 미달 |
| 2단계 macro-F1 | 0.5313 | **0.5582** | ✅ 오히려 상승 |
| **스크리닝 recall** | 0.950 | **0.9303** | ❌ 목표 0.95 미달 |
| 헛알림 | 35% | **67.6%** | |
| 최종 7종 macro-F1 | — | **0.4432** | ← 보고할 숫자 |

**2단계는 개체가 바뀌어도 좋아지는데 1단계만 무너집니다.**
전체 기록: [`docs/results/STEP5_평가_보정_촬영가이드_실측.md`](docs/results/STEP5_평가_보정_촬영가이드_실측.md)

**전부 한 실행에서 잰 값입니다** (2026-08-22, 2시간 18분).
전체 기록: [`docs/results/STEP4D_m2.5_재기준선_실측.md`](docs/results/STEP4D_m2.5_재기준선_실측.md)

| | 지표 | 현재 | 이전(m1.5) | 목표 | |
|---|---|---:|---:|---:|---|
| 1단계 | 정상·이상 AUROC | 0.8155 | 0.8192 | 0.80 | ✅ |
| 2단계 | 병변 6종 macro-F1 | 0.5313 | 0.5697 | 0.55~0.65 | ⚠️ 하단 미달 |
| 2단계 | A6(결절·종괴) recall | 0.340 | 0.405 | 0.50 | ❌ ⚠️ 측정 불가 |
| 파이프라인 | 스크리닝 recall | 0.9501 | 0.9501 | ≥0.95 | ✅ |
| 파이프라인 | 헛알림 비율 | 51.4% | 52.9% | — | 감수 |
| 2단계 | **배율 교란 하락** | **16.0%** | 25.9% | <15% | ◐ 구분 불가 |
| 2단계 | **위치 교란 하락** | **16.2%** | 20.6% | <15% | ◐ 구분 불가 |

★ **크롭 하나로 두 축이 같이 잡혔습니다** — 배율 −9.9%p, 위치 −4.4%p.
그리고 **16.0% 와 목표 15% 는 잡음 안이라 구분되지 않습니다.**
(잡음 폭은 STEP 5 에서 **±5%p** 로 상향했습니다 — 같은 모델을 n=2000/3000 으로
재서 21.6%/16.8% 가 나왔습니다. 이전의 ±3%p 는 낙관적이었습니다)
"미달" 도 "달성" 도 과장이라 ◐ 로 적습니다 → 배율 축은 여기서 닫고 촬영 가이드로.

⚠️ **macro-F1 은 조금 내려갔습니다.** m1.5 두 번(0.5697/0.5536) vs m2.5 두 번
(0.5395/0.5313) — 네 번 다 m1.5 가 위라, "잡음" 이 아니라 **작지만 실재하는 대가**
(−0.027 쯤)로 읽는 게 맞습니다. 그래도 견고성 이득이 잡음의 3배라 m2.5 유지입니다.

### ⚠️ 1단계는 `full`, 2단계는 `m2.5` — 이게 정상입니다

**1단계는 2단계 크롭을 따라오지 않습니다.** (한 번 반대로 적었다가 고친 부분입니다)

1단계에서 ROI 크롭을 쓰면 **크롭 창 크기가 정답을 흘립니다** — 정상 bbox 면적
중앙값이 0.71%, 병변이 1.25% 라 사진을 안 봐도 반쯤 맞힙니다 (지름길 하한선
AUROC 0.6042). 게다가 그 신호는 **배포에 존재하지 않습니다** — 보호자 사진에는
bbox 가 없으니까요. 검증 점수만 올리고 실사용에서 사라지는 최악의 지름길입니다.

우선순위는 `f320`(고정 픽셀) > `full` > ROI 크롭이고, f320 이 없어서 지금은 `full` 입니다.
규칙은 `crop.choose_stage1_tag()` 에 있습니다 (노트북 셀이 아니라 — 규칙 3).

✅ 03 재실행 완료 (2026-08-22). 위 표가 그 결과입니다.

⚠️ **Kaggle 입력에 `dogskin-full` 을 반드시 붙이세요.** 안 붙이면 1단계가 쓸 크롭이
없어서 멈춥니다 (실제로 한 번 그랬습니다 — 이제 6번 셀이 첫 1분에 경고합니다).

✅ 05 완료 (2026-08-22, 24.6분).

### ✅ STEP 6 — 1단계 원인 찾음 (2026-08-23)

**화질 지름길이 실재했습니다.** 사진을 흐리게 하면 기존 1단계가 반토막 났습니다
(macro-F1 0.7430 → 0.3316, balanced-acc 0.5000 = 동전 던지기).
정상 사진 선명도 중앙값이 50, 병변이 274 라 **"흐리면 정상"** 을 배운 것입니다.

2×2 실측 (서브셋 55% · 12에폭):

| 백본 | 증강 | val AUROC | 흐림 하락 |
|---|---|---:|---:|
| effnetv2_s | default | **0.8359** | 47.6% |
| **effnetv2_s** | **photometric** | 0.8284 | **14.0%** ← 채택 |
| resnet50 | default | 0.8116 | 55.4% |
| resnet50 | photometric | 0.8145 | 16.9% |

**두 축 다 사전 등록 기준을 넘었습니다** — 증강 −38.5%p, 백본 +0.0244.

⚠️ **val AUROC 최고(effnetv2_s/default 0.8359)를 안 골랐습니다.** 흐림 하락 47.6%
로 지름길에 기대고 있고, AUROC 차이 0.0075 는 잡음 안입니다. **val 로 고르다
holdout 에서 무너진 게 STEP 5 의 실패**라 같은 실수를 반복하지 않습니다.

전체 기록: [`docs/results/STEP6_1단계_2x2_실측.md`](docs/results/STEP6_1단계_2x2_실측.md)

### ✅ STEP 7 — 1단계 교체 후 재기준선 (2026-08-23)

풀 데이터로 학습 (1단계 18에폭 / 2단계 25에폭 / 2시간 40분). **val 기준**:

| | STEP 4D | **STEP 7** | |
|---|---:|---:|---|
| 1단계 AUROC | 0.8155 | **0.8334** | +0.018 (잡음 밖) ✅ |
| 1단계 precision | 0.650 | **0.664** | |
| 2단계 macro-F1 | 0.5313 | 0.5457 | 잡음 (2단계는 안 바꿈) |
| 헛알림 | 51.4% | **49.5%** | 같은 recall 에서 −1.9%p ✅ |
| 스크리닝 recall | 0.9501 | 0.9501 | 설계값 |

**같은 recall 을 유지하면서 헛알림만 줄었습니다** — 1단계가 실제로 더 잘 가릅니다.
전체 기록: [`docs/results/STEP7_1단계교체_재기준선_실측.md`](docs/results/STEP7_1단계교체_재기준선_실측.md)

### ⚠️ 03 은 holdout 을 열지 않습니다 — 05 가 엽니다

한 번 반대로 적었습니다. **03 = val 까지, 05 = holdout.**
그래서 "1단계가 정말 고쳐졌나" 의 답은 **아직 안 나왔습니다.**

→ **다음: 05 를 돌려 holdout 을 엽니다.** 확인할 것:
1단계 holdout AUROC 0.7412 → ≥0.80 / 스크리닝 recall 0.9303 → ≥0.95 /
최종 7종 macro-F1 0.4432 → 상승.

⚠️ 05 입력에 이번 03 의 `release` 폴더(336MB)를 Private 데이터셋으로 만들어 붙이세요.
`READ_ME_FIRST.txt` 에 `1단계 AUROC 0.8334 / 2단계 macro-F1 0.5457` 이 적혀 있습니다.

### ⚠️ 교란 검사 잡음 폭을 ±8%p 로 다시 올립니다

**2단계는 설정을 하나도 안 바꿨는데** 배율 하락이 실행마다 이렇게 나왔습니다:

| 실행 | 표본 | 배율 하락 |
|---|---:|---:|
| STEP 4D | 2,000 | 16.0% |
| STEP 5 | 2,000 | 21.6% |
| STEP 5 | 3,000 | 16.8% |
| STEP 7 | 2,000 | **23.8%** |

폭이 7.8%p. ±5%p 도 낙관적이었습니다. 이번엔 **1.41x 가 1x 보다 높게** 나와서
"최고점이 1x" 라는 것조차 흔들립니다. 촬영 가이드 밴드 경계를 소수점까지
믿으면 안 됩니다 (형태 — 0.5x 에서 확 나빠짐 — 는 네 번 다 같습니다).



⚠️ Kaggle 에서 돌릴 때는 **[Save Version] → Save & Run All (Commit)** 로 돌리고,
05 에서 **Add Input → Notebook Output** 으로 그 출력을 붙이세요.
`train.import_checkpoints()` 가 경로를 알아서 찾습니다.

### 배율 견고성 — 해상도로 절반쯤 잡았습니다

멀리서 찍은 사진(축소)에서 무너지는 문제입니다. 증강으로 두 번 실패한 뒤
**입력 해상도 224 → 384** 로 크게 줄였습니다.

| | 224px | 384px | |
|---|---:|---:|---|
| 1단계 하락 | 17.2% | **9.1%** | ✅ 목표 달성 |
| 2단계 하락 | 29.5% | **25.9%** | ❌ 남음 |

점수도 같이 올랐습니다 (AUROC 0.8103→0.8192, macro-F1 0.5466→0.5697).
견고성을 얻으려고 정확도를 판 게 아닙니다.

**★ 최악 조건은 단계마다 다릅니다.** (한 번 잘못 적었다가 고친 부분입니다)

| 조건 | 1단계 | 2단계 |
|---|---:|---:|
| 0.5x 축소 | 0.6952 | **0.4066** ← 2단계 최악 |
| 1x 원본 | 0.7474 | 0.5489 |
| 2x 확대 | **0.6790** ← 1단계 최악 | 0.4632 |

1단계는 확대에서, **2단계는 여전히 축소에서** 무너집니다.
1단계 결과를 2단계에 옮겨 적으면 안 됩니다.

**해상도 카드는 거의 소진됐습니다** — 크롭 짧은 변 중앙값이 242px 이고
**384 미만을 74.6% 확대해서 쓰는 중**입니다. 512 로 올려도 없는 디테일은 안 생깁니다.

**증강 축은 닫혔습니다** (2026-08-21, 7종 한 실행 비교). 6종이 default 와 구분되지
않았고, 유일하게 잡음을 넘은 신호는 `zoom_both` 가 **+4.0%p 악화**시킨 것이었습니다.
`photometric` 은 macro-F1 을 0.03 깎았습니다 — 흐림이 지운 건 화질 지름길이 아니라
병변 질감이었습니다. 전체 기록:
[`docs/results/STEP4B_증강스윕_실측.md`](docs/results/STEP4B_증강스윕_실측.md)

→ **남은 배율 문제는 배포 설계로 넘깁니다.** 배율별 점수표에서 1x 가 최고이므로,
그 구도로 찍도록 유도하면 최악 조건 자체가 안 생깁니다.
`robust.usable_range()` 가 그 구간을 학습 없이 뽑습니다 (노트북 05 의 5-b).

### ✅ 촬영 가이드 확정 (STEP 5 실측)

```
권장 : 병변이 화면 가로의 34% ~ 56%   (배율 0.85x~1.4x · 하락 5% 이내)
허용 : 28% ~ 68%                      (배율 0.7x~1.7x · 하락 10% 이내)
위치 : 병변이 화면 중앙에서 15% 이내
```

카메라 UI 에 가이드 프레임을 띄우고, 벗어나면 셔터를 막거나 "조금 더 가까이" 를
띄웁니다. 이 구간 밖 사진은 추론하지 말고 다시 찍게 합니다.

⚠️ **크롭이 m2.5 로 바뀌면서 안내 문구도 바뀝니다.** 1x 기준 병변의 화면 점유율이
m1.5 에서 약 2/3(67%) 였는데 **m2.5 에서는 40%** 입니다. 즉 **더 넓게 찍으라**고
안내해야 합니다 — 움직이는 강아지를 상대로 오히려 쉬운 요구입니다.
`usable_range(crop_margin=...)` 인자가 이 변환을 합니다 (05 가 크롭에서 자동 유도).

### 크롭 창 크기 — ✅ m2.5 채택 (마지막 전처리 축)

크롭 짧은 변 중앙값이 242px 라 **74.6% 를 확대해서 쓰고 있었고**, A1 이 가장 심했습니다
(원본 박스 99px → m1.5 에서 **2.6배** 확대 / m2.5 에서 1.6배).

풀 데이터·같은 실행에서 비교한 결과:

| 크롭 | macro-F1 | 배율 하락 | A1 recall |
|---|---:|---:|---:|
| m1.5 | 0.5536 | 23.2% | 0.560 |
| **m2.5** | 0.5395 | **18.8%** | **0.622** |
| | −0.014 (잡음) | **−4.4%p** | **+0.063** |

**★ 축소는 증강이 아니라 크롭으로 잡혔습니다.** 같은 목표를 두 방법으로 시도했는데
방향이 정반대였습니다:

| 방법 | 하는 일 | 결과 |
|---|---|---:|
| `zoom_both` 증강 | 자른 이미지를 **줄이고 여백으로 채움** (정보를 버림) | +4.0%p 악화 |
| **m2.5 크롭** | **더 넓은 실제 영역**을 자름 (정보를 더함) | **−4.4%p 개선** |

→ "작게 보이는 사진에 강해지려면, **작게 만들지 말고 넓게 찍은 걸 보여줘라.**"

macro-F1 −0.014 는 **잡음입니다** — 같은 두 모델을 교란검사 1x 로 다시 재면 부호가
뒤집힙니다 (m1.5 0.5230 vs m2.5 0.5257). 잡음 밖의 이득 하나, 잡음 안의 손해 하나.

전체 기록: [`docs/results/STEP4C_크롭비교_실측.md`](docs/results/STEP4C_크롭비교_실측.md)

### ⚠️ 열어둔 위험: 1단계는 **먼 사진**으로 배웠습니다

`full` 은 1920×1080 의 중앙 정사각이라, 1단계는 "강아지 전신이 찍힌 사진"으로
학습했습니다 (병변은 22×22px 수준). 그런데 배포에서 보호자는 **촬영 가이드대로
병변에 다가가서** 찍습니다 — 1단계 입장에서는 한 번도 못 본 구도입니다.
`infer.Pipeline` 은 같은 사진을 두 엔진에 그대로 넣으므로, 2단계는 맞고 1단계만
분포가 어긋납니다.

지금 숫자(AUROC 0.8192)는 이 어긋남이 **없는** 검증셋에서 나온 것이라 낙관적일 수
있습니다. 05 의 holdout·촬영 가이드에서 확인하고, 필요하면 f320 재크롭으로 갑니다
(고정 픽셀 창은 배율도 일정하고 구도도 사용자 사진에 가깝습니다).

### ⚠️ A6 recall 은 지금 **잴 수가 없습니다**

같은 m1.5·같은 설정인데 실행이 다르면 A6 recall 이 0.405 → 0.344 로 움직입니다.
검증 표본이 ~256장뿐이라 2σ 가 **±0.085** — 목표 개선폭(+0.095)과 거의 같습니다.

→ **A6 을 고치기 전에 재는 방법을 먼저 고쳐야 합니다** (5-fold 평균 / holdout 합산 /
A6 에도 부트스트랩 CI 병기). 지금 A6 만 보고 설정을 고르면 주사위입니다.

⚠️ **수작업 데이터 큐레이션은 후반에 합니다** (멘토 피드백 5·6번).
클래스 병합·데이터 삭제·라벨 교정은 자동 실험을 다 짜낸 뒤에. 조건이 바뀌면
어떤 데이터가 문제인지도 바뀌는데, 되돌리기는 어렵습니다.

⚠️ **표본 수가 다르면 하락폭을 비교하면 안 됩니다.** 같은 설정을 3,000장/2,000장으로
재서 20.4% / 25.9% 가 나왔습니다. 프리셋 비교는 반드시 같은 실행에서 합니다.

### 결론 난 것 (다시 하지 말 것)

| 질문 | 결론 | 근거 |
|---|---|---|
| 파인튜닝 강도 | 백본 lr ×0.1 → **×0.3**(`moderate`) | macro-F1 +0.065 |
| 크롭 배율 지름길이 있나 | **없음** — CNN 이 크기를 안 씀 (A1 −0.024, A6 −0.125) | 클래스별 하한선 비교 |
| `f320` 재크롭이 필요한가 | **불필요** — 지름길도 없고 배율 견고성에도 도움 안 됨 | 위 두 줄 |
| 증강으로 배율을 잡을 수 있나 | **아니오** — 3회 실패. 384 의 `zoom_both` 는 오히려 악화 | 31.6% / 27.7% / +4.0%p |
| 흐림·노이즈 증강이 도움되나 | **아니오** — macro-F1 −0.03. 질감이 곧 라벨입니다 | `photometric` |
| 증강 폭을 좁히면 나은가 | 0.85 에서 꺾이는 모양은 보이나 **잡음 안** | 17.6→15.9→16.5% |
| 서브셋 하락률을 풀에 대입해도 되나 | **아니오** — 약한 모델은 잃을 것도 적어 덜 떨어집니다 | 서브셋 17.6% vs 풀 25.9% |
| 교란 검사 잡음 폭은 얼마인가 | **±5%p** (±3%p 는 낙관적이었음) | 같은 모델 n=2000 21.6% / n=3000 16.8% |
| 1단계는 일반화되나 | **아니오** — holdout AUROC 0.8143 → 0.7412 | STEP 5 |
| 2단계는 일반화되나 | **예** — holdout 에서 오히려 +0.027 | STEP 5 |
| 거절로 정확도를 올릴 수 있나 | **아니오** — 절반 거절해도 오답 40.9%→29.4% | STEP 5 |
| Grad-CAM 이 배경을 보나 | **아니오** — lift 1.82, 78% 가 우연 이상 | STEP 5 ✅ |
| 1단계에 화질 지름길이 있나 | **예** — 흐리게 하면 0.7430 → 0.3316 (동전 던지기) | STEP 6 |
| photometric 이 1단계에도 나쁜가 | **아니오** — 2단계와 반대. 흐림 하락 −38.5%p, AUROC 는 그대로 | STEP 6 |
| 1단계 백본을 바꾸면 되나 | **예** — effnetv2_s 로 AUROC +0.0244 (서브셋) / +0.018 (풀) | STEP 6·7 |
| 2단계도 effnetv2_s 로 바꿔야 하나 | **모릅니다 — 안 재봤습니다.** 1단계 결과를 옮겨 적으면 안 됩니다 (photometric 이 정반대였음) | 04 에서 |
| 03 이 holdout 을 여나 | **아니오** — 03 은 val 까지. holdout 은 05 | STEP 7 |
| val AUROC 최고를 고르면 되나 | **아니오** — 지름길 모델이 val 에서 높다. 견고성도 같이 볼 것 | STEP 5·6 |
| 해상도를 512 로 더 올릴까 | **아니오** — 크롭이 이미 중앙값 242px | 384 미만 74.6% 확대 중 |
| 해상도를 올리면 되나 | **예** — 224 → 384 로 8~9%p 감소 | 위 표 |
| 크롭 창을 넓히면 되나 | **예** — m1.5 → **m2.5** 로 배율 −9.9%p, 위치 −4.4%p | STEP 4C·4D |
| 위치 교란도 크롭으로 잡히나 | **예** (예상 밖) — 20.6% → 16.2%. 넓게 자르면 병변이 정중앙에만 있지 않음 | STEP 4D |
| m2.5 가 macro-F1 을 깎나 | **조금 깎습니다** — 네 번 재서 m1.5 가 네 번 다 위 (−0.027 쯤) | STEP 4D |
| A6 을 지금 개선할 수 있나 | **아니오** — 잡음 ±0.085 가 목표 개선폭과 같음. 측정부터 | 같은 설정 0.405/0.344 |
| 에폭 12 로 충분한가 | 아니오 — 12에폭 전부 best 갱신. **25**로 | 학습 곡선 |

전체 기록: [`docs/results/STEP4A_베이스라인_실측.md`](docs/results/STEP4A_베이스라인_실측.md)

### 알려진 함정 (전부 실제로 당한 것)

* **노트북 셀은 `git pull` 로 안 바뀝니다.** 첫 셀의 `MY_NOTEBOOK_VERSION` 이
  `config.NOTEBOOK_VERSION` 과 다르면 경고가 뜹니다 → `.ipynb` 를 다시 Import
* **`Path.rglob` 는 심볼릭 링크 하위로 안 들어갑니다** (두 번 당함)
* **부분 업로드**: 크롭이 30%만 올라간 채로 조용히 학습됐습니다 →
  `crop.MIN_CROP_COVERAGE = 0.95` 미만이면 에러
* **두 단계가 서로 다른 백본을 쓸 수 있습니다** — 1단계 effnetv2_s / 2단계 resnet50.
  05 가 "체크포인트는 항상 resnet50" 으로 하드코딩돼 있다가 shape mismatch 로
  죽었습니다 (bn1 24 vs 64). 백본은 **폴더 이름에서** 읽습니다
  (`train.model_key_from_exp`)
* **Kaggle 이미지에도 `/content` 와 `google.colab` 이 있습니다** →
  환경 감지는 Kaggle 신호를 **먼저** 봅니다
* **Kaggle 은 노트북마다 세션이 따로입니다** — 03 이 3시간 걸려 만든 체크포인트는
  05 를 열면 없습니다. 03 을 Commit 으로 돌리고 그 출력을 05 의 입력으로 붙이세요
  (`train.import_previous_run()` 가 가중치 + `stage1_threshold.json` 을 자동 탐색).
  안 붙이면 05 가 시작 직후 죽습니다
* ⚠️ **Kaggle 노트북 하나를 돌려쓰지 마세요.** 03 을 지우고 05 를 붙여넣으면
  05 에 붙일 03 이 **없어집니다** (자기 자신은 입력이 안 됩니다).
  실제로 이렇게 막혔습니다 → `03` / `03b` / `03c` / `05` 를 **각각 별도 노트북**으로.
  이미 덮어썼어도 **예전 버전의 출력은 남아 있습니다** — 그 버전의 Output 탭에서
  **New Dataset (Private)** 으로 만들어 붙이면 됩니다. 재학습 불필요
* **Colab 무료 한도 초과 시 말없이 CPU 런타임을 줍니다** (20~30배 느림) →
  `env.require_gpu()` 가 막습니다
* **증강 병목**: torchvision(PIL) 이 384px 한 장에 22.6ms 였습니다 →
  albumentations 로 **7.3ms** (3.1배). 단 GPU 가 112 img/s 상한이라
  실제 단축은 1.24배입니다

### 증강 규칙 (멘토 피드백)

* **학습 중 실시간 랜덤**만 씁니다. 미리 만들어 저장하지 않습니다
  (`SkinDataset.__getitem__` 에서 매 호출 적용 — 테스트로 못 박아 뒀습니다)
* **검증 변환은 결정론적**으로 유지합니다. 여기를 건드리면 기준선 비교와
  로짓 캐시가 무효가 됩니다 — 그래서 albumentations 는 **학습 경로에만** 넣었습니다
* 배율 범위는 **넓히는 쪽만 시도하지 않습니다.** 좁히는 쪽(`narrow`)도 후보입니다
