# STATUS — 지금 어디까지 왔나

> 마지막 갱신: **2026-09-08** (STEP 38 — 창이 이미 포화. 커버리지 67.9%는 상한)
> 지나온 기록은 [`HISTORY.md`](HISTORY.md), 작업 규칙은 [`CLAUDE.md`](CLAUDE.md).
> 숫자의 원본은 전부 [`docs/results/`](docs/results/) 에 있습니다.

---

## 한 줄

2단계 파이프라인이 holdout 에서 **최종 7종 macro-F1 0.5799**, 병변을 **95.3%**
잡고 정상 사진의 **21.2%** 를 병원으로 보냅니다. 1단계는 풀렸고, 남은 건
배율 견고성(26.8%)과 A4(농포) recall(0.264)입니다.

★ **2026-08-26 — 출력에서 병변 이름을 뺐습니다.** 2단계는 계속 돌리되 화면에는
**확률 분포만** 띄우고 이름은 단정하지 않습니다. 근거와 규격은 아래
[출력 형식](#출력-형식-2026-08-26-확정) 절에.

---

## 🪟 2026-09-08 — **창이 이미 포화돼 있었습니다** (STEP 38)

### ★★★ 지금 앱은 네모를 거의 안 쓰고 있었습니다

`m2.5` 창 = 네모 긴 변 × 2.5. 사진이 1080px 이면:

| 네모 (화면 대비) | m2.5 창 | 잘린 뒤 |
|---:|---:|---:|
| 20% | 540px | 50% |
| 35% | 945px | 88% |
| **47%** | 1269px | **100% — 포화** |
| 59% | 1593px | 100% |

실측 사용자 네모는 **47.2 ~ 70.6%** → **40장 전부(100%)가 포화**입니다.

> **2단계는 이미 사진 전체를 보고 있습니다.**

- *"네모를 잘 받아야 한다"* 는 전제가 **실사용에선 이미 무의미**
- STEP 36 의 "−22~24%" 는 사실 **"크롭이 안 걸렸을 때의 하락"**
- 앱 밴드(`ALLOW 0.28~0.68`)도 포화점 0.47 을 한참 넘게 허용

⚠️ **이 계산은 모델도 GPU 도 필요 없었습니다.** 사람에게 40장을 두 번 그리게
하고 파인튜닝을 세 번 돌린 **뒤에야** 했습니다.

### 네모 잡음 증강 — 기각

| | 라벨 네모 | 사용자 네모 |
|---|---:|---:|
| 기준선 | 0.5022 | 0.3691 |
| **대조군 (잡음 0%)** | **0.7102** | **0.4778** |
| 잡음 50% | 0.6391 | 0.5501 |
| 잡음 100% | 0.6260 | 0.5551 |

사용자 **+0.072~0.077** (문턱 +0.03 통과) 인데 **라벨 −0.071~0.084** 로
허용치(−0.03) 두 배 초과 → **기각**. `p=0.5` 로 섞어도 같습니다.

★ **대조군이 결정적** — 분포 적응만으로 +0.109 오릅니다. 대조군 없이 첫
스모크에서 **+0.111** 을 보고 *"잡음 덕분"* 이라 보고할 뻔했습니다.

⚠️ 창 포화가 사실이면 그 관문은 **일어나지 않는 상황**을 지키는 셈입니다.
그래도 **판정은 안 뒤집습니다** — 결과 보고 전제를 고치면 사후 합리화입니다.
다시 정하려면 **새로 사전등록**해야 합니다.

[`STEP38`](results/STEP38_네모_잡음_증강과_창_포화.md)

---

## 🚧 2026-09-08 — **입력 쪽 길이 다 막혔습니다** (STEP 37)

STEP 36 이 *"사람은 네모 크기를 못 맞춘다"* 로 끝나서 남은 길 둘을 재봤습니다.
**둘 다 닫혔고 사전등록 문턱은 하나도 안 옮겼습니다.**

### 탭 (점 하나만) — 기각

| | |
|---|---|
| 탭 오차 중앙값 | **0.111** (문턱 0.10) |
| 10~90% | 0.015 ~ 0.491 |
| **탭이 병변 안에 든 비율** | **47.5%** |

★ 분포가 **갈라집니다** — 25분위 0.028(아주 정확) vs 75분위 0.328.
경계가 뚜렷하면 콕 찍히고 **번진 것(비듬·각질·태선화)은 못 찍습니다.**
그런 병변엔 사람이 답할 **"중심" 이 애초에 없습니다.**

### 모델이 제안하기 — 기각

1단계를 320px 창 탐지기로 써서 제안을 뽑았습니다 (창 9×9, 점수 가중 무게중심).

| | 모델 | 사람 |
|---|---:|---:|
| 배율 밴드 안 | **39.0%** | 10.0% |
| 위치 밴드 안 | **19.0%** | 12.5% |
| **둘 다** | **6.7%** | 5.0% |

**6.7% < 문턱 50% → 기각.** 모델이 사람보다 두 축 다 나은데 **같이 맞는 일이
드뭅니다.**

⚠️ Grad-CAM lift 4.55 로 기대치를 잡으면 안 됩니다 — 그건 *"배경을 안 본다"*
는 뜻이지 네모를 뽑는 능력이 아닙니다.

### 닫힌 것 정리

```
네모 크기      ❌ 병변 크기와 무관 (상관 −0.05)
점 위치        ❌ 병변 안 47.5%
모델 제안      ❌ 둘 다 6.7%
기본값 조정    ❌ 병변이 화면의 5.6~59.6%
f320 전환      ❌ +0.021 < 0.03
```

→ 남은 것은 **모델을 잡음에 튼튼하게** 만드는 쪽입니다.

[`STEP37`](results/STEP37_사람도_모델도_네모를_못_줍니다.md)

---

## 📏 2026-09-07 — **사람이 그리는 네모** (STEP 36, 마지막 미측정 항목)

### ⚠️ 커버리지 67.9% 는 **상한**입니다

우리 평가는 **전부 라벨 네모**(라벨러가 병변에 딱 맞춰 그린 것)로 잘랐습니다.
실사용 네모로 바꾸면 2단계 macro-F1 이 **상대 22~24%** 떨어집니다
(VL01, n=2,836). 커버리지가 얼마나 떨어지는지는 **아직 안 쟀습니다.**

**발표·문서에서 67.9% 를 인용할 때 이 단서를 같이 다세요.**

### ★ 사람은 병변 크기를 안 맞춥니다

| | 값 |
|---|---|
| 실제 병변 긴 변 | 중앙값 21.5%, 범위 **5.6% ~ 59.6%** |
| 사람이 그린 것 | 중앙값 58.7%, 범위 **47.2% ~ 70.6%** |
| **상관** | **−0.05** |

자극이 완전히 다른 두 측정에서 **2.82배 / 2.80배** 로 같은 값이 나왔습니다.

### ★★★ 앱 밴드가 재는 것 ≠ 모델이 필요한 것

앱 밴드는 `네모 ÷ 화면`(ALLOW 0.28~0.68)인데 모델이 필요한 건 `네모 ÷ 병변`
입니다. 사용자가 그린 47~70% 는 **거의 다 밴드 안**이고 실제로는 병변의 2.80배
입니다. **앱은 병변을 못 보니 이 오차를 검사할 수 없습니다.**
⚠️ 서버도 같은 것을 봅니다 (밴드가 사본) — 둘이 일치하니 아무도 못 봅니다.

### 기각 · 닫음

- **`f320` 전환** — 하락 차이 +0.0214 < 문턱 0.03. ⚠️ n=191 에서 +0.054 였다가
  n=2,836 에서 반토막 (**세 번째** 같은 패턴)
- **기본값 44% 조정** — 어떤 고정값도 5.6~59.6% 를 못 덮습니다

★ **크롭 선택이 지렛대가 아닙니다** — `f320` 은 크기 오차에 면역인데도 22%
떨어집니다. **네모 자체가 지렛대입니다.**

### 남은 길 (제품 결정)

- **자른 결과 미리보기** — 앱이 못 재는 걸 사람이 눈으로
- **밴드가 재는 대상 바꾸기** — 1단계는 이상 부위를 봅니다 (새 설계)

[`STEP36`](results/STEP36_사람이_그리는_네모_실측.md)

---

## 🔬 2026-09-07 — **A5 하향, 그리고 관문의 구멍** (STEP 35, 추론 0회)

사용자가 *"58.4%가 쓸만한 수준이 맞아?"* 라고 물어 혼동행렬을 찍었고, 거기서
**관문이 못 보고 있던 것**이 나왔습니다.

### ① 제 "58.4%도 쓸만합니다" 는 과했습니다

실제 그 병변 중 **이름까지 맞게 말한** 비율 (holdout):

| 계열 | 릴리스 단독 | **3팔 앙상블** |
|---|---:|---:|
| 융기·발진 | 31.9% | **51.3%** |
| 표면 변화 | 71.9% | **78.8%** |
| 미란·궤양 | 38.0% | 37.5% |
| 결절·종괴 | 46.7% | 54.5% |

정확히는 **"문턱은 넘지만 융기·발진을 셋에 하나밖에 못 잡는다"** 입니다.
앙상블을 빼면 사실상 **표면 변화 감별기**가 됩니다.

★ 그리고 **앙상블의 값어치가 커버리지가 아니었습니다** — 융기·발진 포착
1.6배가 본체이고 커버리지 +9.5%p 는 그 결과입니다.

### ② ★ 전체 3.7%가 통과하는 동안 A5 하나가 43.8%

| | 말한 A5 | 하향 |
|---|---:|---:|
| 릴리스 단독 | 1,336 | 37.4% |
| 3팔 앙상블 | 1,420 | **43.8%** |

**전체 하향은 둘 다 3.7%** 로 관문(5%)을 통과합니다. 표면 변화(전체의 51%)가
평균을 떠받쳐서 A5 가 묻힙니다. → `granularity_report` 에 **클래스별 하향**을
넣었습니다 (검사 25 → **33개**).

### ③ 원인은 모델이 아니라 선택 효과

앙상블을 릴리스와 **같은 장수(20,121)만** 말하게 자르면 A5 하향 **38.0%**
(릴리스 37.4%, 차이 +0.6%p). 43.8%는 **6,271장을 더 말하기로 한 대가**입니다.

⚠️ **STEP 34 의 A6 유병률 건과 같은 구조**입니다 — 비율은 분모가 바뀌면 같이
바뀝니다. 하마터면 두 번째로 같은 실수를 할 뻔했습니다.

### ④ ★ 고칠 수 있습니다 — 하향 방지 규칙

> 급한 쪽(미란·궤양 + 결절·종괴) 확률 합이 **0.25 이상**이면 덜 급한 묶음은
> 답 후보에서 뺀다

문턱은 **val 에서 고르고 holdout 은 확인만** 했습니다.

| | 커버리지 | A5 하향 | 전체 하향 |
|---|---:|---:|---:|
| 지금 | 67.9% | 43.8% | 3.7% |
| **규칙 적용** | **66.5%** | **36.8%** | **2.9%** |

**커버리지 1.3%p 로 위험한 오답 7%p. 추론 0회.** 릴리스 단독보다 두 축 다 낫습니다.

✅ **서빙에 들어갔습니다** (2026-09-07 오후) — `config.DOWNGRADE_BLOCK_MIN = 0.25`,
적용 지점은 `agent.lesion_group()` 한 곳입니다.

⚠️ **`stage2.distribution`(6종 분포)은 안 건드립니다.** 바뀌는 건 `stage2.group`
한 줄뿐입니다 → **앱은 고칠 게 없습니다** (앱은 `distribution` 만 읽습니다).

★ 넣으면서 **복사된 로직 하나**를 찾았습니다 — `message.lesion_group_line()` 이
묶음 합·문턱·argmax 를 따로 계산하고 있었습니다. 한쪽만 고쳤으면 **콘솔과 앱이
다른 계열을 말합니다.** 이제 선택은 `agent` 가 하고 `message` 는 문장만 만듭니다.

### ⑤ 대안 둘 기각 · 소득 하나

**팔 가중치**(A5 하향 안 움직이고 커버리지만 −10.4%p)와 **비용 행렬**(같은
커버리지에서 문턱 규칙보다 1~3%p 나쁨) 둘 다 기각. 다만 비용 행렬이 문턱을
**판단의 언어로 번역**했습니다 — `0.25 ≈ "하향은 다른 오답의 2배로 나쁘다"`.
이제 사람이 정할 수 있습니다 (3배면 62.7%, 5배면 55.0%).

> ★ **뭉툭하게 전체를 조이면 커버리지만 잃습니다. 위험한 순간만 표적으로
> 손대야 값을 합니다.** 로짓 보정·팔 가중치·비용 행렬이 전부 전체를 조였고
> 전부 졌습니다.

재현: `uv run python tools/a5_downgrade.py`
[`STEP35`](results/STEP35_A5_하향과_관문의_구멍.md)

---

## 🖥️ 2026-09-07 — **관리자 콘솔에 켰습니다** (배포, 실험 아님)

★ **계열 4군 + 3팔 앙상블이 실제로 돌고 있습니다.** 보호자 앱이 아니라
**관리자 콘솔에만** — 팀이 눈으로 보고 앱을 정하기 위해서입니다.

| | |
|---|---|
| 어디 | `daengs.weareithero.cloud/console/search` → 피부 스크리닝 탭 |
| 무엇 | 6종 막대 **위**에 "시험 중" 계열 카드 한 장 |
| PR | `SAJOYO/DAENGS_dev#291` → `dev` 머지 `81b7265` (= 배포) |
| 스위치 | `DOG_SKIN_SHOW_GROUP=1` (compose 기본값) |
| 앙상블 | 릴리스 `checkpoints/` 에 `stage2_` 폴더 셋 → 자동 3팔 |

로그에 이 줄이 찍히면 3팔입니다:

    [agent] 2단계 앙상블 3팔: stage2_convnextv2_base_…, stage2_effnetv2_s_f320_…, stage2_effnetv2_s_m2.5_…

### ⚠️ 배포하면서 실제로 당한 것 — **코드보다 가중치를 먼저 넣었습니다**

체크포인트 둘을 릴리스 폴더에 **머지 전에** 넣었습니다. 그때 서버가 돌던
`dev` 의 `from_release` 는 앙상블을 모르고, 게다가 `found[st] = …` 로 **덮어씁니다**
(`setdefault` 가 아니라). 이름 순으로 훑으니 마지막인 `stage2_effnetv2_s_m2.5_…`
가 이겨서 — **릴리스의 `convnextv2_base` 가 빠지고 더 약한 팔 하나만 돌았습니다.**

**에러도 경고도 없었습니다.** `POST /screen/v1/screen` 은 200 이었고, 눈치챈 건
로그에 `[agent] … 3팔:` 이 **안 찍혔기 때문**입니다. 앱도 같은 백엔드라 그동안
보호자 쪽도 그 모델을 받았습니다.

★ **규칙: 코드가 먼저, 가중치가 나중.** "릴리스에 넣은 것이 곧 구성" 이라는 설계는
읽는 코드가 새 것일 때만 성립합니다. 그리고 **성공 로그가 없다는 것**이 유일한
신호였습니다 — 실패 로그를 기다렸으면 영영 못 봤습니다.

### 지금 팀이 판단할 것

1. **계열 카드가 얼마나 자주 뜨나** — 열에 일곱쯤이 예상(holdout 커버리지 67.9%).
   안 뜨는 것은 고장이 아니라 확신 `p1 × p(계열)` 이 **0.469** 아래인 것입니다
2. ★ **"표면 변화"(A2+A3)를 한 이름으로 불러도 되나** — 이 병합은 수의피부과 표준이
   명시한 것이 **아니고 제 판단**입니다. 이득의 3분의 2 이상이 여기서 나옵니다
   (42.5% → 62.1%). **콘솔에서 먼저 보려는 이유가 이것입니다**
3. **응답 774ms → 1052ms** 를 참을 만한가 (앱도 같은 백엔드)

⚠️ **긴급도를 같이 띄우지 마세요** — 말한 것의 절반이 한 단계 부풀려집니다
(과잉 52.4%, STEP 30·33).

---

## ✅ 2026-09-07 — **남은 셋을 닫았습니다** (STEP 34)

### ① 하향 문턱 5% 에 닻이 생겼습니다

**제품이 이미 받아들이는 하향**이 기준입니다.

| | 무엇 | 얼마 | 세기 |
|---|---|---:|---|
| 1단계 | 병변인데 "괜찮아 보여요" | **5.70%** | **완전한 하향** (병원 안 감) |
| 2단계 | 조기진료 → 진료권장 | **3.7%** | **부분 하향** (병원엔 감) |

`0.05 < 0.057` — 2단계 하향은 1단계가 이미 받아들이는 것보다 **작아야** 합니다.
클래스별로도 같습니다: 1단계는 **A6 을 8%** 완전히 놓치는데 2단계는 3.7% 부분 하향.
`experiments.STAGE1_MISS_RATE` 와의 관계를 검사가 지킵니다.

### ② ★ A6 경보가 반토막인 이유 — **모델이 아니라 유병률**

**같은 문턱**에서 재보면:

| 문턱 | val 재현율 | **ho 재현율** | val 정밀도 | **ho 정밀도** |
|---:|---:|---:|---:|---:|
| 0.40 | 61.1% | **59.4%** | 81.4% | 71.3% |
| 0.70 | 38.4% | **36.9%** | 90.4% | 85.1% |

**재현율은 거의 같고 정밀도만 낮습니다.** A6 이 holdout 에 **절반만큼 드뭅니다**
(넘긴 사진 중 4.61% → **2.91%**). 부위·청크 구성은 같으니 **빈도만 바뀐 것**입니다.

★ **"정밀도 90%" 는 모델의 성질이 아니라 모델 × 유병률의 성질입니다.**
→ A6 경보는 **재현율로** 정하세요. 문턱 0.40 이면 재현율 약 60% 가 양쪽에서
유지되고 정밀도는 71~81% 사이에서 움직입니다.

### ③ 계열 한 줄을 만들었습니다 — **기본 꺼짐**

```
모양만 보면 **융기·발진** 계열에 가깝습니다. (진단이 아닙니다)
```

`message.SHOW_GROUP` (환경변수 `DOG_SKIN_SHOW_GROUP=1`). 확신도가 **0.469**
미만이면 **아무 말도 안 합니다**. 검사 9개가 켠 상태에서도 **6종 이름이 안 새고**
**긴급도 문구가 안 붙는지**를 지킵니다.

**켜는 것은 제품 결정입니다. 화면은 아직 안 바뀝니다.**

[`STEP34`](results/STEP34_남은_셋을_닫습니다.md)

---

## 🎯 2026-09-07 — **holdout 확인 완료: 버팁니다** (STEP 33)

🚨 holdout 을 열었습니다. **한 번 본 값이고, 나빠서 다시 돌린 게 아닙니다.**

| 알갱이 | val | **holdout** | 차이 |
|---|---:|---:|---:|
| 6종 이름 | 41.4% | **41.1%** | −0.3%p |
| **★ 계열 4군 (권고)** | 69.8% | **67.9%** | **−1.9%p** |
| 계열 4군 · 릴리스 단독 | 59.4% | **58.4%** | −1.0%p |

**관문 셋 다 통과**합니다 — 커버리지 67.9%(≥50%) · 긴급도 하향 **3.7%**(≤5%) ·
A6 오명명 **12.5%**(≤30%). **결정을 바꿀 이유가 없습니다.**

STEP 27~32 가 알갱이·문턱·묶음을 전부 val 에서 골랐는데도 **낙관 편향이
1~2%p 로 작았습니다.**

⚠️ 다만 이 holdout 은 **05·07 이 이미 열어본 적이 있습니다** (STEP 33 에서
검사를 넣다가 발견) — *"한 번만 봤다"* 보다 노출이 큽니다.

### ★ 1단계 — recall 은 같은데 헛알림률이 5.8%p 줄었습니다

| | val | **holdout** |
|---|---:|---:|
| 전체 | 51,983장 (정상 47.4%) | **66,485장 (정상 56.2%)** |
| recall | 0.9434 | **0.9430** |
| **헛알림률** | 24.5% | **18.7%** |
| 넘긴 것 중 헛알림 몫 | 19.0% | **20.3%** |

같은 임계값인데 헛알림률이 좋아졌습니다. **그런데 넘긴 더미 안의 몫은 오히려
늘었습니다** — holdout 에 정상 사진 비율이 더 높아서(47.4 → 56.2%)입니다.
커버리지에 영향을 주는 건 **뒤쪽(몫)** 이라, 계열 4군이 −1.9%p 떨어진 이유의
일부가 이것입니다.

⚠️ **"1단계가 holdout 에서 더 좋다" 로 읽으면 안 됩니다** — 두 청크의
정상/병변 구성이 다를 뿐입니다.

### ⚠️ 하나 크게 떨어졌습니다 — **A6 경보**

| 정밀도 목표 | val 재현율 | **holdout** |
|---:|---:|---:|
| **90%** | 50.4% | **25.1%** |
| 80% | 68.1% | 48.8% |

**반토막입니다.** 다른 지표는 1~2%p 인데 여기만 25%p 이고, holdout 의 A6 이
1,003장이라 표본 탓도 아닙니다. 왜인지는 못 잽니다.

★ **"덩어리가 의심됩니다" 를 90% 정밀도로 켜면 실제 덩어리의 4분의 1만
잡습니다.** val 값으로 기대치를 잡으면 안 됩니다.

### 관문이 holdout 에서도 일했습니다

교과서 2군은 커버리지 1등(72.9%)에 하향 0.0% 인데 **과잉 89.0%**,
긴급도 2등급은 89.4% 에 **하향 40.7%** — STEP 28·30 의 두 관문이 **holdout
에서도 같은 둘을 걸렀습니다.**

### 앙상블도 유지

+10.4%p → **+9.5%p**. 6종 이름에서는 +15.7%p 로 오히려 큽니다.

[`STEP33`](results/STEP33_홀드아웃_확인.md)

---

## ✅ 2026-09-06 — **수의학 축으로 검증 + 앙상블 채택** (STEP 32)

### ① 표준 분류가 우리 묶음을 지지합니다 (다만 그 축으로 묶으면 기각)

수의피부과 표준은 **primary**(병이 만든 것) / **secondary**(그 뒤에 생긴 것) /
**둘 다 됨**(scale·crust·색소) 입니다.

* **A2·A3 가 똑같이 애매한 자리**(둘 다 mixed)이고, 출처가 *"secondary 는
  진단 가치가 낮다"* 고 적습니다 → **A2+A3 병합에 사전 근거**가 생겼습니다
* **A1·A4(≤1cm) vs A6(>1cm)의 1cm 는 교과서 경계** → A6 따로 두기가 표준과 일치

⚠️ 저는 수의사가 아닙니다. **A2+A3 는 확인이 필요합니다.**

### 🚫 그런데 교과서 축으로 2군을 만들면 기각

| 묶음 | 커버리지 | 하향 | **과잉** |
|---|---:|---:|---:|
| **★ 계열 4군 (권고)** | 69.8% | 3.1% | **48.1%** |
| 교과서 primary/secondary (2군) | **74.6%** | **0.0%** | **88.4%** 🚫 |

커버리지 1등에 하향 0.0% 인데, `primary` 에 A6(조기 진료)이 들어 있어
**구진 하나에도 '조기 진료'** 가 붙습니다. ★ **STEP 30 의 과잉 관문이
두 번째로 일한 장면**입니다.

### ② ★ 앙상블 — "추론 3배" 가 틀렸습니다

| 묶음 | **릴리스 단독** | 앙상블 | 값어치 |
|---|---:|---:|---:|
| 6종 이름 | **26.9%** | 41.4% | +14.6%p |
| **계열 4군 (권고)** | **59.4%** ✅ | 69.8% | **+10.4%p** |

계열 4군은 릴리스 단독으로도 문턱을 넘습니다(하향 3.3%). 그래서 처음엔
"필요 없다" 고 적었는데 — **비용을 안 재고 한 말이었습니다.**

배포와 같은 조건(**CPU**, 스레드 4개, 사진 1장) 실측:

| | 릴리스 단독 | 3팔 앙상블 | 배수 |
|---|---:|---:|---:|
| 2단계만 | 633ms | 911ms | 1.44배 |
| **전체 파이프라인** | **774ms** | **1052ms** | **1.36배** |
| 메모리 | 0.34GB | 0.50GB | +0.16GB |

**3배가 아니라 1.36배** — 셋 중 둘이 4.4배 작은 모델(effnetv2_s)입니다.
사진 한 장에 **+278ms**, 0.77초 → 1.05초. **체감이 안 되는 구간**입니다.

→ **+278ms 로 커버리지 +10.4%p 를 삽니다.** 게다가 위치 교란은 오히려
좋아집니다(17.9 → 15.2%, STEP 26). **채택 권고로 바꿉니다.**

⚠️ **반대로 6종 이름을 켜려면 앙상블이 필수**입니다 — 릴리스 단독 26.9% 는
닫음 문턱(30%) 아래입니다.

⚠️ STEP 31 의 69.8% 는 **앙상블 값**이었습니다. 노트북 11 이 릴리스 단독을
안 쟀습니다 — **숫자 옆에 '무엇으로 쟀나' 를 안 적으면 또 이럽니다.**

### 지금 권고

| | |
|---|---|
| 무엇을 | **계열 4군** · 모델은 **3팔 앙상블** |
| 얼마나 | 넘긴 사진의 **69.8%** · 위험하게 틀림 **3.1%** |
| 앙상블 | **채택** — 1.36배(+278ms)로 +10.4%p |

[`STEP32`](results/STEP32_수의학축_검증과_앙상블_값어치.md)

---

## ✅ 2026-09-06 — **전체 데이터 확인 완료: 계열은 말할 수 있습니다** (STEP 31)

캐글 추론 1회로 헛알림 포함 곡선을 전체 데이터에서 다시 쟀습니다.
**VL01 이 넷 다 비관적이었습니다.**

| 알갱이 | VL01 | **전체** | 차이 | 하향 | 과잉 |
|---|---:|---:|---:|---:|---:|
| 6종 이름 | 33.7% | **41.4%** | +7.7%p | — | — |
| **계열 4군** (권고안) | 61.4% | **69.8%** | **+8.4%p** | **3.1%** | 48.1% |
| 계열 3군 | 62.1% | 70.8% | +8.7%p | 3.1% | 48.1% |
| 긴급도 3등급 | 42.7% | 49.2% | +6.5%p | **8.8%** | 6.8% |

**계열 4군이 논의 문턱(50%)을 넉넉히 넘습니다** — 커버리지 69.8% · 하향 3.1%
(허용 5%). A6 안전 관문도 표본 **53 → 859장**에서 **10.1%** (허용 30%) 입니다.
STEP 28 에 "표본 53장이라 안전하다고 말하면 안 된다" 고 적어둔 걱정이 해소됐습니다.

**6종 이름은 41.4%** 로 올랐지만 여전히 닫음(30%)과 논의(50%) 사이 —
**판단 보류** 그대로입니다.

### ★ 1단계 recall 도 VL01 값이었습니다

전체 val 51,983장에서 **0.9434** 입니다 (STATUS·CLAUDE 에 적어온 0.970 은
VL01). 병변 100마리 중 놓치는 게 3마리가 아니라 **5.7마리**입니다.
넘긴 사진 31,827장 중 헛알림 **6,044장(19.0%)**.

### ⚠️ VL01 은 후한 게 아니라 **지표마다 방향이 다릅니다**

macro-F1·A4 에선 낙관적(STEP 23·25), 커버리지·A6 경보에선 비관적(STEP 29·31).
**뭉뚱그리면 계속 틀립니다. 방향을 짐작하지 말고 재세요.**

### 결정에 필요한 숫자는 다 모였습니다

| | |
|---|---|
| 무엇을 | **계열 4군** (융기·발진 / 표면 변화 / 미란·궤양 / 결절·종괴) |
| 얼마나 | 넘긴 사진의 **69.8%** |
| 틀리는 비율 | 20% (목표대로) · **위험하게는 3.1%** |
| A6 오명명 | **10.1%** (n=859) |
| 대가 | 과잉 분류 48.1% · 추론 3배(앙상블 쓸 때만) |

**모델 쪽에서 더 잴 것이 없습니다.** 남은 건 사람의 판단입니다.

[`STEP31`](results/STEP31_이름_커버리지_전체데이터.md)

---

## 🔬 2026-09-06 — **A5 는 데이터로 안 풀립니다** + 관문의 구멍 (STEP 30)

A6 을 따로 빼니 드러난 **미란·궤양(A5) recall 0.541** 을 팠습니다.

**장수 문제가 아닙니다 — 대조군이 바로 옆에 있습니다.**

| | val 장수 | 개체 | recall |
|---|---:|---:|---:|
| A6 | 1,540 | **102** | **0.675** |
| A5 | 1,468 | **113** | **0.541** |

A5 가 개체가 **더 많은데** 0.134 낮습니다. A6 은 데이터 8배로 풀렸는데
(STEP 16, 0.328 → 0.619) A5 는 같은 크기를 받고도 안 풀렸습니다.

### A5 의 상대는 **융기·발진**입니다

짝별 AUROC 최악 셋: A1↔A4 **0.768** · **A4↔A5 0.861** · **A1↔A5 0.877**.

★ **이게 계열 묶음이 A5 를 못 살린 이유입니다.** 4군은 A1↔A4·A2↔A3 를
**묶음 안**으로 넣어 없앴는데, A5↔A1·A4 는 **묶음 사이**라 살아남습니다.

그리고 크기가 매개합니다 — **A5 는 작을수록**(Q1 0.449), **A1 은 클수록**
(Q4 0.424) 틀리고 중앙값이 겹칩니다(134px / 167px).

### 남는 설명: 입체감이 사진에 안 찍힙니다

최악의 짝 셋이 전부 *"솟았나 / 고름이 찼나 / 파였나"* 를 묻습니다.
**대조군이 지지합니다** — A3 의 단서는 **색**이고 A1↔A3 는 0.934 로 잘 갈립니다.
색은 사진에 있고 입체감은 없습니다. ⚠️ 소거법이지 증명이 아닙니다.

→ **고치려면 모델이 아니라 촬영입니다.** 촬영 가이드 축에 두 번째 후보
(비스듬한 조명·두 각도)가 생겼습니다.

### ★ 그리고 제 관문에 구멍이 있었습니다

`granularity_report` 가 **긴급도 하향만** 세고 **과잉은 안 셌습니다.**

| 묶음 | 커버리지 | 하향(위험) | **과잉(안전)** |
|---|---:|---:|---:|
| 6종 | 33.7% | 4.7% | **9.1%** |
| **권고안 4군** | 61.4% | **1.7%** | **49.3%** |

**권고안의 하향 1.7% 는 과잉 49.3% 로 산 것입니다.** 지금은 화면에 긴급도를
안 띄워서 안 보이지만, **계열 이름과 긴급도를 같이 띄우면 절반이 한 단계
부풀려집니다 — 두 기능을 같이 켜지 마세요.**

### 탐색하고 닫은 것

A5 를 융기·발진에 합치면 커버리지 67.4% · A5 포착 **0.804** · 정확도 0.8490 로
전부 낫습니다. **그래도 안 합니다** — 혼동행렬로 만든 묶음이고, 임상 해설이
A1↔A5 를 **🔴 위험한 혼동**으로 꼽았으며, 말한 것의 **29.1%** 가 "조기 진료"
로 나갑니다(권고안 5.0%).

[`STEP30`](results/STEP30_A5_미란궤양과_과잉분류.md)

---

## 🚩 2026-09-06 — **제 근거 하나를 물러야 합니다** (STEP 29, 추론 0회)

STEP 28 에서 "계열 정확도 **0.8365**" 를 앞세웠습니다. **하한선을 안 적었습니다.**

| 묶음 | 하한선(최빈 묶음) | 모델 | **하한선 대비** |
|---|---:|---:|---:|
| 6종 | 35.3% | 0.7037 | **+35.1%p** |
| A6 만 따로 (4군) | 67.1% | 0.8315 | +16.0%p |
| 형태 계열 3군 | 67.1% | 0.8365 | +16.5%p |

'표면 변화'(A2+A3)가 18,349장으로 전체의 67% 입니다. **굵게 묶은 순간 하한선이
0.67 이 됩니다** — 0.70 → 0.84 는 모델이 좋아진 게 아니라 문제가 쉬워진 것이고,
**모델이 실제로 하는 변별 일은 6종에서 더 큽니다.** 작업 규칙 1 위반입니다.

**★ 단 커버리지는 이 비판에서 자유롭습니다.** *'늘 최빈 묶음만 말하기'* 는
오답률이 어디서나 32.9% 라 20% 목표를 **한 번도** 못 지킵니다 — **하한선의
커버리지는 0%** 입니다. 61.4% 는 전부 모델의 확신도 순서가 벌어온 것입니다.

→ **앞으로 정확도 0.84 를 근거로 쓰지 않습니다. 커버리지를 씁니다.**

### ★ 좋은 소식 — A6 경보는 VL01 이 4배 넘게 깎고 있었습니다

표본 124 → **1,540장**(전체 val):

| 정밀도 목표 | 재현율 (전체) | 재현율 (VL01) |
|---:|---:|---:|
| **90%** | **50.4%** | 12.1% |
| 80% | 68.1% | 39.5% |

"덩어리가 의심됩니다" 라고 말하면 **10에 9는 진짜**이고 **실제 덩어리의 절반**을
잡습니다. **릴리스 단독으로도 90%/42.8%** — 추론 3배 없이 켤 수 있습니다.
VL01 숫자로 판단했으면 버렸을 기능입니다.

헛알림이 없어서 낙관적일까 봐 **VL01 에서 두 조건을 다 재봤습니다** — 정밀도
80% 에서 39.5% / 39.5% 로 같습니다. 멀쩡한 피부를 덩어리로 보지는 않습니다.
(STEP 27 에서 각주만 달고 두 달 버린 뒤라, 이번엔 실측했습니다.)

### 4군 안에서 결절·종괴 (거절 없이 전부 말했을 때, 전체 val)

recall **0.645**, 긴급도 하향 **31.0%** — 허용치 5% 를 한참 넘습니다.
**다만 '관찰' 로 부른 건 0.0%** 입니다. 어떤 경로로 틀려도 **병원에 가라는
말은 나갑니다.** ⚠️ STEP 28 의 "하향 1.7%" 는 거절 뒤·VL01 값이라 다릅니다.

### 다음 약점: **미란·궤양(A5) recall 0.510**

A6 을 따로 빼니 A5 가 혼자 남아 드러났습니다 (융기·발진으로 25.8%).
표면 변화가 0.917 로 평균을 떠받치고 있어 가려져 있었습니다.

[`STEP29`](results/STEP29_하한선과_A6경보_전체데이터.md)

---

## ★ 2026-09-06 — **알갱이를 바꾸니 살아납니다** (STEP 28, 추론 0회)

STEP 27 이 *"6종 이름은 33.7%"* 로 끝났습니다. 그런데 **물어야 했던 건
'무엇을' 말할 것인가**였습니다. 같은 모델·같은 확률로 알갱이만 바꿔 봅니다.

| 알갱이 | 커버리지 | 긴급도 하향 | 판정 |
|---|---:|---:|---|
| 6종 이름 | 33.7% | 4.7% | 기각 |
| **형태 계열 3군** (A1·A4 / A2·A3 / A5·A6) | **62.1%** | **1.7%** | **통과** |
| 긴급도 3등급 | 42.7% | 5.6% | 기각 |
| **두 이름 (top-2)** | **80.4%** | **2.1%** | **통과** |
| 긴급도 2등급 | 92.2% | **46.0%** | **기각 (안전 관문)** |

**⑤ 가 관문이 일한 장면입니다** — 커버리지만 봤으면 92.2% 짜리를 골랐을
텐데, 실제로 급한 것의 **46%** 를 "안 급하다" 고 말합니다.

### 묶음은 데이터를 보고 만들지 않았습니다

`config.URGENCY_HINT` 와 임상 해설의 **"🟢 비교적 안전한 혼동"**(A1↔A4 ·
A5↔A6)에서 가져왔고 **둘 다 이 분석보다 먼저** 있었습니다. 셋째 묶음(A2·A3)은
고른 게 아니라 **남은 것**입니다.

### 전체 데이터에서도 재현 (val 27,331행, 병변만)

| 알갱이 | 릴리스 정확도 | 앙상블 정확도 |
|---|---:|---:|
| 6종 | 0.6820 | 0.7037 |
| **형태 계열 3군** | 0.8191 | **0.8365** |

두 이름 적중률 **87.4%**.

### 값이 달라지는 것 둘

* **추론 3배가 '선택' 이 됩니다.** 형태 계열이면 릴리스 단독도 **52.5%** 로
  문턱을 넘습니다 (앙상블은 +9.6%p). 6종 이름일 때만 앙상블이 필수였습니다
  (0.7% → 33.7%).
* **오답률 45.6% 는 겁주는 숫자입니다.** 행동을 바꾸는 오류(긴급도 하향)만
  세면 **11.1%** 입니다 (전체 데이터 13.5%).

### A6 경보는 따로 봐야 합니다

"덩어리가 의심됩니다" 는 커버리지로 재면 최대 3.2% 입니다 (A6 이 그만큼이라).
정밀도·재현율로 보면 **정밀도 50% 에서 재현율 71.8%**. ⚠️ 표본 124장.

### 아직 안 한 것

**화면 문구는 안 바꿨습니다** — `message.compose_screening_message` 그대로.
제품 결정입니다. VL01·val 이고, 전체 확인은 캐글 1회면 됩니다.

[`STEP28`](results/STEP28_무엇을_말할_것인가_알갱이.md)

---

## 🚩 2026-09-06 — **이름을 켤 수 있나: 정직하게 재니 33.7%** (STEP 27, 로컬)

어제 제가 *"커버리지 62.0% → 71.5%, 3.9배 나아졌으니 재검토하자"* 고 말했습니다.
**그 분모가 틀렸습니다.** 그 값들은 전부 **진짜 병변인 사진만** 놓고 잰 것인데,
화면에는 **1단계가 넘긴 사진**이 뜹니다 — 거기엔 멀쩡한 개가 섞여 있고
그 사진에 병변 이름이 붙으면 **무조건 오답**입니다.

    1단계가 넘긴 사진 3,830장 (VL01 val)
      진짜 병변 3,033장 (79.2%)  /  헛알림 797장 (20.8%)  ← 멀쩡한 개

★ **헛알림 20.8% 만으로 목표 오답률(20%)을 넘습니다.** 2단계가 완벽해도
전부에게 이름을 말하면 20.8% 틀립니다.

| 오답률 20% 목표에서 | 커버리지 | 판정 |
|---|---:|---|
| 릴리스 `m2.5` 단독 | **0.7%** | **축을 닫음** |
| **3팔 앙상블** | **33.7%** | **판단 보류 — 멘토 결정** |
| (참고) 옛 분모, 병변만 | 36.1% / 59.1% | 이 숫자와 섞지 말 것 |

### ★ 릴리스가 0.7% 인 건 정확도가 아니라 **과신** 때문입니다

확신도 **최상위 10분위에서 이미 25.1% 틀립니다** — 목표를 지키는 구간이
처음부터 없습니다. 앙상블은 최상위 12.5%, 누적으로 30% 근처까지 버팁니다.
**릴리스의 1분위가 앙상블의 3분위보다 나쁩니다.**

→ **앙상블의 진짜 값어치는 macro-F1 +0.030 이 아니라 커버리지 0.7% → 33.7%
(48배)** 입니다. 확률 평균은 정확도만 올리는 게 아니라 **과신을 걷어냅니다.**

### 덤으로 확인된 것 둘

* **확신도는 `p1 × p2max` 가 맞습니다** — 33.7% vs p2max 만 3.2% / p1 만 0.5%.
  서빙(`infer.Engine`)이 이미 이렇게 하고 있었고, **이제 근거가 생겼습니다.**
* **1단계 임계값 지렛대**: 0.70 으로 올리면 커버리지 44.5% 지만 recall 이
  0.970 → **0.865** (놓침 3마리 → 13마리). **당기지 않습니다.**

### 남은 결정 — 제가 정할 수 없는 구간입니다

33.7% 는 미리 박아둔 논의 문턱(50%)과 닫음 문턱(30%) **사이**입니다.

* 셋 중 하나에게만 이름을 말하는 화면이 일관돼 보일 것인가
* 그 하나마저 다섯 중 하나는 틀립니다
* 대신 이름이 틀려도 **행동("병원 가세요")은 안 바뀝니다** — A6 만 예외
* 추론 3배 값을 여기에 쓸 것인가

⚠️ **VL01 뿐이고 val 입니다.** VL01 은 커버리지에서 **비관적**인 청크라
(STEP 25) 전체에서는 더 높을 수 있습니다 → 캐글 1회면 확인됩니다.
A6 안전 관문 표본은 **53장**으로 얇습니다.

[`STEP27`](results/STEP27_이름을_말할_것인가_정직한_커버리지.md)

---

## ✅ 2026-09-06 — **크롭 뷰 앙상블 채택 가능** (STEP 26, 캐글 추론 25분)

STEP 25 의 마지막 관문이던 **배율 강건성**을 전체 데이터에서 쟀습니다.
걱정은 하나였습니다 — *앙상블이 `f320` 의 거리 의존(36.9%)을 물려받는가?*

| | 배율 하락 | 위치 하락 |
|---|---:|---:|
| 릴리스 `m2.5`(cnx) — 기준선 | **23.5%** | 17.9% |
| `m2.5`(eff) | 25.9% | 13.6% |
| `f320`(eff) | **36.9%** | 17.7% |
| **3팔 앙상블** | **24.4%** | **15.2%** |

**안 물려받습니다.** +0.9%p 는 허용치(+12%p)는 물론 **교란 잡음 ±5%p 안**입니다.
세 팔 하락률의 산술평균(28.8%)보다도 나아서, 섞으면 중간이 되는 게 아니라
**좋은 쪽에 붙습니다.** 위치 교란은 오히려 **좋아집니다**.

그리고 **7조건(배율 5 + 위치 2) 전부에서 앙상블이 모든 단일 팔을 이깁니다**
(+0.012 ~ +0.051). 기준 조건에서만 좋은 종류가 아닙니다.

### 사전등록 관문 4개 — **전부 통과**

| 관문 | 결과 |
|---|---|
| 1. macro-F1 ≥ +0.02, CI 가 0 제외 | +0.0295 [+0.0243, +0.0346] ✅ |
| 2. 배율 하락 악화 ≤ +12%p | 23.5% → 24.4% (**+0.9%p**) ✅ |
| 3. 클래스 손실 ≤ 0.03 | 최악 +0.000 ✅ |
| 4. A4 흩어짐 안 늘어남 | 37.7% → 37.0% ✅ |

**→ 기술 판정 끝. 남은 건 값입니다.**

### 2팔로 못 줄입니다

릴리스+`f320` 2팔은 macro-F1 +0.0245 로 3팔의 83%를 내지만 **흩어짐 관문에서
기각**됩니다 (A4→그밖 37.7% → **38.2%**). macro-F1·accuracy 만 봤으면 33% 싸게
골랐을 겁니다 — `ENS_NO_SCATTER` 가 실제로 일한 장면입니다.

### ⚠️ 이 표의 절대값을 STEP 23·25 와 섞지 마세요

`robust.ZoomView` 는 배율 1.0x 조차 437px→384 중앙크롭이라 표준 평가가
아닙니다. **순위까지 뒤집힙니다** — ZoomView 에서는 릴리스 cnx 가 꼴찌
(0.5291)로 보이지만 표준 평가 전체 val 에서는 **1등**(0.5959)입니다.
비교 가능한 것은 **같은 파이프라인 안의 하락률뿐**입니다.

### 남은 결정은 제품 쪽입니다

추론 3배인데 **2단계는 화면에 병변 이름을 안 띄웁니다.** 이 이득이 보호자에게
닿는 경로는 하나뿐입니다 — **"확신 있을 때만 이름을 말한다"** 로 출력 규격을
바꾸는 것. 커버리지(오답 20% 목표) **71.5%** 는 그 판단을 다시 놓고 볼 만한
숫자입니다 (STEP 11 의 18.2% 는 resnet50·45,885행 시절 값이라 **낡았습니다**).
→ 모델 판정이 아니라 **제품 판정**이라 멘토·사용자와 같이 정합니다.

[`STEP26`](results/STEP26_2단계_앙상블_배율강건성_실측.md)

---

## ★ 2026-09-06 — **2단계를 살릴 길: 크롭 뷰 앙상블** (STEP 25, 로컬, 재학습 0)

STEP 22·23 은 *"m2.5 냐 f320 이냐"* 를 물었습니다. **틀린 질문이었습니다** —
답이 갈렸다는 건 둘이 **다른 실수를 한다**는 뜻이고, 그러면 고를 게 아니라
더하면 됩니다 (한쪽만 맞히는 몫 **27.0%**).

| | macro-F1 | accuracy | A4 recall | A4→그밖 | 배율 하락 |
|---|---:|---:|---:|---:|---:|
| 릴리스 `m2.5`(cnx) | 0.5805 | 0.6689 | 0.356 | 43.6% | 24.6% |
| `f320`(eff) | 0.5873 | 0.6449 | 0.525 | 32.8% | 35.3% |
| **앙상블** | **0.6384** | **0.7047** | **0.540** | **32.2%** | **27.3%** |

짝지은 부트스트랩 CI 가 셋 다 0 을 안 넘고(macro-F1 [+0.040,+0.075]),
**여섯 클래스가 전부 오릅니다.** 사전등록 관문 4개 **전부 통과**.

★ **이득의 출처는 '모델 두 개' 가 아니라 '창 두 개'** — 백본만 바꿔 더하면
+0.019(잡음권), 크롭을 바꿔 더하면 +0.058.

**TTA 는 기각** (±0.003, 부호도 갈림). 마지막 남은 모델 축이었습니다.

### ★ 전체 데이터 확인 (같은 날, GPU 0) — **절반만 재현**

STEP 23 의 두 로짓 + 릴리스 로짓으로 확인했습니다 (TL01+TL02 val **27,331행**,
A4 **1,730장**). ⚠️ VL01 은 여기 안 들어 있습니다 (캐글 크롭 누락).

| | macro-F1 | accuracy | A4 recall | 커버리지(20%목표) |
|---|---:|---:|---:|---:|
| 릴리스 단독 | 0.5959 | 0.6677 | 0.228 | 62.0% |
| **3팔 앙상블** | **0.6254** | **0.7037** | 0.262 | **71.5%** |

**재현 ✅** macro-F1 +0.0295 [+0.024, +0.035] · **여섯 클래스 전부 상승** ·
이름 오답률 33.2% → **29.6%** · 커버리지 +9.5%p

**미재현 ❌** **A4 구조 개선이 사라졌습니다** — VL01 +0.184 → 전체 **+0.034**,
A4→A1 은 오히려 35.1% → 36.8%. 2팔 조합은 흩어짐 관문에서 **기각**.
이유는 STEP 23 과 같습니다 (VL01 A4 196px vs TL02 113px).

**판정: 미완 → 풀렸습니다** — 배율 강건성은 STEP 26 이 캐글에서 쟀고
**통과**했습니다 (23.5% → 24.4%). 바로 아래 절.

[`STEP25`](results/STEP25_2단계_크롭뷰_앙상블_실측.md)

---

## 🚩 2026-09-06 — **2단계 고정 창 기각** (STEP 23, 캐글 9.1시간)

STEP 22 에서 살아남았던 **유일한 개선 후보가 전체 데이터에서 무너졌습니다.**

| | `m2.5` (유지) | `f320` | 차이 |
|---|---:|---:|---:|
| macro-F1 | **0.5894** | 0.5795 | −0.0099 |
| A4 recall | **0.2815** | 0.2561 | −0.0254 |
| A4→A1 | 35.1% | 32.3% | −2.8%p |
| 배율 하락 | **26.0%** | 35.1% | **+9.1%p** |

⚠️ **"나빠졌다" 는 과합니다** — A4 차이 −0.025 는 **차이의** CI(±0.0297) 안입니다.
정확히는 **"이득 없음"**. 명확히 나쁜 건 배율 하락 +9.1%p 하나입니다.

**왜 STEP 22 는 반대였나**: VL01 의 A4 는 **196px**, TL02 는 **113px** 입니다.
기준선도 0.393 vs 0.282 로 다릅니다. 그리고 STEP 23 의 0.2815 는 STEP 16
**holdout 0.264** 와 거의 같습니다 → **VL01 이 낙관적 이상치**였습니다.

★ **제 판정 기준에 구멍이 있었습니다.** "A4→A1 혼동이 줄 것" 은 통과했는데,
줄어든 만큼 **정답이 아니라 나머지 4종으로 흩어졌습니다** (36.8% → 42.1%).
행선지 하나만 보면 '흩어짐' 을 개선으로 오독합니다 →
기준에 `errors.class_dispersion()` 을 같이 넣을 것.

**곁다리**: `effnetv2_s` 는 best_epoch **4 / 7** 로 수렴 — STEP 16 의 "0에폭 best"
는 convnextv2_base 특유였고, **03h 재개 조건은 발동하지 않습니다.**

[`STEP23`](results/STEP23_2단계_고정창_전체_실측.md)

---

## 🔬 2026-09-06 — 1단계 놓침 축을 닫았습니다 (STEP 24, GPU 0)

STEP 23 이 캐글에서 도는 동안 잰 것입니다.

**뜻밖의 발견**: 매니페스트 **365,428행 100%에 `polygon`(병변 외곽선)이 있고**,
`bbox` 는 그 폴리곤의 **외접사각형**입니다 (오차 0px, 표준편차 0). STEP 4A 이후
"병변 크기" 라고 불러 온 건 전부 그 네모였습니다 → `crop.polygon_in_window()`.

| 축 | 판정 | 근거 |
|---|---|---|
| **창 배치(네모 중심이 병변을 벗어남)가 놓침 원인인가** | ❌ **기각** | 크기를 4분위로 고정하면 모양(`slack`·중심이탈·길쭉함) 층 안 rho 가 전부 \|0.074\| 이하이고 **방향이 뒤집힙니다**. 반대로 모양을 고정하면 크기는 남습니다(0.009→0.042). 네모 중심이 병변 밖인 행은 **2.3%**, 놓침 77건 중 6건 |

⚠️ **제 사전등록 판정이 설계부터 틀렸습니다** — 가르는 변수 `occ` 가
`rho(occ, 병변 넓이) = +0.995` 로 크기와 **같은 변수**였습니다. 코드는 조용히
"H-A" 를 찍었습니다. → `PLACEMENT_MAX_COLLINEARITY = 0.80` 잠금장치.
**사전등록이 설계 결함을 막아주지 않습니다.**

⚠️ **재기 어려운 한계**: 365,428장이 사실상 전부 **1920×1080** 이라
`rho(절대 픽셀, 화면 점유율) = +1.000` 입니다. "커서 놓치나 확대해 찍어서
놓치나" 는 이 데이터로 못 가릅니다. 서빙 쪽은 `agent.to_train_space()`(짧은 변
1080 정규화)가 **이미 막고 있습니다** — STEP 24 는 그 장치가 왜 있어야 하는지를
수치로 못 박은 셈입니다. 남는 건 가로세로비 차이와 광학 줌뿐입니다.

[`STEP24`](results/STEP24_1단계_놓침_폴리곤_실측.md)

---

## 🔬 2026-09-05 — A4 원인 추적 (GPU 거의 안 씀)

STEP 16 가중치를 **로컬 RTX 3050** 으로 돌려 재학습 없이 잰 것들입니다.
⚠️ **VL01 청크만** (val 6,332 / 2단계 3,126행 = 전체 val 의 10.9%) — 나머지 크롭이
로컬에 없습니다. **이 안에서의 비교만 유효**합니다.

### 축 두 개를 닫았습니다

| 축 | 판정 | 근거 |
|---|---|---|
| **로짓 보정(prior)으로 A4 살리기** | ❌ **기각** | A4 recall 은 0.356→0.515 로 오르는데 **macro-F1 은 +0.0021** (문턱 +0.02). A2·A3 가 정확히 그만큼 내려가는 **순수 맞바꿈** |
| **청크(다운로드 묶음) 지름길** | ✅ **문제 없음** | 청크만으로 macro-F1 0.2197 (모델의 36.6%) · VL01 안에서만 평가해도 성능 그대로(+0.0194, 잡음 안) · 주된 청크 다른 쌍이 **더** 헷갈림(비 1.04) |

★ **prior 기각이 오늘의 핵심입니다.** prior 는 실재하고 걷어내집니다(A4 쏠림
lift 1.20→0.63). 그런데 걷어내도 **얻는 게 없습니다** — 맞바꿈 곡선 위에서
자리만 옮깁니다. 즉 A4 는 **결정 규칙이 아니라 표현**의 문제이고, 멘토가 말한
*"핵심 표현을 못 잡았다"* 가 서술이 아니라 **개입 실험으로 확인**됐습니다.
[`STEP18`](results/STEP18_로컬로짓_prior기각_실측.md) ·
[`STEP19`](results/STEP19_청크지름길_실측.md)

### ★★ 2단계 고정 창 — 오늘 유일하게 **살아남은** 개선 (STEP 22)

| 크롭 | macro-F1 | A4 recall | A4→A1 | 배율 하락 |
|---|---:|---:|---:|---:|
| `m2.5` (지금) | 0.5520 | 0.393 | 23.6% | 27.9% |
| **`f320`** | 0.5873 | **0.525** | **14.7%** | 33.4% |
| `f448` | 0.5944 | 0.515 | 14.7% | 33.8% |

사전등록 4기준(A4 recall > CI · A4→A1 −3%p · macro-F1 · 배율)을 **둘 다 통과**.
이유는 절대 크기가 아니라 **확대율이 일정해지는 것**으로 보입니다 —
`m2.5` 는 60px 병변을 2.6배 확대하고 400px 를 0.4배 축소합니다.

⚠️ **VL01·effnetv2_s 결과입니다.** A4 의 **88%가 TL02** 에 있고 거기 A4 는
**절반 크기**(113px vs 196px)라 같은 이득이 날지 모릅니다. 그리고 고정 창은
**촬영 거리에 의존**해 촬영 가이드 밴드를 다시 뽑아야 합니다.
→ [`STEP22`](results/STEP22_2단계_고정창_실측.md)

### (기각) 1단계 고정 창이 **큰 병변**을 놓칩니다

| | 1단계 `f320` (고정 창) | 2단계 `m2.5` (비례 창) |
|---|---:|---:|
| rho(병변 크기, 점수) | **−0.201**, 6개 클래스 전부 음수 | 클래스 안에서 전부 0 근처 |

2단계가 **자연 대조군**입니다 — 비례 창이라 채움 비율이 늘 같고, 거기선 효과가
사라집니다. → 원인은 "큰 병변이 어렵다" 가 아니라 **고정 창이 병변으로 꽉 차서
주변 정상 피부가 안 보이는 것**.

창 안 놓침 **1.8%** vs 창 밖 **4.6%** (2.5배), 창의 2배를 넘으면 **8.8%**.
전체 병변의 **23.2%** 가 320px 창을 넘칩니다 (A6 38.7% · A2 27.9% · A3 27.8%).
[`STEP17`](results/STEP17_1단계_병변크기_실측.md)

→ **STEP 20 진행 중**: `f320` vs `f448` 2판 (VL01, 로컬 3050). 판정은
`experiments.stage1_window_report()` 에 사전 등록.
⚠️ 창을 **비례**로 바꾸면 안 됩니다 — STEP 9-A 의 지름길이 돌아옵니다. 고정인 채 크기만.

### ⚠️ 릴리스에서 발견한 문제 세 개

1. **온도 보정이 오히려 나쁩니다** — ECE 0.0287 → **0.0299**, MCE 0.0784 → **0.1008**
   (정확도는 그대로). T=0.889 로 확률을 뾰족하게 만든 게 역효과입니다.
   지금 릴리스는 **보정을 하는 게 안 하느니만 못합니다.**
2. ~~문서와 릴리스 숫자가 어긋납니다~~ → **확인 결과 문서가 맞습니다.**
   STEP16 문서의 배율 표에 **최악점(2.0x)이 빠져 있어** 검산이 안 됐던 것입니다.
   `capture_guide.json` 전체 표로 계산하면 `(0.6078−0.4493)/0.6078 = 26.1%`,
   위치는 28.4% 로 문서(26.8% / 29.2%)와 맞습니다.
   릴리스 JSON 의 0.2053 / 0.3216 은 **표본 수가 다른 별도 측정**으로 보입니다 —
   이 리포가 이미 기록한 "표본만 바꿔도 10.9%p" 범위 안입니다.
   ⚠️ 교훈: **배율 표를 문서에 옮길 땐 최악점을 반드시 포함**하세요. 빼면
   요약값이 표와 안 맞아 보여 없는 문제를 만듭니다.
3. **병변 이름 재공개 조건 중 한 쪽이 통과했습니다** — 릴리스의
   `risk@cov50 = 0.1564`. → **자세한 건 위 「이름을 다시 보여주는 조건」 절**에
   있습니다 (다른 세션이 커버리지 **65.0%** 까지 실측해 뒀습니다).
   여기 중복해 적지 않습니다.

### 기록에 없던 값 — holdout 클래스별 (릴리스 JSON 에서 복원)

| | A1 | A2 | A3 | **A4** | A5 | A6 | A7 |
|---|---:|---:|---:|---:|---:|---:|---:|
| precision | 0.454 | 0.539 | 0.611 | **0.321** | 0.603 | 0.640 | 0.955 |
| recall | 0.605 | 0.748 | 0.762 | **0.250** | 0.412 | 0.567 | 0.788 |
| F1 | 0.519 | 0.627 | 0.678 | **0.281** | 0.490 | 0.601 | 0.863 |

**A4 는 precision 도 최저**입니다. STEP 11 때 "1.43배 남발" 이었는데 지금은
**덜 부릅니다**(≈0.78배) — 8배 데이터 이후 방향이 뒤집혔습니다.

### 로컬 GPU 를 쓸 수 있게 됐습니다

`pyproject.toml` 이 `torch>=2.2` 만 지정해 **윈도우 CPU 휠**(`2.13.0+cpu`)이
깔려 있었습니다 — `torch.cuda.is_available()` 이 조용히 False 였고, 추론이 몇
시간씩 걸려서야 알았습니다. cu126 인덱스를 **윈도우 마커로만** 묶었습니다
(리눅스=Colab/Kaggle 해석은 안 건드림). 이제 RTX 3050 으로 추론·소규모 학습이
로컬에서 됩니다 → **캐글 할당량을 아낍니다.**

---

## 🚨 기준선 — STEP 16 (2026-08-28)

TL01·TL02 를 합쳐 **45,885행 → 365,428행 (8배)**. `--finalize` 를 다시 돌려
**holdout 이 새로 나뉘었습니다.**

> ⚠️ **아래 숫자를 STEP 10 과 직접 비교하지 마세요.** 시험지가 바뀌었습니다.
> 이 값이 새 출발점이고, 비교는 **다음 실험부터** 합니다.

| | val | **holdout** | 목표 | |
|---|---:|---:|---:|---|
| 1단계 AUROC | **0.9504** | *미측정* | ≥0.80 | ✅ |
| 2단계 macro-F1 | 0.5999 | **0.5990** | 0.55~0.65 | ✅ |
| 스크리닝 recall | 0.9560 | **0.9526** | ≥0.95 | ✅ |
| 헛알림 | 27.3% | **21.2%** | | |
| **최종 7종 macro-F1** | 0.5814 | **0.5799** | | ← 보고할 숫자 |
| 2단계 온도 T | | **0.889** | | |

### ★ 보고용 숫자 — 여기 적힌 것만 씁니다

⚠️ **holdout 기준, 결과를 받은 사람 기준.** val 숫자나 사진 기준 비율을 섞어
쓰면 같은 사실이 다른 숫자로 보입니다. 이 블록이 유일한 기준입니다.

holdout **73,853장** (정상 41,402 / 병변 32,451), 임계값 **0.1466**

| 모델이 한 말 | 건수 | 그중 틀린 것 | 비율 |
|---|---:|---:|---:|
| **"병원 가보세요"** | 39,693 | 멀쩡한 개 **8,781** | **22.1%** |
| **"괜찮아요"** | 34,160 | 실제로는 병변 **1,539** | **4.5%** |

사진 기준 (**보고에는 쓰지 마세요**): 헛알림 21.2% / 놓침 4.7%.

### 클래스별 recall (holdout)

| A1 구진 | A2 비듬 | A3 태선화 | A4 농포 | A5 미란 | A6 결절 |
|---:|---:|---:|---:|---:|---:|
| 0.637 | 0.787 | 0.773 | **0.264** | 0.415 | 0.619 |

**A6 은 풀렸습니다** (0.328 → 0.619). **A4 가 새로운 최악**입니다 — 학습 8,225장으로
A5·A6 보다 많은데 제일 못 맞힙니다.

### 이번에 쓴 모델

```
stage1_effnetv2_s_f320_384_n233k_moderate_photometric    156MB  (10에폭)
stage2_convnextv2_base_m2.5_384_n121k_moderate           670MB  (best = 0에폭)
```

⚠️ **2단계 best 가 0에폭입니다.** 14에폭까지 돌렸는데 못 넘었습니다 —
더 돌리는 데 시간 쓰지 마세요. 자세한 곡선은 결과 문서에.

전체 기록: [`docs/results/STEP16_전체데이터_재학습_실측.md`](docs/results/STEP16_전체데이터_재학습_실측.md)

<details>
<summary>이전 기준선 — STEP 10 (2026-08-24, 옛 분할·45,885행)</summary>

| | val | holdout |
|---|---:|---:|
| 1단계 AUROC | 0.9530 | 0.9304 |
| 2단계 macro-F1 | 0.5263 | 0.5645 |
| 스크리닝 recall | 0.9501 | 0.9458 |
| 헛알림 | 24.0% | 33.3% |
| 최종 7종 macro-F1 | 0.5260 | 0.5325 |

holdout 7,134장, 임계값 0.1823. "병원 가보세요" 중 27.5% 가 헛알림,
"괜찮아요" 중 7.0% 가 놓친 병변이었습니다.
[`docs/results/STEP10_확정재학습_holdout_실측.md`](docs/results/STEP10_확정재학습_holdout_실측.md)

</details>

---

## 출력 형식 (2026-08-26 확정)

**멘토 판단: 2단계의 1등을 병변 이름으로 말하지 말 것.** 확률 분포만 넘기고
"자세한 건 판단 불가 → 진료 권함" 으로 끝냅니다. 2단계 모델 자체는 그대로 씁니다.

### 왜 — 실측

| 근거 | 숫자 |
|---|---:|
| 화면에 뜬 병변 이름이 **틀린 비율** (holdout) | **56.6%** |
| ├ 병변이긴 한데 이름이 틀림 | 1,301 / 4,478 |
| └ 애초에 병변이 아님 (헛알림) | 1,233 / 4,478 |
| A6(결절·종괴)를 A2(비듬)로 부름 (val) | 56장 |
| A6 을 A3(태선화)로 부름 (val) | 47장 |
| convnextv2_base vs swinv2_base 격차 | **0.0026** (잡음 ±0.02 안) |

마지막 줄이 결정적입니다. CNN 과 어텐션이라는 **다른 구조**가 0.0026 차이로
겹칩니다 — 막힌 게 모델이 아니라 데이터라는 뜻이고, 백본을 더 갈아도
이 이름은 안 맞습니다. 게다가 틀리는 방향이 나쁩니다 — **결절·종괴를 비듬이라
부르면 보호자가 병원을 미룹니다.**

거절로 덮어보려 했지만 안 됩니다: 오답률 20% 를 맞추려면 커버리지가 **18.2%**
까지 떨어집니다. 그 곡선은 헛알림 1,233건을 **뺀** 것이라 실제는 더 나쁩니다.

### 형식

```
🔎 **피부에 이상 소견이 보입니다.** (이상 가능성 62%)

**어떤 병변인지는 이 사진만으로 판단할 수 없습니다.**
아래는 모델이 비슷하다고 본 정도이며, 진단이 아닙니다.

    비듬·각질·상피성잔고리   31%  ██████
    태선화·과다색소침착      22%  ████
    구진·플라크              18%  ████
    미란·궤양                13%  ███
    농포·여드름               9%  ██
    결절·종괴                 7%  █

→ **수의사 진료를 받아보시기를 권합니다.**
```

`src/infer.compose_screening_message()` — 규격 전문은
[`docs/cautions/03_의료AI_안전설계_원칙.md`](docs/cautions/03_의료AI_안전설계_원칙.md) §7-B,
`tests/test_screening_message.py` 30개가 못 박고 있습니다.

⚠️ 화면에 띄우는 확률은 **깎기 전 원본**(합 = 1)입니다. `Prediction.topk` 는
1단계 확률을 곱해 낮춰둔 거절 판정용 값이라, 그걸 띄우면 여섯 개가 다 작아져
분포로 안 읽힙니다 → `Prediction.stage2_probs` 에 원본을 따로 둡니다.

### 이름을 다시 보여주는 조건 — ★ **한 쪽이 통과했습니다 (STEP 16)**

| 조건 | STEP 11 | **STEP 16** | |
|---|---|---|---|
| ① 6종 macro-F1 이 잡음(±0.02) 밖에서 상승 | 0.5645 | 0.5990 | ⚠️ **판정 보류** — 시험지(holdout)가 달라 직접 비교 불가 |
| ② 커버리지 50% 이상에서 오답률 ≤ 20% | 커버리지 **18.2%** | 커버리지 **65.0%** | ✅ **통과** |

②는 크게 움직였습니다. 열 명 중 둘한테만 말할 수 있던 것이 여섯 명 반이 됐습니다.

```
coverage → risk (병변만, holdout)
  100%  31.7%     80%  24.9%     50%  15.6%
   90%  28.1%     70%  21.5%     ← 20% 지점은 커버리지 65.0%
```

⚠️ **다만 이 곡선은 헛알림을 뺀 것입니다** (분모 = 제대로 라우팅된 병변 30,912건).
보호자 화면에는 헛알림 8,781건에도 이름이 붙고 **그건 무조건 틀립니다.**
화면 기준으로 환산하면:

| 커버리지 | 100% | 80% | 70% | 50% |
|---|---:|---:|---:|---:|
| 화면 오답률 | 46.8% | 41.5% | 38.8% | **34.3%** |

**절반을 거절해도 34.3% 가 바닥입니다** — 라우팅의 22.1% 가 애초에 병변이
아니기 때문입니다.

### 그래서 두 트랙은 분리된 게 아니라 **직렬**입니다

```
1단계 헛알림 22.1% 를 낮춘다  →  화면 오답률의 바닥이 내려간다
                              →  2단계 이름을 다시 띄울 수 있다
```

2단계만 잘해도, 1단계만 잘해도 이름은 못 띄웁니다. **둘 다 필요합니다.**
이름 공개는 멘토링에서 결정할 사안이고, 지금은 **논의할 값어치가 생겼다**
까지가 실측이 말해주는 것입니다.

---

## 앱 연동 (2026-08-26) — 데모까지 됨

안드로이드 앱(**DAENGS_APP**)이 붙을 자리를 만들었습니다. **아직 합치지
않았습니다** — 계약만 먼저 못 박아 양쪽이 동시에 작업할 수 있게 한 것입니다.

```bash
uv run --extra serve python serve.py --mock                          # 화면만
uv run --extra train --extra serve python serve.py --release <폴더>   # 진짜 모델
uv run … python serve.py --release <폴더> --stage1-only               # 1단계만
```

**결정 (2026-08-26): 지금은 STEP 10 릴리스를 2단계까지 그대로 씁니다.**
2단계 백본이 `resnet50` 인 채로요. 다음 주 06 재실행에서 `convnextv2_base` 로
**가중치만 갈아 끼웁니다** — 계약도 크롭 태그도 안 바뀌므로 앱 작업은 지금
시작해도 됩니다.

★ **진짜 모델은 지금 붙일 수 있습니다 — 재학습 필요 없습니다.**
STEP 10(노트북 06)이 만든 Kaggle Output 의 `release/` 폴더를 받아 `--release` 로
주면 끝입니다. 폴더 하나면 되고 GPU 도 필요 없습니다 (CPU 로 한 장에 1~3초).
남은 06 재실행은 **더 좋은 2단계 백본**을 쓰려는 것이지, 지금 돌릴 모델이
없어서가 아닙니다.

`--stage1-only` 는 정상/이상까지만 말하고 병변 분포를 안 냅니다. 어차피 병변
이름은 안 말하기로 했으므로 이것만으로도 제품이 되고, 1단계가 이 프로젝트에서
가장 잘 되는 부분입니다 (holdout AUROC 0.9304).

| 무엇 | 어디 |
|---|---|
| 파이프라인 + JSON 계약 | `src/agent.py` |
| HTTP 서버 + 데모 화면 | `serve.py` / `demo/index.html` (DAENGS 챗봇 재현) |
| 계약 감시 43개 | `tests/test_agent.py` |
| **앱 팀이 읽을 문서** | [`docs/SERVING.md`](docs/SERVING.md) |

**계약에 "1등 병변" 필드가 없습니다.** 주는 순간 앱이 그걸 제일 크게 띄웁니다 —
56.6% 틀린 이름을요. 테스트가 `top1`/`predicted`/`diagnosis` 키를 감시합니다.

### ⚠️ 합치기 전에 풀어야 할 것

| # | 문제 | 상태 |
|---|---|---|
| a | ~~서빙 크롭이 학습 크롭과 다름~~ | ✅ **가이드 프레임을 bbox 로 받아 해결** (아래) |
| a | **서빙 크롭이 학습 크롭과 다름** — 보호자 사진에는 bbox 가 없어 중앙을 씁니다 (1단계 짧은 변의 29.6% = 320/1080) | 대응은 넣었으나 **실측 안 됨** |
| b | 가이드 프레임을 CameraX 프리뷰에 (웹 데모는 `getUserMedia` 로 됨) | 앱 쪽 작업 |
| ~~c~~ | ~~1단계 확률 미보정~~ | ✅ **닫힘 (STEP 16).** 릴리스를 띄워 확인: 1단계 T=0.9225 · 2단계 T=0.8890 · 응답 `stage1.calibrated=true`. ⚠️ 두 단계 다 ECE 가 조금 나빠져서 **성과로 말하면 안 됩니다** |
| ~~d~~ | ~~2단계 백본 풀 학습 확인~~ | ✅ **닫힘 (STEP 16).** `convnextv2_base` 전체 데이터 holdout macro-F1 0.5990 |
| **e** | **화면에 무엇을 말할 것인가** | 🔴 **지금 유일한 열린 문제.** STEP 28~30 이 "계열이면 말할 수 있다" 를 실측했고, 결정은 사람 몫입니다 (`docs/SERVING.md` §6) |

### a — 가이드 프레임 = bbox (2026-08-26 해결)

원래 문제는 "학습은 라벨 bbox 로 잘랐는데 보호자 사진엔 bbox 가 없다" 였습니다.
답은 앱이 어차피 띄워야 하는 것이었습니다 — **가이드 프레임을 bbox 로 받습니다.**
서버는 그걸로 **학습이 쓰는 그 함수**(`crop.crop_window()`)를 부릅니다.

**실측:** 같은 구도를 다른 해상도로 찍어도 창이 **0px 차이**로 같습니다.

| | 학습 창 | 서빙 창 |
|---|---|---|
| 1단계 `f320` | (800, 380, 1120, 700) | (800, 380, 1120, 700) |
| 2단계 `m2.5` | (785, 365, 1135, 715) | (785, 365, 1135, 715) |

⚠️ **두 단계의 사정이 다릅니다.** 1단계 f320 은 네모의 **중심만** 쓰고 창은 320px
고정이라, 네모가 헐렁해도 결과가 같습니다. 2단계 m2.5 는 네모의 **크기**를 쓰므로
그대로 배율이 바뀝니다 — 네모를 가로 +6%p 헐렁하게 잡으면 창이 350 → 620px (1.8배).
그건 우리가 잰 배율 하락(convnextv2 18.4%) 쪽으로 가는 것입니다.

→ 그래서 프레임은 **끌고 늘릴 수 있어야** 합니다. 그리고 밴드(가로 28~68%, 중앙
10% 이내) 밖이면 **모델에 넣기 전에** 재촬영으로 돌려보냅니다.

**남은 것:** 네모의 **분포**는 여전히 다릅니다 — 라벨러는 답을 알고 딱 맞춰
그렸고, 사용자는 모르는 채 대충 맞춥니다.

⚠️ 여기에 한때 "앱으로 찍은 사진 100장에 **수의사 라벨**" 이라고 적었는데
**틀린 계획이었습니다.** 수의사도 앱 사용자도 지금 없고, 앞으로도 이 프로젝트
안에서는 안 생깁니다. 실행할 수 없는 항목을 점검표에 두면 위험이 관리되는
것처럼 보이기만 합니다.

**수의사가 필요 없습니다.** 수의사는 *병명*을 붙이는 사람인데, 여기서 재야 할 건
*네모를 얼마나 잘 그리는가* 입니다. 그 정답(bbox)은 이미 매니페스트 안에 있고,
병명도 이미 압니다.

→ `tools/box_error.py` — AI Hub 사진을 한 장씩 띄우고 사람이 네모를 맞추면
정답 bbox 와 비교합니다. 오차를 **이미 재 둔 교란 곡선**(STEP 10 `usable_range`)에
대입해 예상 하락까지 뽑습니다. 한국 PC 에서 `uv sync` 만으로 돌고 30분이면 됩니다.

**그 곡선을 뒤집으면 앱이 지켜야 할 숫자가 바로 나옵니다:**

| | 네모 크기 오차 (정답 대비) |
|---|---|
| 권장 (하락 5% 이내) | **0.71 ~ 1.18배** |
| 허용 (하락 10% 이내) | **0.59 ~ 1.43배** |

1.5배로 그리면 이미 허용 밖입니다. 생각보다 빡빡합니다.

---

## ✅ (해결됨) 데이터 늘리기 — 맥북으로 받아 왔습니다 (2026-08-28)

> 아래는 **본 PC 한 대만 있었을 때**의 기록입니다. TL01·TL02 를 맥북에서 받아
> 크롭만 옮겨 오는 방식으로 풀었습니다 → [`docs/OTHER_PC.md`](OTHER_PC.md).
> 디스크 계산 자체는 여전히 유효하니 남겨둡니다.

<details>
<summary>당시 기록 (2026-08-26 실측)</summary>


**디스크**

| | |
|---|---|
| 지금 여유 (정리 후) | 72.2GB |
| VL01.zip 까지 지우면 | **93.3GB** |
| TL02 — 크롭이 끝난 **뒤** | 89.9GB (zip 80 + 크롭 9.9) |
| TL02 — **받는 도중 최대치** | **170 ~ 250GB** |
| 이 PC 디스크 **전체** | 237.8GB |

**여유의 문제가 아니라 물리적으로 안 됩니다** — 최대치가 디스크 전체보다 큽니다.

받는 도중 최대치는 추정이 아니라 **aihubshell 소스를 읽은 값**입니다
(`tools/crops.py --shell-peek`, 2026-08-26):

| 줄 | 하는 일 | 그 순간 사는 것 |
|---|---|---|
| 111 | `curl -o download.tar` | tar — 1배 |
| 124 | `tar -xvf download.tar` (뒤에 `rm` 없음) | tar + 풀린 내용 — 2배 |
| 139 | `find\|sort\|xargs cat > "${prefix}"` | tar + 조각 + 합친 것 — 3배 |
| 142 | `rm "${prefix}".part*` | 합친 **다음**이라 최대치 안 줄어듦 |

⚠️ 전에 여기 적었던 "보수 = 압축 해제 때문에 2배" 는 **틀렸습니다.**
우리는 `--mode zip` 으로 압축을 안 풉니다. 근거가 틀렸는데 숫자만 우연히
비슷했고, 제대로 재니 더 커졌습니다.
전체 기록: [`docs/results/AIHUBSHELL_피크_실측.md`](docs/results/AIHUBSHELL_피크_실측.md)

**조각내서 받기도 안 됩니다.** `prepare_local.py --list` 로 확인한 결과
AI Hub 가 **zip 6개**만 filekey 로 줍니다 (TS01/TS02/TL01/TL02/VS01/VL01).
안쪽 파일 단위 키가 없어서 "필요한 라벨만 골라 받기" 도 불가능합니다.

**구글 드라이브로도 안 됩니다.** 문제가 저장 공간이 아니라 "80GB 파일 하나를
로컬에서 랜덤 접근" 이라서요. 드라이브 스트리밍은 파일을 열면 통째로 로컬
캐시에 받습니다.

→ **할 수 있는 유일한 길**: **250GB 이상** 여유가 있는 외장 드라이브에서
`set DOG_SKIN_DATA=E:\daengs_raw` 로 받아 크롭까지 하고, **크롭만**(9.9GB) 옮겨오기.
같은 PC 라도 됩니다 — AI Hub 는 한국 IP 만 보므로 드라이브만 바꾸면 됩니다.

⚠️ 외장 드라이브를 살 때: **exFAT 또는 NTFS** 여야 합니다. FAT32 는 4GB 넘는
파일을 못 만들어서 `download.tar`(80GB)가 첫 줄에서 죽습니다.

💡 크롭은 `--only A5,A6` 으로 더 줄일 수 있습니다 (다운로드는 안 줄어듭니다 —
zip 을 통짜로만 주는 건 `--list` 로 실측).

⚠️ 나중에 늘리게 되면 알아둘 것 — holdout 은 그룹 해시라 결정론적이고
`animal_id`(견종·나이·성별·날짜 대용)도 안정적이지만, 그룹이 `dup_cluster`
(phash)와 합집합이라 **새 사진이 두 클러스터를 이으면 그 그룹의 배정이 바뀝니다.**
전부는 아니어도 일부 숫자는 비교가 깨집니다.

---

</details>

---

## 범위 밖 — 나중에 볼 것

### 안구(眼球) 질환 (2026-08-26 검토, **일단 배제**)

AI Hub 에 반려동물 **안구질환** 데이터셋이 따로 있습니다 (561 은 피부입니다).
"피부가 되면 눈도 되지 않나" — 됩니다. 다만 **지금은 아닙니다.**

**왜 지금 아닌가** (작업 규칙 4: 막힌 원인을 고치기 전에 다음 단계로 안 갑니다)

| 아직 안 끝난 것 | 상태 |
|---|---|
| 2단계 백본 확정 | 06 재실행 대기 (다음 주) |
| 1단계 확률 보정 | 한 번도 안 함 |
| 사용자 네모 오차 실측 | `tools/box_error.py` 로 30분, 아직 안 함 |
| A2 오류 쏠림 원인 | 미측정 |

그리고 GPU 할당량이 병목입니다 (주 30h 중 ~4.8h 남음). 안구를 얹으면
전처리 + 학습으로 또 몇 시간이고, **위 네 개가 그만큼 밀립니다.**

**왜 버리지는 않나 — 틀이 거의 그대로 재사용됩니다**

우리가 만든 건 "피부 모델" 이 아니라 **"사진 한 장 → 2단계 → 이름 안 말하는
출력"이라는 틀**입니다. 안구로 바꿔도 그대로 쓰는 것:

`env` `config` `scan` `labels` `dedup` `split`(개체 단위) `crop` `data` `models`
`train`(이어받기·로짓 캐시) `evaluate` `robust` `gates` `calibrate` `explain`
`message`(표시 규칙) `agent`(계약) `serve` `demo`

바꿔야 하는 것은 `CLASSES`/`CLASS_KO`, 크롭 전략, 촬영 가이드 밴드, 임계값, 문구.

**안구가 오히려 쉬울 수도 있습니다 — 다만 전부 추정입니다**

- 눈은 **경계가 뚜렷한 물체**라 "가이드 프레임 = bbox" 가 더 잘 맞습니다.
  사람이 눈에 네모 맞추는 건 병변에 맞추는 것보다 훨씬 쉬워요 →
  우리가 걱정하는 네모 오차가 작아질 것
- 눈 크기는 개체별 편차가 병변보다 작아서 **배율이 자연히 일정**해집니다.
  이 프로젝트에서 제일 오래 싸운 게 배율 교란이었습니다
- 2단계 이름을 못 말한 이유가 "병변 형태 6종이 서로 너무 비슷" 인데,
  안구 질환은 시각적으로 더 갈릴 수 있습니다

⚠️ **숫자가 하나도 없습니다.** 위 세 줄은 전부 추정이고, 작업 규칙 1 은
추정치를 문서에 쓰지 말라고 합니다 — 그래서 "범위 밖" 절에 두고 결론표에는
안 넣습니다.

**반대로 더 어려운 점**

눈은 **오진 비용이 더 급합니다.** 각막궤양처럼 시간을 다투는 것이 있어서
미탐(놓침)이 피부보다 비쌉니다. 톤을 더 세게(“빨리 진료”) 가져가야 하고,
recall 기준도 다시 정해야 합니다 — 0.95 를 그대로 물려받으면 안 됩니다.

**다시 볼 조건**

1. 다음 주 06 재실행으로 2단계 백본이 확정되고
2. 1단계 보정과 네모 오차 실측이 끝나고
3. 앱 연동이 실제로 붙은 뒤

그때 **작게 시작**하세요 — 안구도 1단계(정상/이상)만 먼저. 지금 `--stage1-only`
가 있는 이유이기도 합니다.

---

## 확정된 설정

| | 값 | 언제 정했나 |
|---|---|---|
| 데이터 | AI Hub 561 · VL01+TL01+TL02 **365,428행 / 개체 5,333마리** | STEP 16 |
| 분할 | 개체 단위 group-aware · fold 0 | |
| 입력 해상도 | **384px** | STEP 4A |
| **1단계** 크롭 | **`f320`** (고정 320px 창) | STEP 9-A |
| 1단계 백본·증강 | **effnetv2_s** + **photometric** | STEP 6 |
| **2단계** 크롭 | **`m2.5`** (ROI · 마진 2.5배) | STEP 4C |
| 2단계 백본 | **convnextv2_base** ✅ (전체 데이터로 확인) | STEP 12·13 → STEP 16 |
| 파인튜닝 | `moderate` (백본 lr ×0.3) | STEP 4A |
| 1단계 임계값 | recall 0.95 를 만족하는 값 (**STEP 16 에서 0.1466**) | ⚠️ 아래 참고 |

### ★ 단계마다 입력이 다릅니다

**1단계는 2단계 크롭을 따라오지 않습니다.** 1단계에서 ROI 크롭을 쓰면 크롭 창
크기가 정답을 흘립니다 (정상 bbox 면적 중앙값 0.71% vs 병변 1.25%, 지름길
하한선 AUROC 0.6042). 그 신호는 **배포에 존재하지 않습니다** — 보호자 사진에는
bbox 가 없습니다.

우선순위는 `f320` > `full` > ROI 크롭이고, 규칙은 `crop.choose_stage1_tag()` 에
있습니다 (노트북 셀 말고 — 작업 규칙 3).

⚠️ **06 은 `STAGE1_CROP = "f320"` 을 못 박습니다.** 예전엔 `choose_stage1_tag()`
가 매 실행 다시 골랐는데, 데이터가 바뀌자 판정이 뒤집혀 **m2.5 로 학습을
시작했습니다** (STEP 16 에서 실제로). 실측으로 닫은 결론을 휴리스틱이 매번
다시 열면 안 됩니다 — 이제 그 함수는 어긋날 때 **알려주기만** 합니다.

---

## ✅ 데이터 합치기 — 끝났습니다 (2026-08-28)

| 청크 | 행 | 담고 있던 클래스 |
|---|---:|---|
| VL01 | 45,885 | 기존 |
| TL02 | 112,773 | A2 · A3 · A4 · A5 · A6 |
| TL01 | 263,340 | **A7 189,376** · A2 · A1 |
| **합계(중복제거·정제 후)** | **365,428** | A1~A7 전부 |

세 청크가 서로 다른 클래스를 담고 있어서 **합쳐야 A1~A7 이 다 모입니다.**
A7(무증상)이 크게 들어온 게 제일 큰 변화였고, 1단계가 거의 균형(A7 49.4%)이
됐습니다. `--finalize` 로 다시 나눈 결과가 위 **STEP 16** 값입니다.

⚠️ 이 과정에서 얻은 함정 세 개(zip 조각 순서 · JSON 조용한 손실 · `unzip` 잘림)는
`CLAUDE.md` 의 「알려진 함정」에 있습니다. 같은 일을 또 하면 다시 밟습니다.

---

## 촬영 가이드 (STEP 16 실측, n=2,000)

```
권장 : 배율 0.7x ~ 1.2x   (하락 5% 이내)  → 네모 크기 0.83~1.43배
허용 : 배율 0.6x ~ 1.4x   (하락 10% 이내) → 네모 크기 0.71~1.67배
위치 : 병변이 화면 중앙에서 10% 이내
```

⚠️ **STEP 16 에서 밴드가 바뀌었습니다** (이전: 권장 0.85~1.4 / 허용 0.7~1.7).
작게 잡는 쪽은 너그러워지고 **크게 잡는 쪽이 빡빡해졌습니다** — 2.0x 에서
macro-F1 이 0.449 까지 떨어집니다.

카메라 UI 에 가이드 프레임을 띄우고, 벗어나면 셔터를 막거나 "조금 더 가까이"
를 띄웁니다. 이 구간 밖 사진은 추론하지 말고 다시 찍게 합니다.
`robust.usable_range()` 가 학습 없이 이 구간을 뽑습니다 (05 의 5-b).

⚠️ 밴드 경계를 소수점까지 믿으면 안 됩니다 (아래 잡음 참고). 형태 — 0.5x 에서
확 나빠짐 — 는 여섯 번 다 같습니다.

그리고 밴드와 **별개로** 문구가 하나 더 있습니다 — **"털을 헤쳐서 피부가 보이게"**.
헛알림 사진통계에서 개입 가능한 축으로 살아남은 게 이것 하나뿐이었습니다
(부위별 연접부 16.6% → 다리 41.0% = 2.5배, 같은 부위 안에서도 `hair` AUROC 0.727).
촬영 거리(`area` 0.462)와 조명은 기각됐으므로 **문구에 넣지 않습니다.**
`demo/index.html` 의 `HAIR_TIP` 한 곳에 있고 세 화면(고르기·카메라·첨부)이
나눠 씁니다 — `tests/test_capture_guide.py` 가 기각된 축이 새어 들어오는지 봅니다.
⚠️ 이건 **관찰**에서 나온 문구입니다. 문구를 띄운 뒤 헛알림이 실제로 줄었는지는
아직 재지 않았습니다 (개입 실험 필요).

---

## ⚠️ 교란 검사 잡음 폭 — 숫자 하나로 판단하지 마세요

**2단계 설정을 하나도 안 바꿨는데** 배율 하락이 실행마다 이렇게 나왔습니다:

| 실행 | 표본 | 배율 하락 |
|---|---:|---:|
| STEP 4D | 2,000 | 16.0% |
| STEP 5 | 2,000 / 3,000 | 21.6% / 16.8% |
| STEP 7 | 2,000 | 23.8% |
| STEP 8 | 2,000 / 3,000 | 20.9% / 25.0% |
| STEP 10 | 2,000 / 3,000 | 15.4% / 18.5% |

폭이 **9.6%p**. ±3%p → ±5%p → **±8%p** 로 두 번 올려 잡았습니다.

⚠️ **같은 실행 안에서 표본 수만 바꿔도 벌어집니다.** STEP 10 위치 하락이
n=2,000 에서 9.3%, n=3,000 에서 20.2% (**10.9%p**). ±8%p 도 모자랍니다.

→ 그래서 `experiments.SCALE_DROP_REJECT_PP` 를 **12%p** 로 잡았습니다.
5~12%p 사이는 탈락도 통과도 아닌 **◐ 구분 불가**로 적습니다.

---

## 열려 있는 문제

| # | 문제 | 숫자 | 성격 |
|---|---|---:|---|
| 1 | 헛알림 — 정상 3장 중 1장이 병원행 | A7 recall 0.667 | 아래 참고 |
| 2 | 2단계 백본 — 후보는 나왔고 **확정만 남음** | +0.088 (서브셋) | 06 풀 학습 대기 |
| 3 | A4(농포·여드름) recall | 0.391 | 2단계 최저 |
| 4 | A1 precision | 0.311 | 헛알림이 A1 으로 몰림 |
| 5 | A6(결절·종괴)을 **잴 수가 없음** | val 0.279 / holdout 0.487 | 측정 문제 |
| 6 | ~~1단계 보정을 한 번도 안 했음~~ **→ 이미 돼 있었습니다** | T=0.9225, ECE 0.0282→**0.0305** | 2단계와 같은 결론 — 고칠 게 없었습니다. 배선도 맞습니다 (`threshold_calibrated` 사용) |

### 1. 헛알림 33% 는 결함이 아닙니다 — **값**입니다

recall 0.95 를 사려고 치른 값입니다. 검증셋 가격표:

| 임계값 | 스크리닝 recall | 헛알림 |
|---:|---:|---:|
| 0.1235 | 0.9705 | 32.1% |
| **0.1823** | **0.9501** | **24.0%** ← 지금 |
| 0.2790 | 0.9193 | 17.1% |
| 0.6302 | 0.8198 | 7.0% |

⚠️ **recall 0.95 라는 숫자를 우리가 실험 없이 정했습니다.** 로드맵 초안에 적은
값이 그대로 남았고, 수의사한테 물어본 적이 없습니다.
→ **멘토링에서 질문할 항목**입니다. 재학습 없이 임계값만 옮기면 됩니다.

**헛알림의 행선지 — 실제 칸** (STEP 11, val 918장):

| 행선지 | 장수 | 비중 |
|---|---:|---:|
| **A2 비듬·각질** | **512** | **55.8%** |
| **A3 태선화·색소침착** | **184** | 20.0% |
| A1 구진·플라크 | 148 | 16.1% |
| A4 / A6 / A5 | 56 / 12 / 6 | 나머지 |

**A2 가 오류 총량 1위입니다** (오탐 966 + 미탐 452 = 1,418). 놓친 병변
196장 중에서도 A2 가 109장(55.6%)입니다.
⚠️ **원인은 아직 측정 안 했습니다** — 라벨 정의 / 표본 수 / 시각적 유사성 중
무엇인지 사진 판독과 임상 확인이 필요합니다.

⚠️ STEP 10 직후 주변합에서 역산해 "A1·A2 가 89%" 라고 적었던 건 **틀렸습니다.**
A1 은 3위(16.1%)이고, A1 의 과예측을 만든 건 정상 사진이 **아니었습니다** —
병변 간 혼동이었습니다 (A3→A1 176장 등). 순 흐름으로 칸을 말하면 안 됩니다.
   판단은 **val** 로 합니다 — holdout 은 같은 모양인지 확인만 합니다.

### 6. 1단계 확률이 이제 화면에 뜹니다 — 보정 (코드는 준비됨)

**2026-08-26: 노트북 06 에 셀을 넣었습니다.** 다음 실행에서 T 와 보정된 임계값이
나옵니다. 실행 전이므로 **아직 숫자는 없습니다.**

같이 잡은 것 두 개:
* `export_release` 가 `temperature.json` 을 안 옮기고 있었습니다 → **지금 쓰는
  릴리스는 2단계도 보정 안 된 채로 서빙됩니다** (`Engine.load` 가 조용히 T=1.0).
  다음 실행부터 실립니다.
* 보정 점수를 만들 때 `calibrate.apply()` 결과에 `stage1_scores()` 를 걸면
  softmax 가 두 번 먹습니다. 그렇게 짰다가 ECE 가 0.03 → 0.20 으로 나빠져 잡았습니다.

아래는 원래 적어둔 배경입니다.



온도 보정은 **2단계에만** 걸려 있습니다 (`ck2.parent/temperature.json`, T=1.1063).
1단계는 한 번도 안 했습니다. 예전엔 1단계 확률이 내부 임계값 비교에만 쓰여서
상관없었는데, 새 출력 형식이 "이상 가능성 62%" 를 **보호자에게 직접 보여줍니다.**
보정 안 된 확률을 사람에게 띄우면 안 됩니다.

→ 06 재실행 때 1단계 로짓으로 온도 보정 + ECE 를 같이 잽니다.
ECE ≥ 0.10 이면 숫자를 빼고 문구만 씁니다 (`docs/cautions/03` §8).

### 5. A6 은 재는 방법을 먼저 고쳐야 합니다

같은 설정·다른 실행에서 A6 recall 이 0.405 → 0.344 로 움직입니다. 검증 표본이
~256장뿐이라 2σ 가 **±0.085** — 목표 개선폭(+0.095)과 거의 같습니다.
holdout(0.487)과 val(0.279)은 0.2 넘게 벌어집니다.

→ **A6 을 고치기 전에 재는 방법을 먼저 고쳐야 합니다** — 5-fold 평균 /
holdout 합산 / A6 에도 부트스트랩 CI 병기. 지금 A6 만 보고 설정을 고르면
주사위입니다.

---

### RunPod 로 돌린다면

[`docs/RUNPOD.md`](RUNPOD.md). STEP 16 에서 겪은 것들:

| 증상 | 원인 |
|---|---|
| 볼륨이 계속 `Disk quota exceeded` | 크롭 22.7GB + 체크포인트. **체크포인트를 `/`(컨테이너 디스크)로** 돌리면 해결 |
| `unzip` 16개가 전부 `Exit 50` 인데 "끝난 것처럼" 보임 | 디스크 가득. → `tools/fetch_crops.py` |
| `load_prepared()` 가 데이터가 있는데도 못 찾음 | 검사 순서 버그 → 고침 |
| DataLoader `Empty` / `_try_get_data` | 커널을 끊으면 워커가 죽음. **로더를 다시 만들면 됨** |
| `IOPub message rate exceeded` | 화면 출력만 끊김. **학습은 정상** — `history.csv` 를 보세요 |
| 셀이 멈춘 것처럼 보임 (GPU 0%) | `crop.switch_tag()` 가 36만 번 파일 확인 중. 네트워크 볼륨이라 느림 |

## 다음 할 일

> 여기 있던 표는 **2026-09-04(STEP 16 직후)** 것이었습니다. STEP 17~30 이
> 그 목록의 대부분을 닫았는데 표는 안 바뀌어서, "지금 뭐 해야 하나" 를
> 물으면 이 파일이 **낡은 답**을 하고 있었습니다. 2026-09-06 기준으로 다시 씁니다.

### 지금 확정된 것 (안 건드립니다)

| | 설정 | 실측 |
|---|---|---|
| **1단계** | `effnetv2_s` + `f320` + `photometric` · 384px · moderate | holdout AUROC **0.9304** · recall **0.970** · 헛알림 **24.9%** |
| **2단계** | `convnextv2_base` + `m2.5` · 384px · moderate | holdout macro-F1 **0.5990** |
| 임계값 | **0.1466** (보정된 값) | recall 0.95 목표에서 뽑음 |
| 온도 | 1단계 **0.9225** / 2단계 **0.8890** | 둘 다 ECE 가 조금 나빠짐 |
| 출력 | **병변 이름 안 띄움** — 확률 분포만 | 결정 재검토 중 (아래 D) |

### 살아 있는 후보 — 전부 **결정 대기**, 재학습 0회

| 후보 | 실측 | 상태 |
|---|---|---|
| **계열 4군 출력** (A1·A4 / A2·A3 / A5 / A6) | 커버리지 **61.4%** · 하향 1.7% · 과잉 49.3% | 관문 통과. **사람 결정** |
| **A6 경보** ("덩어리가 의심됩니다") | 정밀도 **90%** → 재현율 **50.4%** (n=1,540) | 릴리스 단독으로도 됨 (추론 비용 0) |
| **3팔 크롭 뷰 앙상블** | macro-F1 +0.0295 · 배율 하락 23.5→24.4% | 관문 4개 통과. **계열로 말하면 필수 아님** (릴리스 단독도 52.5%) |

### 남은 일 — 세 갈래

| # | 할 것 | 필요한 것 | 왜 |
|---|---|---|---|
| **A** | **헛알림 포함 커버리지를 전체 데이터로** | 캐글 1회 (추론만) | 지금 61.4% 는 **VL01** 값입니다. VL01 은 커버리지에서 **비관적**이라 전체는 더 높을 가능성이 큽니다 |
| **B** | `tools/box_error.py` — 사람이 네모 40개 | **GPU 불필요**, 30분 | 사용자가 그리는 네모가 새 밴드(0.83~1.43배) 안에 드나. 앱 사양의 마지막 미측정 |
| **C** | holdout 확인 (06) | 캐글 | 위 전부 val 입니다 |
| **D** | **무엇을 말할 것인가 — 멘토·사용자 결정** | 사람 | 모델 판정은 끝났습니다. 근거는 `SERVING.md` §6 |

**A·B 는 서로 독립입니다.** B 는 지금 당장 됩니다.

### ⛔ 닫힌 축 — 다시 열지 마세요

| 축 | 왜 닫혔나 | 어디 |
|---|---|---|
| 로짓 보정(prior)으로 A4 살리기 | 순수 맞바꿈 (macro-F1 +0.0021) | STEP 18 |
| 청크 지름길 | 모델이 안 씀 | STEP 19 |
| 1단계 창 크기 (`f448`) | 놓침이 오히려 늘고 화질 의존 두 배 | STEP 20 |
| **2단계 고정 창** | 전체 데이터에서 이득 없음 · 배율 하락 +9.1%p | STEP 23 |
| 1단계 놓침 = 창 배치 | 모양을 고정하면 효과가 사라짐 | STEP 24 |
| **TTA (좌우반전)** | ±0.003, 부호도 갈림 | STEP 25 |
| **1단계 앙상블** | AUROC +0.0019, CI 가 0 포함 | STEP 25 |
| **2팔 앙상블** | 이득의 83% 를 내지만 흩어짐 관문 실패 | STEP 26 |
| 2단계를 더 오래 학습 | 14에폭까지 돌려 0에폭을 못 넘음 | STEP 16 |
| 1단계 백본 교체 | 4.4배 큰 모델도 못 이김 | STEP 14 |
| **A5 를 융기·발진에 병합** | 숫자는 낫지만 임상이 🔴 위험으로 꼽은 구별 | STEP 30 |
| A5 를 데이터로 고치기 | A6 대조군이 같은 크기인데 recall 0.134 높음 | STEP 30 |
| ~~03h 학습률~~ | 재개 조건(`best_epoch ≤ 2`)이 **발동 안 함** — `effnetv2_s` 가 4/7 | STEP 23 |

### 이 컴퓨터에 있는 것 / 없는 것 (자주 헷갈립니다)

| | |
|---|---|
| **있음** | VL01 크롭 (`m2.5` 47,605 · `f320` 47,006 · `f448` 39,508장) |
| **있음** | 전체 val **로짓** 27,331행 × 3모델 ← **로컬 분석의 밑천.** STEP 25~30 이 전부 이걸로 돌았습니다 |
| **있음** | 릴리스 체크포인트 (1·2단계 + 온도 + 임계값) |
| **없음** | TL01·TL02 **크롭** — 그래서 헛알림 포함 곡선을 VL01 로만 잽니다 |
| **못 만듦** | TL02 원본 250GB > 디스크 238GB (여유 30GB). **물리적으로 불가** |
| ⚠️ | `dogskin_full.zip` 은 **`full` 크롭 태그**(안 자른 크롭)이지 "전체 데이터" 가 아닙니다 |

